## OBJECTIVE
TASK REQUIRING AI JUDGMENT: implement network/security/permission monitoring. 4 target items, source=ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml, current_status=MISSING for all 4:
- item_number: 20, requirement: network_usage_logging
- item_number: 21, requirement: ssh_login_anomaly_detection
- item_number: 22, requirement: file_permission_change_tracking
- item_number: 24, requirement: security_event_detection

## SCOPE
INPUT_FILES:
- /opt/veridian/scripts/health-check-15min.py (extend, do not replace)
- /var/log/auth.log (read-only source for item 21)
- /proc/net/dev (read-only source for item 20)
- ai-os/scripts/file_inventory.py (extend hash-tracking to also record st_mode permission bits, do not replace)

OUTPUT_ARTIFACTS (exact):
1. FUNCTION: check_network_usage() in health-check-15min.py
   INPUT: none (reads /proc/net/dev)
   OUTPUT: dict {interface: str, rx_bytes_delta: int, tx_bytes_delta: int} per interface, appended to the existing health-15min.jsonl record under a new "network" key
   CALLED_FROM: existing main health-check loop, same 15-min cadence, no new cron entry

2. NEW_FILE: scripts/security-check.py
   INPUT: none (reads /var/log/auth.log, superboss-register.sqlite file_inventory table)
   OUTPUT: JSON to stdout: {new_source_ips: [str], failed_then_success_bursts: [{ip: str, count: int}], permission_changes: [{path: str, old_mode: str, new_mode: str}]}
   INVOCATION: python3 scripts/security-check.py
   CRON_ENTRY: */15 * * * * /opt/veridian/scripts/run-logged.sh "security-check" /usr/bin/python3 /opt/veridian/scripts/security-check.py >> /opt/veridian/ai-os/logs/security-check-cron.log 2>&1

3. EXTEND: ai-os/scripts/file_inventory.py
   ADD_COLUMN: file_inventory.mode TEXT (octal string, e.g. "0644")
   POPULATE: via os.stat(path).st_mode on each existing scan pass
   NO_SCHEMA_MIGRATION_SCRIPT_NEEDED: ALTER TABLE file_inventory ADD COLUMN mode TEXT, wrapped in try/except for idempotent re-run

4. INTEGRATION: security-check.py findings with new content call scripts/notify-owner.py --subject --body, dedupe-key=finding-type+identifier, same pattern as scripts/health-check-15min.py's existing notify_owner() call

## KNOWN_CONTEXT
zero_duplication_check_performed: true
grep_result: "no hits for network|auth.log|permission-change across scripts/*.py, scripts/*.sh, ai-os/scripts/*.py" (confirmed 2026-07-23, see ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml items 20/21/22/24 evidence field)
scripts/notify-owner.py: EXISTS, VERIFIED_WORKING, message_id=d08a0b6e-35a4-4c70-a0fa-bef7e26be6a9
db_concurrency_constraint: superboss-register.sqlite corrupted 3x today under concurrent write load -- this task's own writes to it (file_inventory mode column) MUST be sequential, single connection, not parallel agents

## SUCCESS_CRITERIA
TEST_1: security-check.py exits 0, produces valid JSON matching OUTPUT schema above, when run manually against live auth.log
TEST_2: health-15min.jsonl's next real write includes non-null "network" key
TEST_3: file_inventory table has mode column populated for >0 rows after one file_inventory.py run
TEST_4: one notify-owner.py call succeeds if security-check.py finds >=1 real finding on this run (real Resend message_id as evidence); if zero findings exist, state that explicitly, do not fabricate a test finding

## EXPECTED_OUTPUT
COMMIT: scripts/health-check-15min.py diff, scripts/security-check.py (new file), ai-os/scripts/file_inventory.py diff
PUSH: yes
UPDATE_FILE: ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml, items [20, 21, 22, 24], status: MISSING -> DONE, evidence: <real file path + line number + test result>
CHECKPOINT: status=pending_review, note=<audit_id from postflight_audit_gate.py if available, else exact commit hash>
NEXT_PHASE: after commit+push+checkpoint complete, run BOTH commands in order:
  1. python3 scripts/veridian-task.py create --title "gap-closing-phase6-<pick-next-concrete-item-numbers>" --repo claude-control --prompt "<precise spec matching this document's structure, targeting the next 3-5 concrete MISSING/PARTIAL items from ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml or ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml -- exact file paths, exact function signatures, exact test criteria, same structure as THIS document, not narrative continuation language>"
  2. systemctl --user start veridian-worker@<new-task-id-from-step-1-output>.service
STOP_CONDITION: if steps 1-2 above are not both executed before this task's own final checkpoint, that is a FAILURE of this task's own EXPECTED_OUTPUT, not optional

## CONSTRAINTS
NO_NEW_TAGGING_SYSTEM: reuse system_index.tags column (built earlier today, do not rebuild)
NO_PARALLEL_DB_WRITES: sequential only for superboss-register.sqlite
EVIDENCE_REQUIRED: every DONE status change cites file:line or command output, zero exceptions

## COMPLEXITY_TIER
judgment
