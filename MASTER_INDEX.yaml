schema_version: 1
audience: ai_and_software_only
protocol: READ_THIS_FILE_FIRST. Before any grep/find/read across this server or the compliance-tracker/projexa/veda-advisors/claude-control
  repos, before writing any new script/table/register, query this file for an existing match. If a match
  exists, use it or extend it -- do not create a parallel mechanism. If check-duplicate (system_index)
  and this file both show no match, proceed, then register the new thing in system_index (index-add) AND
  in this file's relevant list.
built_ts: '2026-07-30T03:57:58.416644+00:00'
built_by: regenerate_master_index.py (automated, wholesale regeneration each run)
audit_passes_complete: 3
audit_passes_required: 3
canonical_path_live: /opt/veridian/ai-os/MASTER_INDEX.yaml
canonical_path_repo: /opt/veridian/repos/compliance-tracker/ai-os/MASTER_INDEX.yaml
sync_mechanism:
  script: /opt/veridian/scripts/system-sync.py
  run_on_demand: python3 /opt/veridian/scripts/system-sync.py [--dry-run] [--check mirror|constitution|unindexed|resume-balance|all].
    Run this whenever you suspect the live server, this file, CONSTITUTION.yaml, or the git-tracked script
    mirrors have drifted apart -- it is the single entrypoint for "sync the whole system," built 2026-07-20
    per Owner directive ("one server, one system," see registries.system_sync_mechanism for full detail).
  run_on_schedule: Wired into crontab via run-logged.sh (registries.auto_execution_wrapper), see registries.system_sync_mechanism.cron_schedule.
  do_not_build_a_new_sync_script: If a future session finds drift/staleness this script's 3 checks don't
    cover, EXTEND this script with a 4th check function -- do not write a parallel sync mechanism. This
    file's own protocol field exists precisely to prevent that failure mode.
root_cause_confirmed:
  finding: Unscoped filesystem search (find/grep without prune) traverses node_modules trees (per-repo,
    multiple GB) and per-task git-worktree copies of the entire repo under ai-os/tasks/*/workspace/ --
    both contain files with basenames identical to canonical files (CONSTITUTION.yaml, CONTROLLER.yaml
    appear 100+ times each across worktree copies alone).
  incident_evidence: Unscoped `find /opt/veridian -iname CONSTITUTION.yaml` returned 200+ hits, ~198 of
    them ai-os/tasks/*/workspace/ worktree copies, 0 informative. Same command with -prune on the excluded
    paths returned in under 2 seconds with the 1 canonical hit.
  fix: Apply exclusion_rules below on every search. Single highest-leverage fix for search speed and wrong-file
    selection.
exclusion_rules:
  must_prune_not_just_exclude: true
  reason: find -not -path still descends into and stats every excluded directory; only -prune skips the
    subtree, which is the actual speed fix.
  prune_paths:
  - '*/node_modules/*'
  - '*/.git/*'
  - /opt/veridian/ai-os/tasks/*/workspace/*
  - /opt/veridian/workspace/claude-cli-work/*
  - /opt/veridian/workspace/main-e2e-check/*
  correct_find_pattern: find <roots> \( -path '*/node_modules' -o -path '*/.git' -o -path '*/ai-os/tasks'
    -o -path '*/workspace/claude-cli-work' \) -prune -o -type f -iname '<pattern>' -print
search_layers_relationship:
  purpose: 'Added 2026-07-30 (master-index-central-entrypoint-confirmation-phase6) directly answering
    the Owner''s question ''is MASTER_INDEX.yaml THE central place to read all documentation.'' Short
    answer: yes for browsable human-readable narrative/scope, but it is one of 4 complementary layers
    of ONE system, not the only mechanism -- a reader who only sees the top of this file should know that
    before assuming the other 3 are redundant or competing. Full sync-direction contract already existed
    at registries.metadata_engine_reconciliation before this note was added; this block is a header-level
    pointer to it, not a duplicate of its content.'
  master_index_yaml: 'THIS file. The BROWSABLE index -- read it first (see protocol field above) for narrative
    scope/status per registry/system. Hand-maintained: registries[] entries are appended per-task, not
    auto-generated from source; only the initiative-status field and the unregistered_mentions_backlog
    get script-corrected on each regenerate_master_index.py run (see regeneration_mechanism below).'
  knowledge_engine: 'ai-os/memory/superboss-register.sqlite''s knowledge_engine table (registries.self_sustaining_system_engine)
    -- the SEARCHABLE, machine-authoritative layer for drift-detection (source:SERVER/VERCEL/SUPABASE/GITHUB/LOCAL
    tagged rows, each with its own last_verified_ts). Query: `superboss-register.py query-knowledge <term>`.
    Complementary, not competing, with this file: this file stays authoritative for human citation/scope
    narrative; knowledge_engine stays authoritative for machine drift-detection -- see registries.metadata_engine_reconciliation
    for the full, already-enforced sync-direction contract between the two.'
  wiring_registry: ai-os/memory/superboss-register.sqlite's wiring_registry table (registries.wiring_engine)
    -- the CODE-LEVEL ENTITY-RELATIONSHIP GRAPH (engine/gateway/table/function/route/file/script/cron_job/ai_role;
    7791 rows at last regeneration run), mechanically generated from 8 live sources (never hand-authored).
    Answers 'what calls / is-called-by what,' a question neither this file nor knowledge_engine's own
    rows answer.
  system_index: ai-os/memory/superboss-register.sqlite's system_index table -- the fast EXISTENCE-CHECK
    layer ('does X already exist before I build it'), queried via `superboss-register.py check-duplicate`/`search`.
    This is the check-before-build gate this file's own top-level protocol field instructs every reader
    to use before writing any new script/table/register.
  protocol_owner_ai_yaml: 'ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml (registries.owner_ai_protocol)
    -- a different axis entirely: it governs HOW work gets done (execution location, communication cadence,
    quality bars, self-correction rules), not WHERE documentation about a given system lives. Complementary
    to this file, not competing with it.'
  agents_md_note: Per-repo AGENTS.md files (compliance-tracker/projexa/veda-advisors) are AI-agent AUTHORIZATION
    rosters (who may act on that specific repo, with what authority) -- confirmed by direct read 2026-07-30,
    NOT a documentation index of any kind. Not a competing mechanism; a different purpose that happens
    to share the 'read this first' instinct.
  readme_server_md_note: /opt/veridian/README-SERVER.md is a real, complementary infrastructure-ops reference
    (access, specs, security config, recovery runbook) for operating the physical/virtual machine itself
    -- not a 'where do I find docs about system/feature X' index. Different scope, not competing.
  read_order_for_a_new_reader: (1) this file's own protocol field for narrative 'where is X', (2) system_index
    check-duplicate before building anything new, (3) knowledge_engine query-knowledge for machine-verified
    freshness of a specific artifact, (4) wiring_registry for a specific call-graph question. 4 complementary
    layers of ONE system -- not 4 competing places to look.
system_boundary:
  name: VERIDIAN AI OS
  subsystems:
    subsystem_1_software_dev_and_ai:
      description: Software Development & AI System -- this ops server (VERIDIAN-DEV, Hetzner 167.233.220.35),
        the autonomous worker fleet, its own SQLite register, and every mechanism this file itself indexes.
        See registries.system_diagram for the full component diagram.
    subsystem_2_software_and_end_user_management:
      description: Actual Software & End-User Management -- the deployed product (compliance-tracker +
        PROJEXA, treated as one integrated software per earlier Owner directive), running on Vercel with
        a Supabase Postgres backend.
    integration_note: Both subsystems act as ONE integrated system, not two separate ones -- see registries.ops_task_sync_bridge
      (subsystem 1 writes task state into subsystem 2's own database) and registries.app_layer_failure_visibility
      (subsystem 1 reads subsystem 2's failure state directly) for the concrete bridges that make this
      true in code, not just in this document.
  repos:
    core_veridian_and_customer_products:
      note: For running VERIDIAN AI OS and developing customer products/projects.
      members:
      - repo: FChecklist/compliance-tracker
        local_path: /opt/veridian/repos/compliance-tracker
        role: Brain repo -- holds every module and function (per its own CONSTITUTION.yaml control_model.rule).
          The app half of subsystem 2.
      - repo: FChecklist/projexa
        local_path: /opt/veridian/repos/projexa
        role: Construction-industry product branch, governed through compliance-tracker's modules (verified
          via veridian-client.ts + /api/v1/projexa/* calls, see CONSTITUTION.yaml control_model).
      - repo: FChecklist/veridian-ui-kit
        local_path: /opt/veridian/repos/veridian-ui-kit
        role: Shared UI component package (@fchecklist/veridian-ui-kit), 0 runtime deps, consumed by the
          product repos.
      - repo: FChecklist/claude-control
        local_path: /opt/veridian/repos/claude-control
        role: Outermost cross-project requirements/decision catalog (registries.cross_project_controller)
          -- config/YAML only, no application code.
    reverse_engineering:
      note: 'For reverse-engineering features -- these hold scraped documentation of THIRD-PARTY systems
        (Infisuite, Odoo, Zoho), not VERIDIAN''s own application code. Verified 2026-07-20: all 3 repos
        contain only docs/ and screenshots/ directories, no source code -- correctly excluded from the
        code catalogs (registries.database_catalog / registries.function_catalog only cover real app codebases).'
      members:
      - repo: FChecklist/odoo-reverse-engineering
        local_path: /opt/veridian/repos/odoo-reverse-engineering
      - repo: FChecklist/infisuite-reverse-engineering
        local_path: /opt/veridian/repos/infisuite-reverse-engineering
        role: 'Reverse-engineered across bharatnet/indyanet/cityline Infisuite tenants (CRM, Billstack,
          Contracts, Ticketing modules). Updated 2026-07-20: merged 2 fully-documented cityline worker
          branches (CRM+Billstack, Contracts -- both had succeeded earlier the same day but were left
          unmerged on origin/worker/* branches) into main, then closed the 3rd (Ticketing), whose 6 role-account
          logins had all genuinely failed automated auth, by riding the Owner''s already-authenticated
          real Chrome session (Claude in Chrome) to reach it directly -- same real-session-reuse pattern
          that separately unblocked Odoo''s Cloudflare Turnstile gate (see odoo-reverse-engineering/PROGRESS.md).
          Notable finding preserved in docs/cityline-contracts/contracts.md: a reproducible crash bug
          makes the Contract entity''s own list/create/view UI unreachable in that tenant.'
      - repo: FChecklist/zoho-reverse-engineering
        local_path: /opt/veridian/repos/zoho-reverse-engineering
        role: Zoho CRM + Zoho Projects field/screen documentation, built 2026-07-20 by navigating a live
          Owner-authenticated session via Claude in Chrome (read-only -- forms opened to enumerate fields
          then cancelled, no records created/edited/deleted). docs/crm/fields.md and docs/projects/fields.md.
    personal_advisory_not_active:
      note: Personal advisory for Owner Rajat Agarwal. Explicitly NOT currently being worked on (Owner's
        own words, 2026-07-20) -- do not pick up gap-closure work here without a direct instruction to
        resume it.
      members:
      - repo: FChecklist/veda-advisors
        local_path: /opt/veridian/repos/veda-advisors
  repos_outside_the_named_7_and_why:
    note: 3 additional repos are cloned on this server but were NOT named in the Owner's 7-repo list --
      disclosed here rather than silently omitted. See registries.other_repos_classified for the original
      classification (still accurate, re-verified 2026-07-20).
    members:
    - veridian-brain (1-file stub SDK package, not a real product)
    - global-revenue-engine (empty repo, .git only)
    - sumeet-spec (single spec .md file, no code)
self_use_task_checklist:
  purpose: 'The one prompt this session (or superboss) reads before and after every task, regardless of
    size. Generated by ai-os/scripts/generate_task_checklist.py -- re-run after any registries: edit that
    changes one of the paths cited in the evidence fields below, do not hand-edit this block.'
  structure_note: 'Restructured 2026-07-23 (per the "EXECUTION_RULES_AUDIT_2026-07-23" analysis''s roadmap_next_phases.pre_execution_checklist_automation
    -- that analysis was never persisted as a real file on disk, corrected 2026-07-24 task-20260724-033446)
    to mirror VERIDIAN_EXECUTION_RULES_2026-07-23.md Part 39''s A-J, 77-item structure one-for-one. Every
    item carries a status: REAL (a real mechanism backs it today, cited in evidence), NOT_YET_AVAILABLE
    (a real, honestly-documented gap), or NOT_MECHANICALLY_TESTABLE (an inherent judgment/discipline item,
    same honest category ai-os/scripts/veridian_self_check.py already uses for its own rule_5). Nothing
    is silently dropped.'
  sections:
  - letter: A
    title: Recover Context
    items:
    - n: 1
      text: Load the complete task context.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py prints the last N instructions/work_items/actions from
        ai-os/memory/superboss-register.sqlite
    - n: 2
      text: Load the Owner, Organization, and End User memory.
      status: NOT_YET_AVAILABLE
      evidence: no such memory store exists yet -- Parts 33-35 of the (never-persisted-to-disk) "EXECUTION_RULES_AUDIT_2026-07-23"
        analysis named this MISSING, explicitly out of scope for that phase (roadmap deferred it to a
        future task; corrected 2026-07-24, task-20260724-033446, to stop citing a file that was never
        actually written)
    - n: 3
      text: Load previous conversations.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py prints instructions.raw_text rows (closest real analog
        to 'conversations' in this system)
    - n: 4
      text: Load previous commitments.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py queries rca_open WHERE rca_status='OPEN_NEEDS_RCA'
        from ai-os/memory/superboss-register.sqlite
    - n: 5
      text: Load unfinished tasks.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py queries work_items + unregistered_mentions WHERE status='NEEDS_REGISTRATION'
    - n: 6
      text: Load pending approvals.
      status: NOT_YET_AVAILABLE
      evidence: no dedicated pending-approvals store exists distinct from work_items.status / gap_queue.yaml
        status
    - n: 7
      text: Load previous decisions.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py prints work_items/actions history rows (each carries
        a status/result already decided)
    - n: 8
      text: Load previous execution plans.
      status: NOT_YET_AVAILABLE
      evidence: no persisted execution-plan artifact store exists; AI planning-mode output is not written
        back to the register DB
  - letter: B
    title: Search System Knowledge
    items:
    - n: 9
      text: Search metadata.
      status: REAL
      evidence: superboss-register.py search / check-duplicate against /opt/veridian/ai-os/memory/superboss-register.sqlite#system_index
    - n: 10
      text: Search YAML files.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py loads MASTER_INDEX.yaml/SYSTEM_MAP.yaml/STANDING_DIRECTIVE.yaml/MASTER_ARCHITECTURE_2026-07-22.yaml
    - n: 11
      text: Search entity relationships.
      status: REAL
      evidence: system_index.calls / called_by columns (superboss-register.py search)
    - n: 12
      text: Search dependency relationships.
      status: REAL
      evidence: system_index.calls / called_by columns (same mechanism as item 11)
    - n: 13
      text: Search business rules.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py prints STANDING_DIRECTIVE.yaml assistant_working_protocol
        rule_* keys; ai-os/RULES_ARTICLES_198.json
    - n: 14
      text: Search workflows.
      status: REAL
      evidence: ai-os/queues/*.yaml structured task records (grep / check-duplicate)
    - n: 15
      text: Search documentation.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py prints MASTER_ARCHITECTURE_2026-07-22.yaml open_questions_for_owner
    - n: 16
      text: Search scripts.
      status: REAL
      evidence: system_index (category/path fields) via check-duplicate -- see /opt/veridian/ai-os/memory/superboss-register.sqlite#system_index
    - n: 17
      text: Search automation.
      status: REAL
      evidence: system_index rows with category in (dispatch_entrypoint, monitor, audit) + crontab -l
    - n: 18
      text: Search Software modules.
      status: REAL
      evidence: 'MASTER_INDEX.yaml registries: section + system_index'
    - n: 19
      text: Search APIs.
      status: NOT_YET_AVAILABLE
      evidence: no ai-os-layer API registry exists; app-layer API routes are compliance-tracker/projexa
        product code, out of this task's scope
    - n: 20
      text: Search configuration files.
      status: REAL
      evidence: STANDING_DIRECTIVE.yaml / AI_ENGINEERING_POLICY.yaml loaded and printed
    - n: 21
      text: Search cache.
      status: REAL
      evidence: work_items.cache_id / ai_cache_id reference the existing glm-response-cache.sqlite L1
        cache (superboss-register.py search)
    - n: 22
      text: Search logs.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py reads ATTENTION.md-adjacent state + runs file_inventory.py
        for a real diff
    - n: 23
      text: Search audit records.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py queries task_audits (new this phase -- see ai-os/scripts/postflight_audit_gate.py)
    - n: 24
      text: Search history.
      status: REAL
      evidence: ai-os/scripts/session_bootstrap.py queries directive_compliance_runs + work_items/actions
        history
  - letter: C
    title: Reuse Before Create
    items:
    - n: 25
      text: Check if Software already exists.
      status: REAL
      evidence: superboss-register.py check-duplicate <term>
    - n: 26
      text: Check if a script already exists.
      status: REAL
      evidence: superboss-register.py check-duplicate --category <script category>
    - n: 27
      text: Check if automation already exists.
      status: REAL
      evidence: check-duplicate + /opt/veridian/scripts/system-sync.py's own do_not_build_a_new_sync_script
        precedent
    - n: 28
      text: Check if workflow already exists.
      status: REAL
      evidence: check-duplicate + grep across ai-os/queues/*.yaml
    - n: 29
      text: Check if business logic already exists.
      status: NOT_YET_AVAILABLE
      evidence: system_index has no distinct 'business_logic' category yet -- only a same-category or
        keyword proxy check is possible today
    - n: 30
      text: Check if API already exists.
      status: NOT_YET_AVAILABLE
      evidence: same gap as item 19 -- no ai-os-layer API registry
    - n: 31
      text: Check if the solution already exists.
      status: REAL
      evidence: check-duplicate (general free-text form)
    - n: 32
      text: Reuse existing assets whenever possible.
      status: REAL
      evidence: check-duplicate's own verdict field ('STOP -- existing mechanism(s) found, review before
        building') is the real, machine-produced trigger for this
  - letter: D
    title: Understand the Task
    items:
    - n: 33
      text: Understand the business objective.
      status: REAL
      evidence: every ai-os task's prompt.txt has a structured OBJECTIVE field (see this task's own prompt.txt)
    - n: 34
      text: Understand the expected outcome.
      status: REAL
      evidence: prompt.txt's SUCCESS_CRITERIA + EXPECTED_OUTPUT fields
    - n: 35
      text: Identify required inputs.
      status: REAL
      evidence: prompt.txt's KNOWN_CONTEXT field
    - n: 36
      text: Identify required outputs.
      status: REAL
      evidence: prompt.txt's EXPECTED_OUTPUT field
    - n: 37
      text: Identify dependencies.
      status: REAL
      evidence: prompt.txt's KNOWN_CONTEXT field (e.g. this phase's own prior-phase commit references)
    - n: 38
      text: Identify risks.
      status: NOT_YET_AVAILABLE
      evidence: the task prompt schema has no dedicated RISKS field
    - n: 39
      text: Identify constraints.
      status: REAL
      evidence: prompt.txt's CONSTRAINTS field
    - n: 40
      text: Identify edge cases.
      status: NOT_YET_AVAILABLE
      evidence: the task prompt schema has no dedicated EDGE_CASES field
  - letter: E
    title: Break Down the Task
    items:
    - n: 41
      text: Break the task into small steps.
      status: NOT_YET_AVAILABLE
      evidence: no general mid-task step-decomposition mechanism -- master-decompose.py exists but is
        scoped to the Owner-directive-to-task dispatch pipeline only (Part 38a), not a reusable per-task
        tool
    - n: 42
      text: 'Classify each step as: Software / Script / Automation / Workflow / AI / Human Approval.'
      status: NOT_YET_AVAILABLE
      evidence: no coded per-step taxonomy exists; PROGRESS.md is free text, not a schema
    - n: 43
      text: Minimise AI work.
      status: REAL
      evidence: registries.credit_spend_governance gates every AI-costing action ('software first, AI
        second' rule enforced before spend)
    - n: 44
      text: Maximise Software execution.
      status: REAL
      evidence: same mechanism as item 43 (registries.credit_spend_governance)
  - letter: F
    title: Create Execution Plan
    items:
    - n: 45
      text: Prepare the execution plan.
      status: REAL
      evidence: 'PROGRESS.md (## Completed / ## Remaining) is this session''s real, git-committed execution-plan
        record, per phase0/phase1 precedent'
    - n: 46
      text: Validate the execution plan.
      status: NOT_YET_AVAILABLE
      evidence: no automated plan-validation script exists
    - n: 47
      text: Verify required resources.
      status: NOT_YET_AVAILABLE
      evidence: no automated resource-verification mechanism exists
    - n: 48
      text: Verify permissions.
      status: REAL
      evidence: scripts/preflight-guard.py hard-gates on disk/mem/circuit-breaker before invoking the
        model
    - n: 49
      text: Verify dependencies.
      status: REAL
      evidence: system_index.calls/called_by (item 11/12) + scope-check.py's file-ownership.yaml cross-check
    - n: 50
      text: Verify configurations.
      status: NOT_YET_AVAILABLE
      evidence: STANDING_DIRECTIVE.yaml is searched/read (item 20) but not verified against a schema
  - letter: G
    title: AI Decision
    items:
    - n: 51
      text: Can Software complete the task? If YES -> Execute through Software. If NO -> Build reusable
        Software first -> Execute through Software -> Call AI only if required.
      status: REAL
      evidence: registries.credit_spend_governance's own 'software first, AI second' rule is exactly this
        gate
  - letter: H
    title: Verification Before Execution
    items:
    - n: 52
      text: Verify no previous commitment is forgotten.
      status: REAL
      evidence: rca_open check (same mechanism as item 4)
    - n: 53
      text: Verify no existing work is duplicated.
      status: REAL
      evidence: check-duplicate (mandatory, same mechanism as item 25)
    - n: 54
      text: Verify no business rule is violated.
      status: NOT_YET_AVAILABLE
      evidence: ai-os/audit198/run-audit.mjs checks RULES_ARTICLES_198.json post-hoc, not as a pre-execution
        gate
    - n: 55
      text: Verify all required context is loaded.
      status: REAL
      evidence: completion of ai-os/scripts/session_bootstrap.py (sections A+B above)
    - n: 56
      text: Verify execution is safe.
      status: REAL
      evidence: scripts/preflight-guard.py hard gate (disk/mem/circuit-breaker) before model invocation
    - n: 57
      text: Verify approval is available if required.
      status: NOT_YET_AVAILABLE
      evidence: same gap as item 6 -- no approval-queue mechanism
  - letter: I
    title: Execute
    items:
    - n: 58
      text: Execute through Software.
      status: REAL
      evidence: registries.credit_spend_governance + this session's own scripts
    - n: 59
      text: Execute AI only where required.
      status: REAL
      evidence: same mechanism as item 58 (registries.credit_spend_governance)
    - n: 60
      text: Validate outputs.
      status: REAL
      evidence: ai-os/scripts/postflight_audit_gate.py's audit_cmd (real command, real exit code, never
        trusted-by-claim)
    - n: 61
      text: Handle failures.
      status: REAL
      evidence: ai-os/scripts/postflight_audit_gate.py writes a FAILED verdict + a real rca_open row for
        later investigation (logs and flags; does not auto-remediate)
    - n: 62
      text: Retry when appropriate.
      status: REAL
      evidence: worker-entrypoint.sh's own checkpoint/resume mechanism (registries.auto_execution_wrapper)
  - letter: J
    title: Post Execution
    items:
    - n: 63
      text: Update Software.
      status: REAL
      evidence: /opt/veridian/scripts/system-sync.py --check mirror + system_index index-add
    - n: 64
      text: Update scripts.
      status: REAL
      evidence: superboss-register.py index-add (re-verifies path, refreshes verified_ts)
    - n: 65
      text: Update automation.
      status: NOT_YET_AVAILABLE
      evidence: no automated crontab/systemd diff-checker exists; changes are verified manually (crontab
        -l / systemctl)
    - n: 66
      text: Update workflows.
      status: NOT_YET_AVAILABLE
      evidence: ai-os/queues/*.yaml is hand/script-edited case-by-case, no generic workflow-update mechanism
    - n: 67
      text: Update metadata.
      status: REAL
      evidence: superboss-register.py index-add
    - n: 68
      text: Update YAML.
      status: REAL
      evidence: 'MASTER_INDEX.yaml registries: section edit + generate_quick_reference.py re-splice'
    - n: 69
      text: Update relationships.
      status: REAL
      evidence: system_index.calls/called_by fields (same mechanism as items 11/12)
    - n: 70
      text: Update documentation.
      status: REAL
      evidence: commit message (WHY not just WHAT) + PROGRESS.md convention
    - n: 71
      text: Update logs.
      status: REAL
      evidence: superboss-register.py log-work / log-action
    - n: 72
      text: Update audit.
      status: REAL
      evidence: ai-os/scripts/postflight_audit_gate.py's task_audits row + this phase's new execution_log
        row
    - n: 73
      text: Update history.
      status: REAL
      evidence: work_items/actions rows (superboss-register.py)
    - n: 74
      text: Update memory.
      status: REAL
      evidence: ai-os/memory/superboss-register.sqlite writes (all of the above)
    - n: 75
      text: Save reusable knowledge.
      status: REAL
      evidence: system_index registration + MASTER_INDEX.yaml registries entry
    - n: 76
      text: Convert repeatable AI work into Software.
      status: REAL
      evidence: 'this exact phase is one instance: generate_task_checklist.py/superboss-register.py extended
        so future tasks reuse a script instead of re-deriving this checklist by prose'
    - n: 77
      text: Reduce future AI dependency.
      status: REAL
      evidence: same evidence as item 76
  coverage_summary:
    sections_covered: 10
    sections_total_in_part_39: 10
    items_total: 77
    items_real: 60
    items_not_yet_available: 17
    items_not_mechanically_testable: 0
  credit_spend_reminder: Every AI-costing action referenced above (items 43/44/51/58/59) is gated by registries.credit_spend_governance
    -- see that entry for the full mechanism. This checklist does not duplicate that gate's own rules,
    only points to it.
canonical_dirs:
  ops_scripts:
    path: /opt/veridian/scripts/
    contents: Shell-layer worker fleet entrypoints, preflight/guardrail scripts, dispatchers, register
      CLI. See file_inventory.scripts.
  ai_os_governance_repo_copy:
    path: /opt/veridian/repos/compliance-tracker/ai-os/
    contents: Git-tracked canonical governance/tracker/tree docs. This is the copy to read/edit -- ai-os/tasks/*/workspace/ai-os/
      copies are ephemeral per-task worktrees, never edit those.
  ai_os_live_runtime_state:
    path: /opt/veridian/ai-os/
    contents: Non-git runtime state -- dispatched task dirs (tasks/), the live task-state ledger (CONTROLLER.yaml,
      see registries.task_state_ledger), cron-fed queues (gap_queue.yaml, queues/), the register DB (memory/),
      response cache (logs/), alerts (logs/ATTENTION.md).
  cross_project_controller_repo:
    path: /opt/veridian/repos/claude-control/
    contents: The real CONTROLLER.yaml (registries.cross_project_controller). Separate GitHub-synced repo,
      one level above every individual project.
  ai_os_logs:
    path: /opt/veridian/ai-os/logs/
    contents: Operational append-only logs and one live alert surface (see registries.attention_alerts).
      Not registers -- do not search these for "does X exist," only for historical/incident evidence.
    files_at_build:
    - ATTENTION.md
    - cost-usage-60min.jsonl, cost-usage-60min.log, cost-usage-cron.log
    - glm-proxy-calls.jsonl, glm-proxy.log
    - glm-response-cache.sqlite
    - health-15min.jsonl, health-15min.log, health-check-cron.log
    - reconciliation-alerts.jsonl
    - superboss-supervision-start.txt
quick_reference:
- id: system_index
  status: live
  path: /opt/veridian/ai-os/memory/superboss-register.sqlite#system_index
  summary: Existence-check for real mechanisms (scripts/services/tables/docs) across the whole ops layer
    -- "does X already exist" before building.
- id: superboss_dialogue_log
  status: live
  path: /opt/veridian/ai-os/memory/superboss-register.sqlite#instructions,work_items,actions
  summary: Owner<->Superboss instruction/work/action audit trail. Not reachable from TypeScript/Vercel
    app code (different host, no bridge built)....
- id: auto_conversation_log
  status: live, deployed and verified 2026-07-20
  summary: 'Root-cause fix for "you not logging every discussion" (Owner, 2026-07-20). Confirmed the gap
    first, not assumed: a direct query before this...'
- id: auto_worker_task_log
  status: live, deployed and verified 2026-07-20
  path: /opt/veridian/scripts/veridian-task.py
  summary: 'The server-side half of the auto-logging fix. Owner: "for your work that CLAUDE CODE CLI or
    CLAUDE CODE is doing and ai superboss, ai...'
- id: auto_execution_wrapper
  status: live, deployed and verified 2026-07-20 (all 3 auto-logging...
  path: /opt/veridian/scripts/run-logged.sh
  summary: 'Closes the honest gap stated in registries.auto_worker_task_log and registries.auto_conversation_log:
    the periodic cron jobs that never...'
- id: worker_fleet_rca_fix_2026_07_20
  status: live, deployed and verified 2026-07-20
  summary: Root-causes and permanently fixes the 47-failed-unit/71.9%-task- failure-rate incident surfaced
    by registries.auto_execution_wrapper's...
- id: system_sync_mechanism
  status: live, deployed and verified 2026-07-20 (real first run...
  path: /opt/veridian/scripts/system-sync.py
  summary: TASKS 2-6 of the Owner's 2026-07-20 systems-design directive, built as ONE unified mechanism
    deliberately, not 5 separate scripts -- the...
- id: ops_task_sync_bridge
  status: code complete, deployed to DB + PR open, live E2E...
  summary: 'TASK 1.1, Owner directive 2026-07-20 ("biggest structural gap: the app''s task-tracking and
    this server''s task-tracking are on two different...'
- id: system_diagram
  status: live, generated, committed 2026-07-20
  path: /opt/veridian/scripts/generate-system-diagram.py
  summary: TASK 3, Owner directive 2026-07-20 ("make a diagram or tree of the whole system"). Software-generated
    per the directive's own "modern...
- id: e2e_ci_playwright_fix_2026_07_20
  status: live, merged, verified in real CI 2026-07-20
  path: /opt/veridian/repos/compliance-tracker/playwright.config.ts
  summary: 'Spawned background task, flagged during this session''s own audit of TASK 5-6 work: the E2E
    Tests CI job crashed with MODULE_NOT_FOUND at...'
- id: app_layer_failure_visibility
  status: live, deployed, verified 2026-07-20
  summary: TASK 2/4, Owner directive 2026-07-20 ("if task fails (any task, ai task, software task), server
    to know, log it") + ("make the complete...
- id: ai_router_migration_inventory_2026_07_20
  status: investigated and documented, migration deliberately...
  summary: TASK 1.3, Owner directive 2026-07-20 ("AI router has deliberately- unmigrated call sites...
    FIX this issue permanently"). Re-verified...
- id: gap_queue_dispatch_hold
  status: live, deployed and verified 2026-07-20 (queue-dispatcher.py...
  path: /opt/veridian/ai-os/gap_queue.yaml (live-only, not git-tracked -- same category as CONTROLLER.yaml)
  summary: 'TASK 5, Owner directive 2026-07-20: analyze the 185 genuinely-open Review-Framework rows (consolidated
    as 25 V2-* tasks in this queue), do...'
- id: cross_project_controller
  status: live
  path: /opt/veridian/repos/claude-control/CONTROLLER.yaml
  summary: Outermost requirements+decision catalog, spans every project (compliance-tracker, projexa,
    veda-advisors, claude-control itself). One level...
- id: task_state_ledger
  status: live
  path: /opt/veridian/ai-os/CONTROLLER.yaml
  summary: DIFFERENT FROM cross_project_controller despite identical basename -- this is a locked, auto-generated
    dump of this server's 291 dispatched...
- id: constitution
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/CONSTITUTION.yaml
  summary: Canonical governance doc. 30 self-graded guardrail protocols (GP-01..GP-30) with code citations.
    Explicitly the single status table for...
- id: master_tracker
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/MASTER-TRACKER.yaml
  summary: The single file for gap analysis / pending work / WIP across the VERIDIAN initiative. Created
    2026-07-12 by consolidating 17 prior...
- id: gap_dispatch_queue
  status: live
  path: /opt/veridian/ai-os/gap_queue.yaml
  summary: Live cron-dispatched queue (1634 lines at build time). Distinct from master_tracker -- this
    is dispatch-mechanics state (what's...
- id: module_pilot_queues
  status: live, pilot scope (backend.yaml exists; only...
  path: /opt/veridian/ai-os/queues/*.yaml
  summary: Module-scoped task queues for the Master/Supervisor pilot (master-decompose.py writes, module-queue-dispatcher.py
    reads). Explicitly reuses...
- id: active_claims
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/ACTIVE-CLAIMS.yaml
  summary: Parallel-session work-claim coordination -- read before picking up any gap to avoid 2 sessions
    building the same thing concurrently....
- id: boss_board
  status: STALE. Self-flagged in its own header -- stops at...
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/BOARD.yaml
  summary: Original ComplianceTrack task board.
- id: boss_identity
  status: live, last content-verified 2026-07-09
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/BOSS.yaml
  summary: Not a tracker -- role/identity config for VEDABOSS (architect role, ComplianceTrack). Different
    purpose from boss_board despite shared...
- id: boss_roster
  status: live, last content-verified 2026-07-09
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/ROSTER.yaml
  summary: Authorized coding agents (VEDABOSS/ZAI/SENTINEL) and roles.
- id: boss_completed_rollup
  status: partial (entries live, rollup fields stale)
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/COMPLETED.yaml
  summary: Two things in one file -- a completed_count/last_completed rollup (stale, stuck 2026-06-28)
    and a separate entries list of WAVE-*...
- id: sentinel_health
  status: STALE. last_check 2026-07-09 (11 days at build time), not...
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/HEALTH.yaml
  summary: CI/branch health spot-check.
- id: sentinel_violations
  status: STALE. last_scan 2026-07-09, explicitly self-described as...
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/VIOLATIONS.yaml
  summary: Secret/.env spot-check log.
- id: sentinel_rules
  status: unspecified
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/SENTINEL.yaml
  summary: Original rule definitions (AR-*, AC-* etc). Superseded in authority by constitution for guardrail
    grading (2026-07-14), but the rule...
- id: lifecycle_state
  status: live, corrected 2026-07-09 (was stuck at a stale stage)
  path: /opt/veridian/repos/compliance-tracker/ai-os/LIFECYCLE.yaml
  summary: Single current_stage marker (MONITOR) for the whole initiative.
- id: os_entrypoint
  status: unspecified
  path: /opt/veridian/repos/compliance-tracker/ai-os/OS.yaml
  summary: Pre-existing "start here" stub for the ai-os/ tree. Rewritten 2026-07-13 after being found
    stale/wrong (false Prisma claim). This...
- id: engine_registry
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/engines/ENGINES.yaml
  summary: VCEL deterministic-computation engine registry (247 engines catalogued, 41 implemented per
    prior session record).
- id: file_ownership_rules
  status: live
  path: /opt/veridian/ai-os/file-ownership.yaml
  summary: Deterministic module-ownership map, cross-checked (not trusted blindly) against AI-declared
    module labels by master-decompose.py.
- id: artifact_registry
  status: NOT_POPULATED. Explicitly a scaffold, not live data. Real...
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/ARTIFACTS.yaml
  summary: Universal Asset Registry scaffold (VERIDIAN.docx Principle 16).
- id: asset_registry_coverage
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/asset-registry-coverage.yaml
  summary: CI-enforced manifest -- every src/lib/db/schema.ts table must appear in exactly one of registered/exempted.
    Not the same thing as...
- id: stale_doc_manifest
  status: live
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/stale-doc-manifest.yaml
  summary: Root-level doc quarantine-banner enforcement list, tied to master_tracker's GAP-UNIFIED-SOT-REMAINDER
    item.
- id: database_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/ai-os/DATABASE_CATALOG.json
  summary: EXHAUSTIVE, mechanically-generated catalog of every table (444), every column per table (name/type/nullability/default/PK/unique/...
- id: function_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/ai-os/FUNCTION_CATALOG.json
  summary: 'EXHAUSTIVE, mechanically-generated catalog of every named function in compliance-tracker''s
    src/ tree (5,019 total): top-level function...'
- id: projexa_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/repos/projexa/ai-os/DATABASE_CATALOG.json, /opt/veridian/repos/projexa/ai-os/FUNCTION_CATALOG.json
  summary: PROJEXA has its own real codebase (371 src/ files) AND its own local database schema (11 tables,
    0 enums -- notifications, chat,...
- id: veda_advisors_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/repos/veda-advisors/ai-os/FUNCTION_CATALOG.json
  summary: veda-advisors has its own real codebase (62 src/ files, 270 functions). No schema.ts found
    -- uses src/lib/db.ts (a Supabase client, not a...
- id: veridian_ui_kit_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/repos/veridian-ui-kit/ai-os/FUNCTION_CATALOG.json
  summary: Small shared UI component package (@fchecklist/veridian-ui-kit, 0 runtime deps, 22 src/ files,
    46 functions). No database.
- id: other_repos_classified
  status: classified, not catalogued (correctly out of scope)
  summary: 'Remaining repos under /opt/veridian/repos/ classified 2026-07-20, no catalog needed (not application
    codebases): veridian-brain...'
- id: ai_roster_catalog
  status: live, generated 2026-07-20
  path: /opt/veridian/ai-os/AI_ROSTER_CATALOG.json
  summary: The "what AI models/roles exist and what they do" half of the Owner's original ask -- previously
    only covered by prose governance docs...
- id: attention_alerts
  status: live
  path: /opt/veridian/ai-os/logs/ATTENTION.md
  summary: Live alert surface. Written by health-check-15min.py (systemd unit failures, stalled checkpoints,
    Mother Router DB reachability, disk/mem,...
registries:
  # --- Carried forward from claude-control's own MASTER_INDEX.yaml during the
  # 2026-08-02 reconciliation (PM decision UMR-20260802-083104-5987, investigation
  # UMR-20260802-080051-6e48): these 17 entries existed only in claude-control's
  # copy, not here -- each individually reviewed for real current validity (not a
  # blind carry-over), none found stale/superseded. See
  # ai-os/MASTER_INITIATIVE_TRACKING_2026-08-02.md for the full review.
- id: browser_automation_profile
  type: browser_install_and_persistent_profile
  path: ai-os/BROWSER_AUTOMATION_PROFILE_2026-07-25.yaml + scripts/browser/persistent-profile.js
    + scripts/browser/smoketest.js + /opt/veridian/browser/
  scope: 'task-20260725-053540-chrome-browser-persistent-profile, instruction INS-20260725-054500:
    installed Google Chrome stable (150.0.7871.186) user-space at /opt/veridian/browser/chrome (no
    root/apt on this box, extracted via dpkg-deb -x, reusing the doc-worker''s existing
    /opt/veridian/workspace/browser-tools/local-libs shared-library extraction plus a small
    extra-libs/ set), created /opt/veridian/browser/{chrome,profile,downloads,uploads,cache,
    screenshots,recordings,temp} with profile/ locked to 700 rajat-only, and verified the
    persistent-profile + headless mechanism end to end (google-chrome --headless=new
    --user-data-dir=/opt/veridian/browser/profile --dump-dom https://example.com/, exit 0, real
    DOM). Wired a Playwright helper (scripts/browser/persistent-profile.js) against the SAME
    playwright install doc-worker-entrypoint.sh''s Playwright MCP server already uses
    (/opt/veridian/workspace/browser-tools, "playwright": "^1.62.0-alpha-1783623505000") -- no
    second parallel install -- with a real smoke test (scripts/browser/smoketest.js,
    launchPersistentContext -> navigate -> extract, real run against https://example.com).
    Also built a hardened, on-demand-only VNC path (TigerVNC, user-space install at
    /opt/veridian/browser/vnc/root, bound to 127.0.0.1 only, SSH-tunnel-reachable, torn down
    by default) for the Owner''s one-time manual Google login -- see
    /opt/veridian/browser/vnc/VNC_LOGIN_PROCEDURE.md. Per the hard constraint that no
    automated process may ever type a Google password/2FA code, that login has NOT been
    completed by this task (BROWSER_AUTOMATION_PROFILE_2026-07-25.yaml meta.login_status:
    NOT_YET_COMPLETED) and this registry entry must be updated once the Owner completes it.'
  status: infrastructure_complete_owner_login_pending
  query_command: python3 scripts/superboss-register.py query-knowledge "browser_automation_profile"
    --tag domain:browser_automation_profile
  next_phase: 'Owner completes the one-time login per VNC_LOGIN_PROCEDURE.md; then update
    BROWSER_AUTOMATION_PROFILE_2026-07-25.yaml login_status and re-verify session reuse via
    scripts/browser/smoketest.js-style checks against a real Google-authenticated page.'

- id: cross_system_verification_summary_2026_07_26
  type: cross_system_verification_and_gap_report
  path: ai-os/MASTER_INDEX.yaml (this entry -- per this task's own CONSTRAINTS, no
    new tracking file created; findings folded into the existing entries they
    correct, this entry is only the required dated roll-up)
  scope: 'Task-20260726-154348''s own real, live-command verification pass across
    Server (this repo + compliance-tracker on-disk state) + Supabase (MCP) + Vercel
    + GitHub, run after PR #84/#86/#567/#79/#80(open)/#82 -- confirming what genuinely
    changed since the session''s own prior census (registries.vercel_github_state_census_2026_07_26)
    and engine inventory (registries.engines_gateways_architecture), and finding/fixing
    2 real, previously-undetected mis-citations plus 4 stale PR/catalog statuses.
    Not a rebuild of either prior artifact -- a fresh, independent re-check that
    updated them in place where they had drifted.'
  status: real_findings_2026_07_26
  engine_coverage_summary: 'Per registries.engines_gateways_architecture (re-confirmed,
    not re-run from scratch by this task -- its own 2026-07-26 reverification stands):
    20/20 named engines have real prior art (0 NONE), 1/20 full coverage (Knowledge
    Engine), 19/20 partial. All 8 phases (0-7) plus gateway_definition_and_inventory
    are status: done. 3 Owner-decision items remain the real blocker to further
    engine-coverage progress (webhook-receiver-network-exposure, pending;
    supabase-management-api-token-refresh and ui-composition-analytics-engine-scope-confirmation,
    both awaiting_owner_decision) -- unchanged by this task, not this task''s to resolve.'
  registries_verified_summary: 'This repo''s MASTER_INDEX.yaml carries 111 real entries
    under registries: (plus 42 short-form entries under quick_reference:, confirmed
    a by-design summary/detail split of a subset of the same ids, not duplication --
    zero duplicate ids exist within registries: itself, checked directly against the
    parsed YAML). `python3 scripts/metadata_reconciliation.py report`, re-run fresh
    by this task, reads the separate, non-git live-runtime copy at
    /opt/veridian/ai-os/MASTER_INDEX.yaml (97 registries -- fewer than this git-tracked
    copy''s 111 because the live-runtime copy lags several of this session''s own
    merges, itself a real, minor, already-documented-below finding) and reports
    registries_no_path_field=6, matching the exact 6 by-design no-path ids
    METADATA_ENGINE_RECONCILIATION_2026-07-24.yaml''s known_limitations already
    names, plus exactly 1 non-file-only entry (audit198_framework''s directory path,
    also already documented as a deliberate exclusion in that same known_limitations
    list) -- 0 new, unexplained path failures beyond what was already documented.'
  new_findings_and_remediation_2026_07_26:
  - finding: 'registries.audit198_framework''s path field said ai-os/scripts/audit198/
      (does not exist) while ai-os/STANDING_DIRECTIVE.yaml was already corrected to the
      real ai-os/audit198/ on 2026-07-22 -- a 4-day-old, real cross-file drift this
      file itself never got fixed for.'
    remediation: 'FIXED by this task -- path and the embedded run command in
      registries.audit198_framework corrected to ai-os/audit198/.'
  - finding: 'registries.engine_registry has always cited ai-os/engines/ENGINES.yaml
      as the "247-engine VCEL registry" -- that file is actually a Z.ai/Claude-Code
      agent-authorization grant (confirmed via git log --follow: one unrelated commit,
      ever) and always has been, not real path drift over time. The real VCEL registry
      (~247 functions across 25 files) is compliance-tracker/src/lib/engines/*.ts, per
      ai-os/system-tree/10-compliance-tracker-governance.yaml#GOV-07. Distinct from,
      and not a collision with, registries.engines_gateways_architecture -- that
      non-collision (SCOPE item 2''s own premise) still holds; this is a mis-citation
      inside engine_registry itself, not a collision between two registries.'
    remediation: 'FIXED in this repo''s copy by this task (path + scope text
      corrected, correction note added). compliance-tracker''s own ai-os/MASTER_INDEX.yaml
      carries the byte-for-byte identical wrong path/summary -- outside this repo''s
      boundary to edit directly; recommend a small follow-up task in that repo mirror
      this same correction.'
  - finding: 'registries.ddl_preflight_authorization_gate (PR #79) and
      registries.owner_engine_task_gateway_integration (PR #82) both still recorded
      "PR open" states that this task''s fresh gh api calls found were already MERGED
      (2026-07-26T11:59:10Z and 2026-07-26T11:02:41Z respectively) -- both merges
      landed after registries.vercel_github_state_census_2026_07_26 ran the same day
      but before this task started.'
    remediation: 'FIXED -- both entries updated to MERGED with merge commit/timestamp
      evidence and a direct on-disk confirmation (scripts/ddl_authorization_check.py
      and the scripts/prompt_gateway/gateway.py wiring are both live on this repo''s
      current master/HEAD).'
  - finding: 'registries.database_catalog''s live-runtime path
      (/opt/veridian/ai-os/DATABASE_CATALOG.json) and compliance-tracker''s own
      on-server clone both still read table_count=444/enum_count=124 even though
      compliance-tracker PR #567 (merged 2026-07-26T10:40:04Z) regenerated the real
      count to 449/124. The on-server compliance-tracker clone is checked out to a
      stale branch 7 commits behind origin/main (PR #567''s merge commit not in its
      ancestry), and the non-git live-runtime copy under /opt/veridian/ai-os/ syncs
      from git on its own schedule, not instantly on merge.'
    remediation: 'NOT fixed by this task (both are live-server state outside this
      repo''s own git history, and CONSTRAINTS forbid building/running a new sync
      mechanism) -- recorded with full evidence in registries.database_catalog''s own
      regen_2026_07_26 field; recommend the next real edit to that compliance-tracker
      clone first fast-forward it to origin/main, and that the live-runtime sync
      mechanism (registries.system_sync_mechanism) be checked for why this lag
      exists if it recurs.'
  policy_gate_fragmentation_cross_reference: 'This task''s own re-check of the 4 new
    gates/integrations this session (DDL gate PR #79 now merged, write-gate PR #80
    still open, OWNER_ENGINE integration PR #82 now merged, lifecycle fix PR #84
    already merged) did not find any NEW disconnected policy/gate mechanism beyond
    the 3 (preflight-guard.py, policy-enforcement-engine.ts, ddl_authorization_check.py)
    registries.engines_gateways_architecture''s own phase_2_scope_judgment_2026_07_26
    already identified and judged in-scope for the parallel Phase 2 policy-unification
    task. Per this task''s own CONSTRAINTS, that fragmentation finding is
    cross-referenced here, not re-solved -- see
    registries.engines_gateways_architecture''s phase_2_scope_judgment_2026_07_26 for
    the actual remediation recommendation (port ddl_authorization_check.py onto
    policy_decision.py''s shared schema).'
  supabase_verification_2026_07_26: 'Fresh, read-only `SELECT COUNT(*) FROM
    drizzle.__drizzle_migrations` against the live compliance-tracker Supabase
    project (pcrjmlpuqsbocqfwoxod), run directly by this task via the Supabase MCP:
    261 rows. Matches compliance-tracker PR #563/#569''s own claimed count exactly --
    no drift found. PR #563 itself remains open (not yet merged into compliance-tracker
    main) as of this task''s check; PR #569 (a separate, already-merged PR) resolved
    a merge conflict on PR #563''s own branch and re-confirmed the same count.'
  github_branch_protection_verification_2026_07_26: 'Fresh `gh api
    repos/FChecklist/<repo>/branches/<default>/protection/required_status_checks`
    for the 2 repos where it is queryable on the current GitHub Free plan
    (compliance-tracker, veda-advisors -- the other 5 repos remain 403 UNVERIFIABLE,
    unchanged, per registries.vercel_github_state_census_2026_07_26''s own
    plan_tier_evidence): compliance-tracker''s contexts list and veda-advisors''
    empty list both match exactly what that census already recorded -- 0 drift found
    for either of the 2 mechanically-checkable repos.'
  cross_system_single_source_confirmation: 'Server (this repo + compliance-tracker),
    Supabase (drizzle.__drizzle_migrations + DATABASE_CATALOG.json), Vercel (3
    projects), and GitHub (7 repos) are each independently re-verifiable via a live
    command cited directly from this one file (see verification_commands under
    registries.vercel_github_state_census_2026_07_26, generator_script under
    registries.database_catalog, and query_command under most other entries) --
    MASTER_INDEX.yaml itself is the one real cross-reference point for all 4 systems
    as of this task''s run, not 4 separately-tracked systems each with their own
    entrypoint doc. This was already true before this task (the prior census/catalog
    tasks built it that way); this task''s contribution is confirming it still holds
    and correcting the 6 stale entries found above so the single source stays
    trustworthy rather than silently drifting.'
  query_command: python3 scripts/superboss-register.py query-knowledge "cross_system_verification_summary_2026_07_26"
    --tag registry-id:cross_system_verification_summary_2026_07_26

- id: ddl_preflight_authorization_gate
  type: policy_engine_gate
  path: scripts/ddl_authorization_check.py
  scope: 'Real incident-driven fix, 2026-07-26: task-20260726-071400''s own dispatch
    prompt authorized live DROP TABLE/CREATE TABLE/ALTER TABLE/CREATE INDEX
    statements against the production Supabase project via the Supabase MCP
    apply_migration tool, executed entirely before any PR/CI/human review -- the
    worker did exactly what its prompt authorized; the real gap was that nothing
    checked what a dispatch prompt itself is allowed to authorize. Fixed by a new
    dispatch-time gate chained into scripts/task-gateway.py''s cmd_start()
    immediately after tight_task_validation.py: any prompt-file referencing a
    DDL-capable Supabase MCP tool or a DDL/DCL keyword is dispatch-blocked unless it
    also contains a `PRE-APPROVED-LIVE-DDL: <real citation>` line, and (round-3
    hardening, real gap found and fixed) that citation is verified for real
    EXISTENCE on disk (a KE-<date>-<time>-<hex> ID or an
    OWNER_DECISIONS_NEEDED_<date>.yaml#<id> reference), not accepted on string shape
    alone. Documented as protocol article A16_ddl_authorization_gate in
    ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml. Architecturally this IS a Policy
    Engine (5) gate in the same shape Phase 2 already unified (deterministic
    pre-execution allow/deny, same task-gateway.py insertion point as
    tight_task_validation.py) -- see registries.engines_gateways_architecture''s own
    phase_2_scope_judgment_2026_07_26 for the judgment that this one, unlike the
    other 3 new items this session, should be folded into Phase 2''s unification as
    a follow-up (it does not yet emit scripts/policy_decision.py''s shared schema).'
  status: 'MERGED -- re-verified live via `gh api repos/FChecklist/claude-control/pulls/79`
    by an earlier task, 2026-07-26: state=closed, merged=true, merged_at=2026-07-26T11:59:10Z
    (commit 970ac5e). A prior registry entry recorded this as "PR open, CONFLICTING";
    that was already stale by the time this task ran. Confirmed live in this repo''s
    current master/HEAD: scripts/ddl_authorization_check.py exists and
    scripts/task-gateway.py references it (DDL_AUTHORIZATION_CHECK constant + dispatch-time
    call), both re-checked directly by that task, not assumed from git log alone.
    Independently re-confirmed by task-20260726-154345-phase-2--policy-rule-decision-engine-uni''s
    own `git log --oneline` re-check, 2026-07-26: "Merge pull request #79 ...
    add-ddl-pre-flight-authorization-gate-to". Previously-reported CONFLICTING/pending-review
    state above is stale, superseded by this line, not corrected in place (same "append, do
    not silently rewrite" discipline this file uses elsewhere). The one real gap this entry
    itself flagged -- "does not yet emit scripts/policy_decision.py''s shared schema" -- is
    now closed by that same task: see registries.policy_gate_registry /
    ai-os/POLICY_GATE_REGISTRY_2026-07-26.yaml for the real port (additive `policy_decision`
    key, all 28 existing tests unmodified and passing).'
  query_command: python3 scripts/superboss-register.py query-knowledge "ddl_preflight_authorization_gate"
    --tag registry-id:ddl_preflight_authorization_gate

- id: interactive_session_write_gate
  type: operator_shell_guard
  path: ai-os/OWNER_DIRECTIVES/interactive-session-guard.bashrc-snippet
  scope: 'Real incident-driven fix, 2026-07-26: an interactive session ran `gh pr
    merge`/direct `git push` against master outside the automated pipeline. Adds
    `gh`/`git` bash function wrappers (deployed to the Owner''s own shell profile on
    VERIDIAN-DEV, ~/.bashrc + ~/.profile, per the PR''s own test plan) that block
    `gh pr merge` / `git push` to master/main unless the shell carries an
    INVOCATION_ID env var (the real signal of a non-interactive/automated
    invocation) -- a real technical guard, not just a documented rule. Documented as
    protocol article A15_interactive_session_write_gate in
    ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml. Operates entirely at the shell
    level, outside the Python task-dispatch pipeline -- NOT a policy_decision_schema
    gate (see registries.engines_gateways_architecture''s own
    phase_2_scope_judgment_2026_07_26 for why this is deliberately excluded from
    Phase 2''s policy-unification scope). Real gap found and fixed during this PR''s
    own verification: `bash -lc ''...''` (login, non-interactive) does not source
    ~/.bashrc by default, which would have silently bypassed the guard.'
  status: 'PR open, pending review -- PR #80 (FChecklist/claude-control), re-verified
    live via `gh api repos/FChecklist/claude-control/pulls/80` by this task,
    2026-07-26: state=open, mergeable=true, mergeable_state=clean -- the CONFLICTING
    state a prior entry recorded has been resolved (rebased) since, real current
    blocker is review/merge decision only, not a git conflict. NOT yet merged into
    this repo''s tracked history -- confirmed by direct file-absence check,
    ai-os/OWNER_DIRECTIVES/interactive-session-guard.bashrc-snippet does not exist
    in this repo''s working tree. The bashrc snippet itself is reported as already
    deployed live to the Owner''s own shell profile on VERIDIAN-DEV directly (per the
    PR''s own test plan), which is a separate action from this repo''s git history and
    does not substitute for merging the PR.'
  query_command: python3 scripts/superboss-register.py query-knowledge "interactive_session_write_gate"
    --tag registry-id:interactive_session_write_gate

- id: module_gap_audit
  type: module_scoped_manual_bridge_audit_extension
  path: ai-os/MODULE_GAP_AUDIT_2026-07-25.yaml + ai-os/MODULE_GAP_AUDIT_MODULE_LIST_2026-07-25.yaml
    + scripts/module_gap_audit_lib.py + scripts/generate_module_gap_audit_module_list.py
    (extends registries.chatgpt_audit_workspace''s scripts/generate_chatgpt_audit_request.py
    and scripts/ingest_chatgpt_audit_response.py with a --module/--repo mode -- same files, no
    parallel scripts)
  scope: 'task-20260725-063204-module-scoped-zai-gap-audit: extends the domain-scoped
    ChatGPT/Z.AI manual-bridge audit (proven live for the Security domain, both ChatGPT and
    Z.AI -- AUDIT-000008/9/10) to real MODULE-scoped requests covering compliance-tracker and
    projexa, so the Owner can have Z.AI''s free chat engine analyze every real module''s
    function, business logic, and input/output for gaps. Zero schema/guard/versioning changes
    -- reuses ai-os/CHATGPT_AUDIT_RECORD_SCHEMA_2026-07-24.yaml, scripts/chatgpt_audit_guard.py,
    scripts/chatgpt_audit_versioning.py, and the schema''s existing ''Modules'' domain subfolder
    exactly as-is. Module boundaries checked for an existing registry first per KNOWN_CONTEXT:
    compliance-tracker has one (compliance.module_registry, a real Postgres table seeded via
    ~30 drizzle/*.sql migrations, 110 real modules, parsed mechanically -- not hand-typed);
    projexa does not (confirmed live), so its modules are mechanically derived from
    src/app/api/<x>/ directory structure (79 real directories) per KNOWN_CONTEXT''s sanctioned
    fallback. File discovery per module unions filename-substring + `git grep -lI` content
    match on snake/kebab/camelCase identifier variants, narrows SQL matches to schema-defining
    migrations, and excludes monolithic shared files (e.g. the 10709-line src/lib/db/schema.ts)
    from auto-discovery. Requests are chunked to a ~400-line/~20KB reference ceiling (whole
    files only, never truncated -- oversized modules split into numbered part files instead).
    First real batch generated this task: 5 request files across 5 real modules (3
    compliance-tracker, 2 projexa) under /opt/veridian/chatgpt-audit/_pending_requests/.
    MODULE_GAP_AUDIT_MODULE_LIST_2026-07-25.yaml (machine-generated, re-run
    scripts/generate_module_gap_audit_module_list.py to refresh) records the real, mechanically-
    computed totals: 189 real modules across both repos, 1195 real request files needed for
    full coverage -- 1190 still remaining after this task''s 5-file first batch.'
  status: infrastructure_live_first_5_modules_generated_1190_of_1195_request_files_remaining
  query_command: 'python3 scripts/generate_chatgpt_audit_request.py --module <module_key> --repo
    <compliance-tracker|projexa> ; python3 scripts/ingest_chatgpt_audit_response.py --module
    <module_key> --repo <repo> --file <response-file> ; python3 scripts/superboss-register.py
    query-knowledge "module_gap_audit" --tag domain:module_gap_audit'
  next_phase: 'Owner works through the first 5 generated request files (see
    MODULE_GAP_AUDIT_2026-07-25.yaml owner_procedure), or a future task batch-generates more of
    the 1190-request-file remaining backlog listed per-module in
    MODULE_GAP_AUDIT_MODULE_LIST_2026-07-25.yaml -- not part of this registry entry''s own
    scope (CONSTRAINTS explicitly caps this task''s batch size).'

- id: owner_engine_document_accuracy_convergence
  type: accuracy_convergence_test_and_result
  path: ai-os/OWNER_ENGINE_DOCUMENT_ACCURACY_CONVERGENCE_2026-07-25.yaml
  scope: >
    Task 1 of 2 (task-20260725-104606, Owner directive 2026-07-25): a real,
    iterative accuracy-convergence test of OWNER_ENGINE
    (scripts/prompt_gateway/gateway.py) against the real Owner-supplied
    document VERIDIAN_Architecture_v2.0.docx -- OWNER_ENGINE had never been
    run against document-scale input before (its only real test fixtures
    were short chat sentences). RUN 1 (unmodified engine) measured a real
    93.94% gap (single-category forcing, template-regex garbling, entity
    list hard-capped at 5, no numeric-fact extraction, table content
    entirely discarded, and a context-window bug that echoed the full raw
    document back into the output). Built a real document-mode engine
    feature (new scripts/prompt_gateway/engine/document_engine.py +
    classifier.py/gateway.py changes) that detects document-scale input and
    preserves real structure (headings, tables, per-section classification,
    uncapped entity extraction with numeric-fact patterns) instead of
    forcing it through the single-sentence compiler. RUN 2 and RUN 3
    (confirmation) both measured 0.0% gap against a fixed, reproducible
    165-item gold-standard content-coverage checklist
    (scripts/owner_engine_convergence/{build_gold_items.py,
    measure_accuracy.py,gold_items.json}). Extracted document text is
    committed at ai-os/owner-attachments/VERIDIAN_Architecture_v2.0_extracted.txt.
    Task 2 (the phased implementation the document itself describes) is
    explicitly OUT OF SCOPE of this registry entry and was not started --
    this convergence result is the Owner's own hard gate before Task 2 may begin.
  status: converged_gap_0.0pct_confirmed_2_consecutive_runs
  query_command: python3 scripts/superboss-register.py query-knowledge "owner_engine_document_accuracy_convergence" --tag domain:owner_engine_document_accuracy_convergence
  reproduce: >
    python3 scripts/prompt_gateway/gateway.py --mode file --input
    <(python3 -c "import json;print(json.dumps({'text':
    open('ai-os/owner-attachments/VERIDIAN_Architecture_v2.0_extracted.txt').read()}))")
    --output /tmp/result.json ; python3 scripts/owner_engine_convergence/measure_accuracy.py
    <(python3 -c "import json;print(json.load(open('/tmp/result.json'))['processing']['machine_prompt'])")
    --summary-only

- id: owner_engine_mandatory_gate
  type: mandatory_gate_wiring_and_machine_language_contract
  path: scripts/task-gateway.py (run_owner_engine_gate, call_machine_contract) + scripts/prompt_gateway/query.py
    + ai-os/OWNER_ENGINE_MACHINE_LANGUAGE_CONTRACT_2026-07-25.yaml + ai-os/OWNER_ENGINE_MANDATORY_GATE_IMPLEMENTATION_2026-07-25.yaml
  scope: 'Task task-20260725-080900-owner-engine-mandatory-gate-implementati implements the
    Owner''s 19-point mandatory-gate protocol (KE-20260725-061008-8423,
    ai-os/OWNER_DIRECTIVES/OWNER_ENGINE_MANDATORY_GATE_2026-07-25.txt) on top of the already-built
    registries.prompt_gateway (OWNER_ENGINE software, not rebuilt). Real changes: (1)
    scripts/task-gateway.py submit now pipes every --source owner raw text through
    scripts/prompt_gateway/gateway.py by default (not opt-in) via run_owner_engine_gate(), and
    every downstream call (keyword extraction, log-instruction, check-duplicate, search,
    query-knowledge, lookup-capability) operates on gateway.py''s final_output, not the raw
    text -- the raw text survives only in log-instruction''s --metadata for audit. (2) a new
    machine-language query interface, scripts/prompt_gateway/query.py, answers 9 close-ended
    question codes about an already-processed chat (by chat_id) from its saved record, with a
    real NEEDS_OWNER_CLARIFICATION decision procedure (confidence < 0.15 OR intent ==
    "UNKNOWN") that is the system''s one real "software could not tell -> ask the Owner"
    trigger. (3) ai-os/OWNER_ENGINE_MACHINE_LANGUAGE_CONTRACT_2026-07-25.yaml defines a
    close-ended request/response envelope, proven usable by query.py and by a real retrofit of
    task-gateway.py''s pre-existing lookup-capability call (underlying superboss-register.py
    command unchanged). Full 19-point honesty breakdown (2 fully code-enforced, 6
    partially/proof-of-concept code-enforced, 11 discipline-only) and all real command
    evidence are in ai-os/OWNER_ENGINE_MANDATORY_GATE_IMPLEMENTATION_2026-07-25.yaml -- most
    of the 19 points (AI<->AI machine language, AI<->its-own-model machine language, 1-line
    Owner English, token-discipline-in-general) have no reachable code boundary from this
    task''s scope and are honestly left as discipline-only, not claimed as enforced.'
  status: live
  query_command: python3 scripts/superboss-register.py query-knowledge "owner_engine_mandatory_gate"
    --tag domain:owner_engine_mandatory_gate
  real_verification: 'python3 scripts/task-gateway.py submit --text "<raw owner text>" --source
    owner --session-id <id> (exit 0, owner_engine_gate.applied=true, real VD- chat_id, real
    gateway.py log lines in /opt/veridian/data/prompt_gateway/logs/veridian_20260725.log,
    sqlite instructions.raw_text column holds the gated output not the raw text) and python3
    scripts/prompt_gateway/query.py --chat-id <that chat_id> --q NEEDS_OWNER_CLARIFICATION
    (exit 0, real structured JSON) -- both real command outputs pasted verbatim in
    ai-os/OWNER_ENGINE_MANDATORY_GATE_IMPLEMENTATION_2026-07-25.yaml, not paraphrased.'
  owner_trigger_phrase: 'Drafted and real-world tested (see implementation doc
    owner_trigger_phrase + real_test_evidence.trigger_phrase_test) but NOT handed to the Owner
    by this task -- final handoff happens separately per this task''s own KNOWN_CONTEXT.'
  next_phase: 'Full-codebase retrofit of the machine-language contract across the remaining
    AI<->software call sites (task-gateway.py''s own log-instruction/check-duplicate/search/
    query-knowledge calls, automation_rule_engine.py, auto_phase_continuation.py, etc.) and,
    if a real AI<->AI call site is ever built, wiring point 16 to it -- both real, separate,
    unscoped follow-ons, not attempted here.'

- id: owner_engine_task_gateway_integration
  type: gateway_wiring_integration
  path: scripts/prompt_gateway/gateway.py + scripts/task-gateway.py + scripts/tight_task_validation.py
  scope: 'Real integration closing a manual-bridging gap the Owner flagged
    repeatedly: an AI session previously had to manually read OWNER_ENGINE''s
    (scripts/prompt_gateway/) output and re-paste it rather than the pipeline
    calling it directly. Adds a new dispatch mode wiring scripts/prompt_gateway/gateway.py
    directly into scripts/task-gateway.py, and separately fixes a real
    credit-accountant.py gap found live this session: task-gateway.py''s cmd_start()
    never called `credit-accountant.py propose`, so worker checkpoint reports always
    failed "no matching approved plan". This is Workflow Engine (8) / G01 Owner
    gateway dispatch-wiring, the kind of work gateway_definition_and_inventory''s own
    what_remains_for_later_phases named as a future "actually build G01-G10" phase --
    NOT Phase 2 policy/rule/decision scope (see
    registries.engines_gateways_architecture''s own phase_2_scope_judgment_2026_07_26).'
  status: 'MERGED -- re-verified live via `gh api repos/FChecklist/claude-control/pulls/82`
    by this task, 2026-07-26: state=closed, merged=true, merged_at=2026-07-26T11:02:41Z
    (commit 9d69805). A prior registry entry recorded this as "PR open, round 2+ in
    progress"; that was already stale by the time this task ran. Confirmed live in
    this repo''s current master/HEAD: scripts/task-gateway.py references
    scripts/prompt_gateway/gateway.py (PROMPT_GATEWAY constant + OWNER_ENGINE gate
    call), re-checked directly by this task, not assumed from git log alone.'
  query_command: python3 scripts/superboss-register.py query-knowledge "owner_engine_task_gateway_integration"
    --tag registry-id:owner_engine_task_gateway_integration

- id: policy_gate_registry
  type: policy_rule_decision_gate_registry
  path: ai-os/POLICY_GATE_REGISTRY_2026-07-26.yaml
  scope: 'Phase 2 extension (task-20260726-154345-phase-2--policy-rule-decision-engine-uni), closing
    the real gap registries.engines_gateways_architecture''s own phase_2_scope_judgment_2026_07_26 note
    flagged: this session added a 3rd (scripts/ddl_authorization_check.py) and 4th (the interactive-session
    write-gate) disconnected policy/gate mechanism after Phase 2 (task-20260724-115041) closed having
    unified only 2. A real, queryable registry of all 4 gates now exists (path/scope/checks/schema_emission
    status per gate), plus the actual schema-port for ddl_authorization_check.py (the one gate whose shape
    genuinely matched Phase 2''s existing unification -- see POLICY_GATE_REGISTRY_2026-07-26.yaml''s own
    schema_emission fields for the honest, judged answer per gate, not a uniform port applied without
    reading what each gate actually is).'
  status: 'DONE, 2026-07-26. 4/4 real gates found and registered: preflight-guard.py and
    ddl_authorization_check.py both emit policy_decision_schema (scripts/policy_decision.py);
    policy-enforcement-engine.ts remains documented-not-yet-ported (compliance-tracker''s own PR, same
    repo-boundary discipline Phase 1/2 established); the interactive-session write-gate is registered but
    marked not_applicable_by_design (a bash-level operator-safety control, not a machine dispatch decision
    -- see that gate''s own schema_emission.detail for the full reasoning). The write-gate''s real content
    was also committed to git for the first time this task
    (ai-os/OWNER_DIRECTIVES/interactive-session-guard.bashrc-snippet, previously only at
    /home/rajat/.claude-interactive-session-guard.bashrc-snippet, un-versioned) along with a new real
    end-to-end test, tests/interactive_session_guard_test.sh (8/8 scenarios pass).'
  query_command: python3 scripts/superboss-register.py query-knowledge "policy_gate_registry" --tag registry-id:policy_gate_registry

- id: prompt_gateway
  type: chat_preprocessing_pipeline
  path: scripts/prompt_gateway/ (config.py, gateway.py, engine/{id_generator,classifier,prompt_engine,
    context_engine,snip_engine}.py) + ai-os/PROMPT_GATEWAY_2026-07-25.yaml
  scope: 'Task task-20260725-053025-zai-prompt-gateway-integration: integrates 9 real, working,
    stdlib-only Python files the Owner had Z.AI build (staged at
    /opt/veridian/workspace/zai-prompt-gateway-staging/) as a pre-processing FILTER that
    classifies/compresses raw owner chat text into a machine_prompt BEFORE it becomes a --text
    argument to the real, unmodified, already-in-production scripts/task-gateway.py dispatcher
    (submit/start/log/close/register-automation/status) -- this module never calls
    task-gateway.py itself and is never a replacement for it. CRITICAL COLLISION AVOIDED: the
    staged entrypoint called itself "task-gateway.py" in its own header and targeted the exact
    live dispatcher path; deployed instead under its own scripts/prompt_gateway/ namespace with
    the entrypoint renamed gateway.py -- scripts/task-gateway.py verified byte-for-byte
    unchanged (sha256 1e9421777219d7561511431219e04c09862d86b2761cf14c882af4599ca9a49b, 642
    lines, before and after). Introduces a 4th, separate ID namespace (VD-YYYYMMDDHHMMSS-XXXX)
    scoped ONLY to this pipeline''s own raw-chat pre-processing records -- explicitly NOT a task
    id, KE- id, or INS- id; see ai-os/PROMPT_GATEWAY_2026-07-25.yaml vd_id_namespace for the full
    distinction. Implements only the PROMPT_PROCESSING_PIPELINE + CONTEXT_MANAGEMENT_PIPELINE +
    a slice of the COMPRESSION_STACK boxes from the Owner''s much larger 19-engine OWNER_ENGINE
    YAML spec -- Temporal (workflow orchestration), OPA (policy), Qdrant (vector store), and
    local-model-inference hosting are explicitly NOT implemented (named out-of-scope in this
    task''s own CONSTRAINTS), and the remainder of the 19-engine spec beyond
    PROMPT_PROCESSING_PIPELINE/CONTEXT_MANAGEMENT_PIPELINE/COMPRESSION_STACK is an unscoped,
    uninventoried gap, not silently folded into "done" here. No systemd service was installed:
    the staged veridian-gateway.service.TEMPLATE assumed a User=veridian system user (does not
    exist; real user is rajat) and a --mode pipe named-pipe daemon with no real streaming
    producer in this system today -- see ai-os/PROMPT_GATEWAY_2026-07-25.yaml systemd_decision
    for the full reasoning; this pipeline runs on-demand CLI-only for now.'
  status: live
  query_command: python3 scripts/superboss-register.py query-knowledge "prompt_gateway" --tag domain:prompt_gateway
  real_verification: 'python3 scripts/prompt_gateway/gateway.py --mode init (exit 0, real dirs
    created under /opt/veridian/data/prompt_gateway/, 6 snippets seeded, real VD- test id
    generated) and echo "<raw chat>" | python3 scripts/prompt_gateway/gateway.py --mode stdin
    --json-only (exit 0, real 66.7% token reduction / 13.2% noise reduction on a representative
    owner-style instruction) -- both real command outputs pasted verbatim in
    ai-os/PROMPT_GATEWAY_2026-07-25.yaml, not paraphrased.'
  next_phase: none dispatched by this task -- if a genuinely streaming chat-ingest use case
    materializes later, fixing and installing the systemd unit (correct User=rajat, correct
    ExecStart path, a real tmpfiles-managed pipe or a per-invocation design) should be its own
    scoped follow-on task, not retrofitted here.

- id: scripts_drift_audit_and_system_integration_confirmation
  type: live_vs_repo_deploy_gap_audit_and_owner_confirmation_answer
  path: ai-os/SCRIPTS_LIVE_VS_REPO_DRIFT_AUDIT_2026-07-25.yaml + ai-os/SYSTEM_INTEGRATION_CONFIRMATION_2026-07-25.yaml
    + scripts/deploy-live-scripts.sh + scripts/sync-repos.sh
  scope: 'task-20260725-092256-fix-live-repo-scripts-drift-and-system-i: confirmed, real,
    previously-undetected finding -- /opt/veridian/scripts/ (the path every cron job and
    every direct `python3 /opt/veridian/scripts/X.py` invocation actually runs) had
    drifted from claude-control''s scripts/ (this session''s own PR merge target) because
    no automatic mechanism ever connected them; sync-repos.sh (every 2h) only ran
    `git pull --ff-only` inside repos/<repo>/ checkouts. A full diff -rq inventory
    classified every entry (operational_only / undeployed_real_feature / stale_live_copy
    / needs_owner_decision, see the drift audit doc) -- 9 stale_live_copy files
    (incl. task-gateway.py, which was missing PR #66''s OWNER_ENGINE gate entirely, and
    veridian-task-watchdog.py, missing 3 governance-mandated monitoring functions never
    actually running) and 25 undeployed_real_feature files (incl. policy_decision.py,
    prompt_gateway/query.py, and the ai-os/20_ENGINES_10_GATEWAYS engines
    context_engine.py/document_engine.py/decision-service.py/automation_rule_engine.py/etc.)
    were deployed live, each verified with a real smoke invocation, not just a file copy.
    Root cause closed permanently: scripts/deploy-live-scripts.sh (new, copies only
    git-tracked scripts/ files repo->live, so operational-only live files -- crontab
    backups, cost/credit scripts, the OpenRouter proxy -- are structurally never touched,
    not excluded via a hand-maintained list) is now called from the end of
    scripts/sync-repos.sh (itself newly git-tracked -- it had 0 prior commits despite
    being the only related cron job). Regenerated ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json
    fresh from the corrected live copy (7658 entities, 75.95% wired) and answered all 6
    of the Owner''s confirmation points with real command-output evidence, including 2
    concrete zero-duplication findings (one resolved, one still open) and naming ~24
    core orchestration scripts that remain outside version control entirely (left alone,
    out of this task''s scope, flagged for a dedicated follow-up).'
  status: live
  query_command: python3 scripts/superboss-register.py query-knowledge "scripts_drift_audit"
    --tag domain:scripts_drift_audit_and_system_integration_confirmation
  real_verification: 'diff -q /opt/veridian/scripts/task-gateway.py
    /opt/veridian/repos/claude-control/scripts/task-gateway.py (no difference);
    grep -c owner_engine_gate /opt/veridian/scripts/task-gateway.py (16); a real
    /opt/veridian/scripts/deploy-live-scripts.sh run (log: deployed=35 unchanged=17
    failed=0 on first run, deployed=0 unchanged=52 failed=0 on the immediate re-run,
    confirming idempotency and that credit-accountant.py -- an operational-only file --
    was untouched, same sha256 before/after).'
  next_phase: 'Owner decision needed on the ~24 untracked core orchestration scripts
    (see drift audit doc needs_owner_decision_summary + the operational_only entry
    flagging them) -- whether/when to onboard them into git, not attempted here.'

- id: security_audit_implementation_plan
  type: dual_audit_cross_reference_and_phased_closure_plan
  path: ai-os/SECURITY_AUDIT_EVALUATION_2026-07-25.yaml + ai-os/SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml
  scope: 'task-20260725-041130-security-audit-evaluation-and-implementa: Owner directive given
    directly in chat 2026-07-25 ("EVALUATE AUDIT BY BOTH CHATGPT AND Z.AI. MAKE IMPLEMENTATION PLAN.
    ALL DOCUMENTATION TO BE SAVED.") -- evaluates the two real, filed Security-domain
    ChatGPT-manual-bridge audits (AUDIT-000009 from ChatGPT, AUDIT-000010 from Z.AI, both at
    /opt/veridian/chatgpt-audit/Security/, both analysing the same 6 real
    repos/compliance-tracker files: drizzle/0007_wave9_unified_api_key_auth.sql,
    drizzle/0126_wave143_visual_design_authoring.sql, and the 4 public auth route handlers under
    src/app/api/auth/) against each other and against the real current source, including the 3
    service-layer files (passcode-login-service.ts, auth-failure-service.ts, sso-service.ts) the
    routes delegate to but neither audit''s files_analysed list names. Real result: 19 distinct
    findings, 15 confirmed/partially_confirmed (3 raised by both audits independently, 4 by
    ChatGPT only, 12 by Z.AI only) and each assigned to one of 7 real implementation phases in
    the companion plan (shared IP-extraction + Supabase-admin-client utils; a new drizzle
    migration closing the Wave 143 interior-schema FK/CHECK/updated_at gaps; email input
    validation reusing the codebase''s own existing isValidEmail(); failure-event rate-limiting +
    body-size caps -- the single highest-severity finding, the only one rated high; structured
    passcode-login error codes + SSO ACS error-code hygiene; SSO login-initiation + failure-event
    audit logging; security response headers), 2 deliberately deferred as codebase-wide
    convention questions out of scope for a security-fix plan (org_id FK convention drift across
    51 drizzle migrations; RLS-policy-boilerplate dedup used identically dozens of times
    elsewhere), and 2 refuted against the real code (ChatGPT''s "no passcode-login audit
    logging" claim -- exists one layer down in passcode-login-service.ts''s recordAttempt(), not
    visible from the route file alone; ChatGPT''s "no governance-metadata linkage" claim --
    contradicted by the analysed files'' own substantial wave/directive-citing headers).'
  status: phase_0_complete_phases_1_through_7_not_yet_dispatched
  query_command: python3 scripts/superboss-register.py query-knowledge "security_audit_implementation_plan"
    --tag domain:security_audit
  next_phase: 'phase_1_shared_auth_utils per ai-os/SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml
    phases[] -- picked up automatically by scripts/auto_phase_continuation.py (same *_PHASE_PLAN_*.yaml
    glob and phases[].id/depends_on/status convention as ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml)
    once this task''s PR merges. phase_4 depends_on phase_1; the other 6 phases have no dependencies
    and can dispatch in any order.'

- id: status_monitor_and_remediation
  type: cron_pipeline
  path: scripts/veridian_status_monitor.py + scripts/veridian_remediation_dispatcher.py
  scope: 'Task task-20260726-053322-status-monitor-and-remediation-dispatche: closes 2 real,
    confirmed gaps -- no proactive status-monitoring software (an Owner/AI asking "status" meant
    manually running gh/task.yaml checks by hand, not reading real software output), and no
    automatic remediation trigger when a real task/PR gets rejected or blocked. Built on tools
    that already exist, no new framework: veridian_status_monitor.py scans every real task.yaml
    under ai-os/tasks/ (in_progress with real elapsed time, blocked within the last 24h with the
    real checkpoint-note/review.json reason), every real open PR across claude-control/
    compliance-tracker/projexa via `gh` (real AUDIT: FAIL comments with no corrective commit
    since, real non-transient mergeStateStatus=DIRTY conflicts), and
    scripts/auto_phase_continuation.py''s own real --dry-run output (phases ready to
    auto-advance), overwriting one live-snapshot artifact (ai-os/LIVE_STATUS_2026-07-26.yaml)
    every run -- not an append log. veridian_remediation_dispatcher.py reads that artifact and
    classifies each finding: the 2 narrow, explicitly-named mechanical/low-risk classes
    (ci_timing_race -- the audit-check CI job started within
    RACE_MAX_GAP_SECONDS=900 of the real AUDIT verdict comment, same root cause as
    supervisor-entrypoint.sh''s own AUDIT-CHECK-RERUN-BLOCK; transient_merge_retry -- a
    previously tier1-approved merge that failed, where a fresh mergeStateStatus check now shows
    CLEAN not DIRTY) are auto-applied directly via the same proven-safe `gh run rerun --failed`
    / `gh pr merge` mechanisms already live in supervisor-entrypoint.sh; everything else (a
    genuine correlated AUDIT: FAIL finding, or a real DIRTY merge conflict) gets a real,
    tight_task_validation.py-passing corrective task prompt drafted to
    ai-os/pending_remediation/<id>.md and flagged in the status artifact''s remediation: section
    as "drafted, needs assistant review before dispatch" -- never auto-dispatched, never merged,
    per this task''s own CONSTRAINTS. Zero AI/LLM calls in either script''s run path.'
  status: live
  real_test_evidence: 'Real run against live production data (2026-07-26): correctly surfaced 3
    real recently-blocked tasks (compliance-tracker PR #561/#562, claude-control PR #72, all
    genuine Superboss-rejected findings), 5 real open PRs with an unfixed AUDIT: FAIL, and 16
    real open PRs with a genuine DIRTY merge conflict across compliance-tracker/projexa -- none
    of this had a structured, checkable artifact before this task. Judgment path proven for real
    against compliance-tracker PR #562 (genuine architecture/security finding: a duplicated,
    unwired prompt-security module) -- drafted prompt written to
    ai-os/pending_remediation/audit-fail-compliance-tracker-562.md, independently verified to
    pass `python3 scripts/tight_task_validation.py` for real, NOT auto-dispatched. Mechanical
    path proven for real against compliance-tracker PR #410 (ci_timing_race, 21s gap) --
    `gh run rerun --failed` applied for real via --apply, independently confirmed via a fresh
    `gh run view` showing the job actually re-triggered (status changed to in_progress). A real
    false positive was caught and fixed during testing: PR #484''s stale 2-day-old audit-check
    run was initially misclassified as a race against an unrelated later re-review comment,
    fixed by adding the same-review-cycle gap threshold, reverified against both the real race
    (#410) and the real non-race (#484).'
  query_command: python3 scripts/superboss-register.py query-knowledge "status_monitor_and_remediation"
    --tag domain:status_monitor_and_remediation
  cron_entry: '*/10 * * * * (ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml id
    veridian-status-monitor-and-remediation-crontab-addition, status approved)'
  still_needs_assistant_judgment: 'Every entry ai-os/pending_remediation/ ever accumulates --
    this script deliberately never resolves a genuine AUDIT: FAIL finding or a real merge
    conflict itself (per CONSTRAINTS); an assistant (or the Owner) must read the drafted prompt,
    confirm the finding is real, and decide whether to dispatch it via task-gateway.py, same
    review step this task''s own real test evidence exercised for compliance-tracker PR #562.'

- id: task_completion_audit_7day
  type: independent_task_completion_verification
  path: ai-os/TASK_COMPLETION_AUDIT_2026-07-26.md + ai-os/TASK_COMPLETION_AUDIT_2026-07-26.json
  scope: >
    task-20260726-163551 (Owner directive: real automated verification of all
    218 real task IDs created in ai-os/tasks/ in the last 7 days, cross-referenced
    against independently-verifiable outcomes -- never task.yaml's own
    self-reported status field). Built ai-os-scripts/verify_7day_task_completion.py,
    which for every task-*/task.yaml created in the last 7 days (219 as of this
    run) resolves its real PR via pr_url.txt / worker-log grep / a
    `gh pr list --repo <repo> --state all` headRefName match (one paginated
    gh pr list call per repo, not per-task gh pr view calls), reads that PR's
    real live state (MERGED/OPEN/CLOSED) from GitHub, and falls back to real
    commit-ancestry evidence (git log origin/<default-branch> --grep=<task_id>
    against the repo's local clone at /opt/veridian/repos/<repo>) when no PR
    exists, or to genuine checkpoint-terminal-state evidence for pure
    investigation/analysis tasks with no code deliverable. Cross-references the
    6 named *_PHASE_PLAN_*.yaml files (20-Engine, Wiring, Auditor, Testing,
    Terminology Standardization, VERIDIAN Architecture v2) for any task whose
    title names a matching phase. Confirmed and generalized (via automatic
    title-vs-actual-PR-number mismatch detection, not hardcoding) both
    already-known real cases this session found: task-20260726-081117 and
    task-20260726-115425 each self-reported "completed, merged autonomously"
    while actually merging an unrelated trivial PR (#565, #569) instead of the
    PR their own title named (#563), which the audit confirms is still OPEN.
    Real result distribution: MERGED 109, NO_PR_FOUND 76, OPEN_NOT_DONE 18,
    INVESTIGATION_ONLY 16 (of 219; 4 task dirs omitted for having no task.yaml
    at all, listed by name in the report, not silently dropped).
  status: audit_generated_2026-07-26_rerunnable
  query_command: python3 ai-os-scripts/verify_7day_task_completion.py --count-only
  verify_command: python3 ai-os-scripts/verify_7day_task_completion.py --out-md ai-os/TASK_COMPLETION_AUDIT_2026-07-26.md --out-json ai-os/TASK_COMPLETION_AUDIT_2026-07-26.json

- id: task_lifecycle_branch_resolution_and_signoff_fix
  type: task_lifecycle_bugfix
  path: scripts/supervisor-entrypoint.sh + scripts/veridian-task.py + scripts/tight_task_validation.py
  scope: 'Real bugfix to the existing task_lifecycle_state_machine (Workflow Engine
    (8) / Decision Engine (7) territory, not a new gate): fixes stale-PR-branch
    resolution and makes HOLD_FOR_OWNER_SIGNOFF a real, mechanically enforced field
    on the task record instead of prose-only guidance an auto-merge step could
    silently skip. Originally shipped as PR #81, which was closed unmerged and
    recreated/superseded by PR #84 after a real branch-resolution bug was found
    against #81 itself -- #84 is the real, current, merged version, not #81.'
  status: 'MERGED -- PR #84 (FChecklist/claude-control, branch
    recovered-lifecycle-fix-e6c7049), merged into master 2026-07-26T10:19:37Z (commit
    581e042, merging e6c7049). Live in this repo''s current master/HEAD, confirmed by
    direct file presence (scripts/tight_task_validation.py and
    scripts/supervisor-entrypoint.sh both carry HOLD_FOR_OWNER_SIGNOFF references on
    master as of 2026-07-26).'
  query_command: python3 scripts/superboss-register.py query-knowledge "task_lifecycle_branch_resolution_and_signoff_fix"
    --tag registry-id:task_lifecycle_branch_resolution_and_signoff_fix

- id: vercel_github_state_census_2026_07_26
  type: infrastructure_state_snapshot
  path: ai-os/MASTER_INDEX.yaml (this entry -- inline census, no separate tracking
    file created per this task's own CONSTRAINTS; SYSTEM_MAP.yaml and
    MASTER_ARCHITECTURE_2026-07-22.yaml referenced by registries.system_map_2026_07_22
    / registries.master_architecture_2026_07_22 do not exist on disk or in git history
    for this repo -- confirmed via `git log --diff-filter=A --name-only` returning zero
    hits -- so this file's own registries[] section is the correct, real existing home)
  scope: 'Real, fresh (not from memory) census of everything live across the 3 linked
    Vercel projects and 7 GitHub repos under FChecklist, built by direct `vercel` CLI
    and `gh api`/`gh pr`/`gh repo` calls run 2026-07-26T10:33:37Z. Supersedes any
    prior-session verbal/chat record of this same state (KNOWN_CONTEXT for this task
    cited a prior session finding that Vercel plan is Hobby and blocks custom
    environments, and described claude-control PR #84 as "being verified" -- both
    checked fresh below; the Hobby-plan finding held up, the PR #84 status did not
    (see discrepancies_vs_known_context).'
  status: live_snapshot_2026_07_26
  vercel:
    team_scope: meet-track-s-projects (id team_Iqx3zyb7sDdsdzcNskCFFsHD)
    plan_tier: 'Hobby -- reconfirmed real, current tier via `GET /v2/teams` (billing.plan=="hobby"),
      queried 2026-07-26. Matches the prior-session finding cited in this task''s KNOWN_CONTEXT.
      Vercel custom environments require a Team/Pro-or-above plan, so this team cannot use
      them -- any existing doc describing a Vercel "custom environment" for
      compliance-tracker/projexa/veda-advisors is describing something not actually
      obtainable on the real current plan.'
    projects:
      veridian-compliance-ai:
        project_id: prj_mRRWcMvhyuxgRZtcfp4ArSzcOvII
        github_repo: FChecklist/compliance-tracker
        framework: Next.js (bun run build / bun install)
        latest_production_deployment: 'Ready, ~2h old as of the query (age is relative
          to 2026-07-26T10:33:37Z query time), served at https://projexa-ai.com. A
          Preview deployment was actively Building at query time (2m old), consistent
          with the compliance-tracker PR #563 work happening in a parallel task this
          same session.'
        env_vars_present: [OPS_SYNC_SECRET, AI_TEAM_LOG_SECRET, CEREBRAS_API_KEY, GROQ_API_KEY,
          APP_RUNTIME_DATABASE_URL, CRON_SECRET, OPENROUTER_API_KEY, DATABASE_URL,
          AI_CONFIG_ENCRYPTION_KEY, NODE_ENV, NEXT_PUBLIC_APP_URL, RESEND_API_KEY,
          SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_ANON_KEY,
          NEXT_PUBLIC_SUPABASE_URL]
        env_var_staleness_note: 'All 21 rows (16 distinct keys, several duplicated per-environment)
          are 6-28 days old as of this query -- none flagged as recently rotated. No
          secret values were read or printed, only key names and per-environment presence,
          per this task''s CONSTRAINTS.'
      projexa:
        project_id: prj_JA9mwUdOfW3SKSxjG4jdPo0R2iVM
        github_repo: FChecklist/projexa
        framework: Next.js (npm/yarn/pnpm/bun build, no custom command pinned)
        latest_production_deployment: 'Ready, ~7d old, served at https://projexa-ai.com --
          same production domain as veridian-compliance-ai (both projects share the
          projexa-ai.com apex domain; this is real current Vercel domain-assignment state,
          not a data-entry error in this census).'
        env_vars_present: [VERIDIAN_PLATFORM_APPLICATION_KEY, SUPABASE_DB_PASSWORD,
          VERIDIAN_API_KEY, VERIDIAN_API_BASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
          NEXT_PUBLIC_SUPABASE_URL]
        env_var_staleness_note: All 6 keys are Production-only (no Preview/Development
          rows exist for this project) and 11-12 days old as of this query.
      veda-advisors:
        project_id: prj_sGnvct3DikYopNJ6G3lpmZFhsEJI
        github_repo: FChecklist/veda-advisors
        framework: Next.js (npm/yarn/pnpm/bun build, no custom command pinned)
        latest_production_deployment: Ready, ~28d old, served at https://veda-advisors.vercel.app.
        env_vars_present: [NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
          SUPABASE_SERVICE_ROLE_KEY]
        env_var_staleness_note: All 3 keys are 32 days old as of this query -- the
          stalest of the 3 projects' env var sets.
  github:
    org_or_user_account: FChecklist is a personal User account (not a GitHub Organization
      -- `gh api orgs/FChecklist` returns 404), confirmed real-plan-tier evidence below.
    plan_tier_evidence: 'GitHub Free (private-repo tier), inferred from a real, reproducible
      403 on every private repo''s branch-protection endpoint: `gh api
      repos/FChecklist/<private-repo>/branches/<default>/protection` returns
      {"message":"Upgrade to GitHub Pro or make this repository public to enable this
      feature.","status":"403"} for claude-control, projexa, veridian-brain, and
      sumeet-spec (all private). Only the 2 PUBLIC repos (compliance-tracker,
      veda-advisors) return real branch-protection data -- see required_checks below.
      This means required-status-checks cannot be mechanically verified via the REST
      branch-protection API for any private repo on the current plan; PR mergeability
      for those repos was instead confirmed via each PR''s own mergeable/mergeable_state
      field plus check-runs, both queried fresh below.'
    repos:
      compliance-tracker:
        visibility: PUBLIC
        default_branch: main
        open_pr_count: 30
        required_status_checks_contexts: [Lint, Type Check, Build, audit-check,
          Guardrail Presence Check, Asset Registry Coverage Check, Unit Tests]
        required_checks_source: 'live `gh api repos/FChecklist/compliance-tracker/branches/main/protection/required_status_checks`
          --jq ''.contexts'', queried 2026-07-26 -- no prior tracking file in this repo
          documented an expected contexts list to diff against, so this is the first
          real record of it, not a drift confirmation against a stale doc.'
        active_workflows: [ai-dispatch.yml, ai-prompt-evals.yml, ai-team-workforce.yml,
          browser-ux-test.yml, build-doc-processing-image.yml, ci.yml, claude.yml,
          codeql.yml, doc-processing-job.yml, mandatory-audit-check.yml, sentinel.yml,
          sync-vercel-env.yml]
      claude-control:
        visibility: PRIVATE
        default_branch: master
        open_pr_count: 7
        required_status_checks_contexts: 'UNVERIFIABLE -- 403 (private repo, Free plan);
          see plan_tier_evidence above. No CI workflow beyond claude.yml exists in this
          repo (confirmed via `gh api .../actions/workflows` and via zero check-runs
          returned for PR #79/#80/#82''s head SHAs), so there is no required-status-check
          drift to detect here in practice -- PRs #79/#80 are blocked by real git merge
          conflicts (mergeable_state=dirty), not by failing checks.'
        active_workflows: [claude.yml]
      projexa:
        visibility: PRIVATE
        default_branch: main
        open_pr_count: 2
        required_status_checks_contexts: 'UNVERIFIABLE -- 403 (private repo, Free plan);
          see plan_tier_evidence above.'
        active_workflows: [ci.yml, claude.yml]
      veda-advisors:
        visibility: PUBLIC
        default_branch: main
        open_pr_count: 11
        required_status_checks_contexts: 'none configured (empty list) -- branch
          protection on main requires 1 approving PR review but zero named status
          checks, confirmed live via the same protection/required_status_checks
          endpoint used for compliance-tracker above'
        active_workflows: [ai-dispatch.yml, ci.yml, codeql.yml, sentinel.yml]
      global-revenue-engine:
        visibility: PRIVATE
        default_branch: 'NONE -- repo is real but empty (size=0 bytes, zero commits,
          confirmed via `gh api repos/FChecklist/global-revenue-engine`), so it has no
          default branch, no PRs, no workflows, and branch-protection/required-checks
          concepts do not apply. Listed in this task''s KNOWN_CONTEXT as one of the 7
          repos to catalogue; this census confirms its real current state is "created,
          never populated," not "populated but unreachable."'
        open_pr_count: 0
        active_workflows: []
      veridian-brain:
        visibility: PRIVATE
        default_branch: main
        open_pr_count: 0
        required_status_checks_contexts: 'UNVERIFIABLE -- 403 (private repo, Free plan);
          see plan_tier_evidence above.'
        active_workflows: 'none -- no workflow files at all (dormant repo, last push
          2026-07-09)'
      sumeet-spec:
        visibility: PRIVATE
        default_branch: main
        open_pr_count: 0
        required_status_checks_contexts: 'UNVERIFIABLE -- 403 (private repo, Free plan);
          see plan_tier_evidence above.'
        active_workflows: 'none -- no workflow files at all (dormant repo, last push
          2026-07-09)'
    open_prs_confirmed_fresh_2026_07_26_via_gh_api_not_memory:
      claude-control_79: 'DDL pre-flight authorization gate. AS RECORDED 2026-07-26T10:33:37Z:
        state=open, draft=false, mergeable=false, mergeable_state=dirty. SUPERSEDED --
        re-verified by this later task (registries.ddl_preflight_authorization_gate,
        same day): state=closed, merged=true, merged_at=2026-07-26T11:59:10Z. Merged
        after this census ran, before this later task started -- left here as the
        historical record this census actually captured, not corrected in place.'
      claude-control_80: 'Interactive write-gate. AS RECORDED 2026-07-26T10:33:37Z:
        state=open, draft=false, mergeable=false, mergeable_state=dirty. UPDATED --
        re-verified by this later task (registries.interactive_session_write_gate,
        same day): still state=open (not merged) but mergeable_state is now clean
        (rebased since this census ran), not dirty.'
      claude-control_82: 'OWNER_ENGINE integration into task lifecycle. AS RECORDED
        2026-07-26T10:33:37Z: state=open, draft=false, mergeable=true,
        mergeable_state=clean. SUPERSEDED -- re-verified by this later task
        (registries.owner_engine_task_gateway_integration, same day): state=closed,
        merged=true, merged_at=2026-07-26T11:02:41Z. Merged after this census ran,
        before this later task started -- left here as the historical record this
        census actually captured, not corrected in place.'
      claude-control_84: 'DISCREPANCY vs KNOWN_CONTEXT: KNOWN_CONTEXT described this as
        "recovered lifecycle-fix, being verified" (implying still open). Real current
        state, confirmed via `gh api repos/FChecklist/claude-control/pulls/84`:
        state=closed, merged=true, merged_at=2026-07-26T10:19:37Z, merged_by=FChecklist.
        It was merged during this same session, before this census ran (also visible
        as commit 581e042 at the top of `git log` for this workspace). This census
        records it as MERGED, not open/being-verified -- KNOWN_CONTEXT was already
        stale by the time this task started.'
      compliance-tracker_563: 'Migration-drift audit. state=open, draft=false,
        mergeable=true, mergeable_state=blocked (GitHub''s "blocked" = required checks/reviews
        not all green yet, distinct from a git conflict). Real check-runs as of this
        query: audit-check=FAILURE, Metadata Index Coverage Check=FAILURE, E2E
        Tests=in_progress, all 15 other checks=SUCCESS/neutral. Matches KNOWN_CONTEXT''s
        "real conflict, being resolved in a parallel task this same session" -- this
        census only catalogues that real current state and does not touch or resolve it,
        per this task''s own explicit instruction not to duplicate that parallel work.'
  discrepancies_vs_known_context:
    - 'claude-control PR #84 was already merged (2026-07-26T10:19:37Z) by the time this
      census ran, contradicting KNOWN_CONTEXT''s "being verified" framing -- see
      open_prs_confirmed_fresh_2026_07_26_via_gh_api_not_memory.claude-control_84 above.'
    - 'The prior-session Vercel Hobby-plan / custom-environments finding cited in
      KNOWN_CONTEXT was NOT found recorded in any tracking file inside this
      (claude-control) repo -- it may live in a different repo''s docs or only in a
      prior chat session. This census independently reconfirms it is still real and
      current regardless of where it was first recorded.'
    - 'No existing tracking file in this repo previously documented an expected GitHub
      required-status-checks list for any repo, so "matches what is documented" could
      only be evaluated for compliance-tracker and veda-advisors going forward from this
      census (the first real record) -- there was nothing stale to detect yet for those
      two, and the other 5 repos are structurally unverifiable via API on the current
      GitHub Free plan.'
  verification_commands:
    vercel_plan: "curl -s -H \"Authorization: Bearer $VERCEL_ACCESS_TOKEN\" https://api.vercel.com/v2/teams
      | jq '.teams[] | select(.slug==\"meet-track-s-projects\") | .billing.plan'"
    vercel_projects: "vercel project ls --token $VERCEL_ACCESS_TOKEN --scope meet-track-s-projects"
    vercel_env: "vercel env ls --token $VERCEL_ACCESS_TOKEN --scope meet-track-s-projects
      --project <name>"
    github_prs: 'gh pr view <n> --repo FChecklist/<repo> --json state,mergeable,mergeStateStatus
      (NOTE -- gh pr view --json silently truncated long string field values with a
      literal "..." mid-string when several fields were requested together during this
      task''s own execution; `gh api repos/<owner>/<repo>/pulls/<n>` did not exhibit this
      and is the more reliable command for scripted use)'
    github_branch_protection: "gh api repos/FChecklist/<repo>/branches/<default-branch>/protection/required_status_checks
      --jq '.contexts'"

- id: veridian_architecture_v2_implementation
  type: gap_analysis_and_phased_build_plan
  path: ai-os/VERIDIAN_ARCHITECTURE_V2_GAP_ANALYSIS_2026-07-25.yaml + ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml
  scope: 'Phase 0 (task-20260725-112909-veridian-architecture-v2-task2-phase0-ga) of the Owner''s
    Task 2 (VERIDIAN_Architecture_v2.0.docx-driven implementation), following
    task-20260725-104606-owner-engine-document-accuracy-convergen (PR #69, merged), which proved
    OWNER_ENGINE reproduces the document''s real full text (ai-os/owner-attachments/VERIDIAN_Architecture_v2.0_extracted.txt)
    with 0.0% gap and deliberately did not act on the document''s content, per the Owner''s own
    instruction. This task read that extracted text in full and produced a real, itemized
    105-requirement gap analysis (85 not_implemented, 20 partially_implemented via real but
    narrow/differently-scoped prior art -- scripts/prompt_gateway/, knowledge_engine,
    repos/compliance-tracker/src/app/litert-spike/, llm-client.ts, permission-service.ts, and the
    20-Engines/10-Gateways architecture pattern -- 0 already_implemented) against the current real
    system state. Headline finding: the document specifies a browser-first local-LLM "Prompt
    Intelligence Platform" (WebLLM/WebGPU/WebNN/DSPy/74 engines/41-stage pipeline/L0-L9 cache) that
    is a genuinely different kind of software than either compliance-tracker/projexa (server-rendered
    business SaaS) or this ops system''s own prompt_gateway (server-side chat pre-processing) --
    real overlap is narrow and explicitly cited per item, never assumed from name collisions alone
    (9 confirmed-and-rejected naming coincidences with the existing 20-Engines architecture are
    documented explicitly in the gap analysis). The companion 10-phase plan sequences all 105 items
    by real dependency (registry/schema foundation first, browser execution and caching in the
    middle, DSPy/gateway-integration/YCML documentation last), matches
    scripts/auto_phase_continuation.py''s exact phases[] schema, and is confirmed picked up by a
    real `python3 scripts/auto_phase_continuation.py --dry-run --limit 0` run (next_phase:
    phase_1_prompt_registry_lifecycle_foundation, would_dispatch: true).'
  status: phase_1_through_phase_3_merged_phase_4_open_unmerged_phase_5_through_10_not_started
  status_correction_2026_07_27: 'CORRECTED by task-20260727-055448-owner-engine-task2-phase0--real-gap-anal:
    this entry previously read status: phase_0_gap_analysis_and_plan_complete_phase_1_not_yet_dispatched,
    which was stale -- phase_1 (compliance-tracker PR #559), phase_2 (PR #560), and phase_3 (PR #561)
    were all real and MERGED by 2026-07-26 (independently verified via `gh pr view <n> --json
    state,mergedAt`, not just trusting task.yaml status fields), and phase_4 (PR #562, defense-in-depth
    security) is open awaiting conflict resolution + review. This task produced a fresh, dated gap
    analysis and phase-plan refresh reflecting that real state -- see path_2026_07_27 below -- rather
    than silently leaving this entry''s status stale for a future session to re-discover the same drift.'
  path_2026_07_27: ai-os/OWNER_ENGINE_TASK2_GAP_ANALYSIS_2026-07-27.yaml + ai-os/OWNER_ENGINE_TASK2_PHASE_PLAN_2026-07-27.yaml
  query_command: python3 scripts/superboss-register.py query-knowledge "VERIDIAN_Architecture_v2"
    --tag domain:veridian_architecture_v2
  owner_decision_filed: 'ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml id
    veridian-architecture-v2-target-repo-anchor -- confirm compliance-tracker (the only repo with
    any real browser-native prior art, litert-spike) as the target repo for phases 2-10''s
    application code, since claude-control itself is config/YAML-only. STILL UNRESOLVED as of
    2026-07-27 even though phases 1-3 already merged real code into compliance-tracker on the
    strength of the plan''s own stated default, not an actual Owner answer -- now higher-stakes to
    resolve explicitly (see gap analysis item platform-anchor-repo-undecided).'
  evidence_chain:
    - task-20260725-104606-owner-engine-document-accuracy-convergen (PR #69, merged)
    - task-20260725-112909-veridian-architecture-v2-task2-phase0-ga
    - task-20260725-160908-task2-dedup-integration-audit
    - task-20260725-174858-phase1-prompt-registry-version-lifecycle (compliance-tracker PR #559, merged)
    - task-20260725-231836-phase2-prompt-compiler-pipeline-core-non (compliance-tracker PR #560, merged)
    - task-20260726-035742-phase3-business-rule-permission-policy-c (compliance-tracker PR #561, merged)
    - task-20260726-043023-phase4-defense-in-depth-prompt-security (compliance-tracker PR #562, open/unmerged)
    - task-20260727-055448-owner-engine-task2-phase0--real-gap-anal (this task)
  next_phase: 'phase_4_defense_in_depth_security_quality (compliance-tracker PR #562) needs its merge
    conflicts resolved and a review pass, per ai-os/OWNER_ENGINE_TASK2_PHASE_PLAN_2026-07-27.yaml
    phase 4 -- this is the fastest real next step (existing code, not new engineering). Phases 5-10
    remain not_started per ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml''s own detailed
    scope for each.'

- id: owner_ai_protocol
  type: owner_ai_protocol_and_memory_consolidation
  path: ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml + ai-os/OWNER_DIRECTIVES/MEMORY_OWNER_AI.yaml
  scope: '2026-07-26 server-side consolidation (task-20260726-051203, mirroring an equivalent laptop-side
    consolidation already done): every real Owner<->AI protocol/directive/rule file previously scattered
    under ai-os/OWNER_DIRECTIVES/ (7 files) folded into exactly ONE protocol file (PROTOCOL_OWNER_AI.yaml
    -- execution location, OWNER_ENGINE gate, communication channels, traceability, completion discipline,
    7-step workflow, quality bars, software-execution-only rule, task lifecycle state machine, Owner<->AI
    runbook) and exactly ONE lessons/incidents memory file (MEMORY_OWNER_AI.yaml -- self-correction rules,
    dated real incidents, open coordination gaps). 4 of the 7 original files (OWNER_ENGINE_MANDATORY_GATE_2026-07-25.txt,
    CHATGPT_AUDIT_AND_PROMPT_LIBRARY_WORKSPACE_2026-07-24.txt, WIRING_ENGINE_SOFTWARE_2026-07-24.txt,
    BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt) were RETAINED at their original path (not deleted)
    because they are cited by exact path elsewhere in the real system -- each now carries a "SUPERSEDED
    BY PROTOCOL_OWNER_AI.yaml" header note. Enforced going forward by scripts/check_single_protocol_file.py,
    wired into scripts/preflight-guard.py''s check_single_protocol_file() (runs before every real task
    dispatch).

    '
  status: consolidated_2_canonical_files_plus_4_citation_retained_2026-07-26
  query_command: python3 scripts/superboss-register.py query-knowledge "owner_ai_protocol" --tag domain:owner_ai_protocol
  verify_command: python3 scripts/check_single_protocol_file.py
- id: engines_gateways_architecture
  type: engine_inventory_capability_schema_and_phase_plan
  path: ai-os/20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml + ai-os/CAPABILITY_REGISTRY_SCHEMA_2026-07-24.yaml
  scope: 'Phase 0 (task-20260724-053213, instruction INS-20260724-053123-0e5a) of the VERIDIAN 20-ENGINE/
    10-GATEWAY architecture: a real, verified inventory mapping all 20 named engines (Intent, Context,
    CapabilityRegistry, Planning, Policy, Rule, Decision, Workflow, Automation, Integration, Document,
    Notification, Data, Metadata, Knowledge, Learning, UIComposition, Analytics, Audit, Observability)
    to existing prior art in compliance-tracker/projexa/veda-advisors + ai-os/+scripts, produced by ai-os-scripts/generate_engines_gateways_inventory.py
    (every exists_as path live-verified). Result: 19/20 partial coverage, 1/20 full (Knowledge Engine)
    -- zero engines are NONE, confirming the Owner''s standing instruction that most engines already exist
    under a different name. Also designs the Capability Registry Engine''s PART4 schema (the piece flagged
    as most important missing) with a lookup_contract and 5 real populated capabilities, verified by ai-os-scripts/generate_capability_registry_candidates.py.
    Distinct from registries.auditor_engine (Engines 19/20, Audit/Observability, cross-linked not re-planned)
    and unrelated to compliance-tracker/ai-os/engines/ENGINES.yaml (a namespace collision, confirmed this
    session, not prior art for this registry).'
  status: phase_0_through_phase_5_complete_phase_6_plus_not_yet_dispatched
  query_command: python3 scripts/superboss-register.py query-knowledge "engines_gateways_architecture"
    --tag domain:engines_gateways_architecture
  next_phase: phase_6_intent_context_planning_learning (depends_on phase_1 + phase_5, both now done) and
    phase_7_ui_composition_analytics per ai-os/20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml phases[]
    -- each dispatched as a separate task, not part of this registry entry's own scope.
  phase_1_capability_registry_live_wiring: 'DONE (task-20260724-083420, 2026-07-24) -- real capability_registry
    table live in ai-os/memory/superboss-register.sqlite (extends the same DB as knowledge_engine, scripts/superboss-register.py
    register-capability/lookup-capability/ list-capabilities CLI), populated with 5 real, hand-verified
    VERIDIAN capabilities (0 invented), and scripts/task-gateway.py submit now calls lookup-capability
    before every task dispatch (Engine 3 is now live, not planned-only -- see 20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml
    phases[0].status_detail for full evidence). Query: python3 scripts/superboss-register.py list-capabilities'
- id: auditor_engine
  type: audit_domain_inventory_and_phase_plan
  path: ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml + ai-os/AUDITOR_ENGINE_EVENT_SCHEMA_2026-07-24.schema.json
    + ai-os/AUDITOR_ENGINE_FINDING_RECORD_SCHEMA_2026-07-24.schema.json + ai-os/AUDITOR_ENGINE_TOOL_SMOKETEST_EVIDENCE_2026-07-24.yaml
  scope: Phase 0 (task-20260724-042659, instruction INS-20260724-042330-8e4b) of the VERIDIAN AUDITOR
    ENGINE -- a real, verified inventory + phased build plan (Phase 0-8) covering PART1-6 of the Owner's
    directive. Distinct from registries.audit198_framework (a different, pre-existing, narrower AI-prompt-based
    198-rule VERIDIAN-specific engine -- not modified). This registry is for the standards-based engine
    (OWASP/WCAG/ISO/TOGAF/BPMN/OpenAPI/etc. citations), one per each of the 15 audit domains (business-capability,
    ddd, enterprise-architecture, clean-architecture, product-quality, data-model, workflow, api-contract,
    metadata, security, ux, ai-governance, integration, test-coverage, documentation) plus a PART6 observability/audit-trail
    layer (7 named repos, one event schema, OpenTelemetry + pgAudit wiring -- design only this phase,
    enforcement is Phase 7). This phase also installed and real-smoke-tested 5 standalone OSS scanners
    (Gitleaks 8.30.1, Trivy 0.72.0, Spectral 6.16.2, Checkov 3.3.8, OWASP Dependency-Check 12.2.2 Core)
    with real findings captured against compliance-tracker -- see the smoketest-evidence file above for
    exact commands/output, and knowledge_engine (source:LOCAL tag) for each tool's own registered row.
  status: phase_0_complete_phase_1_security_domain_complete_test_coverage_and_ux_subscopes_not_started
  query_command: python3 scripts/superboss-register.py query-knowledge "auditor_engine" --tag domain:auditor_engine
  phase_1_security_domain: 'task-20260724-083724-phase1-auditor-engine-security-scanners (2026-07-24)
    wired gitleaks/trivy/checkov into a real cron-scheduled pipeline (ai-os/scripts/audit_pipeline_security.py,
    0 5 * * * via run-logged.sh) that writes finding-record-schema-conformant rows into a new audit_findings
    table in this same knowledge_engine DB. Real first run against compliance-tracker: 29 findings (27
    gitleaks, 0 trivy -- documented bun.lock gap unchanged, 2 checkov). Dependency-Check still excluded
    pending an NVD_API_KEY. See AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml phases[1].evidence for full
    detail.'
  next_phase: Phase 1's own test-coverage and ux sub-scopes (coverage-threshold gate, @playwright/test
    for veda-advisors, axe-core against running routes), then Phase 2 (API contract authoring + linting)
    per AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml phases[] -- each dispatched as a separate task, not
    part of this registry entry's own scope.
- id: self_sustaining_system_engine
  type: knowledge_registry_and_software_catalog
  path: ai-os/memory/superboss-register.sqlite (knowledge_engine table) + ai-os/SOFTWARE_CATALOG.yaml
  scope: 'Single entrypoint for Knowledge Engine Phase 2 (task-20260724-033446): extends knowledge_engine
    (Phase 1, 9 SERVER rows) to 26 rows across 5 source-tagged categories -- SERVER/VERCEL/SUPABASE/GITHUB/LOCAL
    -- each with a real verification_status; populates entity_relationships for every row (26/26 non-empty);
    adds a real, machine-generated ai-os/SOFTWARE_CATALOG.yaml (scripts/cron/systemd inventory, generated
    by ai-os/scripts/generate_software_catalog.py, not hand-typed); wires a real re-verify of touched
    knowledge_engine rows into scripts/task-gateway.py close; resolves the 3 Phase-1 PATH_MISSING drift
    rows (1 now VERIFIED_MATCH, 2 intentionally left PATH_MISSING with corrected citing text, logged);
    adds artifact_path to knowledge_engine_fts (fts5_relevance_tuning). Full detail: ai-os/STANDING_DIRECTIVE.yaml
    v2_self_sustaining_system_engine. Known gap: SUPABASE Management API refresh degraded pending a working
    SUPABASE_ACCESS_TOKEN (see ai-os/KNOWLEDGE_ENGINE_PHASE3_CANDIDATES_2026-07-24.yaml, ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml#supabase-management-api-token-refresh
    -- a real Owner-dashboard credential dependency, not a code gap). Phase 5 (task-20260724-135834) shipped
    a real mitigation, not a full fix: a per-project anon-key /auth/v1/health fallback keeps SUPABASE
    rows genuinely live-checked every cron tick even while the Management API token stays broken. Phase
    5 also reconciles this registry entry against MASTER_INDEX.yaml''s own registries: section -- see
    registries.metadata_engine_reconciliation below.'
  status: live
  query_command: python3 scripts/superboss-register.py query-knowledge "<search>" [--tag source:VERCEL|source:SUPABASE|source:GITHUB|source:LOCAL|source:SERVER]
  refresh_commands: python3 scripts/knowledge_registry_multisource.py (every 6h via crontab); python3
    scripts/generate_software_catalog.py (daily 04:15 via crontab)
- id: metadata_engine_reconciliation
  type: sync_direction_contract_and_enforcement_script
  path: ai-os/METADATA_ENGINE_RECONCILIATION_2026-07-24.yaml + scripts/metadata_reconciliation.py
  scope: 'Phase 5 (task-20260724-135834, phase_5_metadata_knowledge_consolidation) of the VERIDIAN 20-ENGINE/10-GATEWAY
    architecture (ai-os/20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml), closes engines 14/15 (Metadata/Knowledge).
    Reconciles this registries: section against knowledge_engine (registries.self_sustaining_system_engine)
    -- full merge judged too disruptive (91 heterogeneous entries, actively cited by id across this whole
    session), so this documents and ENFORCES a sync direction instead: MASTER_INDEX.yaml stays authoritative
    for human citation/scope narrative, knowledge_engine stays authoritative for machine drift-detection,
    and every registries[] entry''s real on-disk file path(s) get a knowledge_engine row tagged registry:<id>
    as the queryable back-reference between the two. Live-run 2026-07-24: 74 new rows registered + 14
    existing rows tagged (of 88 real file-path tokens extracted from 83/91 registries -- 6 entries have
    no path field by design, 2 named directory-only/not-yet-created paths, both listed honestly in the
    script''s own report rather than silently skipped); re-run confirmed idempotent (0 new, 88 no-op re-tags,
    knowledge_engine row count unchanged).'
  status: live
  query_command: python3 scripts/metadata_reconciliation.py report
  do_not_build_a_parallel_reconciliation_mechanism: if a future session needs to check registries<->knowledge_engine
    sync drift again, re-run scripts/metadata_reconciliation.py (safe, idempotent) rather than writing
    a second reconciliation script.
- id: rules_articles_198
  type: guardrail_ruleset
  path: ai-os/RULES_ARTICLES_198.json
  scope: 'Owner directive 2026-07-21: the 99 Rules + 99 Articles governing the complete VERIDIAN AI OS
    system, supplied by the Owner (RULES.xlsx), extracted losslessly. THIS IS A LIVE, SYSTEM-WIDE GUARDRAIL
    -- every AI agent (Mother Router, Superboss, Supervisor, any worker doing software development) shall
    keep these 198 items in consideration WHILE doing work, not just as something audited after the fact.
    Complements ai_engineering_policy (the HOW, a build-vs-buy playbook) and ai-os/CONSTITUTION.yaml (a
    differently- numbered, separately-sourced rule registry, zero literal overlap confirmed via grep)
    -- this file is the Owner''s own canonical 198-point checklist, distinct from both.'
  status: live
  mandatory_before: doing any work on this system -- FChecklist, VERIDIAN AI OS, compliance-tracker, PROJEXA,
    server, GitHub, Vercel, Supabase, product, project, brands, AI models, end user
  verification_mechanism: registries.audit198_framework (re-runnable, evidence-based scoring against this
    exact file)
- id: audit198_framework
  type: verification_mechanism
  path: ai-os/scripts/audit198/ (run-audit.mjs, evidence-engine.mjs, category-checkers.mjs, rules-taxonomy.mjs,
    constitution-index.mjs, results/audit198-results.json, results/audit198-summary.md)
  scope: 'Re-runnable, software-driven gap-audit engine for rules_articles_198 (above) -- run: node ai-os/scripts/audit198/run-audit.mjs.
    Groups the 198 items into 33 thematic categories, each backed by a generic evidence- gathering + verdict
    engine (file-marker checks, repo-wide grep with file:line citations, cross-reference against ai-os/CONSTITUTION.yaml''s
    own status vocabulary). Verdict vocabulary: ENFORCED / PARTIALLY_ENFORCED / POLICY_ONLY / NOT_APPLICABLE_YET
    / NOT_YET_BUILT / NEEDS_HUMAN_JUDGMENT / EVIDENCE_UNAVAILABLE. THIS is the objective proof mechanism
    for whether the system-wide guardrail above is actually being honored -- re-run it, don''t just assert
    compliance.'
  status: live, real 198/198 run completed 2026-07-21, actively being extended by a multi-wave gap-closure
    initiative (see ai-os/boss/ACTIVE-CLAIMS.yaml, "Close all 176 non-ENFORCED items")
  do_not_build_a_parallel_audit_mechanism: if a future session needs a different verification angle for
    these 198 items, EXTEND this framework's category-checkers.mjs with a new check, or improve evidence-engine.mjs's
    detection logic when a real capability exists but isn't being recognized -- never weaken a checker
    just to inflate a score, and never build a second scoring script for the same 198 items.
- id: standing_task_directive
  type: guardrail_ruleset
  path: ai-os/STANDING_DIRECTIVE.yaml
  scope: 'Owner directive 2026-07-21: machine-readable encoding of the Owner''s single-line standing per-task
    prompt. Cite this file (by id or path) with any task instead of restating the full protocol -- it
    points to precheck (MASTER_INDEX.yaml+system_index), execution (SSH-only, VERIDIAN-DEV), guardrails
    (rules_articles_198 + ai_engineering_policy incl. automation_scope_boundary), logging (ACTIVE-CLAIMS.yaml/
    COMPLETED.yaml/system_index), verification (audit198_framework re-run, never self-certified), and
    complete sync scope (server, GitHub, Vercel, Supabase).'
  status: live
  mandatory_before: doing any work on this system when cited alongside a task -- same mandatory_before
    class as rules_articles_198 and ai_engineering_policy, which this file references rather than duplicates
- id: ai_engineering_policy
  type: policy_doc
  path: ai-os/AI_ENGINEERING_POLICY.yaml
  scope: 'Owner directive 2026-07-21: mandatory engineering playbook for the whole integrated system (FChecklist,
    VERIDIAN AI OS, compliance-tracker, PROJEXA, server, GitHub, Vercel, Supabase, product, project, brands,
    AI models, end user). A 30-item build-vs-buy priority order (existing capability/config/package/ framework/...
    before new code), explicit always_prefer / never_behave_like_a_human_operator lists, and a continuous
    optimization checklist. Complements, does not duplicate, ai-os/RULES_ARTICLES_198.json / ai-os/CONSTITUTION.yaml''s
    own declarative "software first, AI second" rules -- those declare WHAT the system must be; this file
    is the operational HOW, a playbook to consult per-task rather than a per-item scored registry.'
  status: live
  mandatory_before: starting any engineering, software-development, systems-management, or process-design
    task on this system
- id: system_index
  type: sqlite_table
  path: /opt/veridian/ai-os/memory/superboss-register.sqlite#system_index
  scope: Existence-check for real mechanisms (scripts/services/tables/docs) across the whole ops layer
    -- "does X already exist" before building.
  query_cmd: python3 /opt/veridian/scripts/superboss-register.py check-duplicate "<query>" [--category
    <cat>]
  row_count_at_build: 28
  status: live
  mandatory_before: writing any new script, table, service, or doc on this server
- id: superboss_dialogue_log
  type: sqlite_tables
  path: /opt/veridian/ai-os/memory/superboss-register.sqlite#instructions,work_items,actions
  scope: Owner<->Superboss instruction/work/action audit trail. Not reachable from TypeScript/Vercel app
    code (different host, no bridge built). Manual logging (an AI agent choosing to call log-instruction/log-work/log-action)
    still exists for milestone granularity -- see registries.auto_conversation_log for the unconditional,
    platform-enforced layer added 2026-07-20.
  query_cmd: python3 /opt/veridian/scripts/superboss-register.py search "<query>"
  doc: /opt/veridian/repos/compliance-tracker/ai-os/SUPERBOSS_REGISTER.md
  status: live
- id: auto_conversation_log
  type: hook_pipeline
  scope: 'Root-cause fix for "you not logging every discussion" (Owner, 2026-07-20). Confirmed the gap
    first, not assumed: a direct query before this fix found 13 instructions / 60 work_items / only 3
    actions logged for the entire day against 150+ real tool calls -- manual, milestone-granularity logging
    was the actual cause. Replaced with Claude Code''s own hook system, which fires unconditionally on
    every prompt and every tool call regardless of whether an AI agent remembers to log it.'
  components:
    local_hooks:
      path: C:\Users\Dell\Downloads\Claude Code\.claude\hooks\log-prompt.ps1, log-tool-use.ps1, log-turn-end.ps1,
        sync-queue-to-server.ps1
      registered_in: C:\Users\Dell\Downloads\Claude Code\.claude\settings.local.json (hooks.UserPromptSubmit,
        hooks.PostToolUse[Bash], hooks.Stop)
      design: Hooks write instantly to a local NDJSON queue (.claude/hooks-queue/conversation-log.ndjson)
        -- zero network I/O in the interactive path, since this session's own SSH connection showed real,
        repeated transient timeouts all evening and a live SSH call inside every hook would have made
        every single prompt/tool-call fragile and slow. The Stop hook starts a DETACHED background sync
        process (Start-Process -WindowStyle Hidden) that drains the queue asynchronously -- the interactive
        turn never waits on it. Every hook script is wrapped in try/catch that swallows all errors and
        always exits 0 with no blocking "decision" field -- confirmed via Claude Code's hook exit-code
        semantics that this can never block or fail the actual interaction.
    sync_script:
      path: C:\Users\Dell\Downloads\Claude Code\.claude\hooks\sync-queue-to-server.ps1
      design: Renames the live queue to a timestamped pending-*.ndjson file (atomic, avoids a race with
        hooks still appending), uploads it in ONE scp, runs ONE remote batch-import invocation, deletes
        the pending file only on confirmed success. On any failure (confirmed live during testing -- a
        real SSH connection timeout hit on the very first sync attempt), the pending file is preserved
        and retried on the next Stop event -- no queued event is ever silently lost, errors land in hooks-queue/sync-errors.log.
    batch_importer:
      path: /opt/veridian/scripts/batch-import-conversation-log.py
      mirror: /opt/veridian/repos/compliance-tracker/ai-os/scripts/batch-import-conversation-log.py
      design: 'Bulk-inserts a whole NDJSON batch into instructions/actions directly via sqlite3, reusing
        superboss-register.py''s own schema/ID conventions exactly (not a parallel logging path). Real
        bug found and fixed before this was trusted: the first real batch run (395 rows) hit a genuine
        UNIQUE constraint collision on action_id -- the CLI''s original _new_id() (timestamp + 4-hex-random)
        is safe for one-ID-per-process- invocation use but not for a tight loop minting hundreds of IDs
        within the same wall-clock second (a real birthday-paradox collision, not a freak accident). Fixed
        with a monotonic per-process counter appended to every ID.'
    end_to_end_test: 'Full pipeline verified with real data before being trusted: (1) manual synthetic
      payloads through all 3 hook scripts, confirmed correct queue writes and exit 0; (2) live background
      sync, which hit and correctly recovered from a real transient SSH timeout without losing the pending
      file; (3) verified the synced rows are real and FTS-searchable in the register; (4) all manual-test
      rows cleaned up afterward so they don''t pollute the real record.'
  historical_backfill:
    scope: This session's own history (this VERY conversation) was backfilled retroactively, not just
      logged going forward -- answers the Owner's direct question "is our current discussion... logged...
      even after few months."
    method: backfill-session-transcript.py parses the local Claude Code session JSONL transcript directly
      (12MB, 2812 raw entries for this session alone), extracting only genuine user-typed messages (string
      content, not tool_result feedback wrapped as a user-role turn) and assistant text blocks (not the
      individual historical tool_use blocks -- that granularity is captured live going forward by the
      hooks, not retroactively reconstructed, an explicit, deliberate scope limit, not an oversight).
    result: 39 real user instructions + 395 assistant response excerpts imported for this session, tagged
      utm_campaign=conversation-log-backfill (distinct from conversation-log-auto, the live hook-captured
      campaign, so the two are never confused). First imported row verified to be this session's actual
      first message.
    scope_not_covered: Other, earlier Claude Code sessions on this laptop (dozens of other .jsonl transcript
      files exist under C:\Users\Dell\.claude\projects\C--Users-Dell-Downloads-Claude-Code\) were NOT
      backfilled this pass -- only the current session. A full-history backfill across all prior sessions
      is real, scoped, larger follow-up work, not done here.
  query_examples:
  - python3 superboss-register.py search "<keyword>"
  - 'SQL: SELECT * FROM instructions WHERE utm_campaign IN (''conversation-log-auto'',''conversation-log-backfill'')
    ORDER BY ts'
  status: live, deployed and verified 2026-07-20
  scope_gap_at_build_time: 'This registry entry, as first built, only covered the laptop-side interactive
    Claude Code CLI session -- confirmed by the Owner same day ("we are not auto updating everything")
    to be an incomplete fix: the autonomous worker/supervisor/doc-worker fleet dispatched ON THIS SERVER
    (queue-dispatcher.py, module-queue-dispatcher.py, master-decompose.py, systemd-run tasks) was never
    touched. Closed by registries.auto_worker_task_log, same day, same pass.'
- id: auto_worker_task_log
  type: python_instrumentation
  path: /opt/veridian/scripts/veridian-task.py
  mirror: /opt/veridian/repos/compliance-tracker/ai-os/scripts/veridian-task.py
  scope: 'The server-side half of the auto-logging fix. Owner: "for your work that CLAUDE CODE CLI or
    CLAUDE CODE is doing and ai superboss, ai agents, software are doing need to be automatically updated
    in the server." registries.auto_conversation_log only covered the interactive laptop session -- this
    covers every dispatched AI worker/supervisor/doc-worker task, regardless of what created it.'
  design: 'veridian-task.py''s cmd_create/cmd_checkpoint/cmd_record_usage are the single choke point EVERY
    dispatch path already goes through (queue-dispatcher.py, module-queue-dispatcher.py, dispatch-docworker-task.sh,
    a manual CLI call) -- instrumenting this one file once, rather than each caller separately, is the
    same "hook the choke point" principle as the interactive UserPromptSubmit/PostToolUse hooks, applied
    to the dispatch layer. _auto_log_task_event() shells out to superboss-register.py''s own already-tested
    log-work/log-action CLI (not a parallel write path), reusing the ai_task_id convention documented
    in that script''s own header (the existing task_id reused verbatim, never reissued). No network-latency
    design concern here unlike the laptop hooks -- this all runs locally on the server already. Wrapped
    in try/except that swallows all errors: a logging failure must never break real task lifecycle management,
    which is this script''s actual job.'
  tested: _auto_log_task_event() called directly in isolation against a synthetic fake task dict (not
    a real dispatched task, to avoid triggering a real git worktree/systemd service) before trusting it
    -- confirmed correct rows landed in work_items (ai_task_id correctly populated, not a new ID) and
    actions, then cleaned up. Confirmed `veridian-task.py status` still runs correctly end-to-end against
    real existing task data post-edit, before calling this deployed.
  scope_not_covered: cmd_resume_context (read-only, no state change, not logged by design). The cron-job
    gap noted here originally is CLOSED as of the same day -- see registries.auto_execution_wrapper.
  status: live, deployed and verified 2026-07-20
- id: auto_execution_wrapper
  type: shell_wrapper
  path: /opt/veridian/scripts/run-logged.sh
  mirror: /opt/veridian/repos/compliance-tracker/ai-os/scripts/run-logged.sh
  scope: 'Closes the honest gap stated in registries.auto_worker_task_log and registries.auto_conversation_log:
    the periodic cron jobs that never call veridian-task.py and were never covered by the interactive-session
    hooks either. Owner asked directly: "in modern software without AI, how do you ensure tasks/actions
    are logged with id and time/date" -- answer applied here is the standard middleware/aspect pattern:
    a single wrapper every job runs THROUGH, so coverage does not depend on the wrapped script''s own
    author remembering to add logging (the same failure mode fixed twice already this session, now fixed
    at the process- supervision layer instead of the application layer).'
  design: Generates a unique exec_id + UTC start timestamp before running the real command; logs a job_start
    action immediately, runs the command with combined stdout+stderr capture, logs a job_end action (status
    completed/failed, real exit code, duration, output excerpt) after. Echoes the original output and
    exits with the original exit code -- fully transparent to cron and to any pre-existing `>> logfile
    2>&1` redirect, which all 4 already- redirected jobs keep working exactly as before. Each register
    call individually wrapped in a 10s timeout so a slow/broken register write can never delay or block
    the real job -- logging is best-effort, never load-bearing.
  crontab_coverage: 'All 7 ACTIVE crontab entries as of 2026-07-20 now route through this wrapper: sync-repos.sh,
    sync-vercel-env.sh, sync-verdian-ai-data.sh, sync-controller-back.py, queue-dispatcher.py, health-check-15min.py,
    cost-usage-60min.py. The 1 explicitly disabled entry (#DISABLED-2026-07-18 supervisor-sweep.sh) was
    left untouched, not re-enabled -- re-enabling a disabled job was never asked for. Original crontab
    backed up to /opt/veridian/scripts/crontab-backup-2026-07-20.txt before this change (reversible).'
  tested: 'Wrapper tested standalone with both a success command and a deliberately-failing command (exit
    7) before touching the real crontab -- confirmed correct exec_id/timestamp/status/exit_code/ duration/output
    landed in the register for both paths, correct exit code propagated back to the caller in both cases.
    Then run manually against the REAL health-check-15min.py through the wrapper (not a synthetic stand-in)
    before trusting it on a live schedule -- this real run surfaced a genuine, serious, pre- existing
    production finding unrelated to this fix: 47 systemd units in failed state and a 71.9% task failure
    rate (207/288), both already tracked by health-check-15min.py''s own alerting (registries.attention_alerts),
    now also correctly logged as a real job_end action with that exact output captured.'
  real_unattended_cron_verification: 'CONFIRMED 2026-07-20T10:40:01Z: queue-dispatcher''s real, natural
    10-minute cron tick fired with zero manual intervention (crontab was installed ~10:29, prior firing
    at 10:30 was still the old/unwrapped entry, this 10:40 firing was the first fully unattended one under
    the new wrapper). Produced a matching job_start/job_end pair (exec_id=1e314ccbb970, status=completed,
    exit_code=0, real output "Running workers: 0, free slots: 5 / PROGRESS: 4/25 groups completed") --
    proves the crontab entry itself works end to end, not just manual invocation of the same command line.'
  status: live, deployed and verified 2026-07-20 (all 3 auto-logging layers -- interactive session, worker-task
    dispatch, cron -- now confirmed with real evidence, not just claimed)
- id: worker_fleet_rca_fix_2026_07_20
  type: rca_and_permanent_fix
  scope: Root-causes and permanently fixes the 47-failed-unit/71.9%-task- failure-rate incident surfaced
    by registries.auto_execution_wrapper's first real health-check-15min.py run. Not the same incident
    as RCA_OVERNIGHT_DISPATCH_FAILURE_2026-07-20.md (that RCA is about the dispatch LOOP not firing overnight
    due to a laptop-bound Claude Desktop scheduled task; this RCA is about dispatched tasks failing downstream
    once running). Both concern the same overnight window, different mechanisms, cross-referenced here
    to prevent conflation.
  root_cause_1: OpenRouter account genuinely exhausted (total_credits=$40, total_usage=$40.07+, confirmed
    via OpenRouter's own /api/v1/credits endpoint -- ground truth, not this proxy's internal tracker).
    preflight-guard.py's existing check_proxy_health() only tracked the LOCAL proxy's own cumulative spend
    counter, which had drifted out of sync with the real account. Every worker's cheap 5-token canary
    call kept passing preflight (cheap enough to still succeed near-zero balance); the real task prompt
    (up to 64000 max_tokens) then hit a live OpenRouter 402, which systemd retried 3x per unit into a
    permanent failed state -- across at least 47 units.
  root_cause_1_deeper_bug: '`claude -p --output-format json` returns process exit code 0 even when the
    underlying API call failed (error captured only inside the JSON payload as is_error:true / api_error_status:402).
    This silently bypassed worker-entrypoint.sh''s entire `if [ "$EXIT_CODE" -ne 0 ]` failure path --
    no failure signature was ever recorded, so preflight-guard.py''s own circuit breaker (2 identical
    signatures trips it) had zero data to trip on despite 3 real, identical failures per unit. Confirmed
    directly: .failure_signatures.json did not exist for a sample failed task.'
  root_cause_2: 'A genuinely SEPARATE, second root cause found for 2 of the 47 units (deliberately not
    assumed to share root_cause_1 -- verified via recover-failed-workers.py reading each unit''s real
    result.json before touching it): these 2 legitimately hit the Claude CLI''s own --max-budget-usd $10
    per-task cap (terminal_reason:budget_exhausted, real ~$10.05-10.32 costs, 42-55 real turns) -- a guardrail
    working as designed, but worker-entrypoint.sh fell through to the same generic retry-storm path as
    any other failure instead of a clean hard-stop.'
  fix_1: 'preflight-guard.py: new check_openrouter_balance() function queries https://openrouter.ai/api/v1/credits
    directly (ground truth, cannot drift). Fails OPEN on any network/parse error on the check itself (never
    blocks real work over a check hiccup); fails CLOSED (hard stop, no API call made) if real remaining
    balance < $0.10. Ops-layer twin of the app-layer checkOpenRouterBalance() in cost-policy.ts. Wired
    into main() between check_proxy_health() and canary_call().'
  fix_2: 'worker-entrypoint.sh: (a) added "openrouter_balance_exhausted" to the hard-stop (non-retryable)
    reason list; (b) added an API_IS_ERROR detection block right after the main claude -p invocation that
    reads $MAIN_OUT''s own is_error field and forces EXIT_CODE=1 when true despite the process''s own
    exit 0, closing the deeper bug above; (c) enhanced record_failure_signature() to also hash result.json''s
    real API error text, not just worker.log (which is empty for this failure class); (d) added a second,
    distinct hard-stop block detecting subtype==error_max_budget_usd / terminal_reason==budget_exhausted
    in $MAIN_OUT, routing to checkpoint(status=blocked) + best-effort git commit/push of whatever progress
    existed + systemctl --user disable + explicit kill/wait of the background checkpoint loop + exit 0
    -- no retry, since a retry would very likely spend another full $10 hitting the identical wall.'
  fix_3_recovery_script:
    path: /opt/veridian/scripts/recover-failed-workers.py
    scope: 'Converts veridian-worker@ units stuck in systemd''s permanent failed state (pre-dating the
      fixes above) into an accurate blocked state via the now-fixed preflight guard. Safety: reads each
      unit''s REAL result.json before touching it -- only resets+starts units confirmed to match the 402-balance
      pattern (string match on api_error_status:402), explicitly does NOT touch veridian-docworker@ units
      (different template, different entrypoint, --no-proxy mode) or any unit whose result.json doesn''t
      match, printing those separately for manual review rather than assuming one cause explains everything.'
  deployed_and_verified: 'Both worker-entrypoint.sh fixes deployed live (2 backups taken: .bak-2026-07-20-rca
    before fix_2a/b/c, a second implicit backup via the same pattern before fix_2d). Fix 1+2a/b/c verified
    via one real live systemd unit reset+restarted, reaching a clean blocked state instead of retry-storming.
    Fix 2d verified via both previously-excluded max-budget-usd units, reset+restarted, both confirmed
    status:blocked, service disabled cleanly, zero retry-storm. recover-failed-workers.py dry-run confirmed
    44/46 as 402-pattern (2 correctly excluded, matching root_cause_2) before running for real: all 44
    recovered. Final state 2026-07-20T11:2x: 0 failed veridian-worker@ units remain (down from 47); OpenRouter
    balance itself still exhausted ($-0.07 remaining as of this check) so the 44+2=46 recovered units
    now correctly sit in blocked state with an accurate diagnostic, not a dead unexplained failure.'
  correction_2026_07_20_audit_round_2: 'This entry originally claimed here (and in audit_log pass 11)
    that recovered units "will resume automatically once credits are added, no further code change needed."
    VERIFIED FALSE on audit: queue-dispatcher.py''s retry logic (TERMINAL_BAD handling in sync_dispatched_statuses())
    only re-touches gap_queue.yaml items still in ''dispatched'' status -- 21 of the 46 recovered tasks
    were already marked stuck_needs_human (a terminal state it never revisits) BEFORE this pass''s recovery,
    and the other 25 were never tracked in gap_queue.yaml at all (dispatched via a different, one-off
    mechanism). Neither group would have resumed on its own. Corrected by building registries.system_sync_mechanism.checks.resume_balance_blocked_check,
    which operates directly on CONTROLLER.yaml/systemd instead of gap_queue.yaml''s narrower tracking,
    so it covers both groups uniformly. That check now makes the original claim actually true going forward
    (verified via a monkeypatched-balance dry-run: correctly identified all 47 balance-blocked tasks as
    resumable, took no action in dry-run mode as designed).'
  known_gap_not_fixed_this_pass: doc-worker-entrypoint.sh (separate script, veridian-docworker@ template,
    used for real-Claude-subscription browser reverse-engineering tasks, runs preflight-guard.py with
    --no-proxy so check_openrouter_balance() correctly does not apply there) does NOT have the same API_IS_ERROR
    / CLI_HIT_BUDGET_CAP detection blocks as worker-entrypoint.sh. Currently 0 failed docworker units
    (not urgent), but the same exit-code-0-on-API-error class of bug could affect it too if its own claude
    -p invocation ever hits a real-subscription-side error. Not in scope for this RCA (zero failures currently,
    no evidence of the bug manifesting there) -- flagged honestly rather than silently left uninvestigated.
  status: live, deployed and verified 2026-07-20
- id: system_sync_mechanism
  type: python_script
  path: /opt/veridian/scripts/system-sync.py
  mirror: /opt/veridian/repos/compliance-tracker/ai-os/scripts/system-sync.py
  scope: 'TASKS 2-6 of the Owner''s 2026-07-20 systems-design directive, built as ONE unified mechanism
    deliberately, not 5 separate scripts -- the directive itself (TASK 3/4: "System is broken. Its working
    in parts. Make it integrated... You are facing it again and again. Though you are updating few files
    other are getting missed.") is a direct complaint about exactly the kind of proliferating-parallel-mechanisms
    failure mode a 5th/6th/7th standalone script would repeat. Every check is mechanical (file existence,
    git-log date comparison, path diffing) -- zero AI judgment embedded in detection, by design (judgment
    calls like "what should this doc''s corrected sentence say" are left to whoever reads the findings,
    not auto-generated).'
  checks:
    mirror_drift_check: Diffs /opt/veridian/scripts/*.py|sh and MASTER_INDEX.yaml against their git-tracked
      repo-mirror counterparts. SAFE to auto-fix (pure file copy, staged not committed -- committing still
      requires the same manual verify-then-commit discipline used all session). First real run found 17
      files with ZERO mirror at all -- including queue-dispatcher.py, which fires live via cron every
      10 minutes and had no git-tracked copy whatsoever (a genuine, serious instance of the exact "files
      getting missed" pattern TASK 3/4 named). All 17 copied into the mirror and committed same pass.
    constitution_staleness_check: Extracts every backtick-quoted src/lib/*.ts path from CONSTITUTION.yaml's
      mechanism/evidence fields (same claim shape claim-verification.ts already checks for AI OUTPUT,
      GP-08/GP-09 -- reused here to check the DOCUMENT itself, not duplicated logic), compares each referenced
      file's last git- commit date against CONSTITUTION.yaml's own last-commit date. Report-only, appends
      to registries.attention_alerts, never auto-edits the doc. First real run found 1 apparent broken
      reference (src/lib/veridian-client.ts) that was actually a cross-repo false positive -- the file
      legitimately lives in FChecklist/projexa, not compliance-tracker, and CONSTITUTION.yaml's control_model.products
      section correctly describes other repos' files as compliance evidence. Fixed by checking all 3 known
      repos (compliance-tracker, projexa, veda-advisors) before flagging broken -- 0 genuine findings
      after the fix.
    unindexed_files_check: Walks canonical_dirs.ops_scripts and .ai_os_governance_repo_copy, flags any
      file MASTER_INDEX.yaml never mentions by name. Report-only, appends to registries.attention_alerts.
      First real run found 15 ai-os/ root .md docs genuinely absent from this file's text (not even in
      file_inventory.ai_os_governance_docs's own unannotated_note, which only said "several" without naming
      them) -- see that field's now-explicit list, generated from this check's output rather than hand-typed.
    resume_balance_blocked_check: 'Added during this file''s own audit round 2 (self-audit found a false
      claim in this registry entry''s earlier text -- see registries.worker_fleet_rca_fix_2026_07_20.correction_2026_07_20_audit_round_2
      for the full finding). Queries the real OpenRouter balance; if restored above the safety floor,
      finds every task whose LAST checkpoint note is the openrouter_balance_exhausted hard stop (parsed
      from task.yaml''s real checkpoints list, not string- matched against the whole file) and restarts
      its systemd unit directly -- covers every balance-blocked task regardless of whether queue-dispatcher.py''s
      own gap_queue.yaml ever tracked it. Verified via a monkeypatched-balance dry-run (not yet verified
      against a real restored balance, since the account is still genuinely exhausted as of this pass
      -- that real-money end-to-end proof can only happen once the Owner adds credits). EXTENDED 2026-07-20
      (Owner TASK 5, new directive): consults get_held_task_ids() -- currently sourced from gap_queue.yaml.held_task_ids
      when gap_queue.yaml.dispatch_paused is true -- and skips (reports HELD, does not restart) any task_id
      on that list. Added because 11 of the 21 held V2-plan tasks are also balance-blocked and would otherwise
      have been silently auto-resumed by this exact check the moment the Owner adds credits, directly
      contradicting "stop these jobs." See ai-os/GAP_ANALYSIS_2026-07-20_HOLD.md for the full analysis
      and which 21 task_ids are held and why.'
  cron_schedule: 'Added to crontab via run-logged.sh (same wrapper pattern as the other 7 jobs, registries.auto_execution_wrapper):
    `0 */6 * * * python3 /opt/veridian/scripts/system-sync.py --check all`. Every 6 hours -- frequent
    enough to catch drift before it compounds, infrequent enough that report-only findings in ATTENTION.md
    do not spam past what a human/AI can realistically act on between reads.'
  status: live, deployed and verified 2026-07-20 (real first run found and closed 1 genuine gap class
    -- 17 unmirrored live scripts -- and surfaced 2 more for follow-up -- 15 unindexed docs (named, not
    yet individually catalogued), 0 genuine constitution staleness findings this pass)
- id: ops_task_sync_bridge
  type: cross_system_bridge
  scope: 'TASK 1.1, Owner directive 2026-07-20 ("biggest structural gap: the app''s task-tracking and
    this server''s task-tracking are on two different machines with zero bridge"). Verified before building
    (not assumed): platform.task_register does not exist in the app DB; the 2 real candidates (compliance.activity_log,
    compliance.orchestra_executions) are both org_id NOT NULL -- customer-scoped business activity, wrong
    category for internal engineering task state. Built a new, minimal, non-org-scoped table instead (same
    convention as platform.module_registry / platform.ai_model_registry).'
  components:
    db_table: 'platform.ops_dev_tasks (Supabase project pcrjmlpuqsbocqfwoxod, verdian-ai). RLS: SELECT
      true for app_runtime (matches ai_model_registry precedent), service_role bypass for writes. Deployed
      live via migration, confirmed 0 new security-advisor findings.'
    schema_ts: src/lib/db/schema.ts -- opsDevTasks table def, typecheck clean.
    api_route: POST /api/internal/ops-task-sync -- OPS_SYNC_SECRET bearer auth (new dedicated secret,
      not reusing CRON_SECRET, to keep this endpoint's blast radius separate from Vercel's own cron jobs).
      Same isAuthorized() pattern as every other /api/internal/* route. Upserts by ops_task_id.
    ops_side_writer: 'veridian-task.py -- new _sync_to_app() function, called alongside the existing _auto_log_task_event()
      choke point for create/checkpoint (not record_usage, which never changes status). Same fail-open
      discipline: reads OPS_SYNC_SECRET from /opt/veridian/shared/.env, 5s timeout, swallows all exceptions
      -- a sync failure must never block real task lifecycle management.'
    correction_2026_07_20: 'This entry originally claimed "the ops server has no direct Postgres credential"
      as the reason to build the bearer-token API-route bridge instead of a direct DB write. VERIFIED
      FALSE while building TASK 2/4 the same day: a real, working DATABASE_URL already sits in compliance-tracker/.env.local
      on this server, already actively used by health-check-15min.py''s check_mother_router_db() (confirmed
      live: 11 rows in platform.ai_model_registry, exact match against the same table queried independently
      via the Supabase MCP tool). The bearer-token route design itself is still kept -- correctly reasoned
      now: don''t widen the use of an existing broad-access credential for a new write purpose when a
      narrow, single-purpose, independently-revocable channel serves the same need (capability separation
      / least privilege), not "no credential exists at all." Corrected in place per this session''s established
      discipline of flagging its own false claims rather than leaving them uncorrected.'
    pr: FChecklist/compliance-tracker#502 (feature/ops-task-sync-bridge). Tier2 (schema + new secret)
      -- held for Owner sign-off, not auto-merged, per this repo's own protocol. Mandatory audit-check
      CI gate passed (structured 8-field verdict posted, see PR comments).
  verified: npx tsc --noEmit clean (both the schema.ts change and the route.ts add, run separately). Supabase
    get_advisors (security) shows zero new findings attributable to this table. RLS policy content confirmed
    via direct pg_policies query to match the precedent table exactly.
  known_gap_honest: 'NOT yet verified end-to-end against a real live request -- blocked by two independent
    external constraints hit while trying: (1) Vercel hit its account-wide build-rate-limit (24h cooldown,
    unrelated to this change) right as the Preview env var was added, so no fresh preview build exists
    with OPS_SYNC_SECRET present; (2) even an older already-built preview deployment returns a 302 --
    Vercel''s own deployment-protection SSO wall, not this route''s logic. Real E2E proof will happen
    naturally once this PR merges to production (a separate deploy path/quota) -- which requires Owner
    sign-off anyway, so this is not blocking anything that wasn''t already gated. Flagged honestly rather
    than claimed as tested.'
  status: code complete, deployed to DB + PR open, live E2E verification pending merge
- id: system_diagram
  type: generated_documentation
  path: /opt/veridian/scripts/generate-system-diagram.py
  output: ai-os/SYSTEM_DIAGRAM.md (Mermaid, git-tracked)
  scope: TASK 3, Owner directive 2026-07-20 ("make a diagram or tree of the whole system"). Software-generated
    per the directive's own "modern software already has packages, use them" instruction -- Mermaid (native
    GitHub + Artifact rendering), diagram content lives as structured data (SUBSYSTEMS/EDGES) in the generator
    script, not hand-drawn. Reflects the Owner's own two-subsystem framing (Software Development & AI
    System vs Software & End-User Management) verbatim.
  content: 23 components, 21 edges, all verified to actually exist this session (not aspirational) --
    includes both new cross-system bridges (TASK 1.1 write path, TASK 2/4 read path) and the 35 unmigrated
    router call sites as an explicitly labeled known gap.
  to_update: Edit SUBSYSTEMS/EDGES in generate-system-diagram.py and re-run -- do not hand-edit SYSTEM_DIAGRAM.md's
    generated content directly.
  status: live, generated, committed 2026-07-20
- id: e2e_ci_playwright_fix_2026_07_20
  type: ci_fix
  path: /opt/veridian/repos/compliance-tracker/playwright.config.ts
  pr: FChecklist/compliance-tracker#505 (merged)
  scope: 'Spawned background task, flagged during this session''s own audit of TASK 5-6 work: the E2E
    Tests CI job crashed with MODULE_NOT_FOUND at config-load time on every single PR (#501-504, all unrelated
    diffs), silently meaning the E2E suite had never actually run in CI.'
  finding: 'playwright.config.ts imported from "playwright/test" on the unverified assumption that playwright
    was resolvable as a transitive dependency via promptfoo. Verified false directly: no node_modules/playwright
    existed, no playwright/@playwright/test reference anywhere in package.json. `bunx playwright test`
    fetches its OWN isolated playwright@latest into /tmp/bunx-*/ whenever the package isn''t a real project
    dependency, completely bypassing node_modules -- reproduced the exact CI failure locally this way
    before touching anything.'
  fix: Added @playwright/test as a real devDependency (the officially supported setup), imported from
    it directly. bun install resolved and locked @playwright/test@1.61.1 cleanly.
  verified: 'Reproduced the failure locally first, then the fix, both against the exact command CI runs
    (bunx playwright test --pass-with-no-tests) -- not just "it compiles." Confirmed in REAL GitHub Actions
    CI (not just locally): E2E Tests job passed on PR #505''s run, log shows chromium installed + playwright
    test executed cleanly with no MODULE_NOT_FOUND. Confirmed merged content is genuinely on origin/main
    via git show.'
  status: live, merged, verified in real CI 2026-07-20
- id: app_layer_failure_visibility
  type: monitor_extension
  scope: 'TASK 2/4, Owner directive 2026-07-20 ("if task fails (any task, ai task, software task), server
    to know, log it") + ("make the complete server one integrated system"). Real gap found: this ops server
    (health-check-15min.py) had visibility into its OWN systemd worker fleet''s failures only (check_tasks())
    -- zero visibility into app-layer (Vercel/Supabase) task failures.'
  design_decision: Discovered while investigating (see registries.ops_task_sync_bridge.correction_2026_07_20)
    that a real, working DATABASE_URL already exists on this server (compliance-tracker/.env.local), already
    used read-only by check_mother_router_db(). Reused that exact same proven connection pattern instead
    of routing this through the new ops_task_sync_bridge (which is a WRITE path, app<-ops direction) --
    this is a READ, ops<-app direction, and the existing DB connection is the more direct, already-proven
    path for it. No new infrastructure, no new credential.
  implementation: 'health-check-15min.py gained check_app_layer_failures(): queries compliance.orchestra_executions
    for status=''failed'' rows in the last 15 minutes (the only currently-populated app-layer failure
    signal -- activity_log has 0 rows as of this pass, a separate, already-disclosed gap, not fabricated
    data). Wired into main(): called alongside check_mother_router_db(), added to the JSONL record, and
    raises a real anomaly (written to registries.attention_alerts) when recent_failed_count > 0.'
  verified: 'Isolated function test with default 15-min lookback: reachable true, recent_failed_count
    0 (accurate -- no recent failures exist). Re-tested with an artificially wide lookback to prove the
    query logic actually finds real rows, not trivially returning zero: correctly found the 2 known historical
    failed rows. Full script run end-to-end: no crash, existing anomaly detection (systemd/task-failure-rate/disk/mem)
    unaffected, new field present in the JSONL record.'
  status: live, deployed, verified 2026-07-20
- id: ai_router_migration_inventory_2026_07_20
  type: investigation_and_documentation
  scope: TASK 1.3, Owner directive 2026-07-20 ("AI router has deliberately- unmigrated call sites... FIX
    this issue permanently"). Re-verified mother-router.ts's own scope-decision comment (written when
    Mother Router shipped) rather than trusting it or blindly forcing a mass migration to satisfy the
    directive's letter.
  finding: 'The comment''s "~23+3" call-site estimate was stale. Mechanical grep (excluding mother-router.ts
    itself, tests, and the 4 modules being called) found 35 unique files still calling resolveModelConfig()
    or checkTierEligibility() directly: 6 API routes + 29 lib files, spanning core business logic -- crm-service.ts,
    fde-service.ts, gst/ai-review-report.ts, construction-ai-service.ts, ticket-intelligence-service.ts,
    veri-meeting-service.ts, chat-service.ts among them. (35 is a grep-based count, ~1 possible false
    positive from a comment mention in db/schema.ts not re-verified individually -- disclosed, not hidden.)'
  decision: NOT migrated this pass, deliberately. The original author's own risk assessment ("several
    are guardrail-critical dispatch paths") holds up under re-verification -- these are genuinely customer-
    facing, revenue- and compliance-adjacent paths. Forcing a 35-file mass migration in one pass, with
    zero ability to live-test any of it (Vercel's account-wide build-rate-limit was active the same day
    this was checked -- see registries.ops_task_sync_bridge), would be the reckless shortcut this session's
    own verification discipline exists to prevent, not a genuine fix. Updated mother-router.ts's own header
    comment with the accurate count and this reasoning (real, valuable, low-risk documentation fix) and
    recorded the full 35-file list here rather than leaving the directive's "fix this permanently" unaddressed.
  recommended_follow_up: A properly scoped, incrementally-tested migration -- a handful of files at a
    time, each with its own test coverage and a real deploy to verify against, prioritizing the lowest-risk
    files first (e.g. module-rules-resolver.ts, ingest/extractor.ts) and leaving the guardrail-critical
    dispatch route (api/ai/team/dispatch/route.ts) and core revenue-path services for last, reviewed individually.
    Not started this pass.
  status: investigated and documented, migration deliberately deferred with reasoning
- id: gap_queue_dispatch_hold
  type: dispatch_pause_flag
  path: /opt/veridian/ai-os/gap_queue.yaml (live-only, not git-tracked -- same category as CONTROLLER.yaml)
  analysis_doc: ai-os/GAP_ANALYSIS_2026-07-20_HOLD.md (git-tracked)
  scope: 'TASK 5, Owner directive 2026-07-20: analyze the 185 genuinely-open Review-Framework rows (consolidated
    as 25 V2-* tasks in this queue), do NOT work on them, stop auto-dispatch. gap_queue.yaml gained top-level
    dispatch_paused/pause_reason/held_task_ids (21 task_ids, the 21 non-completed of 25 V2 items). Enforced
    in 2 places: queue-dispatcher.py''s main() (checks dispatch_paused first, dispatches nothing if true)
    and registries.system_sync_mechanism.checks.resume_balance_blocked_check (skips held_task_ids). To
    release: set dispatch_paused to false or remove specific IDs from held_task_ids -- no code change
    needed, the pause is data-driven.'
  status: live, deployed and verified 2026-07-20 (queue-dispatcher.py confirmed printing PAUSED and dispatching
    nothing; resume_balance_blocked_check confirmed via monkeypatched dry-run -- 21 held, 0 resumed)
- id: cross_project_controller
  type: yaml_file
  path: /opt/veridian/repos/claude-control/CONTROLLER.yaml
  scope: Outermost requirements+decision catalog, spans every project (compliance-tracker, projexa, veda-advisors,
    claude-control itself). One level above any single project's own tracker. Points at, never duplicates,
    project-local trackers (explicit in its own header).
  git: github.com/FChecklist/claude-control -- pull before read
  status: live
  read_priority: 1
- id: task_state_ledger
  type: yaml_file
  path: /opt/veridian/ai-os/CONTROLLER.yaml
  scope: DIFFERENT FROM cross_project_controller despite identical basename -- this is a locked, auto-generated
    dump of this server's 291 dispatched worker-task execution states (service names, checkpoints, task_dir
    paths). Not git-tracked, not human-maintained.
  name_collision_warning: true
  name_collision_detail: Two files named CONTROLLER.yaml exist (repos/claude-control/CONTROLLER.yaml =
    cross_project_controller, ai-os/CONTROLLER.yaml = this entry). Confirmed 2026-07-20, not yet resolved
    by rename -- deferred because this file is actively read-modify-written with a lock (.controller.lock)
    by veridian-task.py (the live dispatch CLI), and renaming safely requires updating 3 script references
    (veridian-task.py, repair_controller.py, cost-reconciliation.py) plus re-verifying live dispatch end
    to end. Real open follow-up, not silently dropped.
  writer: /opt/veridian/scripts/veridian-task.py (primary, locked read-modify-write)
  rebuild_tool: /opt/veridian/scripts/repair_controller.py (corruption recovery only)
  reader: /opt/veridian/scripts/cost-reconciliation.py
  status: live
- id: constitution
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/CONSTITUTION.yaml
  scope: Canonical governance doc. 30 self-graded guardrail protocols (GP-01..GP-30) with code citations.
    Explicitly the single status table for guardrail grading -- do not re-grade in any other file. Superseded
    ai-os/sentinel/SENTINEL.yaml as the authority (per SENTINEL.yaml's own 2026-07-14 note).
  status: live
  read_priority: 2
- id: master_tracker
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/MASTER-TRACKER.yaml
  scope: The single file for gap analysis / pending work / WIP across the VERIDIAN initiative. Created
    2026-07-12 by consolidating 17 prior tracker/gap-backlog files (5,538 lines) -- those originals are
    archived (see tree_lineage.tree4_unified.subdirs), not maintained. Update discipline is in the file's
    own header (move closed items out, don't let it re-sprawl past ~1000 lines).
  status: live
  last_verified_modified: '2026-07-19T08:15:39+05:30'
  known_gap: No confirmed programmatic link to gap_dispatch_queue -- checked 2026-07-20, queue-dispatcher.py
    does not reference this file. The two can drift; reconciling them is real open follow-up.
  read_priority: 3
- id: gap_dispatch_queue
  type: yaml_file
  path: /opt/veridian/ai-os/gap_queue.yaml
  scope: Live cron-dispatched queue (1634 lines at build time). Distinct from master_tracker -- this is
    dispatch-mechanics state (what's queued/dispatched/blocked for the worker fleet), master_tracker is
    the human-readable gap narrative. See known_gap above on master_tracker.
  dispatcher: /opt/veridian/scripts/queue-dispatcher.py (cron)
  status: live
- id: module_pilot_queues
  type: yaml_files
  path: /opt/veridian/ai-os/queues/*.yaml
  scope: Module-scoped task queues for the Master/Supervisor pilot (master-decompose.py writes, module-queue-dispatcher.py
    reads). Explicitly reuses gap_dispatch_queue's dispatch machinery (veridian-task.py create, worker/supervisor
    systemd, tier1/tier2 audit flow) -- not a parallel dispatch implementation, same engine, different
    queue source. Not a duplicate of gap_dispatch_queue.
  status: live, pilot scope (backend.yaml exists; only backend/database modules active per master-decompose.py's
    PILOT_MODULES)
- id: active_claims
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/ACTIVE-CLAIMS.yaml
  scope: Parallel-session work-claim coordination -- read before picking up any gap to avoid 2 sessions
    building the same thing concurrently. Explicitly referenced (not duplicated) by cross_project_controller.
  status: live
  read_priority: 4
- id: boss_board
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/BOARD.yaml
  scope: Original ComplianceTrack task board.
  status: STALE. Self-flagged in its own header -- stops at 2026-06-29, does not reflect ~114 later waves.
    Superseded by master_tracker for current state. Kept for pre-2026-06-29 historical detail only.
- id: boss_identity
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/BOSS.yaml
  scope: Not a tracker -- role/identity config for VEDABOSS (architect role, ComplianceTrack). Different
    purpose from boss_board despite shared boss/ directory.
  status: live, last content-verified 2026-07-09
- id: boss_roster
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/ROSTER.yaml
  scope: Authorized coding agents (VEDABOSS/ZAI/SENTINEL) and roles.
  status: live, last content-verified 2026-07-09
- id: boss_completed_rollup
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/boss/COMPLETED.yaml
  scope: Two things in one file -- a completed_count/last_completed rollup (stale, stuck 2026-06-28) and
    a separate entries list of WAVE-* completion records (the VERIDIAN.docx joint plan's real log, tracked
    independently of the stale rollup fields per the file's own note). Read entries, not the rollup fields,
    for current state.
  status: partial (entries live, rollup fields stale)
- id: sentinel_health
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/HEALTH.yaml
  scope: CI/branch health spot-check.
  status: STALE. last_check 2026-07-09 (11 days at build time), not re-run since. Authority ceded to constitution
    (GP protocols) per sentinel/SENTINEL.yaml's own 2026-07-14 note.
- id: sentinel_violations
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/VIOLATIONS.yaml
  scope: Secret/.env spot-check log.
  status: STALE. last_scan 2026-07-09, explicitly self-described as "spot-check only," does not cover
    SENTINEL.yaml's full validation_rules set (e.g. AR-001, AC-002 not re-audited).
- id: sentinel_rules
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/sentinel/SENTINEL.yaml
  scope: Original rule definitions (AR-*, AC-* etc). Superseded in authority by constitution for guardrail
    grading (2026-07-14), but the rule definitions themselves are not necessarily migrated -- relationship
    not fully re-verified this pass, real follow-up.
- id: lifecycle_state
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/LIFECYCLE.yaml
  scope: Single current_stage marker (MONITOR) for the whole initiative.
  status: live, corrected 2026-07-09 (was stuck at a stale stage)
- id: os_entrypoint
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/OS.yaml
  scope: Pre-existing "start here" stub for the ai-os/ tree. Rewritten 2026-07-13 after being found stale/wrong
    (false Prisma claim). This MASTER_INDEX.yaml is a more complete, machine-oriented version of the same
    "start here" job -- OS.yaml should be updated to point here, not treated as a competing entrypoint.
    Real follow-up, not done this pass.
- id: engine_registry
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/engines/ENGINES.yaml
  scope: VCEL deterministic-computation engine registry (247 engines catalogued, 41 implemented per prior
    session record).
  status: live
- id: file_ownership_rules
  type: yaml_file
  path: /opt/veridian/ai-os/file-ownership.yaml
  scope: Deterministic module-ownership map, cross-checked (not trusted blindly) against AI-declared module
    labels by master-decompose.py.
  status: live
- id: artifact_registry
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/ARTIFACTS.yaml
  scope: Universal Asset Registry scaffold (VERIDIAN.docx Principle 16).
  status: NOT_POPULATED. Explicitly a scaffold, not live data. Real gap.
- id: asset_registry_coverage
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/asset-registry-coverage.yaml
  scope: CI-enforced manifest -- every src/lib/db/schema.ts table must appear in exactly one of registered/exempted.
    Not the same thing as system_index (that indexes ops mechanisms; this indexes DB tables' asset-registration
    compliance status specifically).
  enforced_by: scripts/check-asset-registry-coverage.mjs (CI)
  status: live
- id: stale_doc_manifest
  type: yaml_file
  path: /opt/veridian/repos/compliance-tracker/ai-os/registry/stale-doc-manifest.yaml
  scope: Root-level doc quarantine-banner enforcement list, tied to master_tracker's GAP-UNIFIED-SOT-REMAINDER
    item.
  enforced_by: scripts/check-doc-quarantine-banner.mjs (CI)
  status: live
- id: database_catalog
  type: json_file
  path: /opt/veridian/ai-os/DATABASE_CATALOG.json
  scope: EXHAUSTIVE, mechanically-generated catalog of every table (444), every column per table (name/type/nullability/default/PK/unique/
    enum-values), every foreign key, every index, and every enum (124, with full value lists). Generated
    by reading schema.ts's live Drizzle table objects via drizzle-orm's own is(PgTable)+ getTableConfig()
    introspection -- not text-parsing, not AI-written. 100% complete by construction, cross-verified 3
    ways (Drizzle API count, raw grep count, spot-checked column contents all agree). Superior in completeness
    to tree_lineage.tree3_system_tree's hand-written domain summary for per-table/per-column detail --
    that file stays useful for domain grouping and workflow narrative, this file is the ground-truth column-level
    reference.
  generator_script: /opt/veridian/repos/compliance-tracker/ai-os/scripts/extract-db-schema-catalog.mjs
    -- REUSABLE ACROSS REPOS as of 2026-07-20 (dynamic import(), not a static import statement). Copy
    the script into the target repo's own tree (module resolution for both the script's own `drizzle-orm`
    import AND the dynamically-imported schema.ts's own imports must resolve from the SAME node_modules
    -- confirmed this fails across repos, e.g. compliance-tracker's tsx binary cannot resolve projexa's
    drizzle-orm). Run as `cd <repo> && node_modules/.bin/tsx ai-os/scripts/extract-db-schema-catalog.mjs
    <absolute-path-to-schema.ts>`. Absolute path required -- a relative path is treated as a bare module
    specifier and fails.
  query_examples:
  - jq '.tables[] | select(.table_name=="organisations")' DATABASE_CATALOG.json
  - jq '.table_count, .enum_count' DATABASE_CATALOG.json
  - jq '.enums[] | select(.pg_name=="user_role")' DATABASE_CATALOG.json
  stale_doc_correction: tree3_system_tree and its own header claimed 377 tables / 106 enums (stated as
    a direct schema.ts read at the time). This catalog's 3-way-verified count is 444 tables / 124 enums
    as of 2026-07-20 -- the schema grew after that doc was last verified; it was not wrong when written,
    it went stale. This is the corrected, current, re-verifiable number going forward.
  status: live, generated 2026-07-20
  regenerate: run generator_script anytime schema.ts changes; safe, read-only, no DB connection required
- id: function_catalog
  type: json_file
  path: /opt/veridian/ai-os/FUNCTION_CATALOG.json
  scope: 'EXHAUSTIVE, mechanically-generated catalog of every named function in compliance-tracker''s
    src/ tree (5,019 total): top-level function declarations, const-assigned arrow/function-expression
    exports, class methods, constructors, and get/set accessors. Each entry carries file path, line number,
    kind, exported flag, async flag, class_name (if a method), full parameter list (name/type/optional),
    and a JSDoc one-line summary where present. Generated by parsing every .ts/.tsx file''s AST via the
    TypeScript compiler package directly (parse-only, no type-checker/Program, no project-wide resolution
    needed) -- not regex text-matching, not AI-written. Nothing existed at this granularity anywhere in
    the system before 2026-07-20.'
  generator_script: /opt/veridian/repos/compliance-tracker/ai-os/scripts/extract-function-catalog.mjs
    -- REUSABLE ACROSS REPOS as of 2026-07-20 (repo root + output path are now argv[2]/argv[3], not hardcoded).
    Copy into the target repo's own tree so its `typescript` import resolves from that repo's node_modules.
    Run as `node extract-function-catalog.mjs <absolute-repo-root> <absolute-output-path>` (plain node,
    no tsx needed -- Node's native TS support handles the .mjs script itself; completes in under 2 seconds
    even for 1,600+ files).
  query_examples:
  - jq '.functions[] | select(.name=="runRole")' FUNCTION_CATALOG.json
  - jq '.functions[] | select(.file | contains("cost-policy"))' FUNCTION_CATALOG.json
  - jq '.function_count, .by_kind' FUNCTION_CATALOG.json
  build_notes: 'First version undercounted class-related functions: missed ConstructorDeclaration entirely
    (custom Error subclasses across the codebase use `constructor(...)`) -- caught via a targeted grep
    spot-check before trusting the output, fixed to add constructor/get/set-accessor handling, count went
    from 5,011 to 5,019 (+8 constructors, 0 accessors found -- none exist in this codebase, confirmed
    by spot-check before assuming the 0 was itself a bug). 3 known functions (checkOpenRouterBalance,
    runRole, checkCostPolicy) spot-verified against file/line/export/async facts already established earlier
    this session -- all 3 matched exactly.'
  status: live, generated 2026-07-20
  regenerate: run generator_script anytime src/ changes; safe, read-only, no DB/build step required
  scope_note: 'compliance-tracker specifically. CORRECTED 2026-07-20 (was wrongly stated as "PROJEXA/veda-advisors
    consume via API, no separate codebase" -- confirmed false on inspection: both have their own package.json+src/
    trees. See registries.projexa_catalog and registries.veda_advisors_catalog for their own, separate
    catalogs, same generator scripts, parameterized by repo path.'
- id: projexa_catalog
  type: json_files
  path: /opt/veridian/repos/projexa/ai-os/DATABASE_CATALOG.json, /opt/veridian/repos/projexa/ai-os/FUNCTION_CATALOG.json
  central_mirror: /opt/veridian/ai-os/catalogs/projexa/
  scope: PROJEXA has its own real codebase (371 src/ files) AND its own local database schema (11 tables,
    0 enums -- notifications, chat, membership, credentials layer), distinct from the compliance-tracker
    business-logic tables it consumes via API. 992 functions catalogued. Generated with the same extract-db-schema-catalog.mjs
    / extract-function-catalog.mjs scripts, parameterized by repo path (scripts now take the schema path
    / repo root as argv[1] instead of a hardcoded compliance-tracker path -- confirmed working via a regression
    test against compliance-tracker's own 444/124/5019 numbers before reuse, all matched exactly).
  generator_note: Requires `bun install` in projexa/ first (node_modules did not exist on this server
    before 2026-07-20) -- bun binary is at ~/.bun/bin/bun, not on PATH in non-interactive SSH sessions.
  pr_status: Pushed via PR https://github.com/FChecklist/projexa/pull/50, not main directly -- a pre-existing
    uncommitted local change to .github/workflows/ci.yml (unrelated CI guardrail work, not mine, faithfully
    restored via git stash after pulling) blocked direct push (OAuth token lacks `workflow` scope). Catalog
    commits were cherry-picked onto a clean branch off origin/main to avoid that blocker; the workflow-touching
    commit remains local-only on projexa's main branch, unpushed, pending a token with workflow scope
    or manual push by the Owner.
  status: live, generated 2026-07-20
- id: veda_advisors_catalog
  type: json_file
  path: /opt/veridian/repos/veda-advisors/ai-os/FUNCTION_CATALOG.json
  central_mirror: /opt/veridian/ai-os/catalogs/veda-advisors/FUNCTION_CATALOG.json
  scope: veda-advisors has its own real codebase (62 src/ files, 270 functions). No schema.ts found --
    uses src/lib/db.ts (a Supabase client, not a Drizzle schema module), so no DATABASE_CATALOG entry
    exists for this repo -- confirmed absence, not an oversight.
  pr_status: Pushed via PR https://github.com/FChecklist/veda-advisors/pull/16 -- main is branch-protected
    on this repo (direct push rejected with GH006), unlike projexa's main.
  status: live, generated 2026-07-20
- id: veridian_ui_kit_catalog
  type: json_file
  path: /opt/veridian/repos/veridian-ui-kit/ai-os/FUNCTION_CATALOG.json
  central_mirror: /opt/veridian/ai-os/catalogs/veridian-ui-kit/FUNCTION_CATALOG.json
  scope: Small shared UI component package (@fchecklist/veridian-ui-kit, 0 runtime deps, 22 src/ files,
    46 functions). No database.
  pr_status: pushed directly to master, no branch protection, no conflict.
  status: live, generated 2026-07-20
- id: other_repos_classified
  type: inline_note
  scope: 'Remaining repos under /opt/veridian/repos/ classified 2026-07-20, no catalog needed (not application
    codebases): veridian-brain (package.json but effectively a 1-file stub SDK package, packages/brain-sdk/src/
    -- not worth a dedicated catalog run at this size); claude-control (pure YAML/config, see registries.cross_project_controller);
    infisuite-reverse-engineering and odoo-reverse-engineering (this session''s own scraped third-party-system
    documentation, not this system''s code); global-revenue-engine (empty repo, .git only, no committed
    content yet); sumeet-spec (single spec .md file, no code).'
  status: classified, not catalogued (correctly out of scope)
- id: ai_roster_catalog
  type: json_file
  path: /opt/veridian/ai-os/AI_ROSTER_CATALOG.json
  source_of_truth: /opt/veridian/repos/compliance-tracker/src/lib/ai-team/roster.ts (AI_TEAM_ROSTER export)
  scope: The "what AI models/roles exist and what they do" half of the Owner's original ask -- previously
    only covered by prose governance docs (AI_ORCHESTRA_HIERARCHY.md), never mechanically extracted. 195
    roles total (190 LLM-backed via OpenRouter, 2 isCodeOnly/deterministic, 3 isHuman/interactive-only),
    with by-team and by-model breakdowns, plus the guardrail/audit-org/ operational role-key lists exactly
    as team-service.ts's own helper functions (allGuardrailRoles, allAuditOrganizationRoles, operationalRoles)
    would return them.
  generator_script: /opt/veridian/repos/compliance-tracker/ai-os/scripts/extract-ai-roster-catalog.mjs
    -- dynamic import() of the real, evaluated roster.ts module (not text-parsing the object literal array).
    Run as `node extract-ai-roster-catalog.mjs <absolute-path-to-roster.ts> <absolute-output-path>`.
  stale_doc_correction: 'roster.ts''s OWN header comment (written 2026-07-07) claims "the founder''s full
    ~30-role company org chart." The live, evaluated array has 195 roles as of 2026-07-20 -- not wrong
    when written, gone stale as the 5 AUDIT_* teams grew to 130 roles between them. Same pattern as tree3_system_tree''s
    stale 377-table claim: a real number in a comment/doc drifts from the code it describes unless something
    re-derives it mechanically.'
  query_examples:
  - jq '.by_team' AI_ROSTER_CATALOG.json
  - jq '.roles[] | select(.roleKey=="super_boss")' AI_ROSTER_CATALOG.json
  - jq '.roles[] | select(.team=="AUDIT_BUSINESS_ASSURANCE") | .roleKey' AI_ROSTER_CATALOG.json
  status: live, generated 2026-07-20
  regenerate: run generator_script anytime roster.ts changes; safe, read-only, no API calls
- id: attention_alerts
  type: markdown_file
  path: /opt/veridian/ai-os/logs/ATTENTION.md
  scope: Live alert surface. Written by health-check-15min.py (systemd unit failures, stalled checkpoints,
    Mother Router DB reachability, disk/mem, task failure-rate threshold) and cost-usage-60min.py (OpenRouter
    balance, token_usage_ledger cross-check, spend-delta threshold). Added 2026-07-20 Pass 2 -- referenced
    in Pass 1 but not formally catalogued as a registry in its own right.
  status: live
- id: credit_spend_governance
  type: python_script
  path: /opt/veridian/scripts/credit-accountant.py
  mirror: /opt/veridian/repos/compliance-tracker/ai-os/scripts/credit-accountant.py
  integrates_with: /opt/veridian/scripts/preflight-guard.py (propose gate, universal choke point) and
    /opt/veridian/scripts/worker-entrypoint.sh (report calls, both main invocation and auto-fix retry
    loop)
  rule: 'ZERO TOLERANCE on credit wastage -- Owner directive 2026-07-20, verbatim: "the system will spend
    $1 after that spend is over, the software will tell to mother router, mother router will tell to Claude
    Code CLI subscription via SSH Session. Claude Code CLI subscription will analyze the spend of that
    $1... Will check if that is not being wasted. Then Claude Code CLI subscription will release another
    $1 to the system... without Claude Code CLI subscription permission, the mother router cannot spend
    any credit... Claude Code CLI subscription need not take any permission from the owner. This is an
    auto process... Claude Code CLI subscription will check if work could have been completed without
    AI and via software package... ZERO TOLERANCE on credit wastage." This is a strict, standing rule:
    no metered AI credit spend (OpenRouter/ GLM-5.2) may occur anywhere in this system without a prior
    propose approval and a following report outcome, enforced automatically, with no owner-in-the-loop
    step required for routine operation.'
  mechanism: 'Two-gate ledger (SQLite, /opt/veridian/ai-os/memory/credit-ledger.sqlite): propose runs
    BEFORE metered spend, report runs AFTER. The accountant is the Claude Code CLI subscription itself
    (CLAUDE_CODE_OAUTH_TOKEN, flat-rate, verified separate from the metered OpenRouter pool -- ANTHROPIC_API_KEY
    is explicitly disabled on this server, no ANTHROPIC_BASE_URL override exists) -- gating the metered
    resource with the unmetered one, per the Owner''s own "software first, AI second, minimal credits
    only when needed" principle. Deterministic, zero-cost checks run first every time: OpenRouter real
    balance (fails closed below $0.10), existing-capability search via superboss-register.py check-duplicate
    (rejects if software/a prior mechanism already covers the plan), and increment-sequencing (hard-blocks
    only on an explicit prior REJECTED outcome, not a merely-pending report, to avoid permanently deadlocking
    a task on an unrelated crash). Only when none of those can decide does it escalate to a real, bounded
    (--max-budget-usd 0.05, 60s timeout) claude -p judgment call, which must answer exactly PASS, FAIL,
    or REDIRECT (REDIRECT = "use existing software instead of AI").'
  fails_closed: 'Deliberate asymmetry from every other guardrail in this codebase (which mostly fail OPEN
    on their own infrastructure hiccups, e.g. check_proxy_health/check_openrouter_balance in preflight-guard.py,
    to avoid blocking legitimate work on a check''s own flakiness). This gate fails CLOSED: any accountant-call
    timeout/error/unparseable response is treated as a REJECTION, never a pass-through. An approval gate
    whose entire purpose is preventing waste must default to NO SPEND under ambiguity, not the reverse
    -- a broken accountant halting all metered spend server-wide is the intended, safe failure mode, surfaced
    as a high-priority health-check-15min.py anomaly (check_credit_accountant_health, built and live-verified
    2026-07-20) so it gets fixed fast rather than silently blocking work forever.'
  enforcement_point: Wired into preflight-guard.py's check_credit_accountant_approval(), called from __main__
    for BOTH the proxy path and the --no-proxy (doc-worker) path -- every dispatcher (worker-entrypoint.sh,
    doc-worker-entrypoint.sh, and anything else that calls preflight-guard.py, which is every dispatch
    path in this system) is covered automatically without needing to be individually taught about the
    accountant. The auto-fix quality-gate retry loop in worker-entrypoint.sh is a second, separate real-spend
    event not covered by the single upfront preflight call -- it has its own propose/report pair wired
    directly into the retry loop.
  tested: 'Live-tested end-to-end on the real server AND on the real worker fleet: deterministic balance
    rejection (real, then-exhausted OpenRouter balance), deterministic existing-capability rejection,
    real claude -p PASS/FAIL/REDIRECT verdicts on both a thin/trivial test plan (correctly rejected) and
    a genuinely substantive real-task plan (correctly approved), full preflight-guard.py chain end-to-end
    via a throwaway test task directory, and confirmed post-deploy that the live ledger (credit-ledger.sqlite)
    already has real accumulated entries from the live worker fleet using the gate in production. 3 real
    bugs found and fixed during testing before trusting it: keyword-extraction over-matching (fixed by
    requiring explicit caller-supplied --search-terms, never auto-derived from plan prose), missing CLAUDE_CODE_OAUTH_TOKEN
    propagation in a non-systemd subprocess context (fixed via get_claude_oauth_env()), and an increment-sequencing
    deadlock risk (fixed to hard-block only on an explicit prior rejection, not a merely-pending report).'
  status: 'Complete and live: propose gate deployed in preflight-guard.py, report gate deployed in worker-entrypoint.sh
    (main invocation success/failure + auto-fix retry loop) AND doc-worker-entrypoint.sh (single report
    call, --actual-spend-usd always 0 since that task family draws $0 from the metered pool by design
    -- CLI''s own cost estimate kept in the outcome text for informational value only, never conflated
    with real metered spend), health check deployed in health-check-15min.py, ledger pruning deployed
    (credit-accountant.py prune subcommand, 30-day retention, daily cron via run-logged.sh), all mirrored
    to compliance-tracker and committed (docs/cost-control-2026-07-20 branch). Deployment was interrupted
    mid-pass by an unrelated SSH outage (ssh.service found disabled on the server, fixed via Hetzner Rescue
    System -- see [[feedback_ssh_outage_diagnostic_procedure]] in assistant memory, not a MASTER_INDEX.yaml
    concern) -- resumed and completed same day.'
  audit_findings_2026_07_20: '3-round audit (same day) found and fixed 1 real production bug: credit_accountant_rejected
    was missing from worker-entrypoint.sh''s hard-stop list, so the new gate''s rejections fell through
    to the "transient, let systemd retry" branch -- confirmed live: 163 real tasks stuck in a restart-storm,
    1954 rejected propose calls in ~40 minutes for the identical real reason (exhausted OpenRouter balance).
    Fixed by adding it to the hard-stop condition, same treatment as circuit_breaker_tripped/budget_exhausted/
    openrouter_balance_exhausted (all 4 share the same property: blind retry reproduces the identical
    rejection until a human intervenes). Also closed 2 disclosed-but-not-yet-fixed gaps from that same
    audit: doc-worker-entrypoint.sh''s missing report call, and credit-ledger.sqlite''s missing pruning
    mechanism (both described above). No metered money was ever actually wasted by the restart-storm bug
    -- every rejection happens before any real spend -- but the churn itself was real, unnecessary load,
    now stopped.'
- id: system_map_2026_07_22
  type: dependency_graph
  path: ai-os/SYSTEM_MAP.yaml
  scope: machine/AI-only dependency graph of VERIDIAN Layer-1/2/3 architecture, built 2026-07-22
  status: partial_26_of_28_subsystems
  mandatory_before: any new architecture analysis, per rule_1_system_map_required
  verification_mechanism: ai-os/scripts/veridian_self_check.py checks completeness
- id: master_architecture_2026_07_22
  type: consolidated_index
  path: ai-os/MASTER_ARCHITECTURE_2026-07-22.yaml
  scope: single-file consolidation of 5 architecture analyses, 360-degree synthesis, brand model, verified
    subsystem status
  status: live
  mandatory_before: any brand/layer/subsystem question
- id: session_bootstrap_script
  type: python_script
  path: ai-os/scripts/session_bootstrap.py
  scope: reads real register history (SQLite, not keyword search) plus living docs
  status: live
  mandatory_before: any new task, per rule_2_no_forgetting
- id: file_edit_guard_script
  type: python_script
  path: ai-os/scripts/file_edit_guard.py
  scope: catches lost headers/top-level keys after any tracked YAML/JSON edit
  status: live
  mandatory_before: declaring any file edit done, per rule_4_self_review_pass
- id: veridian_self_check_script
  type: python_script
  path: ai-os/scripts/veridian_self_check.py
  scope: THE ONE COMMAND -- mechanically tests STANDING_DIRECTIVE.yaml compliance against live state
  status: live
  mandatory_before: any new task, run first always
- id: postflight_audit_gate_script
  type: python_script
  path: ai-os/scripts/postflight_audit_gate.py
  scope: universal post-completion audit gate -- only path allowed to write DONE/FAILED status, per rule_8
  status: live
  mandatory_before: declaring ANY task (any actor) DONE or FAILED
  verification_mechanism: task_audits table in superboss-register.sqlite
- id: audit_recheck_2026_07_22
  type: evidence_file
  path: ai-os/AUDIT_RECHECK_2026-07-22.json
  scope: retroactive verification of 198-rule audit and Mother Router bypass count, 2 self-corrections
    made mid-session
  status: partial_see_file
- id: anthropic_openrouter_proxy_py
  type: script
  path: scripts/anthropic_openrouter_proxy.py
  purpose_from_own_header: 'Minimal local proxy: translates Anthropic Messages API requests (what Claude'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: anthropic_openrouter_proxy_v2_py
  type: script
  path: scripts/anthropic_openrouter_proxy_v2.py
  purpose_from_own_header: 'v2 (2026-07-20, Owner directive "zero credit wastage"): adds two hard,'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: batch_import_conversation_log_py
  type: script
  path: scripts/batch-import-conversation-log.py
  purpose_from_own_header: Batch-imports queued conversation-log events (UserPromptSubmit /
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: cost_reconciliation_py
  type: script
  path: scripts/cost-reconciliation.py
  purpose_from_own_header: Weekly (or on-demand) reconciliation report. Q6's "keep it working" check --
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: cost_usage_60min_py
  type: script
  path: scripts/cost-usage-60min.py
  purpose_from_own_header: VERIDIAN-DEV hourly cost/usage check. Zero AI cost -- pure deterministic
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: dispatch_docworker_task_sh
  type: script
  path: scripts/dispatch-docworker-task.sh
  purpose_from_own_header: 'Atomic docworker dispatch, 2026-07-20. Closes a confirmed, RECURRING gap:
    `veridian-task.py create` auto-starts the wrong GLM-routed veridian-worker@ unit before a manual `disable`
    (run as a separate,'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: doc_worker_entrypoint_sh
  type: script
  path: scripts/doc-worker-entrypoint.sh
  purpose_from_own_header: Entrypoint for a systemd-managed VERIDIAN DOC worker. Same checkpoint / resume
    / commit-push pattern as worker-entrypoint.sh, but deliberately DIFFERENT in two ways, both intentional
    for this task fam
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: gap_status_py
  type: script
  path: scripts/gap-status.py
  purpose_from_own_header: Quick X/Y status check for the gap queue, plus anything needing a decision.
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: generate_quick_reference_py
  type: script
  path: scripts/generate_quick_reference.py
  purpose_from_own_header: 'TASK 6 (Owner directive 2026-07-20: "make MASTER_INDEX.yaml logical for'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: generate_task_checklist_py
  type: script
  path: scripts/generate_task_checklist.py
  purpose_from_own_header: 'generate_task_checklist.py -- Owner directive 2026-07-20: "make a prompt'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: health_check_15min_py
  type: script
  path: scripts/health-check-15min.py
  purpose_from_own_header: VERIDIAN-DEV 15-minute health check. Zero AI cost -- pure deterministic
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: import_memory_history_py
  type: script
  path: scripts/import-memory-history.py
  purpose_from_own_header: 'One-time bulk import: local memory index (MEMORY.md, ~60 entries spanning'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: master_decompose_py
  type: script
  path: scripts/master-decompose.py
  purpose_from_own_header: Master role (on-demand, not a standing daemon -- see HETZNER-04 controller
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: module_queue_dispatcher_py
  type: script
  path: scripts/module-queue-dispatcher.py
  purpose_from_own_header: Master/Supervisor pilot dispatcher. Reads one or more module-scoped queue
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: preflight_guard_py
  type: script
  path: scripts/preflight-guard.py
  purpose_from_own_header: Pre-flight guard, run BEFORE the main claude -p invocation in
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: quality_gate_sh
  type: script
  path: scripts/quality-gate.sh
  purpose_from_own_header: Runs whatever quality gates are actually detectable in a workspace (lint, build,
    test) before a worker's changes are allowed to reach pending_review. Gracefully skips gates that don't
    apply (e.g. a do
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: queue_dispatcher_py
  type: script
  path: scripts/queue-dispatcher.py
  purpose_from_own_header: Autonomous dispatcher for /opt/veridian/ai-os/gap_queue.yaml. Run via cron.
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: recover_failed_workers_py
  type: script
  path: scripts/recover-failed-workers.py
  purpose_from_own_header: RCA recovery script, 2026-07-20. Converts veridian-worker@ units stuck in
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: repair_controller_py
  type: script
  path: scripts/repair_controller.py
  purpose_from_own_header: Rebuilds ai-os/CONTROLLER.yaml from scratch by scanning the authoritative
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: risk_tier_py
  type: script
  path: scripts/risk-tier.py
  purpose_from_own_header: Deterministic (non-AI) risk-tier classifier for a task's diff against its
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: scope_check_py
  type: script
  path: scripts/scope-check.py
  purpose_from_own_header: 'Enforces the Master/Supervisor pilot''s "no collision" rule: every file a'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: superboss_register_py
  type: script
  path: scripts/superboss-register.py
  purpose_from_own_header: Superboss Register -- three searchable trees, server-side, SQLite+FTS5.
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: supervisor_entrypoint_sh
  type: script
  path: scripts/supervisor-entrypoint.sh
  purpose_from_own_header: 'VERIDIAN-DEV server-side Superboss: reviews a task''s branch for real (not
    a self-report), classifies risk tier deterministically, and either merges autonomously (tier1 + approved),
    holds for human sig'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: supervisor_sweep_sh
  type: script
  path: scripts/supervisor-sweep.sh
  purpose_from_own_header: 'Safety net: catches any task stuck in pending_review with no review.json,
    meaning its immediate supervisor trigger was missed (crash, systemd hiccup).'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: sync_controller_back_py
  type: script
  path: scripts/sync-controller-back.py
  purpose_from_own_header: 'Automates SYNC rule #14 from SUPERBOSS_DISPATCH_PROMPT.md: whenever a server'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: sync_repos_sh
  type: script
  path: scripts/sync-repos.sh
  purpose_from_own_header: 'Pulls latest commits for all mirrored repos. Safe: fast-forward only, never
    overwrites local changes.'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: sync_vercel_env_sh
  type: script
  path: scripts/sync-vercel-env.sh
  purpose_from_own_header: Refreshes .env.local for the 3 linked Vercel projects with current production
    env vars.
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: sync_verdian_ai_data_sh
  type: script
  path: scripts/sync-verdian-ai-data.sh
  purpose_from_own_header: 'Full refresh of the verdian-ai Supabase local mirror (schema + data). DESTRUCTIVE
    to the local mirror: drops and recreates public+compliance schemas each run. Intended to run at low-traffic
    hours (see'
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: task_template_py
  type: script
  path: scripts/task-template.py
  purpose_from_own_header: Renders a standardized task dict (TASK ID/MODULE/OBJECTIVE/FILES ALLOWED/
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: tight_task_validation_py
  type: script
  path: scripts/tight_task_validation.py
  purpose_from_own_header: Faithful Python port of src/lib/task-tightening.ts's validateTightTask()
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: worker_entrypoint_sh
  type: script
  path: scripts/worker-entrypoint.sh
  purpose_from_own_header: 'Entrypoint for a systemd-managed VERIDIAN AI worker. Runs Claude Code headlessly
    against an isolated git worktree, checkpoints periodically, resumes from the last checkpoint on restart
    (server reboot '
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: file_inventory_py
  type: script
  path: ai-os/scripts/file_inventory.py
  purpose_from_own_header: Real, exhaustive file inventory of the canonical VERIDIAN system-of-record
    (ai-os/ + scripts/),
  status: live_file_confirmed_2026_07_22
  note: registered 2026-07-22 as part of closing the 32-of-41-unregistered-scripts gap -- purpose text
    is the script's OWN header comment, not an assistant-authored description
- id: system_memory_architecture_2026_07_22
  type: architecture_blueprint
  path: ai-os/SYSTEM_MEMORY_ARCHITECTURE.yaml
  purpose_from_own_header: 'Master blueprint: 7-layer memory architecture (this machine, Superboss, Mother
    Router, AI routers, AI agent roster, software-run, external platforms), interconnection graph, per-layer
    ACTIVE vs DESIGNED_NOT_BUILT status, machine-only audience'
  status: live_2026_07_22
  note: consolidates and formalizes cross_system_memory_map_2026_07_22 (already in this same file) into
    one authoritative design document -- read this first for 'is layer X's memory real or designed'
- id: terminology_standardization
  type: variable_dictionary_guardrail_and_phase_plan
  path: ai-os/VARIABLE_DICTIONARY_2026-07-24.yaml + ai-os/TERMINOLOGY_GUARDRAIL_2026-07-24.py + ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml
  scope: 'Phase 0 (task-20260724-070131, instruction INS-20260724-070047-3a2d) of VERIDIAN Terminology
    Standardization: a real Variable Dictionary (320 <Entity.Attribute> placeholders, one per real column
    of the top-20 most-referenced tables in registries.database_catalog''s 444, ranked by a combined declared-FK
    + naming-convention "_id" reference-count score -- only 7/444 tables carry a declared SQL FK, so the
    naming-convention signal dominates; example values sourced from real dev-seed rows fetched live via
    Supabase MCP against the verdian-ai project backing compliance-tracker''s schema.ts, 182/320 real,
    138/320 clearly-marked synthetic, sensitive columns like password_hash always forced synthetic); a
    designed (not yet CI-wired) Terminology Guardrail lint script flagging hardcoded human-example literals
    (placeholder company/person names, fake email domains, bare ISO dates, PAN/GSTIN-shaped literals)
    not expressed as a registered placeholder, plus unregistered-placeholder-token drift, smoke-tested
    against 3 real compliance-tracker files (communication-drafting-service.ts, orchestra-mock-data.ts,
    db/seed.ts -- 36 real findings); and a 6-phase rollout plan (dictionary coverage expansion, directory-scoped
    rollout, CI enforcement wiring modeled on compliance-tracker ci.yml''s real guardrail-presence/asset-registry
    -coverage/metadata-index-coverage jobs, migration of existing hardcoded examples, full enforcement)
    with an 8-edge dependency table.'
  status: phase_0_through_phase_5_complete_all_6_phases_complete_verified_2026-07-29
  query_command: python3 scripts/superboss-register.py query-knowledge "variable_dictionary" --tag domain:terminology_standardization
  phase_2_note: 'task-20260724-115921-phase2-directory-scoped-guardrail-rollou (2026-07-24): ran the unmodified
    TERMINOLOGY_GUARDRAIL_2026-07-24.py across all 4 compliance-tracker directory priority tiers (src/lib/services,
    src/lib top-level, src/app/api, src/db/seed.ts+test fixtures) -- 1341 files scanned, 488 findings
    baseline (310 required-fix candidates, 178 seed/test exemption candidates). Full detail: ai-os/guardrail-findings/PHASE_2_BASELINE_SUMMARY_2026-07-24.yaml
    and this phase''s own real_evidence_2026-07-24 block in the phase plan file.'
  next_phase: phase_3_ci_enforcement_wiring per ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml
    phases[] -- external_depends_on compliance-tracker/.github/workflows/ci.yml (external repo), must
    be dispatched as, or coordinate with, a compliance-tracker-scoped task, not part of this registry
    entry's own scope.
  note_on_repo_vs_live_drift: This task also added the identical entry to the git-tracked /opt/veridian/repos/claude-control
    ai-os/MASTER_INDEX.yaml (branch worker/task-20260724-070131-veridian-terminology-standardization-pha).
    Pre-existing drift between this live file and that repo copy (e.g. registries.auditor_engine present
    here but not yet in that branch; registries.testing_engine_irvf present in that branch but not yet
    here) predates this task and was not caused or resolved by it -- out of this task's own CONSTRAINTS
    scope.
  status_corrected_from: phase_0_phase_1_phase_2_complete_phase_3_not_yet_dispatched
  status_verification:
    checked: true
    phase_plan: /opt/veridian/ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml
    total_phases: 6
    old_max_complete_phase: 2
    real_max_complete_phase_found: 5
    evidence:
    - repo: FChecklist/compliance-tracker
      pr: 552
      title: 'Phase 3: wire terminology guardrail into CI as a diff-scoped coverage-gate job'
      merged_at: '2026-07-24T14:08:41Z'
      matched_phase: phase_3_ci_enforcement_wiring
    - repo: FChecklist/compliance-tracker
      pr: 553
      title: 'Phase 4: migrate hardcoded terminology examples to registered placeholders'
      merged_at: '2026-07-24T21:45:17Z'
      matched_phase: phase_4_existing_hardcoded_example_migration
    - repo: FChecklist/claude-control
      pr: 18
      title: 'Phase 0: VERIDIAN Terminology Standardization -- Variable Dictionary, guardrail design,
        phase plan'
      merged_at: '2026-07-24T07:22:52Z'
      matched_phase: phase_0_dictionary_guardrail_design_phase_plan
    - repo: FChecklist/claude-control
      pr: 32
      title: 'Phase 2: directory-scoped guardrail rollout (488-finding baseline)'
      merged_at: '2026-07-24T12:08:07Z'
      matched_phase: phase_2_directory_scoped_rollout
    - repo: FChecklist/claude-control
      pr: 41
      title: 'Close phase_3_ci_enforcement_wiring: real review + merge of compliance-tracker#552'
      merged_at: '2026-07-24T14:21:22Z'
      matched_phase: phase_3_ci_enforcement_wiring
    - repo: FChecklist/claude-control
      pr: 53
      title: 'Close phase_4_existing_hardcoded_example_migration: real review + merge of compliance-tracker#553'
      merged_at: '2026-07-24T21:47:10Z'
      matched_phase: phase_4_existing_hardcoded_example_migration
    - repo: FChecklist/claude-control
      pr: 23
      title: 'Phase 1 terminology dictionary expansion: TOP_N 20->60, guardrail wider run'
      merged_at: '2026-07-24T09:28:05Z'
      matched_phase: phase_1_dictionary_coverage_expansion
    - repo: FChecklist/claude-control
      pr: 56
      title: phase5-full-repo-wide-enforcement-dictionary-completion
      merged_at: '2026-07-24T22:22:38Z'
      matched_phase: phase_5_full_enforcement_and_dictionary_completion
    - repo: FChecklist/claude-control
      pr: 48
      title: phase4-migrate-existing-hardcoded-examples-to-registered
      merged_at: '2026-07-24T18:42:00Z'
      matched_phase: phase_4_existing_hardcoded_example_migration
    derived_ts: '2026-07-29T18:50:58.038061+00:00'
    method: gh pr list --state merged --search <keyword>, real merge state, titles matched to the initiative's
      own phase plan by topical token overlap (never by phase-number coincidence alone) -- never trusts
      a self-reported status field.
- id: testing_engine_irvf
  type: route_registry_coverage_methodology_and_phase_plan
  path: ai-os/ROUTE_REGISTRY_SCHEMA_2026-07-24.yaml + ai-os/ROUTE_COVERAGE_METHODOLOGY_2026-07-24.yaml
    + ai-os/TESTING_ENGINE_PHASE_PLAN_2026-07-24.yaml
  scope: 'Phase 0 (task-20260724-063645, VERIDIAN Testing Engine / IRVF -- Intent-Route-Verification-Framework)
    of the platform''s route-level testing engine: a real route registry (5 populated_routes, one per
    real capability from registries.engines_gateways_architecture''s Capability Registry, each with a
    real expected_path of engine hops), a 9-type coverage methodology (each type either MEASURABLE_TODAY
    with a real computed value or NOT_YET_MEASURABLE with an explicit, specific gap and unblocking_phase),
    and this engine''s own phased build plan (TESTING_ENGINE_PHASE_PLAN_2026-07-24.yaml phases[] + dependency_table),
    sequenced after registries.engines_gateways_architecture and registries.auditor_engine where genuinely
    dependent (e.g. integration_coverage blocked on Auditor Engine''s own api-contract domain authoring
    real OpenAPI docs).'
  status: phase_0_complete_phase_1_plus_not_yet_dispatched
  query_command: python3 scripts/superboss-register.py query-knowledge "testing_engine_irvf" --tag domain:testing_engine_irvf
  next_phase: Phase 1 (route/capability auto-generation + leaf-enumeration script per ROUTE_COVERAGE_METHODOLOGY_2026-07-24.yaml's
    capability_coverage gap) per TESTING_ENGINE_PHASE_PLAN_2026-07-24.yaml phases[] -- dispatched as a
    separate task, not part of this registry entry's own scope.
- id: chatgpt_audit_workspace
  type: sandboxed_audit_workspace_and_guard
  path: /opt/veridian/chatgpt-audit/ (live, 23 domain subfolders) + ai-os/CHATGPT_AUDIT_RECORD_SCHEMA_2026-07-24.yaml
    + scripts/chatgpt_audit_guard.py + scripts/chatgpt_audit_versioning.py + scripts/generate_chatgpt_audit_index.py
    + ai-os/CHATGPT_AUDIT_MASTER_CATALOGUE_2026-07-24.yaml + ai-os/CHATGPT_AUDIT_BLOCKER_2026-07-24.yaml
  scope: 'Instruction INS-20260724-141101-89d8 (task-20260724-141323-chatgpt-audit-workspace-infrastructure):
    real infrastructure for a future ChatGPT-generated audit workspace -- the LLM-execution step itself
    is explicitly out of scope for this task (no OPENAI_API_KEY exists yet, see ai-os/CHATGPT_AUDIT_BLOCKER_2026-07-24.yaml).
    Built: (1) a real, live sandboxed write directory at /opt/veridian/chatgpt-audit/ with 23 domain subfolders
    (Architecture/Business/Capabilities/Modules/ Metadata/Database/Rules/Workflow/APIs/UI/Reports/Security/Performance/AI/Prompts/Routes/Testing/
    Dependencies/Integrations/Observability/Release/Recommendations/History); (2) scripts/chatgpt_audit_guard.py
    -- mechanical access control, not a policy statement: guarded_open()/ guarded_write() refuse any resolved
    path outside that directory before a byte is written, verified live this session (--test-path against
    a real compliance-tracker file rejects with exit 1; against the workspace itself allows with exit
    0; a lookalike-prefix path also rejects; a real guarded write succeeds in-scope and is refused, with
    nothing written, out-of-scope); (3) scripts/chatgpt_audit_versioning.py -- real, flock()-protected
    incrementing audit_id + timestamped filename allocation (<Subfolder>/AUDIT-<id>-<ts>.yaml), verified
    to never overwrite (re-checks the target path is free before returning it); (4) ai-os/CHATGPT_AUDIT_RECORD_SCHEMA_2026-07-24.yaml
    -- the audit-record field schema (Audit ID through Confidence %, 15 fields) plus the 9 linkage fields
    (Capability/Workflow/Metadata/Business Rule/Database/API/UI/Report/Document); (5) scripts/generate_chatgpt_audit_index.py
    -- scans the live workspace and machine-generates both a local searchable index (/opt/veridian/chatgpt-audit/Metadata/INDEX.yaml)
    and ai-os/CHATGPT_AUDIT_MASTER_CATALOGUE_2026-07-24.yaml (0 records today, by design -- see (6));
    (6) ai-os/CHATGPT_AUDIT_BLOCKER_2026-07-24.yaml -- documents exactly what is needed to go live (a
    real OPENAI_API_KEY) and the exact call shape (model left for the Owner to name, prompt template,
    read scope, guard-only output path) a future task will use once that key exists.'
  status: infrastructure_100pct_ready_blocked_on_owner_supplied_openai_api_key
  query_command: ls /opt/veridian/chatgpt-audit/ ; python3 scripts/chatgpt_audit_guard.py --test-path
    <path> ; python3 scripts/chatgpt_audit_versioning.py allocate --subfolder <Domain> ; python3 scripts/generate_chatgpt_audit_index.py
  owner_decision: ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml#chatgpt-audit-openai-api-key
  next_phase: Once the Owner supplies OPENAI_API_KEY and names the model (see owner_decision above), a
    future task wires the real ChatGPT call using ai-os/CHATGPT_AUDIT_BLOCKER_2026-07-24.yaml's exact_call_shape_once_key_exists
    -- not part of this registry entry's own scope.
- id: chatgpt_prompt_library
  type: sandbox_workspace_schema_and_coverage_tooling
  path: ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml + ai-os/CHATGPT_PROMPTLIB_BLOCKER_2026-07-24.yaml
    + scripts/chatgpt_promptlib_guard.py + scripts/generate_prompt_coverage_report.py + scripts/detect_prompt_duplicates.py
  scope: 'task-20260724-141538-chatgpt-prompt-library-infrastructure (instruction INS-20260724-141341-5f67):
    real infrastructure for the VERIDIAN ChatGPT Prompt Library Workspace. Built: (1) the sandbox itself
    (/opt/veridian/chatgpt-prompt-library/, 15 real subfolders -- CSV, Intent, Keywords, Variables, Capabilities,
    Entities, Modules, Domains, Routes, Coverage, Validation, Duplicates, Statistics, Reports, History),
    write-restricted via scripts/chatgpt_promptlib_guard.py (no existing guard pattern to reuse yet --
    the sibling task-20260724-141323-chatgpt-audit-workspace-infrastructure had not started, see that
    entry below once it lands); (2) the real 15-column per-prompt CSV schema (ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml),
    reusing registries.variable_dictionary''s `<Entity.Attribute>` placeholder convention rather than
    rebuilding it; (3) a real coverage report (scripts/generate_prompt_coverage_report.py) that reads
    registries.engines_gateways_architecture''s live capability_registry (10 real rows) and real CSV/
    data to compute Capability/Prompt/ Intent-Coverage-pct -- run for real this task, honest 0pct baseline
    (every real capability listed missing, since 0 prompts exist); (4) a duplicate-detection script (scripts/detect_prompt_duplicates.py),
    proven via --test-fixture against a synthetic 3-row fixture with a planted duplicate (no real prompts
    exist yet to test against); (5) ai-os/CHATGPT_PROMPTLIB_BLOCKER_2026-07-24.yaml, documenting the exact
    remaining gap (a real OPENAI_API_KEY -- confirmed absent from /opt/veridian/shared/.env, placeholder
    comment only -- and the Owner-named model) and the exact capability->N-prompts generation call shape,
    registered as OWNER_DECISIONS_NEEDED_2026-07-23.yaml id chatgpt-promptlib-openai-api-key. The actual
    LLM-execution step (ChatGPT generating the 10,000 real prompts) is explicitly out of this task''s
    scope.'
  status: infrastructure_100pct_ready_generation_blocked_on_owner_supplied_openai_api_key
  query_command: python3 scripts/superboss-register.py query-knowledge "chatgpt_prompt_library" --tag
    domain:chatgpt_prompt_library
  cross_link_note: registries.chatgpt_audit_workspace (sibling task-20260724-141323-chatgpt-audit-workspace-infrastructure)
    had not landed as of this entry -- cross-link once it registers; do not build a second sandbox-guard
    pattern from scratch if that task's scripts/chatgpt_audit_guard.py already exists by the time a future
    phase touches this.
  next_phase: 'A future task, once the Owner supplies a real OPENAI_API_KEY and names the model: the actual
    capability->N-prompts generation call per ai-os/CHATGPT_PROMPTLIB_BLOCKER_2026-07-24.yaml''s exact_generation_call_shape_once_unblocked,
    writing real CSV/ rows only via scripts/chatgpt_promptlib_guard.py, then re-running generate_prompt_coverage_report.py
    and detect_prompt_duplicates.py in real (non---test-fixture) mode.'
- id: wiring_engine
  type: entity_relationship_schema_and_mechanical_registry
  path: ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml + ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json + ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml
  scope: 'Phase 0 (task-20260725-032718-wiring-engine-phase0-schema-registry, instruction INS-20260725-032606-0c52)
    of the VERIDIAN WIRING ENGINE: a real entity-relationship schema (entity_record_schema: engine|gateway|
    table|function|route|file|script|cron_job|ai_role, reusing knowledge_engine''s own last_verified_ts/
    verification_status fields and {path,relationship_type,evidence} shape verbatim -- see WIRING_ENGINE_SCHEMA_2026-07-25.yaml
    meta.precedent) and scripts/generate_wiring_registry.py, which mechanically populates the registry
    from 8 real live sources (DATABASE_CATALOG.json, FUNCTION_CATALOG.json, AI_ROSTER_CATALOG.json, 20_ENGINES_10_GATEWAYS_PHASE_PLAN''s
    engine/gateway inventories, ROUTE_REGISTRY_SCHEMA''s populated routes, SOFTWARE_CATALOG.yaml''s scripts/cron,
    and the live knowledge_engine + capability_registry sqlite tables) -- never hand-authored. Real run
    2026-07-25: 7642 entities, all 9 entity_types non-zero (engine=20, gateway=10, table=444, function=5019,
    ai_role=195, route=5, script=40, cron_job=14, file=1895). Distinct from registries.engines_gateways_architecture
    (which names/inventories the 20 engines/10 gateways themselves) and registries.self_sustaining_system_engine
    (knowledge_engine''s own artifact tracking) -- this registry''s one addition is a single cross-source
    relationship graph spanning all of them, most notably confirming a measured fact: of 19 real hops_through
    relationships derived from ROUTE_REGISTRY_SCHEMA''s 5 populated routes, ZERO target a gateway entity
    -- the Owner''s suspected "no real end-to-end gateway flow proven yet" gap, now a mechanically-confirmed
    number, not an impression.'
  status: phase_0_complete_phase_1_not_yet_dispatched
  query_command: python3 scripts/superboss-register.py query-knowledge "wiring_engine" --tag domain:wiring_engine
  refresh_command: python3 scripts/generate_wiring_registry.py (every 6h via crontab, see OWNER_DECISIONS_NEEDED_2026-07-23.yaml
    id wiring-engine-generate-registry-crontab-addition)
  next_phase: phase_1_end_to_end_gateway_verification per ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml
    phases[] -- picked up automatically by scripts/auto_phase_continuation.py (its PLAN_GLOB was widened
    from a hardcoded *_PHASE_PLAN_2026-07-24.yaml to *_PHASE_PLAN_*.yaml as part of this same task so
    this new, later-dated plan is discovered without further manual intervention) once this task's PR
    merges.
- id: umr_tasks_registry
  type: dispatch_queue_task_ledger
  path: ai-os/memory/superboss-register.sqlite#umr_tasks
  scope: Real, live table (104 rows as of 2026-07-30) written by scripts/resource_governor.py's submit()/dispatch_one()
    (registries -- see the Resource Governor section of this file's own quick_reference/history) and read
    by superboss-register.py's `heartbeat --task-identity` command (Stage 3 reconciliation-sweep prerequisite
    -- stamps last_heartbeat=now() on the active row). Was a foundational live dispatch-queue mechanism
    with ZERO prior mention anywhere in this file despite being referenced by name in prior session memory
    -- added 2026-07-30 during the master-index-central-entrypoint-confirmation pass.
  status: live
  query_command: python3 scripts/resource_governor.py --query-umr --limit <n>
- id: capability_services_pair
  type: application_service_pair
  path: compliance-tracker/src/lib/services/capability-learning-service.ts + compliance-tracker/src/lib/services/capability-audit-service.ts
  scope: App-layer (subsystem_2) TypeScript services -- distinct from the ai-os-layer registries.engines_gateways_architecture
    phase_1_capability_registry_live_wiring (Engine 3, the AI-dispatch capability lookup table in superboss-register.sqlite's
    capability_registry table). Both files confirmed to exist on disk 2026-07-30; had zero prior mention
    in this file. Existence-confirmed only this pass -- internal wiring/callers not independently re-verified.
  status: live_existence_confirmed_2026-07-30
- id: veri_chat
  type: end_user_facing_feature
  path: compliance-tracker/src/app/api/veri-chat + compliance-tracker/src/components/veri-chat/ + compliance-tracker/src/lib/services/veri-chat-service.ts
    + compliance-tracker/src/lib/services/veri-chat-v2-enablement-service.ts
  scope: Real, existing end-user chat feature (subsystem_2, app layer) -- API route, React components,
    and 2 backing services all confirmed present on disk 2026-07-30. Had ZERO prior entry in this file
    despite being a named, real, end-user-facing surface -- a genuine coverage gap this pass closes. Existence-confirmed
    only; feature behavior/wiring to the Mother Router / AI roster not independently re-verified this
    pass.
  status: live_existence_confirmed_2026-07-30
- id: browser_intent_cache
  type: application_feature
  path: compliance-tracker/src/lib/browser-intent-cache.ts
  scope: 'Matches prior session record ''VERIDIAN Browser Intent Cache + Palette'' (PR #324) -- IndexedDB
    workflow-recall palette. Real file confirmed to exist 2026-07-30; was never catalogued in this file
    despite being a named, merged PR feature. Known honest gap carried over from that PR''s own record:
    encryption and attachments were not built.'
  status: live_existence_confirmed_2026-07-30
- id: sap_reports_projexa_completion_effort
  type: data_migration_and_report_mapping_effort
  path: 'ai-os/memory/sap_mapping.sqlite (tables: sap_modules, sap_reports, ingest_log) + task dirs: ai-os/tasks/task-20260728-050704-sap-informed-veridian-phase-0-baseline,
    task-20260728-054819-adopted-adopt-pr-615-sap-phase-0-baseline-inven, task-20260728-123644-classify-87-sap-reports-into-engine-trac,
    task-20260728-160934-cross-reference-sap-reports-vs-existing, task-20260728-033242-adopted-real-audit-pr-603-projexa-erp-e2e-re'
  scope: SAP-informed VERIDIAN/PROJEXA reports-completion effort, dispatched 2026-07-28. Real sqlite DB
    (692KB, 3 real tables) + 5 real task directories confirmed to exist on disk 2026-07-30. Had ZERO prior
    mention anywhere in this file. This entry is existence-cataloguing only -- per-task phase/completion
    status was NOT re-verified this pass; read each task's own task.yaml/PROGRESS.md for that.
  status: existence_confirmed_2026-07-30_phase_detail_not_reverified
- id: cron_systemd_consolidation_2026_07_29
  type: automation_infrastructure_inventory
  path: /home/rajat/.config/systemd/user/veridian-cron-*.timer (18 confirmed) + veridian-cron-*.service
    (matching 18) + 9 additional non-cron units (veridian-worker@.service, veridian-supervisor@.service,
    veridian-docworker@.service, veridian-worker-enduser@.service, veridian-task-watchdog.service+.timer,
    veridian-directive-engine.service, veridian-glm-proxy.service, veridian-governor-tick.service)
  scope: '18 systemd-timer-based replacements for the periodic workload that used to run via rajat''s
    crontab, before the entire crontab was stopped during the 2026-07-26 OOM incident (see MEMORY_OWNER_AI.md)
    and never restored as cron -- it was consolidated into systemd --user timers instead: dispatch-tick,
    phase-continuation-tick, status-remediation-tick, health-check-15min, cost-usage-60min, file-inventory,
    generate-wiring-registry, knowledge-registry-multisource, software-catalog-gen, security-check, audit-pipeline-security,
    credit-ledger-prune, sync-repos, sync-vercel-env, sync-verdian-ai-data, sync-controller-back, system-sync,
    veridian-self-check. Confirmed live via direct `ls` 2026-07-30 (18 *.timer files, exact count). Had
    ZERO prior mention in this file or in system_index -- a real, live, currently-unregistered piece of
    production automation infrastructure.'
  status: live
  honest_gap: Not yet registered in system_index either as of this pass -- this MASTER_INDEX.yaml entry
    is the first place this consolidation is catalogued anywhere. A follow-up `index-add` pass per unit
    (or one rollup row) is still needed for system_index's own check-duplicate coverage.
  closed_set_rule: 'Documented 2026-07-30 (campaign cron-consolidation-reaudit-phase6): these 18 units
    are a closed set -- see ~/.config/systemd/user/README.md (new, this pass) plus a matching header
    comment prepended to each of the 18 .service files. A new periodic need is added as a new
    ExecStart step or trigger condition inside one of these 18 existing units where it genuinely fits;
    a genuinely new cadence/purpose requires an explicit Owner decision to add unit #19, never a
    default/automatic action by any script, worker, or session.'
  reaudit_2026_07_30: 'Independently re-verified this pass: all 18 timers present/enabled/active with
    next-run times spread across distinct minutes (not clustered the way the old crontab was);
    list-unit-files shows exactly 18 .service + 18 .timer, no duplicates or orphans; crontab -l
    confirmed every real job line still carries its #STOPPED-ALL-CRON-2026-07-26# or
    #PAUSED-OOM-INCIDENT-2026-07-26 marker, only the XDG_RUNTIME_DIR= env line is uncommented; the
    full 19-old-job to 18-new-unit mapping was confirmed correct via each units real ExecStart= line,
    not just name-matching (only supervisor-sweep + queue-dispatcher collapse into dispatch-tick).
    REAL GAP FOUND: the shared ConditionPathExists=!.../resource-governor-EMERGENCY_STOP gate tripped
    again (ATTENTION.md entry timestamped 2026-07-29T21:08:00 UTC, same near-zero-metrics
    false-positive signature already flagged in PROTOCOL_OWNER_AI.md section 16) only minutes into
    this rollout, and stayed tripped for hours with no logged clear event. Effect: the 10
    lower/medium-frequency units (sync-repos, sync-vercel-env, sync-verdian-ai-data, cost-usage-60min,
    credit-ledger-prune, generate-wiring-registry, knowledge-registry-multisource,
    software-catalog-gen, audit-pipeline-security, system-sync) hit ConditionResult=no (start
    condition unmet) at every real trigger since rollout -- zero real payload executions, despite
    systemd reporting no failure, since a condition-skip is not a failure exit code and never shows up
    in an ordinary journal failure grep. The 8 higher-frequency units (10-30 minute cadence:
    dispatch-tick, phase-continuation-tick, status-remediation-tick, health-check-15min,
    security-check, file-inventory, veridian-self-check, sync-controller-back) had enough trigger
    attempts to also land in a clear window and ran for real, with real register and log evidence.
    The sentinel file was gone and current metrics read 0/0/0/0 by the time this audit checked
    (~2026-07-30 03:44-04:02 UTC); not yet independently reconfirmed that the 10 gated units succeed
    on their own next real fire. This is the same recurring EMERGENCY_STOP false-positive bug already
    flagged as unresolved -- observed for a third real time, and this time it silently defeated more
    than half of a brand-new rollouts real execution coverage without tripping any alarm a normal
    status check would catch. Worth a dedicated fix to the trip logic itself, not just another manual
    clear.'
- id: utm_traceability_convention
  type: cross_cutting_data_convention
  path: ai-os/memory/superboss-register.sqlite's instructions/work_items/actions tables (utm_source/utm_medium/utm_campaign/utm_content/utm_term
    columns) + every log-instruction/log-work/log-action CLI invocation across this whole system
  scope: The standing traceability convention (not a single script or table of its own) tagging every
    real Owner instruction and every real AI/software work item, letting a query filter by utm_campaign
    the same way a marketing UTM tag filters ad traffic -- e.g. this exact task logs itself with utm_campaign='master-index-central-entrypoint-confirmation-phase6-2026-07-30'.
    Referenced ad hoc in ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml and ai-os/STANDING_DIRECTIVE.yaml's
    schema block, but never had its own single MASTER_INDEX.yaml entry describing it as a standing convention
    until this pass.
  status: live
  query_command: python3 scripts/superboss-register.py search '<keywords>' (then filter results by utm_campaign
    in the JSON output), or a raw SELECT ... FROM instructions WHERE utm_campaign='<value>'
- id: dispatch_tick_heartbeat
  type: real_time_state_file
  path: ai-os/DISPATCH_TICK_HEARTBEAT.json
  scope: 'Added 2026-08-02 (PM decision UMR-20260802-074346-a9b9), extending the existing
    dispatch-tick.py (runs every 10min via veridian-cron-dispatch-tick.timer, laptop-independent,
    already EMERGENCY_STOP-gated) rather than a new cron/systemd timer. Written every tick by
    stuck_task_and_heartbeat_tick(): (a) a real heartbeat (current timestamp, real load average,
    whether EMERGENCY_STOP is set, real blocked/running/total task counts) so any future check --
    this laptop, a different tool, anywhere else -- reads one file instead of running several
    separate SSH commands; (b) a real, evidenced list of tasks with status=blocked and no new
    checkpoint in STUCK_TASK_THRESHOLD_SECONDS (default 30min, env-overridable), each with real
    blocked-duration and the last real checkpoint note. Strictly read-only with respect to task
    state -- never auto-resolves a stuck task or dispatches anything; a blocked task still needs a
    real PM decision. Undated/continuously overwritten, same convention as PHASE_READY_CACHE.json.'
  status: live
  query_command: cat /opt/veridian/ai-os/DISPATCH_TICK_HEARTBEAT.json | python3 -m json.tool
tree_lineage:
  tree1_audit_tree:
    path: /opt/veridian/repos/compliance-tracker/ai-os/audit-tree/
    role: Requirement-doc interpretation (9 source docs mapped to 28 domains).
    status: HISTORICAL_INPUT. archive/ subdir explicitly retired, folded into tree4.
  tree3_system_tree:
    path: /opt/veridian/repos/compliance-tracker/ai-os/system-tree/
    role: Audited live-codebase tree (614 API routes / 377 DB tables / 130 pages / 57 AI roles, per prior
      session verification).
    status: HISTORICAL_INPUT. Evidence source for tree4, not itself re-read by tree4. Its 377-table/106-enum
      counts are confirmed STALE as of 2026-07-20 -- see registries.database_catalog for the current,
      mechanically-verified 444/124.
  tree4_unified:
    path: /opt/veridian/repos/compliance-tracker/ai-os/tree4-unified/
    role: CANONICAL CURRENT merge of tree1+tree3, 149 sub-branches, granular status plus gap backlog plus
      input/output/guardrail specs.
    status: live, built 2026-07-11, includes its own 2-round self-audit (TREE4-AUDIT-ROUND-1.md, TREE4-AUDIT-ROUND-2.md)
    entrypoint: tree4-unified/00-INDEX.md
    subdirs:
      completion_plan: tree4-unified/50-completion-plan/ (active plan)
      completion_plan_archive: tree4-unified/50-completion-plan/archive/ -- 13 retired priority-tracker
        files, explicitly archived, folded into master_tracker per master_tracker's own header.
  read_order: tree4_unified.entrypoint first; tree1_audit_tree and tree3_system_tree only if tree4 references
    them for detail.
  basename_collision_note: 00-INDEX.md exists 4 times (audit-tree/, system-tree/, tree4-unified/, tree4-unified/50-completion-plan/archive/)
    and README.md exists 2 times (audit-tree/source-documents/, tree4-unified/50-completion-plan/archive/).
    Low risk -- each sits in a distinctly-named, already-catalogued directory above -- but any search
    for a bare "00-INDEX.md" or "README.md" basename must include the parent dir in the match, confirmed
    2026-07-20 Pass 2.
databases:
- path: /opt/veridian/ai-os/memory/superboss-register.sqlite
  tables:
  - instructions
  - work_items
  - actions
  - system_index
  see: registries.system_index, registries.superboss_dialogue_log
- path: /opt/veridian/ai-os/logs/glm-response-cache.sqlite
  tables:
  - cache
  scope: L1 exact-match response cache for the GLM/OpenRouter proxy.
  writer_reader: /opt/veridian/scripts/anthropic_openrouter_proxy_v2.py
- path: app-level Postgres/Supabase (444 tables, 124 enums)
  see: registries.database_catalog
  note: Full column-level detail lives in registries.database_catalog (mechanically generated, ground
    truth). tree_lineage.tree3_system_tree's 377/106 counts are the stale, superseded numbers -- see registries.database_catalog.stale_doc_correction.
file_inventory:
  ops_scripts:
    count: 28
    count_note: 'PHASE1 FINDING (2026-07-23, task-20260723-060600-execution-rules-phase1-tagging-extension):
      this count (and system_index_tagged below, pre-phase1) is stale against the live filesystem, discovered
      while tagging. Real live count via glob below is 38, not 28 -- 30 scripts were named here (12 tagged
      + 18 not_yet_tagged) of which 1 (software-request-analyzer.py) does not exist on disk (never found,
      not deleted this phase -- removed from the list below rather than left as a phantom tagged entry),
      and 9 real scripts were never named here at all: batch-import-conversation-log.py, credit-accountant.py,
      generate_quick_reference.py, generate-system-diagram.py, generate_task_checklist.py, launch-interactive-claude.sh,
      notify-owner.py, run-logged.sh, system-sync.py. Not fixed this phase (recounting/renaming this inventory
      is separate scope from tagging it) -- real, documented gap for a future pass, per this task''s own
      SCOPE (fix the 2 named gaps, note anything else rather than force it).'
    glob: /opt/veridian/scripts/*.{py,sh}
    system_index_tagged:
    - worker-entrypoint.sh
    - doc-worker-entrypoint.sh
    - supervisor-entrypoint.sh
    - master-decompose.py
    - preflight-guard.py
    - tight_task_validation.py
    - superboss-register.py
    - health-check-15min.py
    - cost-usage-60min.py
    - anthropic_openrouter_proxy_v2.py
    - recover-failed-workers.py
    - anthropic_openrouter_proxy.py
    - cost-reconciliation.py
    - dispatch-docworker-task.sh
    - gap-status.py
    - import-memory-history.py
    - module-queue-dispatcher.py
    - quality-gate.sh
    - queue-dispatcher.py
    - repair_controller.py
    - risk-tier.py
    - scope-check.py
    - supervisor-sweep.sh
    - sync-controller-back.py
    - sync-repos.sh
    - sync-vercel-env.sh
    - sync-verdian-ai-data.sh
    - task-template.py
    - veridian-task.py
    system_index_tagged_note: 'PHASE1 (2026-07-23): the 18 real scripts formerly listed under not_yet_tagged
      are now real rows in system_index with a tags column (module:<name>), verified via postflight_audit_gate.py
      AUD-20260723-063804-0d89cc. Also re-added the 11 real scripts previously listed here (software-request-analyzer.py
      excluded, see count_note) -- system_index itself had been emptied to 0 rows by the prior phase''s
      sqlite corruption+reinit (ai-os/memory/superboss-register.sqlite), so those 11 were, in the live
      DB, exactly as untagged as the 18 until this same phase restored them. 29 total real rows now in
      system_index, all with a real tags entry (before this phase: 0).'
    not_yet_tagged: []
  ai_os_governance_docs:
    count: 107
    roots:
    - ai-os/ root .md/.yaml
    - ai-os/audit-tree/
    - ai-os/boss/
    - ai-os/engines/
    - ai-os/registry/
    - ai-os/sentinel/
    - ai-os/system-tree/
    - ai-os/tree4-unified/
    see: registries (annotated), tree_lineage (the tree docs specifically)
    unannotated_note: Several dated point-in-time docs exist at ai-os/ root (INCIDENT_11K_API_CALLS_RCA.md,
      RCA_OVERNIGHT_DISPATCH_FAILURE_2026-07-20.md, REVIEW_FRAMEWORK_DECISIONS_2026-07-19.md, STAGING_ENV_2026-07-20.md,
      priority18b/21/22 design docs, 2045-GAP-CLOSURE-10-DOLLAR-PLAN.md) -- these are dated incident/decision
      records, not living registries; correct to read once for history, not to treat as a search target
      for current state. Not individually re-verified this pass.
    unindexed_docs_named_2026_07_20: 'registries.system_sync_mechanism''s unindexed_files_check mechanically
      found these 15 ai-os/ root docs genuinely absent from this file''s text (a stricter bar than the
      "several" above, which was itself an approximation): AIROUTER_SOFTWARE_TEAM_AUDIT_LOG.md, AI_CACHE_AND_TRIAGE_ARCHITECTURE.md,
      BRAIN.md, COST-CONTROL.md, DCMD-SCHEMA-DESIGN.md, REVIEW_FRAMEWORK_V2-8_MOBILE_UX_CROSSREF_2026-07-20.md,
      SELF-CHECK.md, SOFTWARE_TEAM.md, STATUS-REPORT.md, SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19.md,
      SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19_v2.md, priority18b_stage0_design.md, priority21_workspace_memory_design.md,
      priority22_ling_trial_findings.md, priority22_officecli_feasibility.md. Named here rather than individually
      catalogued -- most are the same class of dated point-in-time record as the unannotated_note above
      (design docs, audit logs, status snapshots); BRAIN.md and SELF-CHECK.md are the 2 exceptions that
      read as living docs and are real candidates for a proper registries entry in a future pass, not
      done this pass (scope was naming the gap, not individually re-verifying and cataloguing 15 files''
      content).'
audit_log:
- pass: 1
  ts: '2026-07-20T08:11:00+00:00'
  type: discovery_and_build
  method: Live SSH verification of every path/relationship above (file heads read, git log checked, script
    grep'd for cross-references) -- no memory-only claims. exclusion_rules discovered empirically (unscoped
    find incident) not assumed.
  known_incomplete:
  - file_inventory.ops_scripts.not_yet_tagged
  - file_inventory.ai_os_governance_docs.unannotated_note
  - registries.sentinel_rules relationship to constitution
  - registries.task_state_ledger rename not yet performed
- pass: 2
  ts: '2026-07-20T08:40:00+00:00'
  type: independent_duplication_and_gap_recheck
  method: Basename-collision scan across both catalogued ai-os dirs (find -prune, group by basename, count>1).
    Cross-check of ai-os/logs/ actual directory listing against what Pass 1 had referenced vs formally
    catalogued.
  found_and_fixed:
  - ai-os/logs/ (10 files, incl. the live ATTENTION.md alert surface) was referenced but not in canonical_dirs
    or registries -- added canonical_dirs.ai_os_logs and registries.attention_alerts.
  - 00-INDEX.md (4x) and README.md (2x) basename repeats across tree dirs, not previously flagged -- added
    tree_lineage.basename_collision_note.
  found_not_fix_needed:
  - MASTER_INDEX.yaml itself appears 2x (live path + repo mirror) -- by design, both paths already declared
    at top of file, not a collision.
  still_incomplete: same 4 items as pass 1's known_incomplete, not addressed this pass (pass 2 scope was
    duplication/collision re-check, not deep per-file verification).
- pass: 3
  ts: '2026-07-20T08:52:00+00:00'
  type: mechanical_path_existence_verification
  method: Automated walk of the deployed YAML extracting every absolute `path:` value (32 distinct), checked
    each against the live filesystem with os.path.exists(). Not a re-read of file contents -- a structural
    existence proof that every reference this file makes actually resolves.
  result: 31 of 32 real paths confirmed to exist. 1 flagged by the script was a glob pattern (/opt/veridian/ai-os/queues/*.yaml),
    a false positive from the check tool not expanding wildcards -- manually confirmed the directory and
    its 1 file (backend.yaml) are real. Zero actual missing paths.
  found_and_fixed:
  - file_inventory.ai_os_governance_docs.count was 105 (Pass 1's original census, taken before this file
    itself was added to the tree) -- corrected to 107 (105 + this file's own live+repo copies).
  sign_off: 3 of 3 required passes complete. Every path claim in this file is mechanically verified to
    resolve. Remaining known_incomplete items (ops_scripts.not_yet_tagged, governance_docs.unannotated_note,
    sentinel_rules-to-constitution relationship, task_state_ledger rename) are real, intentionally deferred
    follow-up work -- not gaps in this file's own internal accuracy, which is what the 3-pass audit was
    scoped to verify. This file does not itself claim those items are done.
- pass: 4
  ts: '2026-07-20T09:00:00+00:00'
  type: exhaustive_table_and_function_catalog_phase1_tables
  trigger: 'Owner: existing indexes were still "half work" -- domain-level summaries, not complete per-table/per-column
    ground truth. Owner approved (AskUserQuestion) an automated code-reading script over manual AI-written
    descriptions, tables first then functions as phase 2.'
  method: 'Wrote extract-db-schema-catalog.mjs, using drizzle-orm''s own is(value, PgTable) + getTableConfig()
    introspection on the live schema.ts module (imported directly, zero DB connection needed). First version
    had 2 real bugs, both caught before reporting any number as final: (1) enum detection checked constructor.name===''PgEnum''
    which never matches -- pgEnum() exports are callable functions, not plain objects; (2) needed an explicit
    is(value,PgTable) gate, verified via a separate diagnostic script to have zero false positives, before
    trusting getTableConfig()''s success as "is a table."'
  result: '444 tables, 124 enums, 249 other schema exports (relations helpers) -- cross-verified 3 independent
    ways: drizzle-orm''s own is(PgTable) API count, raw grep count on schema.ts text, and manual spot-check
    of one table''s full column list against a known prior manual note. All 3 agree. This corrects tree3_system_tree''s
    stale 377/106 claim (schema grew after that doc was last verified).'
  output: /opt/veridian/ai-os/DATABASE_CATALOG.json (1.4MB, full column/FK/index detail per table)
  next: Phase 2 (function-level catalog) not started this pass -- larger scope (functions spread across
    hundreds of files vs. one schema.ts), deliberately sequenced after tables per Owner's approved plan.
- pass: 5
  ts: '2026-07-20T09:19:00+00:00'
  type: exhaustive_function_catalog_phase2
  trigger: 'Owner confirmed phase 2 (function catalog) and generalized the principle: "for all our work
    make a small script that can be run by you and data collected."'
  method: Wrote extract-function-catalog.mjs using the TypeScript compiler package's own AST parser directly
    (ts.createSourceFile, parse-only -- no Program/type-checker needed, so no project-wide type resolution
    cost). Walked every FunctionDeclaration, const-assigned Arrow/FunctionExpression, MethodDeclaration,
    ConstructorDeclaration, and Get/SetAccessor across all 1,623 .ts/.tsx files under src/.
  result: 5,019 functions (4,527 function_declaration, 479 arrow_const, 8 class_constructor, 5 class_method,
    0 accessors). Completed in 1.5 seconds. First run (before the constructor fix) found 5,011 and undercounted
    class-related entries -- caught via targeted grep spot-check of known `export class` files before
    trusting the number, not assumed complete on the first pass. 3 known functions (checkOpenRouterBalance,
    runRole, checkCostPolicy) spot-verified against facts already established earlier this session --
    exact match on file/line/exported/async for all 3.
  output: /opt/veridian/ai-os/FUNCTION_CATALOG.json (1.4MB)
  standing_principle_recorded: 'Owner directive, generalized beyond this task: prefer a small, re-runnable
    script that reads the real system directly over AI-written summaries, for any future data-collection
    need on this server. Applies to the "software first, AI second" mandate already in ai-os/CONSTITUTION.yaml,
    made explicit for indexation/catalog work specifically.'
- pass: 6
  ts: '2026-07-20T09:45:00+00:00'
  type: whole_system_scope_expansion
  trigger: 'Owner: "finish this for the whole system FChecklist, VERIDIAN, PROJEXA, Compliance-tracker"
    -- flagged that function_catalog''s scope_note claiming PROJEXA/veda-advisors are API-only was an
    unverified assumption.'
  method: Checked system_index/MASTER_INDEX first per the new standing prompt (no exact duplicate found).
    Enumerated every repo under /opt/veridian/repos/ (11 total), checked each for package.json+src/ to
    distinguish real codebases from config/doc/stub repos, rather than trusting the prior claim.
  result: The prior scope_note was WRONG. PROJEXA (371 files, own 11-table schema, 992 functions) and
    veda-advisors (62 files, 270 functions, no schema.ts -- uses a Supabase client not Drizzle) both have
    real, separate codebases. veridian-ui-kit (22 files, 46 functions) was not previously accounted for
    at all. Both extraction scripts were parameterized (dynamic import() / argv-based paths instead of
    hardcoded compliance-tracker paths) and regression-tested against compliance-tracker's own known numbers
    (444/124/5019, exact match) before reuse on the other repos -- confirming the refactor didn't silently
    change behavior.
  real_blockers_hit_and_resolved:
  - projexa had zero node_modules installed on this server -- installed via ~/.bun/bin/bun install (bun
    not on PATH in non-interactive SSH sessions).
  - Cross-repo script invocation fails silently in confusing ways (tsx module resolution is tied to the
    invoking binary's own repo, not the dynamically-imported file's location) -- resolved by copying the
    generator script into each target repo's own tree so both its own and the dynamically-imported schema's
    imports resolve from the same node_modules.
  - projexa's git push was rejected (remote had 2 newer commits); pull required stashing pre-existing,
    real, uncommitted local work (a CI guardrail check, not mine) to avoid losing it -- stashed, pulled,
    restored, resolved one real .gitignore merge conflict (non-overlapping additive, both sides kept).
  - schema.ts changed during that pull (a notifications table was added) -- caught before pushing, catalog
    regenerated against the new HEAD rather than shipping a catalog already 1 commit stale.
  - 'Final push blocked by GitHub: OAuth token lacks `workflow` scope, and the restored CI guardrail commit
    touched .github/workflows/ci.yml, blocking the whole branch push. Resolved via AskUserQuestion (Owner
    chose): cherry-picked only the 2 catalog commits onto a clean branch off origin/main, opened https://github.com/FChecklist/projexa/pull/50.
    The workflow-touching commit remains local-only on projexa main, not lost, pending Owner action.'
  - veda-advisors' main is branch-protected (GH006) -- pushed via branch + PR (https://github.com/FChecklist/veda-advisors/pull/16)
    instead, same pattern as the projexa workaround.
  other_repos_classified: veridian-brain (1-file stub, no catalog run), claude-control (config-only, already
    covered), infisuite-reverse-engineering + odoo-reverse-engineering (this session's own third-party-system
    docs, not this system's code), global-revenue-engine (empty), sumeet-spec (single spec file) -- all
    confirmed correctly out of scope for a code catalog, not silently skipped.
  next: AI model/role catalog (the other half of the Owner's original ask) not yet started this pass --
    separate task, governance docs exist but haven't had the mechanical-extraction treatment.
- pass: 7
  ts: '2026-07-20T09:48:00+00:00'
  type: ai_model_role_catalog
  trigger: 'Same instruction as pass 6 ("finish this for the whole system"), second half: the AI model/role
    side, previously only covered by prose docs.'
  method: Found the real code-level source of truth first (src/lib/ai-team/roster.ts's AI_TEAM_ROSTER
    export) rather than extracting from AI_ORCHESTRA_HIERARCHY.md's prose. Wrote extract-ai-roster-catalog.mjs,
    dynamically importing the real, evaluated array (same technique as database_catalog, not AST text-parsing,
    since this is declarative data rather than code structure).
  result: '195 roles (190 LLM-backed, 2 code-only, 3 human) -- the file''s own header comment claims "~30-role
    company org chart," written 2026-07-07, now stale: the 5 AUDIT_* teams alone grew to 130 roles since.
    4 known roles (founder_ceo, super_boss, executive_advisor, cost_policy_engine) spot-verified against
    the file''s own documented claims about them -- exact match on all 4.'
  output: /opt/veridian/ai-os/AI_ROSTER_CATALOG.json
  committed: compliance-tracker commit b0a01530, pushed cleanly (no branch-protection/token-scope issue
    on this repo).
  original_directive_status: 'Both halves of the Owner''s "finish this for the whole system" instruction
    are now closed: whole-system code/DB catalogs (registries.projexa_catalog, .veda_advisors_catalog,
    .veridian_ui_kit_catalog) and the AI model/role catalog (this entry). Real, intentionally-deferred
    follow-up remains (registries.task_state_ledger rename, the projexa workflow-file commit awaiting
    a workflow-scope token, master_tracker<-> gap_dispatch_queue reconciliation) -- documented, not silently
    dropped.'
- pass: 8
  ts: '2026-07-20T10:10:00+00:00'
  type: automatic_conversation_and_action_logging
  trigger: 'Owner: "whenever script is run, did it get logged... is our current discussion... logged...
    with proper tags... if not, why? because this is the main root cause of issue." Direct, pointed challenge
    to verify rather than assume the register''s own completeness.'
  method: 'Queried the register directly before answering: confirmed only 13 instructions / 60 work_items
    / 3 actions logged for the whole day against 150+ real tool calls, and confirmed the conversation
    text itself was never in the register at all -- only in a local Claude Code transcript file, never
    synced. Root cause: manual, discipline-dependent logging at milestone granularity, exactly as the
    Owner suspected. Confirmed via AskUserQuestion before installing anything that changes behavior for
    every future session: Owner approved automatic hooks.'
  result: registries.auto_conversation_log built, tested end-to-end with real data (including a real transient-SSH-timeout
    recovery), and deployed live. This session's own history (39 instructions, 395 actions) backfilled
    retroactively, confirmed to start from this session's real first message. A real ID-collision bug
    in the batch importer was found and fixed before the backfill was trusted, not silently accepted from
    the first (crashed) attempt.
  sign_off: 'Logging is now enforced by Claude Code''s own hook system, not by an AI agent''s memory --
    this is what "by default, permanently" means as an engineering fact, not a promise. The design fails
    open (a broken/slow sync never blocks the interaction) and fails loud to a local error log (never
    silently drops a queued event). Known, honest scope limit: only THIS session was backfilled; other
    prior sessions on this laptop were not, and are real, separate follow-up work if wanted.'
- pass: 9
  ts: '2026-07-20T10:22:00+00:00'
  type: server_side_auto_logging_worker_tasks
  trigger: 'Owner, same day, after reviewing pass 8: "we are not auto updating everything... for your
    work that CLAUDE CODE CLI or CLAUDE CODE is doing and ai superboss, ai agents, software are doing
    need to be automatically updated in the server by a script." Correctly identified that pass 8''s hooks
    only covered the interactive laptop session, not the autonomous dispatch fleet running on the server
    itself.'
  method: Found the real choke point before writing anything -- veridian-task.py's cmd_create/cmd_checkpoint/cmd_record_usage
    are the single functions every dispatch path (queue-dispatcher.py, module-queue-dispatcher.py, dispatch-docworker-task.sh,
    manual CLI calls) already goes through. Instrumented once there rather than separately in every caller.
    Backed up the original file before editing, syntax-checked locally and remotely before swapping in,
    tested the new function in isolation against a synthetic task (not a real dispatch, to avoid triggering
    a real git worktree/systemd service) before trusting it, then confirmed `veridian-task.py status`
    still works correctly against real existing task data post-edit.
  result: 'registries.auto_worker_task_log deployed and verified. Combined with pass 8, both halves the
    Owner asked about are now covered: the interactive session (hooks) and the autonomous dispatch fleet
    (veridian-task.py instrumentation).'
  sign_off: 'Honest remaining gap, stated plainly: the periodic cron jobs that never call veridian-task.py
    at all (health-check-15min.py, cost-usage-60min.py) are not instrumented by this pass -- they already
    write to registries.attention_alerts, which is indexed and searchable, just not via instructions/actions
    specifically. Real, scoped, undone follow-up if the Owner wants full coverage of that layer too, not
    silently claimed as done.'
- pass: 10
  ts: '2026-07-20T10:40:30+00:00'
  type: cron_execution_wrapper_zero_gap
  trigger: 'Owner: "in modern software without AI, how do you ensure that tasks, actions are properly
    logged with id and time date... ensure that the server have that script that can do it for every task,
    every encounter in server... make it end to end. zero gap, 100% coverage. no assumptions." Direct
    follow-through on pass 9''s stated honest gap.'
  method: Answered the conceptual question first (the middleware/aspect pattern -- a single wrapper every
    job runs through, so coverage never depends on the wrapped script's own author remembering to log),
    then applied it concretely. Checked the FULL crontab before assuming scope (`crontab -l`) rather than
    trusting the 2 job names quoted back in the prior turn -- found 8 entries, 7 active + 1 already disabled,
    not 2. Built run-logged.sh, tested standalone (success path + deliberate-failure path) before touching
    the real crontab, backed up the original crontab (reversible), installed the wrapped version preserving
    every existing >>logfile redirect and leaving the disabled entry untouched, then verified with a REAL
    job run through the wrapper (not just a synthetic test) before trusting it live.
  result: All 7 active cron jobs now route through run-logged.sh. Real production run (health-check-15min.py)
    surfaced a genuine, serious, pre-existing operational finding unrelated to this fix (47 failed systemd
    units, 71.9% task failure rate) -- reported to Owner immediately, not buried in a log file. Waited
    for and confirmed the actual unattended cron tick (queue-dispatcher at 2026-07-20T10:40:01Z, zero
    manual intervention) logged correctly -- proves the crontab entries themselves work, not just manual
    invocation of the same command line.
  sign_off: 'All 3 auto-logging layers identified across this session are now built AND verified with
    real evidence, not claimed: (1) interactive laptop session via Claude Code hooks (registries.auto_conversation_log),
    (2) autonomous worker/ supervisor/doc-worker dispatch via veridian-task.py instrumentation (registries.auto_worker_task_log),
    (3) periodic cron jobs via run-logged.sh (registries.auto_execution_wrapper). "Zero gap" claim is
    scoped honestly: it covers every currently-active, currently-known execution path on this server as
    enumerated by direct inspection (not assumed) -- a genuinely new execution path added later (a new
    cron entry, a new dispatch mechanism) would need the same treatment applied to it explicitly, this
    is not a universal, permanently-self-extending guarantee.'
- pass: 11
  ts: '2026-07-20T11:30:00+00:00'
  type: rca_permanent_fix_and_real_recovery
  trigger: 'Owner: "identify the RCA, do proper gap identification, implement the fix for permanent solution.
    No shortcuts. No partial work. 100% proper completion. FIX this issue permanently by script or wrapper
    that makes it software driven." Direct follow-through on pass 10''s surfaced finding (47 failed units,
    71.9% failure rate).'
  method: Diagnosed root cause with hard evidence (queried OpenRouter's own /credits endpoint directly,
    not the local proxy's tracker) before writing any fix. Found and fixed a second, deeper bug along
    the way (claude -p returning exit 0 on API-level errors, defeating the circuit breaker entirely).
    Built recover-failed-workers.py to safely recover the 46 affected units, deliberately checking each
    unit's REAL result.json before touching it rather than assuming one cause explained all 47 -- this
    surfaced a genuinely SEPARATE second root cause (2 units legitimately hit the CLI's own max-budget-usd
    cap) which got its own dedicated fix rather than being folded into the first. Every fix live-verified
    against a real systemd unit before being trusted, not just syntax-checked.
  result: See registries.worker_fleet_rca_fix_2026_07_20 for full detail. 0 failed veridian-worker@ units
    remain (down from 47). All 46 converted to accurate blocked state with real diagnostics. OpenRouter
    balance itself is still exhausted -- this is a real money problem for the Owner to resolve, explicitly
    not something this pass could or should fix in code; the recovered units will resume automatically
    once credits are added.
  sign_off: 'TASK 1 of the Owner''s 6-part directive (2026-07-20) complete: permanent, software-driven
    fix deployed and verified live, not claimed. TASKS 2-6 (constitution staleness detection, unified
    system-sync.py, cron wiring) and the required 3-round audit of that work remain -- not started this
    pass, tracked separately.'
- pass: 12
  ts: '2026-07-20T12:15:00+00:00'
  type: unified_sync_mechanism_build_and_verify
  trigger: 'Owner TASKS 2-6: constitution staleness detection, "system is broken, working in parts, make
    it integrated," "files getting missed," a script referenced FROM this file that syncs the whole system
    on demand, and the same running on a schedule. Owner: "YOU ARE THE SYSTEMS DESIGNER. THINK TO MAKE
    IT PERFECT."'
  method: 'Deliberately built ONE mechanism (system-sync.py) instead of 5 separate scripts -- building
    another standalone script per task would have repeated the exact "working in parts" failure mode the
    directive itself names. See registries.system_sync_mechanism for the 3 checks and their real first-run
    findings. Every check verified against real, not hypothetical, drift: ran --dry-run first, inspected
    findings before trusting them (caught and fixed 1 real false-positive bug -- cross-repo file references
    -- before it could hide genuine future findings under noise), then ran for real, then re-ran after
    fixes to confirm 0 findings, then verified the actual cron-wrapped invocation (run-logged.sh "system-sync"
    ...) produces the identical result before trusting the schedule.'
  result: '1 genuine, serious gap closed: 17 live scripts (including queue-dispatcher.py, firing every
    10 minutes via cron) had ZERO git-tracked mirror -- now all mirrored and committed. 1 false- positive
    class fixed in the checker itself before it shipped (cross-repo CONSTITUTION.yaml references). 15
    previously-unnamed governance docs now explicitly named (see file_inventory.ai_os_governance_docs.unindexed_docs_named_2026_07_20).
    All 3 checks confirmed at 0 findings after fixes applied and this file redeployed with the new names
    embedded -- re-ran system-sync.py --check all a final time to prove the loop closes (a finding disappears
    once genuinely fixed, not just logged).'
  sign_off: 'TASKS 2-6 of the Owner''s 6-part directive complete: a single, cron-scheduled (every 6h),
    on-demand-invokable, MASTER_INDEX.yaml- registered mechanism now continuously re-checks the 3 highest-
    value drift classes found this session, extending in place (registries.system_sync_mechanism.do_not_build_a_new_sync_script)
    rather than by further script proliferation. Honest scope note: these 3 checks are the ones this session''s
    own work surfaced as real -- a genuinely new drift class discovered later needs a 4th check function
    added here, this is not a claim that every possible form of system drift is now covered. Remaining:
    the Owner''s required 3-round audit of everything built this pass (TASK 52) -- not started yet.'
- pass: 13
  ts: '2026-07-20T12:30:00+00:00'
  type: required_3round_audit_round_1_verification
  trigger: 'Owner: "After doing it. Audit 1 it, analyze the gaps, fill it. Audit 2 it... Audit 3 it..."
    -- required final step of the 6-part directive, covering TASK 1 (worker_fleet_rca_fix) and TASKS 2-6
    (system_sync_mechanism) together.'
  method: 'Re-verified every claim made in the two new registry entries against real, current server state
    (not re-reading what was already written): 0 failed veridian-worker/docworker units, all 3 worker-entrypoint.sh
    fix markers present in the live file, check_openrouter_balance() actually wired into preflight-guard.py''s
    main(), the crontab entry actually installed, git working tree clean, PR #501 open and mergeable,
    live MASTER_INDEX.yaml byte-identical to its repo mirror.'
  result: 8/8 checks passed. No discrepancy found between claimed and actual state.
- pass: 14
  ts: '2026-07-20T12:45:00+00:00'
  type: required_3round_audit_round_2_deeper_edge_case
  trigger: Same directive, round 2 -- deeper edge-case check, not just re-verifying surface claims.
  method: Picked the single most load-bearing claim in registries.worker_fleet_rca_fix_2026_07_20 ("recovered
    units will resume automatically once credits are added") and traced it against the ACTUAL retry logic
    in queue-dispatcher.py, rather than trusting the claim because it sounded plausible.
  found_and_fixed: 'The claim was FALSE. queue-dispatcher.py''s sync_dispatched_statuses() only revisits
    gap_queue.yaml items still in ''dispatched'' status; 21 of the 46 recovered tasks were already stuck_needs_human
    (terminal, never revisited) before this pass''s recovery even ran, and the other 25 were never tracked
    in gap_queue.yaml at all. Built registries.system_sync_mechanism.checks.resume_balance_blocked_check
    to close the gap directly (operates on CONTROLLER.yaml/systemd, covers both groups uniformly), corrected
    the false claim in place with an explicit correction note (audit_log is append- only per its own header,
    but registries reflect current truth -- the false claim is preserved, visible, and explicitly marked
    corrected, not silently rewritten). Verified the new check''s logic via a monkeypatched-balance dry-run:
    correctly identified all 47 balance-blocked tasks, took zero action in dry-run mode. Not yet verified
    against a real restored balance (the account is still genuinely exhausted -- that proof needs the
    Owner to add credits first, honestly flagged as not yet end-to-end proven).'
  sign_off: 'This is exactly the kind of gap the required audit exists to catch: a claim that was reasonable-sounding
    but unverified, found false by actually tracing the code path instead of re-reading the claim. 1 real
    gap found, 1 real fix built and deployed, corrected in place rather than silently patched over.'
- pass: 15
  ts: '2026-07-20T13:00:00+00:00'
  type: required_3round_audit_round_3_final_signoff
  trigger: Same directive, round 3 -- final mechanical verification and honest close-out.
  method: 'Re-ran system-sync.py --check all one final time after all round- 1/2 fixes and this file''s
    own edits were deployed, to prove findings actually disappear once genuinely fixed rather than just
    being logged and forgotten. Separately checked this file''s OWN standing protocol rule ("register
    the new thing in system_index AND in this file''s relevant list") against what had actually been done
    -- found a real gap: none of this pass''s 4 new/changed scripts (preflight-guard.py, worker-entrypoint.sh,
    recover-failed-workers.py, system-sync.py) had been registered in system_index (the SQLite 4th tree),
    only in this YAML file. Fixed immediately via superboss-register.py index-add for all 4 (IDX-20260720-075710-066a,
    IDX-20260720-075711-f6b8, IDX-20260720-114948-a0f2, IDX-20260720-114948-05c7).'
  result: 'mirror_drift/constitution_staleness/unindexed_files: 0/0/0 findings -- confirmed the loop closes,
    not just claimed. Only remaining finding across all 4 checks: resume_balance_blocked correctly reports
    the real, external, unresolved condition (OpenRouter balance still exhausted) rather than a false
    positive or an internal bug -- this is the correct, expected steady state, not a gap. system_index
    registration gap found and closed same pass.'
  sign_off: 'TASKS 1-6 of the Owner''s 2026-07-20 6-part directive are complete, live, and verified against
    real server state -- not claimed from memory. Required 3-round audit complete: round 1 verified 8/8
    surface claims, round 2 found and fixed 1 real functional gap (false auto-resume claim), round 3 found
    and fixed 1 real process gap (system_index registration) and confirmed the sync loop genuinely closes.
    Honest remaining items, not silently dropped: (1) OpenRouter balance is still genuinely exhausted
    -- real money problem, Owner action required, not a code gap; (2) doc-worker-entrypoint.sh does not
    yet have the same API_IS_ERROR/CLI_HIT_BUDGET_CAP fixes (0 current failures there, flagged not urgent);
    (3) resume_balance_blocked_check is logic- verified but not yet proven end-to-end against a real restored
    balance; (4) TASK 46 (the broader "full system re-evaluation" task-tracker entry from before this
    6-part directive) remains open in the task tracker, superseded in substance by this pass''s work but
    not formally closed out; (5) BRAIN.md and SELF-CHECK.md were named in file_inventory.ai_os_governance_docs
    as real candidates for a proper registries entry, not yet done.'
- pass: 16
  ts: '2026-07-20T13:15:00+00:00'
  type: task5_gap_analysis_and_dispatch_hold
  trigger: 'New Owner directive, TASK 5: "The remaining 185 gaps of 2056 which are still open, analyze
    them. What are they and how important. Dont work on them. Tell Mother Router to stop these jobs."
    Part of a larger 6-task directive; TASK 5 executed first as the fastest, most bounded piece.'
  method: 'Traced "185 of 2056" to its real source (not assumed): reconciles exactly to SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19_v2.md''s
    ~170 genuinely-open + ~15 deferred-routed-to-execution rows. Found these 185 rows are already consolidated
    into 25 V2-* dispatch tasks in gap_queue.yaml (21 non-completed). Checked queue-dispatcher.py''s actual
    dispatch logic (not assumed) -- confirmed it only fires from gap_queue.yaml''s own static list, currently
    empty of dispatchable items, so nothing was actively auto-dispatching. But found a real, live risk:
    11 of the 21 held items are ALSO balance-blocked and would have been silently auto-resumed by resume_balance_blocked_check()
    (built this same session) the moment the Owner adds OpenRouter credits -- a direct conflict with this
    new directive that would not have been caught without explicitly cross-checking the two mechanisms
    against each other.'
  result: 'Built a data-driven pause (gap_queue.yaml.dispatch_paused + held_task_ids), enforced in both
    queue-dispatcher.py and resume_balance_blocked_check(), both verified live. Wrote ai-os/GAP_ANALYSIS_2026-07-20_HOLD.md:
    reconciliation, current status breakdown (4 completed / 11 stuck-on-the-now-fixed-balance- incident
    / 10 possible-duplicate-skips), and a real importance read of all 25 items by category (4 High: delegation-expiry
    auth gap, storage RLS+backup, payroll audit, GST e-invoicing correctness -- all security/compliance/money-correctness,
    not polish).'
  sign_off: Analysis delivered, zero work done on the 185 gaps themselves per the explicit "don't work
    on them" instruction. Dispatch hold is permanent and data-driven (release by flipping one boolean,
    no code change) -- not a one-time manual stop that silently expires.
- pass: 17
  ts: '2026-07-20T13:40:00+00:00'
  type: task6_master_index_clarity_restructure
  trigger: 'Owner directive, TASK 6: "read the MASTER_INDEX.yaml again, and make it logical for you or
    any ai to work. Things should not be complicated. It should be logical, structural, stepwise, exact
    input, output. Proper documented."'
  method: 'Did not rewrite the 41 existing registries'' detailed prose (real risk of breaking real content
    for cosmetic gain, and this file''s audit_log is legitimately a narrative log -- narrative IS appropriate
    there). Instead: (1) added an explicit numbered "HOW TO USE THIS FILE" section at the top (5 concrete
    steps); (2) defined a formal schema (id/status/path + a priority-ordered free- text field) every entry
    under registries: should follow going forward; (3) built ai-os/scripts/generate_quick_reference.py
    -- mechanically extracts a one-line summary from each registry''s own scope/ purpose/finding/decision
    field (whichever it uses) rather than hand-summarizing 41 entries under time pressure, which risks
    inconsistent quality. Caught and fixed a real bug in the generator''s first draft before trusting
    it: manual f-string quote-wrapping broke on entries whose extracted text itself contained double quotes
    -- switched to yaml.safe_dump for correct escaping, verified by round-tripping the output back through
    yaml.safe_load before splicing it into this file.'
  result: 'quick_reference: (41 entries, id+status+path+one-line summary) inserted between the header
    and registries: -- scan that first, only drill into the matching full entry under registries: when
    needed. Regeneration is now the documented, repeatable path for keeping it in sync (not a one-time
    snapshot that goes stale the way ai_router_migration_inventory''s "~23+3" estimate did).'
  sign_off: This is a structural fix, not a content rewrite -- the 41 existing entries' detail is unchanged
    and un-retrofitted with the new schema fields (disclosed, not silently declared complete). Any registry
    entry touched in a FUTURE pass should pick up id/status/path if missing at that time, rather than
    a dedicated retrofit pass being required first.
- pass: 18
  ts: '2026-07-20T13:45:00+00:00'
  type: required_3round_audit_round1_and_2
  trigger: Owner's standing 3-round audit requirement, applied to all of TASKS 1.1/1.2/1.3/2-4/3/5/6 built
    this pass.
  round_1_method: 'Re-verified 7 concrete claims against real state rather than re-reading what was written:
    PR #503/#504 merged (gh pr view), PR #502 still open, gap_queue.yaml dispatch_paused still true (21
    held), queue-dispatcher.py actually prints PAUSED and dispatches nothing, system-sync.py --check all
    clean (0/0/0/1-expected), platform.ops_dev_tasks table exists and returns real query results, SYSTEM_DIAGRAM.md
    contains valid mermaid, git working tree clean across all 3 touched branches, crontab has the system-sync
    entry.'
  round_1_result: 9/9 checks passed, no discrepancy found.
  round_2_method: 'Deeper edge-case check: what ELSE touches gap_queue.yaml besides queue-dispatcher.py
    (the file TASK 5''s pause lives in)? Found 2 other scripts referencing it -- generate-system-diagram.py
    (false positive, just a text label in a diagram node, no actual file access) and gap-status.py (real
    open(), read-only).'
  round_2_found_and_fixed: 'gap-status.py crashed with KeyError on doc[''total_findings''] -- every single
    invocation, not an edge case. Traced whether this was caused by today''s gap_queue.yaml rewrite (TASK
    5) before fixing: checked the PRE-edit backup taken at the start of TASK 5 (gap_queue.yaml.bak-2026-07-20-pretask5)
    -- it ALSO lacks total_findings, proving this is a pre-existing, dormant bug, not a regression introduced
    this pass. Fixed with a defensive .get() fallback rather than fabricating a total_findings value that
    was never actually computed anywhere. Verified: script now runs and prints real status (4/25 completed,
    correct stuck_needs_human/skipped_possible_duplicate breakdown) instead of crashing.'
  round_2_additional_verification: 'Confirmed both PR #503 and #504''s merged content is genuinely on
    origin/main (not just claimed) -- git show origin/main:<file> | grep for a marker string unique to
    each change, both found.'
  sign_off: 1 real, pre-existing (not caused this pass) bug found and fixed during round 2 -- exactly
    the kind of finding this audit discipline exists to catch, whether or not it was this session's own
    doing.
- pass: 19
  ts: '2026-07-20T13:50:00+00:00'
  type: required_3round_audit_round3_final_signoff
  trigger: Same standing requirement, round 3 -- final mechanical verification and honest close-out.
  method: Re-ran system-sync.py --check all one final time after the gap-status.py fix to confirm nothing
    else regressed (0/0/0/1- expected, unchanged). Compiled a complete honest gap list rather than declaring
    the 6-task directive fully closed.
  honest_gap_list:
  - 'PR #502 (TASK 1.1) remains open, held for Owner sign-off (Tier2: schema + new secret) -- by design,
    not an oversight.'
  - 'platform.ops_dev_tasks has 0 rows -- expected, the write path isn''t live in production until #502
    merges.'
  - OpenRouter account balance is still genuinely exhausted -- real money problem, Owner action required,
    not a code gap.
  - 35 AI-router call sites remain unmigrated (TASK 1.3) -- deliberately deferred with documented reasoning,
    not silently dropped.
  - doc-worker-entrypoint.sh still lacks the API_IS_ERROR/ CLI_HIT_BUDGET_CAP fixes worker-entrypoint.sh
    got earlier this session -- disclosed then, still true now, 0 current failures there so still not
    urgent.
  - E2E Tests CI job is broken repo-wide (MODULE_NOT_FOUND on every PR, confirmed pre-existing and unrelated
    to any of today's diffs) -- flagged via a spawned background task, not fixed (genuinely out of scope
    for this directive).
  - MASTER_INDEX.yaml's 41 pre-existing registry entries were not retrofitted with the new id/status/path
    schema (TASK 6) -- disclosed explicitly in that entry's own sign_off, not silently implied complete.
  - gap-status.py's total_findings field is now a graceful "unknown" rather than a real computed value
    -- the underlying question of what should populate it was not investigated further (out of scope,
    a pre-existing gap this pass only made non-crashing).
  sign_off: 'All 6 tasks of the Owner''s 2026-07-20 directive (TASK 1 findings 1-4, TASK 2, TASK 3, TASK
    5, TASK 6) are complete, live- verified against real server/repo/database state where a live test
    was possible, and honestly gapped where it was not (Vercel rate-limit + deployment protection blocking
    full E2E proof of the still-open PR). Required 3-round audit complete: round 1 verified 9/9 surface
    claims clean, round 2 found and fixed 1 real pre-existing bug (not this pass''s own regression, confirmed
    via the pre-edit backup), round 3 compiled the complete honest gap list above rather than declaring
    zero-gap completion.'
- pass: 20
  ts: '2026-07-30T03:57:58.416644+00:00'
  type: central_entrypoint_confirmation_and_coverage_gap_closure
  trigger: 'Owner direct question: ''is there a central place where all documentation can be read -- is
    MASTER_INDEX.yaml that place?'' Follows the 2026-07-29/30 regenerate_master_index.py build (see regeneration_mechanism
    below) which proved this file''s own ''mechanically generated'' claim was previously false for registries[]
    prose (only initiative-status gets script-corrected).'
  method: 'Read this entire live file in full (2953 lines). Checked for competing ''where do I find docs''
    mechanisms by direct inspection, not assumption: knowledge_engine/wiring_registry/system_index (all
    real, all complementary per registries.metadata_engine_reconciliation''s existing sync-direction contract),
    PROTOCOL_OWNER_AI.yaml (real, different axis -- process not location), per-repo AGENTS.md (real, but
    an agent-authorization roster, not a docs index), README-SERVER.md (real, but an infra-ops reference,
    not a docs index). Then live-verified 9 named candidate coverage items against the real filesystem/DB
    (find + sqlite3 queries, not memory claims).'
  result: '7 of 9 named items were REAL and had ZERO prior entry in this file -- added as new registries[]
    entries this pass: umr_tasks_registry, capability_services_pair, veri_chat, browser_intent_cache,
    sap_reports_projexa_completion_effort, cron_systemd_consolidation_2026_07_29, utm_traceability_convention.
    1 named item (task_capabilities) does NOT exist as a real table anywhere -- recorded honestly as a
    real gap (real_file: null), not fabricated (see unverified_findings_open_2026_07_22.items or the dedicated
    gap note added this pass, not silently dropped). 1 item (capability-learning-service.ts / capability-audit-service.ts)
    was real and added as capability_services_pair.'
  also_added: search_layers_relationship header block (see top of file) -- the MASTER_INDEX-vs-knowledge_engine-vs-wiring_registry-vs-system_index-vs-PROTOCOL_OWNER_AI
    relationship was already real and already documented (registries.metadata_engine_reconciliation) but
    buried ~840 lines in; a reader who only reads the header would not know these are complementary rather
    than redundant. This block is a pointer, not a duplicate.
  honest_gaps_not_closed_this_pass:
  - cron_systemd_consolidation_2026_07_29's 18 units are still not registered in system_index (only in
    this file's registries[] section) -- a follow-up index-add pass is needed for check-duplicate coverage.
  - The 7 new entries' internal wiring/behavior was existence-confirmed only (file/table/task-dir presence
    verified directly) -- not independently re-verified for correctness of behavior, the way registries.wiring_engine
    or registries.database_catalog were built via a dedicated extraction script.
  - task_capabilities remains genuinely unbuilt -- this pass only records that honestly, it does not build
    it.
cross_system_memory_map_2026_07_22:
  principle: 'Owner directive 2026-07-22: every memory layer across every system must be interconnected
    via software, using each system''s own native ID as the cross-reference key -- NOT a new universal
    ID scheme (already rejected once this session, see synthesis_2026_07_22_360_degree_review in STRATEGIC_PLAN).'
  layers:
    this_machine_assistant_memory:
      status: REAL_BUG_FOUND_AND_FIXED_NOT_YET_PROVEN
      store: C:\Users\Dell\.claude\projects\...\memory\*.md (this Windows laptop) plus .claude\hooks-queue\conversation-log.ndjson
        (auto-capture attempt)
      cross_ref_key: memory filename slug
      root_cause_found_2026_07_22: the local PostToolUse-intended auto-log hook (log-tool-use.ps1) was
        wired under PreToolUse in settings.local.json -- fires before tool_output exists, so every captured
        entry's output_excerpt was empty (verified directly, not inferred). This is exactly why a discovery
        ('3 files reference vercel/supabase/github terms') was not automatically persisted -- the safety
        net meant to catch it was structurally broken, not merely unused.
      second_issue_found: the hook had not fired at all for this entire multi-hour session until the exact
        moment of this investigation -- cause not identified, flagged unresolved
      fix_applied: moved the hook entry from hooks.PreToolUse to hooks.PostToolUse in settings.local.json,
        verified as valid JSON with correct entry counts
      honest_limitation: NOT proven to actually capture output within the same running session -- hooks
        load at session start, so this needs a fresh session to verify output_excerpt is populated. Do
        not claim fixed-and-working without checking that first.
    superboss_server:
      status: ACTIVE
      store: ai-os/memory/superboss-register.sqlite
      cross_ref_key: instruction_id / work_item_id / software_task_id / ai_task_id
    per_task_memory:
      status: ACTIVE
      store: ai-os/tasks/<id>/task.yaml
      cross_ref_key: task id (matches software_task_id/ai_task_id in the register)
      count_confirmed_2026_07_22: 291
    mother_router:
      status: CONFIRMED_GAP
      detail: only ONE router file exists in the entire codebase (src/lib/ai-router/mother-router.ts)
        -- corrects last turn's looser phrasing. Zero persistence (no pgTable/sqlite/memory keyword found).
        Fully stateless per call.
    ai_agents_roster:
      status: CONFIRMED_GAP
      detail: src/lib/ai-team/roster.ts (195 roles) is a static in-code array, zero persistent per-role
        memory found.
    software_run_logs:
      status: ACTIVE
      store: ai-os/logs/*.jsonl (health-15min, cost-usage-60min, etc.)
      cross_ref_key: ts (ISO timestamp) -- confirmed real structured JSONL, not plain text, verified by
        direct read 2026-07-22
    github:
      status: ACTIVE_NATIVE_NOT_CROSS_LINKED
      store: git commit hashes / PR numbers, queried live via gh CLI
      gap: no register row currently links a work_item_id to the git commit/PR it produced -- should be
        added as a field, not a new store
    vercel:
      status: ACTIVE_NATIVE_NOT_CROSS_LINKED
      store: Vercel's own deployment history, queried via Vercel CLI/API
      gap: same as github -- no register linkage yet
    supabase:
      status: ACTIVE_NATIVE_NOT_CROSS_LINKED
      store: the live Postgres DB itself (444 tables) plus DATABASE_CATALOG.json as its schema index
      gap: same as github/vercel -- schema is indexed, but no register linkage from a work_item to which
        migration/table it touched
  cross_reference_convention: every log-work call going forward SHOULD include, where applicable, the
    real external ID it produced (a git commit hash, a PR number, a Vercel deployment ID, a migration
    filename) in its --content field, so a work_item can be traced to what it actually changed on each
    platform. This is a PRACTICE to adopt, not a new schema -- the log-work --content field already supports
    free text.
  honest_scope_of_what_was_built_tonight: 'This map is real and checkable -- every ACTIVE/GAP status above
    was verified via a live script this session, not assumed. What was NOT built: automated bidirectional
    sync (e.g. a webhook that auto-logs every GitHub push, or a cron that reconciles Supabase migrations
    against the register). That is a genuine multi-session integration project, not something to claim
    done at the end of an already very long session.'
  success_criteria_status:
    zero_repetition: PARTIAL -- true for ai-os/+scripts/ (file_inventory.py catches it); not yet checked
      for repos/ or the external platforms
    zero_duplication: PARTIAL -- same scope limitation as above
    everything_run_by_automation_not_ai: PARTIAL -- true for Superboss's own close-out (postflight_audit_gate.py);
      NOT true for Mother Router/roster (no automation exists there because no memory exists there yet)
    regular_automated_updates: TRUE for cron-based logs (15min/60min); NOT true for cross-platform reconciliation
      (no such job exists yet)
unverified_findings_open_2026_07_22:
  principle: 'Owner directive 2026-07-22: every discovery becomes a durable memory layer, not a chat-only
    mention -- this list exists so a loose end is never lost between turns or sessions.'
  items:
  - id: UNVERIFIED-001
    finding: 'grep for vercel_deployment|supabase_migration|github_commit matched 3 files: ai-os/planning/evidence/audit198-rerun-2026-07-21/audit198-results.json,
      ai-os/audit198/results/audit198-results.json, ai-os/DATABASE_CATALOG.json'
    status: OPEN_NOT_VERIFIED
    unknown: whether these are real working cross-references or passing text mentions (e.g. an audit rule
      description, a schema field name) -- not yet read in detail
    next_step: read the actual matching lines in each of the 3 files before either building on them or
      discarding them as noise
  - id: TASK_CAPABILITIES_NOT_REAL
    finding: The Owner's own request text named a `task_capabilities` table (alongside umr_tasks) as something
      to confirm coverage for. Direct query of superboss-register.sqlite's sqlite_master 2026-07-30 confirms
      NO table by this name exists anywhere on the server. A grep for the literal string 'task_capabilities'
      only matches stale, unrelated text inside old per-task git-worktree copies of VARIABLE_DICT/COMPLETED/MASTER-TRACKER
      files under ai-os/tasks/*/workspace/ -- not a real source reference.
    real_file: null
    distinction: Not the same gap as registries.engines_gateways_architecture's capability_registry (that
      one is AI-DISPATCH-capability-level, i.e. 'what can an AI role/agent do,' and is real and live).
      A task_capabilities table, if it is meant to mean per-TASK capability requirements/grants, does
      not exist as a distinct table under any name found this pass.
    status: OPEN_NOT_BUILT
    recorded_per: 'PROTOCOL_OWNER_AI.md section 14 (real-file grounding pass) -- ground every named concept
      to a real file/table before treating it as binding; record real_file: null honestly rather than
      fabricating an entry for a non-existent artifact.'
regeneration_mechanism:
  script: /opt/veridian/scripts/regenerate_master_index.py
  run_id: REGEN-20260729-185054-5cfe5b
  run_ts: '2026-07-29T18:50:58.043886+00:00'
  source_registry_counts:
    capability_registry: 11
    wiring_registry: 7791
    knowledge_engine: 350
    system_index: 114
    instructions: 1455
    work_items: 633
    unregistered_mentions: 8
    task_audits: 16
  initiative_status:
    scope_this_run:
    - terminology_standardization
    entries_checked_for_phase_evidence: 1
    entries_corrected_this_run: 1
    corrections:
    - id: terminology_standardization
      old_status: phase_0_phase_1_phase_2_complete_phase_3_not_yet_dispatched
      new_status: phase_0_through_phase_5_complete_all_6_phases_complete_verified_2026-07-29
    matcher_known_limitation: The phase-matcher (topical token-overlap between a merged PR's title and
      the initiative's own phase-plan names) has a confirmed false-positive rate on initiatives with short/generic
      phase names -- see the script's own module docstring and apply_status_corrections()'s docstring.
      Only run --apply with an explicit --only allowlist of entries a human has actually reviewed.
  unregistered_mentions_backlog:
    resolved_this_run: 7
    still_needs_registration: 0
    resolved:
    - path: scripts/test_check_crontab_unauthorized_change.py
      rows:
      - UNREG-92ca6195
      system_index_id: IDX-auto-20260729185058-3ed86c
      was_already_registered: false
    - path: ai-os/generated/generate_task_checklist-latest.yaml
      rows:
      - UNREG-6f25c181
      system_index_id: IDX-auto-20260729185058-f07118
      was_already_registered: false
    - path: ai-os/generated/generate_quick_reference-latest.yaml
      rows:
      - UNREG-8ae206a5
      system_index_id: IDX-auto-20260729185058-503d8d
      was_already_registered: false
    - path: ai-os/SYSTEM_DIAGRAM.md
      rows:
      - UNREG-4b82e592
      system_index_id: IDX-auto-20260729185058-1e6fda
      was_already_registered: false
    - path: ai-os/MASTER_INDEX.yaml
      rows:
      - UNREG-11362ad4
      system_index_id: IDX-auto-20260729185058-ef0fda
      was_already_registered: false
    - path: ai-os/scripts/generate_software_catalog.py
      rows:
      - UNREG-3bda38ed
      - UNREG-ce50422c
      system_index_id: IDX-auto-20260729185058-051081
      was_already_registered: false
    - path: ai-os/scripts/knowledge_registry_multisource.py
      rows:
      - UNREG-c8a55ca3
      system_index_id: IDX-auto-20260729185058-1659e7
      was_already_registered: false
    still_flagged: []
    note: postflight_audit_gate.py flags real ai-os/scripts paths mentioned in task --content but missing
      from the registries into this backlog. This field exists so the backlog is visible every run, never
      silent, even for rows this script could not safely auto-resolve.
  not_wired_into_cron: Manual tool only for now -- cron is disabled server-wide pending separate remediation;
    schedule this once that lands.
