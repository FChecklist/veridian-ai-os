dispatch_paused: true
pause_reason: 'Owner directive 2026-07-20: analyze the 185 open Review-Framework gaps (this queue is their
  task-shaped form, 25 V2 tasks), do NOT work on them further until explicitly released. Applies to all
  non-completed items -- queue-dispatcher.py and system-sync.py.resume_balance_blocked_check() both must
  refuse to touch held_task_ids while this flag is true.'
paused_at: '2026-07-20T12:16:01.719562+00:00'
held_task_ids:
- task-20260720-022700-superboss-v2-plan--finish-the-uae-countr
- task-20260720-022708-superboss-v2-plan--shared-cross-repo-pro
- task-20260720-022710-superboss-v2-plan--byob-bring-your-own-a
- task-20260720-025001-superboss-v2-plan--decisions-of-record
- task-20260720-031002-superboss-v2-plan--persistent-vercel-sta
- task-20260720-035002-superboss-v2-plan--surface-loop-derived
- task-20260720-050001-retry-2--superboss-v2-plan--delegation
- task-20260720-051002-retry-2--superboss-v2-plan--serverless
- task-20260720-051004-retry-2--superboss-v2-plan--chat-contex
- task-20260720-051006-retry-2--superboss-v2-plan--preview-dep
- task-20260720-052001-retry-2--superboss-v2-plan--storage-rls
- task-20260720-053002-retry-2--superboss-v2-plan--crm-perform
- task-20260720-054001-retry-2--superboss-v2-plan--hr-performa
- task-20260720-054004-retry-2--superboss-v2-plan--multi-offic
- task-20260720-054006-retry-2--superboss-v2-plan--prompt---ca
- task-20260720-055002-retry-2--superboss-v2-plan--search-perf
- task-20260720-060002-retry-2--superboss-v2-plan--e-invoicing
- task-20260720-055007-superboss-v2-plan--executive-reporting-d
- task-20260720-055009-superboss-v2-plan--remove-anthropic-api
- task-20260720-055011-superboss-v2-plan--crm-contacts-list-rou
- task-20260720-060006-superboss-v2-plan--continue-the-autonomo
queue:
- id: v2-1
  category: SUPERBOSS_V2_PLAN
  sub_category: Finish the UAE country pack (Multi-Country pluggability proof)
  row_count: 1
  findings:
  - severity: medium
    parameter: Finish the UAE country pack (Multi-Country pluggability proof)
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-1 -- Finish the UAE country pack (Multi-Country pluggability proof)


    ### V2-1 — Finish the UAE country pack (Multi-Country pluggability proof) [D1]

    - READY: yes (Priority 19 Part 2 Workstream B/C already started this)

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-1-UAE-COUNTRY-PACK

    - MODULE: compliance-tracker (+ projexa)

    - OBJECTIVE: Prove the multi-country architecture generalizes by finishing UAE as the second country
    (tax fields, e-invoice format, currency) behind the existing country-config abstraction. Closes CSV
    rows #02/#03 + the "Country-Specific Compliance" tranche.

    - READ FIRST: `control/priority19_dubai_e2e_testing_plan.md`; the existing country-config/`erp_statutory_rules`/`erp_income_tax_slabs`
    tables; `src/lib/services/` country-scoped services; ACTIVE-CLAIMS (Priority 19 Part 2 Workstream
    A/B/C claims).

    - WHAT TO BUILD: Complete the UAE statutory-rule seed + tax-field wiring + e-invoice (UAE FTA) format
    alongside India; verify both resolve through the same country-config path with no India-specific hardcoding.
    Real tests for both countries.

    - CONSTRAINTS: No live government-API calls without sandbox creds (see C12/V2-21). Tier2 if schema
    touched — supervisor holds. Register claim.

    - DONE CRITERIA: UAE + India both pass the same country-config test suite; CSV rows re-scored closed;
    tsc/lint/test clean; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-022700-superboss-v2-plan--finish-the-uae-countr
- id: v2-2
  category: SUPERBOSS_V2_PLAN
  sub_category: Unified bottom-nav strip
  row_count: 1
  findings:
  - severity: medium
    parameter: Unified bottom-nav strip
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-2 -- Unified bottom-nav strip


    ### V2-2 — Unified bottom-nav strip [D2]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-2-UNIFIED-NAV

    - MODULE: compliance-tracker (+ veridian-ui-kit)

    - OBJECTIVE: Build the unified bottom-nav strip the design law calls for, on top of veridian-ui-kit
    v0.2.2''s shell. Closes CSV row #05 + the Navigation tranche.

    - READ FIRST: `veridian-ui-kit` shell components; `src/components/AppSidebar.tsx`; the design law
    doc; ACTIVE-CLAIMS (uikit-migration claims, merged).

    - WHAT TO BUILD: A unified nav surface (bottom strip + existing sidebar harmonized), responsive, tokens
    from the kit. Replace per-page ad-hoc nav. Real component tests.

    - CONSTRAINTS: Reuse kit components; no new token system. Tier1 if kit-only, tier2 if app-shell schema
    touched. Register claim.

    - DONE CRITERIA: Unified nav live across all `(app)` pages; design-law conformance; tsc/lint/test
    clean; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: completed
  retry_count: 0
  task_id: task-20260720-022703-superboss-v2-plan--unified-bottom-nav-st
- id: v2-3
  category: SUPERBOSS_V2_PLAN
  sub_category: Verify-and-close Fixed Assets + Change Orders (already shipped)
  row_count: 1
  findings:
  - severity: medium
    parameter: Verify-and-close Fixed Assets + Change Orders (already shipped)
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-3 -- Verify-and-close Fixed Assets + Change Orders (already shipped)


    ### V2-3 — Verify-and-close Fixed Assets + Change Orders (already shipped) [D3/D4]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker (verification + tests, not a build)

    - TASK ID: V2-3-VERIFY-WAVEB-SHIPPED

    - MODULE: compliance-tracker (+ projexa)

    - OBJECTIVE: Confirm the already-shipped Fixed Assets (CRUD+approval+validation) and Change Orders
    (write path + e-signature auto-transition) surfaces meet the CSV''s "Critical" correctness bar; add
    any missing edge-case test; re-score the rows to No-Gap. NOT a re-build.

    - READ FIRST: §1.1 live-code paths; `erp-fixed-assets-service.test.ts`; `construction-change-order-service.ts`;
    the CSV rows #43/#44/#59.

    - WHAT TO BUILD: Targeted correctness tests (approval-flow state machine, business-rule validation,
    e-signature transition) if not already covered; a short evidence note in `ai-os/` citing the routes/pages
    that close each row.

    - CONSTRAINTS: Additive tests + docs only; no schema. Register claim.

    - DONE CRITERIA: Edge-case tests green; evidence note written; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: completed
  retry_count: 0
  task_id: task-20260720-022706-superboss-v2-plan--verify-and-close-fixe
- id: v2-4
  category: SUPERBOSS_V2_PLAN
  sub_category: Shared cross-repo prompt-pattern module
  row_count: 1
  findings:
  - severity: medium
    parameter: Shared cross-repo prompt-pattern module
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-4 -- Shared cross-repo prompt-pattern module


    ### V2-4 — Shared cross-repo prompt-pattern module [D5]

    - READY: yes (low priority)

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-4-SHARED-PROMPT-PATTERNS

    - MODULE: cross-repo (compliance-tracker + projexa)

    - OBJECTIVE: Build a shared prompt-pattern module (e.g. in veridian-ui-kit or a new shared lib) consumed
    by both repos so worker-agent prompts stop diverging. Closes CSV row #60.

    - READ FIRST: both repos'' prompt-construction sites; `src/lib/ai-router/`; roster.ts.

    - WHAT TO BUILD: A versioned shared module of prompt patterns + adoption in one call site per repo
    as proof.

    - CONSTRAINTS: Additive; no behavior change to existing prompts in-scope. Register claim.

    - DONE CRITERIA: Module + 2 adoptions + tests; PRs in both repos; rows re-scored.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-022708-superboss-v2-plan--shared-cross-repo-pro
- id: v2-5
  category: SUPERBOSS_V2_PLAN
  sub_category: BYOB bring-your-own-AI-model
  row_count: 1
  findings:
  - severity: medium
    parameter: BYOB bring-your-own-AI-model
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-5 -- BYOB bring-your-own-AI-model


    ### V2-5 — BYOB bring-your-own-AI-model [D6]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-5-BYOB-AI-MODEL

    - MODULE: compliance-tracker

    - OBJECTIVE: Build the per-tenant custom-LLM/API-key integration into the AI routing layer (the SECOND
    BYOB interpretation; the first — white-label branding — is already shipped per §1.1). A tenant configures
    their own model+key; the Mother Router prefers it for that tenant''s dispatches, still through `checkTierEligibility()`.
    Closes CSV row #66''s AI-model half.

    - READ FIRST: `src/lib/ai-router/mother-router.ts` + `roster-overrides.ts`; `ai-config-crypto.ts`
    (encrypt the key); `CONSTITUTION.yaml` ai_orchestra_tiers; AGENTS.md Rule 9 (no guardrail weakened).

    - WHAT TO BUILD: A per-org `tenant_ai_config` table (encrypted API key + model id + optional base
    URL) + a Mother Router override path that, when present, prefers the tenant''s model but STILL runs
    `checkTierEligibility()` (an ineligible tenant model silently downgrades, never bypasses the guardrail).
    Settings UI for the tenant admin. Real tests including the guardrail-no-bypass case.

    - CONSTRAINTS: **Never bypass `checkTierEligibility()`** (Rule 9). Encrypt keys at rest. Tier2 (schema+crypto)
    — supervisor holds. Register claim.

    - DONE CRITERIA: Tenant can configure+use their own model; guardrail-no-bypass test green; tsc/lint/test
    clean; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-022710-superboss-v2-plan--byob-bring-your-own-a
- id: v2-6
  category: SUPERBOSS_V2_PLAN
  sub_category: Decisions-of-record (docs-only close for the hold/document rows)
  row_count: 1
  findings:
  - severity: medium
    parameter: Decisions-of-record (docs-only close for the hold/document rows)
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-6 -- Decisions-of-record (docs-only close for the hold/document rows)


    ### V2-6 — Decisions-of-record (docs-only close for the hold/document rows) [D7/D9/C13/C16/C17/C18/C19]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker (docs-only)

    - TASK ID: V2-6-DECISIONS-OF-RECORD

    - MODULE: compliance-tracker (ai-os/)

    - OBJECTIVE: Record the §2 decisions that close rows without code: CRM/ERP connector hold (D7/D11),
    no-second-cost-source (D9), bank-credential target-lowered-to-3 (C13), market-fit-deferred-no-customer-base
    (C16), OPENAI_API_KEY-provisioning-formal-request (C17), metadata-driven-no-action (C18), Supabase-IPv4-free-half
    (C19). Each row gets a one-paragraph decision record in `ai-os/` so the CSV re-score has evidence.

    - READ FIRST: §2 of this file; the relevant CSV rows.

    - WHAT TO BUILD: A single `ai-os/REVIEW_FRAMEWORK_DECISIONS_2026-07-19.md` with one entry per decided/recorded
    row (decision + rationale + authority basis). No code.

    - CONSTRAINTS: Docs-only. Register claim.

    - DONE CRITERIA: Doc exists, covers every (b)/(c)-docs row; PR open (tier1).


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-025001-superboss-v2-plan--decisions-of-record
- id: v2-7
  category: SUPERBOSS_V2_PLAN
  sub_category: Persistent Vercel staging env + per-env var scoping
  row_count: 1
  findings:
  - severity: medium
    parameter: Persistent Vercel staging env + per-env var scoping
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-7 -- Persistent Vercel staging env + per-env var scoping


    ### V2-7 — Persistent Vercel staging env + per-env var scoping [D8]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-7-STAGING-ENV

    - MODULE: compliance-tracker

    - OBJECTIVE: Enable a persistent `staging` environment via Vercel env-var scoping (no extra spend
    at current tier) and extend `sync-vercel-env.yml` to scope variables per environment. Closes CSV rows
    #37/#39.

    - READ FIRST: `.github/workflows/sync-vercel-env.yml`; `vercel.json`; Vercel env-scoping docs.

    - WHAT TO BUILD: A scoped `staging` env config + workflow update that writes staging-specific vars
    only to staging. Document the smoke-test expectation on staging previews.

    - CONSTRAINTS: No paid Vercel plan change. CAUTION: gh-token lacks `workflow` scope — a PR touching
    `*.yml` under `.github/workflows/` cannot be pushed by this token; scope the workflow change for an
    Owner push or use a token with `workflow` scope. Register claim.

    - DONE CRITERIA: Staging env scoped; workflow updated (or staged for Owner push with the scope limitation
    documented); PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-031002-superboss-v2-plan--persistent-vercel-sta
- id: v2-8
  category: SUPERBOSS_V2_PLAN
  sub_category: Mobile field UX cross-reference to projexa
  row_count: 1
  findings:
  - severity: medium
    parameter: Mobile field UX cross-reference to projexa
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-8 -- Mobile field UX cross-reference to projexa


    ### V2-8 — Mobile field UX cross-reference to projexa [D10]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker (verification + docs)

    - TASK ID: V2-8-MOBILE-UX-CROSSREF

    - MODULE: cross-repo

    - OBJECTIVE: Close the compliance-tracker mobile-field-UX row by confirming the field-usable Site
    Diary + Attendance mobile UX lives in projexa and cross-referencing it; add a note in compliance-tracker''s
    `ai-os/` pointing there. Closes CSV row #64.

    - READ FIRST: projexa''s site-diary + attendance screens; the CSV row.

    - WHAT TO BUILD: A short cross-reference note + (optional) a small "open in PROJEXA" deep-link from
    compliance-tracker where relevant.

    - CONSTRAINTS: Docs + optional minor link. Register claim.

    - DONE CRITERIA: Cross-reference note written; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: completed
  retry_count: 0
  task_id: task-20260720-034002-superboss-v2-plan--mobile-field-ux-cross
- id: v2-9
  category: SUPERBOSS_V2_PLAN
  sub_category: Surface loop-derived insights conversationally
  row_count: 1
  findings:
  - severity: medium
    parameter: Surface loop-derived insights conversationally
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-9 -- Surface loop-derived insights conversationally


    ### V2-9 — Surface loop-derived insights conversationally [D12]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-9-LOOP-INSIGHTS-NOTIFY

    - MODULE: compliance-tracker

    - OBJECTIVE: Surface relevant loop-derived insights to affected users via the existing notification
    channels. Closes CSV row #18.

    - READ FIRST: the loop/insight service; `src/lib/services/` notification paths; the CSV row.

    - WHAT TO BUILD: A notification-emission hook from the loop service to existing channels + tests.

    - CONSTRAINTS: Reuse existing notification infra. Register claim.

    - DONE CRITERIA: Insights surface to affected users; tests; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-035002-superboss-v2-plan--surface-loop-derived
- id: v2-10
  category: SUPERBOSS_V2_PLAN
  sub_category: Sentry DSN startup check
  row_count: 1
  findings:
  - severity: medium
    parameter: Sentry DSN startup check
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-10 -- Sentry DSN startup check


    ### V2-10 — Sentry DSN startup check [C1]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker

    - TASK ID: V2-10-SENTRY-DSN-CHECK

    - MODULE: compliance-tracker

    - OBJECTIVE: Add a startup check that logs a warning if `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` is missing.
    Closes CSV row #10 (the code half; provisioning the DSN is an Owner-dashboard action recorded in V2-6/C17).

    - READ FIRST: `sentry.server.config.ts`/`sentry.edge.config.ts`; Next.js instrumentation/startup hooks.

    - WHAT TO BUILD: A startup check + a test that asserts the warning fires when unset and is silent
    when set.

    - CONSTRAINTS: Read-only on Sentry config except the check. Register claim.

    - DONE CRITERIA: Check + test green; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: completed
  retry_count: 0
  task_id: task-20260720-035005-superboss-v2-plan--sentry-dsn-startup-ch
- id: v2-11
  category: SUPERBOSS_V2_PLAN
  sub_category: Delegation expiry enforcement audit + test
  row_count: 1
  findings:
  - severity: medium
    parameter: Delegation expiry enforcement audit + test
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-11 -- Delegation expiry enforcement audit + test


    ### V2-11 — Delegation expiry enforcement audit + test [C2]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-11-DELEGATION-EXPIRY

    - MODULE: compliance-tracker

    - OBJECTIVE: Audit every authorization checkpoint (not just listing views) for delegation-expiry enforcement;
    add a regression test. Closes CSV row #11.

    - READ FIRST: the delegation/proxy-approval service + every `requireAuth()`/role check site that consults
    delegation.

    - WHAT TO BUILD: A shared expiry-check used at every authorization time + a test proving an expired
    delegation is rejected at a non-listing checkpoint.

    - CONSTRAINTS: Additive; no behavior change for valid delegations. Register claim.

    - DONE CRITERIA: Audit doc + shared check + test; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-050001-retry-2--superboss-v2-plan--delegation
- id: v2-12
  category: SUPERBOSS_V2_PLAN
  sub_category: Serverless resource-limit tradeoff doc + heavy-workload audit
  row_count: 1
  findings:
  - severity: medium
    parameter: Serverless resource-limit tradeoff doc + heavy-workload audit
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-12 -- Serverless resource-limit tradeoff doc + heavy-workload audit


    ### V2-12 — Serverless resource-limit tradeoff doc + heavy-workload audit [C3]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-12-SERVERLESS-LIMITS

    - MODULE: compliance-tracker

    - OBJECTIVE: Document the explicit serverless resource-limit tradeoff and audit the heaviest workloads
    against it; identify any that need a dedicated worker/queue. Closes CSV row #13.

    - READ FIRST: Vercel function limits; the heaviest API routes (payroll runs, report generation, bulk
    ops).

    - WHAT TO BUILD: A doc in `ai-os/` +, if a workload genuinely exceeds limits, a follow-up task to
    move it to a queue (that part may be tier2 + separate).

    - CONSTRAINTS: Docs + audit; queue migration is a separate gated task if needed. Register claim.

    - DONE CRITERIA: Doc + audit table; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-051002-retry-2--superboss-v2-plan--serverless
- id: v2-13
  category: SUPERBOSS_V2_PLAN
  sub_category: Chat context + terminology + mode-pill analytics
  row_count: 1
  findings:
  - severity: medium
    parameter: Chat context + terminology + mode-pill analytics
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-13 -- Chat context + terminology + mode-pill analytics


    ### V2-13 — Chat context + terminology + mode-pill analytics [C4]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-13-CHAT-CONTEXT-ANALYTICS

    - MODULE: compliance-tracker

    - OBJECTIVE: Verify and wire `contextEntityId` data fetch into the AI reply prompt; add an org-glossary
    hook into the system prompt; add usage analytics on mode-pills vs free-text. Closes CSV rows #16/#17/#20.

    - READ FIRST: chat reply prompt construction; `contextEntityId` plumbing; existing analytics infra.

    - WHAT TO BUILD: context fetch wiring + glossary hook + mode-pill analytics event; tests.

    - CONSTRAINTS: No new analytics vendor; reuse existing. Register claim.

    - DONE CRITERIA: Three rows'' fixes shipped + tested; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-051004-retry-2--superboss-v2-plan--chat-contex
- id: v2-14
  category: SUPERBOSS_V2_PLAN
  sub_category: Preview deployment spot-check
  row_count: 1
  findings:
  - severity: medium
    parameter: Preview deployment spot-check
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-14 -- Preview deployment spot-check


    ### V2-14 — Preview deployment spot-check [C5]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker (verification)

    - TASK ID: V2-14-PREVIEW-SPOTCHECK

    - MODULE: compliance-tracker

    - OBJECTIVE: Live spot-check a real preview URL from the most recent open PR; record pass/fail. Closes
    CSV row #38.

    - READ FIRST: an open PR''s preview URL.

    - WHAT TO BUILD: A short verification note in `ai-os/`.

    - CONSTRAINTS: Docs-only. Register claim.

    - DONE CRITERIA: Note written; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-051006-retry-2--superboss-v2-plan--preview-dep
- id: v2-15
  category: SUPERBOSS_V2_PLAN
  sub_category: Storage RLS + backup PITR + Supabase monitoring audit
  row_count: 1
  findings:
  - severity: medium
    parameter: Storage RLS + backup PITR + Supabase monitoring audit
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-15 -- Storage RLS + backup PITR + Supabase monitoring audit


    ### V2-15 — Storage RLS + backup PITR + Supabase monitoring audit [C6]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-15-SUPABASE-DR-AUDIT

    - MODULE: compliance-tracker

    - OBJECTIVE: `get_advisors` audit on `storage.objects` RLS for both buckets; verify PITR/backup via
    Supabase MCP/dashboard; document RTO/RPO; confirm Sentry monitoring activation. Closes CSV rows #40/#41/#42.

    - READ FIRST: Supabase MCP; `sentry.*.config.ts`; the bucket definitions.

    - WHAT TO BUILD: An audit doc + any missing RLS policy fix (tier2 if schema/policy) + RTO/RPO statement.

    - CONSTRAINTS: The DSN-provisioning half is Owner-dashboard (V2-6/C17); the audit is code/MCP. Tier2
    if RLS policy changed — supervisor holds. Register claim.

    - DONE CRITERIA: Audit doc + RTO/RPO; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-052001-retry-2--superboss-v2-plan--storage-rls
- id: v2-16
  category: SUPERBOSS_V2_PLAN
  sub_category: CRM performance-under-load indexes + load-test harness
  row_count: 1
  findings:
  - severity: medium
    parameter: CRM performance-under-load indexes + load-test harness
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-16 -- CRM performance-under-load indexes + load-test harness


    ### V2-16 — CRM performance-under-load indexes + load-test harness [C7]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-16-CRM-PERF-INDEXES

    - MODULE: compliance-tracker

    - OBJECTIVE: Add the named composite indexes (`(org_id,status,created_at)` on leads; `(org_id,stage)`
    on opportunities; accounts/contacts/pipeline/dashboard/sales-engine/VERI-reward indexes) + a synthetic
    load-test harness. Closes CSV rows #45-#51.

    - READ FIRST: `schema.ts` CRM tables; existing indexes; the load-test precedent.

    - WHAT TO BUILD: A migration adding the indexes + a load-test script (50k-row synthetic) + results
    doc.

    - CONSTRAINTS: Tier2 (drizzle + live DB) — supervisor holds. Register claim.

    - DONE CRITERIA: Indexes applied + load-test results recorded; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-053002-retry-2--superboss-v2-plan--crm-perform
- id: v2-17
  category: SUPERBOSS_V2_PLAN
  sub_category: HR performance/error-handling + payroll rate audit
  row_count: 1
  findings:
  - severity: medium
    parameter: HR performance/error-handling + payroll rate audit
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-17 -- HR performance/error-handling + payroll rate audit


    ### V2-17 — HR performance/error-handling + payroll rate audit [C8]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-17-HR-PERF-VALIDATION

    - MODULE: compliance-tracker

    - OBJECTIVE: Employees invite/onboarding validation UX cross-check; payroll rate-table seed audit
    against current-FY rates (the CA-review half is deferred-on-real-external-reviewer; the seed-audit
    + GstRt parity is code); load-test harnesses for payroll/recruitment/attendance/vendor scorecards;
    caching for HR dashboard KPIs. Closes CSV rows #52-#58.

    - READ FIRST: `employee_profiles` validation; `erp_statutory_rules`/`erp_income_tax_slabs`; HR service
    routes.

    - WHAT TO BUILD: Validation UX fixes + rate-seed audit doc + indexes/caching + load-test harness.

    - CONSTRAINTS: CA/payroll-specialist rate verification = real-external reviewer → that half stays
    deferred (record in V2-6). Tier2 if schema — supervisor holds. Register claim.

    - DONE CRITERIA: Code halves shipped + audited; deferred half recorded; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-054001-retry-2--superboss-v2-plan--hr-performa
- id: v2-18
  category: SUPERBOSS_V2_PLAN
  sub_category: Multi-office selector correctness audit
  row_count: 1
  findings:
  - severity: medium
    parameter: Multi-office selector correctness audit
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-18 -- Multi-office selector correctness audit


    ### V2-18 — Multi-office selector correctness audit [C9]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-18-MULTI-OFFICE-FILTER-AUDIT

    - MODULE: compliance-tracker

    - OBJECTIVE: Audit each major module (ERP, HR, tasks, reports) for correct `branchId` filtering, mirroring
    the currency audit. Closes CSV row #61.

    - READ FIRST: the currency audit (CSV parameter 68); `branchId`/`officeId` plumbing across modules.

    - WHAT TO BUILD: An audit doc + fixes for any module missing the filter + tests.

    - CONSTRAINTS: Additive; tier2 only if schema. Register claim.

    - DONE CRITERIA: Audit + fixes + tests; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-054004-retry-2--superboss-v2-plan--multi-offic
- id: v2-19
  category: SUPERBOSS_V2_PLAN
  sub_category: Prompt & Cache real production metrics
  row_count: 1
  findings:
  - severity: medium
    parameter: Prompt & Cache real production metrics
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-19 -- Prompt & Cache real production metrics


    ### V2-19 — Prompt & Cache real production metrics [C10]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-19-CACHE-METRICS

    - MODULE: compliance-tracker

    - OBJECTIVE: Wire real production cache hit-rate + cost-savings metrics recording into the Prompt
    & Cache framework (framework exists from PR #323; metrics emission doesn''t). Closes CSV rows #62/#63.

    - READ FIRST: `src/lib/llm-client.ts`/cache framework; `platform.ai_routing_audit_log`; the prompt-cache
    Phase 1 PR #323.

    - WHAT TO BUILD: Metrics emission + a small read view/report; tests.

    - CONSTRAINTS: No new metrics vendor. Tier2 if schema. Register claim.

    - DONE CRITERIA: Metrics recording live + report; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-054006-retry-2--superboss-v2-plan--prompt---ca
- id: v2-20
  category: SUPERBOSS_V2_PLAN
  sub_category: Search performance EXPLAIN ANALYZE + GIN index
  row_count: 1
  findings:
  - severity: medium
    parameter: Search performance EXPLAIN ANALYZE + GIN index
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-20 -- Search performance EXPLAIN ANALYZE + GIN index


    ### V2-20 — Search performance EXPLAIN ANALYZE + GIN index [C11]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-20-SEARCH-PERF

    - MODULE: compliance-tracker

    - OBJECTIVE: `EXPLAIN ANALYZE` on Standard search at realistic volume; add `pg_trgm`/GIN index if
    the plan says. Closes CSV row #67.

    - READ FIRST: the search service/query; existing indexes; `pg_trgm` extension availability.

    - WHAT TO BUILD: A migration adding the GIN index + the EXPLAIN results doc.

    - CONSTRAINTS: Tier2 (migration + live DB) — supervisor holds. Register claim.

    - DONE CRITERIA: Index applied + EXPLAIN doc; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-055002-retry-2--superboss-v2-plan--search-perf
- id: v2-21
  category: SUPERBOSS_V2_PLAN
  sub_category: E-invoicing per-line GstRt fix + IRP format scaffolding
  row_count: 1
  findings:
  - severity: medium
    parameter: E-invoicing per-line GstRt fix + IRP format scaffolding
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-21 -- E-invoicing per-line GstRt fix + IRP format scaffolding


    ### V2-21 — E-invoicing per-line GstRt fix + IRP format scaffolding [C12]

    - READY: yes (GstRt half); GSP-sandbox half blocked on Owner-provisioned creds

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-21-EINVOICING-GSTRT

    - MODULE: compliance-tracker

    - OBJECTIVE: Fix the per-line GstRt tracking gap in code; add the UAE/India e-invoice format scaffolding
    behind the country-config (ties to V2-1). Closes CSV row #70''s code half; the GSP-sandbox live-test
    half stays deferred on Owner-provisioned creds (record in V2-6).

    - READ FIRST: the e-invoice service; `erp_invoice_lines` GstRt field; V2-1''s country-config.

    - WHAT TO BUILD: per-line GstRt fix + format scaffolding + tests; the live-IRP-sandbox test is a separate
    gated task.

    - CONSTRAINTS: No live IRP call without sandbox creds. Tier2 if schema. Register claim.

    - DONE CRITERIA: GstRt fix + scaffolding + tests; deferred half recorded; row partly re-scored; PR
    open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: stuck_needs_human
  retry_count: 3
  task_id: task-20260720-060002-retry-2--superboss-v2-plan--e-invoicing
- id: v2-22
  category: SUPERBOSS_V2_PLAN
  sub_category: Executive reporting drill-down + cadence scheduled job
  row_count: 1
  findings:
  - severity: medium
    parameter: Executive reporting drill-down + cadence scheduled job
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-22 -- Executive reporting drill-down + cadence scheduled job


    ### V2-22 — Executive reporting drill-down + cadence scheduled job [C14]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L3 Feature Worker

    - TASK ID: V2-22-EXEC-REPORTING

    - MODULE: compliance-tracker

    - OBJECTIVE: Build/verify the drill-down from each executive dashboard tile to transaction-level detail
    + a scheduled job that reads `report_definitions.cadence` and triggers delivery. The domain-expert
    scoring review stays deferred (real-external reviewer). Closes CSV rows #74/#75; #73 stays deferred.

    - READ FIRST: executive dashboard tiles + routes; `report_definitions` cadence field; the scheduler
    precedent.

    - WHAT TO BUILD: drill-down wiring + cadence job + tests.

    - CONSTRAINTS: Tier2 if schema. Register claim.

    - DONE CRITERIA: Drill-down + cadence job + tests; #73 deferred recorded; rows re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-055007-superboss-v2-plan--executive-reporting-d
- id: v2-23
  category: SUPERBOSS_V2_PLAN
  sub_category: Remove ANTHROPIC_API_KEY dead code path
  row_count: 1
  findings:
  - severity: medium
    parameter: Remove ANTHROPIC_API_KEY dead code path
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-23 -- Remove ANTHROPIC_API_KEY dead code path


    ### V2-23 — Remove ANTHROPIC_API_KEY dead code path [C15]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L1 Code Worker

    - TASK ID: V2-23-REMOVE-DEAD-ANTHROPIC-PATH

    - MODULE: compliance-tracker

    - OBJECTIVE: Remove the dead `ANTHROPIC_API_KEY` code path (the secondary `claude-task` dispatch path
    "has never had a working job behind it" per AGENTS.md + `Study_by_Claude.md`). Closes CSV row #01.

    - READ FIRST: the `claude-task`/`ANTHROPIC_API_KEY` call sites; AGENTS.md Claude Code (Secondary Agent)
    note; `Study_by_Claude.md` ANTHROPIC_API_KEY discussion.

    - WHAT TO BUILD: Remove the dead path (or gate it behind an explicit opt-in flag with a deprecation
    note); keep `CLAUDE_CODE_OAUTH_TOKEN` path intact; tests.

    - CONSTRAINTS: Do NOT remove the legitimate `CLAUDE_CODE_OAUTH_TOKEN`-based path. Register claim.

    - DONE CRITERIA: Dead path removed/gated; tests green; row re-scored; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-055009-superboss-v2-plan--remove-anthropic-api
- id: v2-24
  category: SUPERBOSS_V2_PLAN
  sub_category: CRM Contacts list route + page (the one genuinely-missing Wave B piece)
  row_count: 1
  findings:
  - severity: medium
    parameter: CRM Contacts list route + page (the one genuinely-missing Wave B piece)
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-24 -- CRM Contacts list route + page (the one genuinely-missing Wave B piece)


    ### V2-24 — CRM Contacts list route + page (the one genuinely-missing Wave B piece) [from §1.1]

    - READY: yes

    - SOFTWARE TEAM LEVEL: L2 Sequential Worker

    - TASK ID: V2-24-CRM-CONTACTS-SURFACE

    - MODULE: compliance-tracker

    - OBJECTIVE: Complete the CRM Contacts surface: a list `GET /api/crm/contacts` route (only `[id]`
    exists today) + `crm/contacts/page.tsx` list page. The `crmContacts` table + `crm-accounts-service`
    already exist; this is the missing HTTP/UI surface. Closes the CRM Accounts & Contacts "surface incomplete"
    finding.

    - READ FIRST: `api/crm/contacts/[id]/route.ts`; `crm-accounts-service.ts`; `crm/accounts/page.tsx`
    as the page template.

    - WHAT TO BUILD: List route (org-scoped, paginated, RLS) + list page + tests, mirroring the Accounts
    surface.

    - CONSTRAINTS: `requireAuth()` + RLS; no schema change (table exists). Tier1. Register claim.

    - DONE CRITERIA: List route + page + tests; tsc/lint/test clean; PR open.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-055011-superboss-v2-plan--crm-contacts-list-rou
- id: v2-25
  category: SUPERBOSS_V2_PLAN
  sub_category: Continue the autonomous gap_queue (system-driven, NOT a manual dispatch)
  row_count: 1
  findings:
  - severity: medium
    parameter: Continue the autonomous gap_queue (system-driven, NOT a manual dispatch)
    gap_identified: ''
    recommended_approach: ''
  full_prompt: 'TASK: V2-25 -- Continue the autonomous gap_queue (system-driven, NOT a manual dispatch)


    ### V2-25 — Continue the autonomous gap_queue (system-driven, NOT a manual dispatch) [from v1]

    - READY: yes (monitor, don''t dispatch)

    - SOFTWARE TEAM LEVEL: L4 Coding Supervisor (monitoring)

    - TASK ID: V2-25-GAP-QUEUE-MONITOR

    - MODULE: compliance-tracker

    - OBJECTIVE: The `REVIEW-FRAMEWORK-GAPS-QUEUE-01` autonomous dispatcher (`gap_queue.yaml` + `queue-dispatcher.py`
    cron) is live and self-fills the 3-worker cap from the remaining ~170 genuinely-open rows. This task
    is to MONITOR it (throughput, failures, decision-blocked skip rate) and feed it the §3 re-decisions
    so it stops skipping rows that are now decidable. NOT a manual duplicate dispatch.

    - READ FIRST: `gap_queue.yaml` + `queue-dispatcher.py`; the §3 decisions to un-skip.

    - WHAT TO BUILD: Apply the §3 status flips to `gap_queue.yaml` so decidable rows re-enter the queue;
    a monitoring note.

    - CONSTRAINTS: Do not duplicate-queue. Register claim.

    - DONE CRITERIA: Queue updated with §3 decisions; monitoring note written; PR open.


    ---


    ## 5. NOTES / HONEST LIMITATIONS


    - **This plan is grounded in live state read 2026-07-19 ~17:15 UTC.** It drifts as in-flight PRs merge;
    a supervisor re-reads `ACTIVE-CLAIMS.yaml` + `gh pr list` before each dispatch.

    - **The biggest correction to v1:** the 9-workstream "redo fresh" directive is already satisfied for
    6 of 9 areas by complete server-side implementations (§1.1). v1 marked them BLOCKED on laptop work
    that, per the Owner''s own no-laptop rule, was never going to merge; the server already built the
    full-depth versions. v2 records this and schedules only the genuinely-missing pieces (CRM Contacts
    surface V2-24, BYOB AI-model V2-5) plus verify-and-close (V2-3) — not wasteful full re-builds. This
    is a disclosed decision with a real cost.

    - **Decision authority used here, plainly disclosed (§2):** the Owner pre-authorized every non-money
    decision. v2 makes ~13 scope/design decisions (D1-D12) and ~40 code-closable/record decisions (C1-C19)
    under that authority. The ~22 rows that genuinely need real money (SOC2 audit, external pentest, live
    payment-gateway merchant account, paid training-content licensing, Supabase IPv4 add-on) are explicitly
    KEPT deferred — decision authority does not extend to spending. The OPENAI_API_KEY rows are neither
    money nor code — they''re an Owner config action, formally requested via V2-6/C17.

    - **projexa has no `ai-os/` tree / ACTIVE-CLAIMS.yaml** (verified this session). Cross-registration
    in compliance-tracker''s ACTIVE-CLAIMS satisfies the "both repos" instruction per the established
    precedent; documented in §3, not silently skipped.

    - **`ai-os/SOFTWARE_TEAM.md` now exists (PR #483 merged)** — the v1 "honest limitation" that it didn''t
    exist yet is resolved. All V2 tasks route through the real L1-L4 ladder documented there.

    - **Tier2 sign-off** still applies to any task touching schema/`drizzle/` (V2-1, V2-5, V2-7, V2-15,
    V2-16, V2-17, V2-18, V2-19, V2-20, V2-21, V2-22) — supervisor holds for Owner sign-off per the tiered
    trust model; do not self-merge.

    - **gh-token `workflow` scope limitation** (memory: `gh-token-lacks-workflow-scope`) affects V2-7
    and v1 TASK 1/10 — any PR touching `.github/workflows/*.yml` cannot be pushed by this token; document
    the limitation in the PR and stage the workflow change for an Owner push.


    CONSTRAINTS (apply on top of anything stated above):

    - Server-side only, routes through the Mother Router''s software_team scope -> GLM-5.2 via OpenRouter,
    cheapest real provider at dispatch time.

    - Register your claim in the relevant repo''s ai-os/boss/ACTIVE-CLAIMS.yaml before real work, per
    this repo''s own protocol -- check for collisions with any other active claim or open PR on the same
    file/module scope first.

    - Maintain PROGRESS.md with ''## Completed'' / ''## Remaining'' checklists as usual.

    - Commit + push incrementally. Open a real PR (even WIP-labeled if you must stop early) rather than
    holding everything uncommitted.

    - Tier1 (docs/tests/additive-only, no schema/auth/RLS/payment/billing/.env changes) may be merged
    autonomously once CI is genuinely green on all required checks. Tier2 always holds for Owner sign-off,
    no exceptions, regardless of audit verdict.

    - If the finding turns out to already be resolved, or doesn''t match what you find in the current
    code, say so in PROGRESS.md rather than making an unnecessary change -- the codebase has moved since
    this plan was written.'
  status: skipped_possible_duplicate
  retry_count: 1
  task_id: task-20260720-060006-superboss-v2-plan--continue-the-autonomo
