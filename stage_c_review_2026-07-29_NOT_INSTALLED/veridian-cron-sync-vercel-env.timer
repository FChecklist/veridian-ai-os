[Unit]
Description=Run veridian-cron-sync-vercel-env on its scheduled cadence (jittered -- see the paired .service for why)

[Timer]
OnCalendar=*-*-* 0/2:11:00
RandomizedDelaySec=300
AccuracySec=30s
Persistent=false

[Install]
WantedBy=timers.target
