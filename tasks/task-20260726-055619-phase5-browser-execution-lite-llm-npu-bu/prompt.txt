## OBJECTIVE
Extend the ONLY real browser-native prior art in this whole system (repos/compliance-tracker/src/app/litert-spike/ + litert-spike-embeddings/, gap analysis known_partial_prior_art.litert_spike) from an unregistered app-router debug spike into the document's tiered Browser Execution Engine (NPU -> Built-in AI -> Lite LLM -> Transformers.js -> Server, each with real fallback). This is the single largest, most novel engineering surface in this whole plan (10 named browser engines + 2 tech-stack tables with 0-to-minimal prior art each) -- explicitly the first phase this task's own gap analysis flags against the owner_decision_dependency, since it commits the most new, product-shaping engineering effort. AMENDED 2026-07-25 (corrects and replaces the amendment rejected in pull request 72 -- see scope note below) per the Owner's real verbatim UX directive (/opt/veridian/ai-os/OWNER_DIRECTIVES/BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt) -- this phase builds two real end-user input surfaces -- a structured mode-pill/option-chain component (Option 1) and a free-text chat component (Option 2), both browser-only per the Owner's own words "FOR END USER THE BROWSER IS THE ONLY WAY TO INTERACT WITH THE SYSTEM" -- and this phase's own browser-native execution tiers perform the FIRST pass, producing phase_2's machine-language-output contract. The SECOND pass is deterministic server-side SOFTWARE execution, reconciled against registries.credit_spend_governance below -- not a mandatory second AI call; Gateway G05 remains this phase's existing Tier-5 escalation-only integration_point, not a new mandatory hop.

## SCOPE
- Gap items closed: engine-browser-execution, engine-browser-lite-llm (deepen litert-spike; evaluate WebLLM as the document's own named technology vs. continuing with LiteRT -- a real technology-choice decision this phase must make and justify, not silently keep LiteRT by default), engine-browser-npu, engine-browser-builtin-ai, engine-browser-mcp (distinct from ai-os/BROWSER_AUTOMATION_PROFILE_2026-07-25.yaml's own session-tooling scope per gap analysis's explicit rejection of that as a match), engine-browser-function, engine-browser-storage, engine-browser-sync, engine-browser-worker (deepen), engine-browser-transformers, engine-model-selection, engine-execution-planner, engine-server-escalation (deepen llm-client.ts into the real Tier-5 target), pipeline-input-acquisition (browser-cache-check sub-scope only -- full cache hierarchy is phase_6), pipeline-execution, stack-browser-compute, stack-parallelism (deepen), litert-spike-unregistered (register it in MASTER_INDEX.yaml as this phase's own first, cheap step).
- FIRST STEP, before any new code: register litert-spike/litert-spike-embeddings in MASTER_INDEX.yaml registries: (closes gap-analysis item litert-spike-unregistered) so this phase's own starting point is discoverable for any future session, per this system's own zero-duplication protocol.
- OWNER-VERIFIED UX REQUIREMENT (2026-07-25, corrects and replaces the amendment rejected in PR #72, which (a) cited OWNER_DECISIONS_NEEDED_2026-07-23.yaml id veridian-architecture-v2-target-repo-anchor as containing the Owner's quotes "in full" when that entry only ever held a paraphrase, and (b) upgraded Gateway G05 to a mandatory second AI pass on every completed end-user request without reconciling registries.credit_spend_governance -- both defects are corrected below, not just reworded) -- concrete deliverables, in addition to the gap items above: (1) a structured end-user input component realizing the Owner's own verbatim words (/opt/veridian/ai-os/OWNER_DIRECTIVES/BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt, knowledge_engine KE-20260725-184749-e0e0) "dynamic mode pills + dynamic option chain" (Option 1); (2) a free-text chat input component realizing the Owner's own words "END USER direct writing the input in the chat via web browser" (Option 2); both must be real, browser-only surfaces per the Owner's own words "FOR END USER THE BROWSER IS THE ONLY WAY TO INTERACT WITH THE SYSTEM", calling into phase_2's Prompt Compiler Engine / Stage 4 Machine Prompt Builder to produce the machine-language-output contract; (3) the browser-to-server handoff itself -- this phase's own FIRST-pass browser-native execution tiers (NPU/Built-in AI/Lite LLM/Transformers.js) produce that machine-language output per the Owner's own words "The browser-native AI platform makes the complete machine language output of the end user prompt and gives to the SOFTWARE to execute. FIRST execution in the browser-native AI platform , SECOND execution in the SERVER by the SOFTWARE. SOFTWARE FIRST APPROACH.", which is then handed to deterministic server-side SOFTWARE (the same execution model OWNER_ENGINE/task-gateway.py already uses) for the real SECOND pass. CREDIT-GOVERNANCE RECONCILIATION (corrects PR #72's core defect): this SECOND-pass SOFTWARE execution is NOT a second mandatory AI call on every request -- Gateway G05 stays exactly this phase's existing Tier-5 escalation-on-insufficiency fallback (engine-server-escalation, see integration_point below), invoked only as-needed WITHIN the software execution, consistent with registries.credit_spend_governance's standing "software first, AI second, minimal credits only when needed" propose/report two-gate rule (ai-os/MASTER_INDEX.yaml), i.e. AI credits spent only when a specific request genuinely requires them. Any phase_5 implementation task that would route a genuinely new AI-call pattern through G05 beyond this existing Tier-5 role must go through that same propose/report gate; it must not silently add new metered AI volume. NOTE ON SOURCING: neither "mode pill" nor "option chain" is literal source-document vocabulary (ai-os/owner-attachments/VERIDIAN_Architecture_v2.0_extracted.txt) -- they are the Owner's own UX vocabulary from the verbatim directive cited above (grepped the extracted source and confirmed zero matches for "pill", "option chain", or "chat input"); the document-side anchors remain the abstract "User Input" entry point of 2.2 Stage 1 (Input Acquisition Layer, line 213-215) and the Browser Execution Engine (2.1 Browser Execution Engines, line 144) -- this phase's own dispatched compliance-tracker task must name the specific real component files/interfaces for both input surfaces and the handoff call, since they do not exist yet.

## KNOWN_CONTEXT
This is phase_5_browser_execution_tiers of ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml, part of the VERIDIAN_Architecture_v2.0.docx -- Phased Implementation Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_5_browser_execution_tiers, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260725-231836-phase2-prompt-compiler-pipeline-core-non. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_5_browser_execution_tiers" --tag domain:veridian-architecture-v2-0-docx` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_5_browser_execution_tiers" ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own phases[] entry for phase_5_browser_execution_tiers in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
