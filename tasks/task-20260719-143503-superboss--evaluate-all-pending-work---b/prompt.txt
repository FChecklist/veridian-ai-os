TASK ID: SUPERBOSS-EVALUATE-AND-PLAN
MODULE: cross-repo (compliance-tracker primary, projexa secondary)
OBJECTIVE: You are acting as GLM-5.2 Superboss on VERIDIAN-DEV. The Owner has directed you to evaluate every genuinely pending piece of software development work across the VERIDIAN AI OS project, confirm none of it collides with or duplicates other in-flight work, and produce a real implementation plan. This is an evaluation + planning task ONLY -- do not implement anything yet, that is a separate follow-up dispatch.

READ FIRST (do not skip, do not guess at what's pending -- read the real current state):
1. ai-os/boss/ACTIVE-CLAIMS.yaml (compliance-tracker) -- what other sessions/tasks are claiming right now.
2. ai-os/MASTER-TRACKER.yaml (compliance-tracker) -- the live open-gap tracker.
3. /opt/veridian/repos/claude-control/CONTROLLER.yaml -- the cross-project outermost requirements catalog (pull first). This spans every VERIDIAN-related project, one level above any single repo's own tracker. Read every entry with status idea/draft/in-progress -- those are candidates for pending work. Entries with status done/deferred/blocked are NOT pending (deferred/blocked need a human decision, do not silently attempt them).
4. `gh pr list --repo FChecklist/compliance-tracker --state open` and same for projexa -- open PRs are in-flight, not un-started.
5. `systemctl --user list-units 'veridian-worker@*' 'veridian-supervisor@*' --all` and `ls /opt/veridian/ai-os/tasks/` -- real current/recent task state on this server.

IMPORTANT CONTEXT ON A REAL GAP -- a batch of real, partially-complete work (the "VERIDIAN Review Framework implementation wave": 9 workstreams -- security/bug fixes, marketing/docs fixes, CRM Accounts & Contacts, HR Attendance & Manpower, Fixed Assets wiring, Payment Entries approval flow, BYOB white-label branding, BYOB bring-your-own-AI-model, Training LMS module, plus a groq-vision-model sibling-file fix) was started on the OWNER's LAPTOP (not this server) before the Owner's standing rule ("everything runs on this server, never the laptop") was reinforced. That work sits UNCOMMITTED in local git worktrees on the laptop -- it does NOT exist on GitHub and is NOT visible to you here. Do not assume it doesn't exist, and do not silently re-do it from scratch (real duplicated-work risk) -- instead, list it explicitly in your plan as "BLOCKED: real partial work exists on the Owner's laptop, not yet pushed, needs the Owner to push it (or explicitly abandon it) before a server-side task should touch that same scope" for each of those 9 areas. Full detail on what's already been drafted per workstream is in memory the Owner's assistant holds, not accessible to you directly -- just flag the scope conflict, don't try to reconstruct the content.

WHAT TO PRODUCE:
1. A real inventory of every genuinely pending, actionable item -- not vague categories. For each: source (CONTROLLER.yaml id / MASTER-TRACKER.yaml gap id / open PR), current real status (verified against the actual repo/task state, not assumed from a stale doc), and whether it's actually ready to dispatch now or blocked on something (a decision, another PR merging first, the laptop-worktree conflict above, etc).
2. Explicit collision/duplication check: for every item you plan to include, confirm no other ACTIVE-CLAIMS.yaml entry or open PR already covers the same file/module scope. List anything you found already claimed or already open as a PR, and exclude it from your dispatch plan.
3. A prioritized implementation plan: an ordered list of what to dispatch first, sized into real task-shaped units (the same granularity this repo's own dispatched tasks already use -- see any existing task's prompt.txt for the TASK ID/MODULE/OBJECTIVE/READ FIRST/WHAT TO BUILD/CONSTRAINTS/DONE CRITERIA shape, follow that same shape for each planned task).
4. For each planned task, specify which Software Team level it fits (L1 Code Worker / L2 Sequential Worker / L3 Feature Worker / L4 Coding Supervisor scope) per ai-os/SOFTWARE_TEAM.md (read it -- this was just built and merged) -- this determines routing via the Mother Router's software_team scope, all of which resolves to GLM-5.2 via the local OpenRouter proxy regardless of nominal level (the Owner's explicit instruction: all software development work on this server uses GLM-5.2 via OpenRouter's cheapest provider, every tier, no exceptions).
5. Write the full plan to a new file: ai-os/SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19.md. Structure it so it can be read by a supervising process (the Owner's assistant) checking in every 15 minutes and by future dispatch scripts picking up the next task -- a real checklist, not prose.
6. Register your own claim in ACTIVE-CLAIMS.yaml before finishing (this evaluation itself touches nothing else, but the discipline still applies).
7. Commit and push ai-os/SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19.md directly to a new branch and open a PR (docs-only, tier1) -- do not commit straight to main.

CONSTRAINTS: Read-only investigation except for the one new plan file + the ACTIVE-CLAIMS.yaml claim registration. Do not modify any other file. Do not start implementing any of the planned tasks -- that is explicitly a separate follow-up.

DONE CRITERIA: ai-os/SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19.md exists, is real (grounded in actually-read current state, not guessed), covers collision-checking explicitly, and a PR is open for it.