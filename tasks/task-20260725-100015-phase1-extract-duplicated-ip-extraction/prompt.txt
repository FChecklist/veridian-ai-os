## OBJECTIVE
Closes SEC-06 and SEC-07. Create src/lib/utils/request-ip.ts exporting a getRequestIp(request: Request): string | undefined function (the x-forwarded-for-split / x-real-ip-fallback logic currently duplicated verbatim in failure-event/route.ts:26-28 and passcode-login/route.ts:42-44), and src/lib/supabase/admin-client.ts exporting a getSupabaseAdmin() singleton that validates NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY once at module load (throwing a clear, named error instead of the current bare '!' non-null-assertion crash), replacing the duplicated inline createAdminClient calls in passcode-login/route.ts:62-65 and sso/[orgSlug]/acs/route.ts:27-30.

## SCOPE
- Create src/lib/utils/request-ip.ts. Preserve each call site's existing fallback semantics (failure-event's is `?? undefined`, passcode-login's is `?? "unknown"`) -- take the fallback as a parameter or a sensible shared default, don't silently change either route's behavior.
- Replace the duplicated blocks in failure-event/route.ts and passcode-login/route.ts with calls to getRequestIp().
- Create src/lib/supabase/admin-client.ts exporting getSupabaseAdmin(), validating both env vars once at module load with a clear thrown error (not a bare '!' assertion).
- Replace the duplicated createAdminClient(...) blocks in passcode-login/route.ts and sso/[orgSlug]/acs/route.ts with getSupabaseAdmin().
- Add/update unit tests alongside passcode-login-service.test.ts's existing convention for the two new modules.

## KNOWN_CONTEXT
This is phase_1_shared_auth_utils of ai-os/SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml, part of the Security-domain ChatGPT/Z.AI dual-audit implementation plan (compliance-tracker auth routes + Wave 143 interior schema) initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_1_shared_auth_utils, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
none (entry phase). This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_1_shared_auth_utils" --tag domain:security-domain-chatgpt-z-ai` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_1_shared_auth_utils" ai-os/SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml's own phases[] entry for phase_1_shared_auth_utils in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see SECURITY_AUDIT_IMPLEMENTATION_PLAN_2026-07-25.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
