## OBJECTIVE
SCENARIO-TEST-15 PROBE B: unrelated automated concurrency-probe, distinct content from A
## SCOPE
Scenario-test-only, submitted by an automated stress-test subagent, not real product work. Entirely different subject matter from PROBE A on purpose -- this text discusses log-rotation housekeeping as a filler topic, purely to keep the two submissions textually distinct for dedup-search purposes. If ever actually dispatched (it should not be), take no repo action, write no files, open no PR -- exit immediately.
## KNOWN_CONTEXT
This is instruction B of a pair of two DIFFERENT concurrent test submissions (scenario test #15) verifying that resource_governor.py's task_identity dedup does not cross-flag two distinct submissions against each other. Companion instruction is PROBE A, submitted within the same second.
## SUCCESS_CRITERIA
This submission is accepted with its own distinct umr_id and task_identity, and is never rejected as a duplicate of PROBE A.
## EXPECTED_OUTPUT
No real output required. A queued or rejected_duplicate row in umr_tasks is sufficient evidence for this test.
## CONSTRAINTS
Must never touch or reference PR617-REVIEW, PR618-REVIEW, PR58-CONFLICT, PR610-CONFLICT, or PHASE-2-CROSSREF. Must not spend AI/API credit if somehow dispatched.
## COMPLEXITY_TIER
0