## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not mechanical): become a continuous, self-sustaining gap-closing worker against ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml's 187 real distinct gaps (259 raw, deduplicated). Close one real, verified, evidence-backed batch of gaps, update the audit document with real before/after status, then self-dispatch the next batch via the same mechanism, continuing indefinitely -- not capped at 3 phases like earlier today -- until gaps are genuinely exhausted, or a batch is genuinely blocked on an Owner decision (architectural tradeoff, destructive action, ambiguous requirement), whichever comes first.

## SCOPE
In scope: work through ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml's items in a sensible order (small, well-defined, low-risk wiring/config gaps first; large architectural items like the SUPERBOSS-IN-SERVER execution core or the 4-repo merge get their own dedicated, separately-scoped phase, not bundled with small items). Each phase: (1) pick the next coherent batch (3-8 related gaps, similar in kind), (2) verify current real state before touching anything (do not trust the audit doc's status if it is more than a few hours stale -- re-check), (3) build/fix for real, (4) verify with real evidence (command output, file existence, a real test where applicable), (5) update the audit doc's status for those specific items, (6) commit and push, (7) self-dispatch the next phase.
Out of scope for automatic self-dispatch (these require an Owner decision first, do not attempt automatically): the 4-repo merge (compliance-tracker + claude-control + projexa + veridian-ui-kit) -- this is destructive/hard-to-reverse at the repo level; the veda-advisors live-Supabase-coupling severance found earlier today -- cutting a live cross-write needs Owner sign-off first. For both, write a fully scoped, ready-to-execute task description into ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml instead of executing, and continue with other work.

## KNOWN_CONTEXT
Lessons from today's ~15 dispatch cycles, apply these from the start so self-dispatched phases do not need external help fixing their own prompts:
(1) Every self-dispatched prompt.txt MUST use the labeled format: ## OBJECTIVE / ## SCOPE / ## KNOWN_CONTEXT / ## SUCCESS_CRITERIA / ## EXPECTED_OUTPUT / ## CONSTRAINTS / ## COMPLEXITY_TIER -- free prose gets silently truncated to 300 chars by the accountant's plan-derivation fallback.
(2) The OBJECTIVE must open by explicitly naming what requires real judgment (cross-referencing live state, inferring meaning from free text, resolving ambiguity) -- opening with "read this file" or a mechanical-sounding task gets REDIRECTed as not needing AI.
(3) Avoid "do not X" phrasing whose content-words (ignoring do/not/never) overlap with a positive statement elsewhere in the prompt -- the tight-task-schema validator is a bag-of-words contradiction checker, not semantic; it false-flags "do not build a second Y" against "build Y" even when they agree. Prefer positive phrasing ("extend the existing Y") over prohibitions.
(4) Word "etc." and other items on the validator's ambiguity-phrase list (as needed, if necessary, use your judgment, some kind of) trigger a real rejection -- write out the actual list instead.
(5) Subscription-billed work (the normal case, this task included) is NOT gated by the $1 accountant anymore -- confirmed and fixed today (preflight-guard.py, --no-proxy path). Do not add artificial spend-approval framing to self-dispatched prompts, it is not needed and was a source of false rejections before the fix.
(6) superboss-register.sqlite has corrupted 3 times today, always in the actions_fts table, always correlating with heavy CONCURRENT write activity (multiple parallel agents/processes hitting it at once). Prefer sequential work within a phase over spawning multiple parallel agents that all write to this file simultaneously. If a phase must fan out to parallel agents for a genuinely parallelizable piece of work, have only ONE of them (or a final synchronization step) do the actual superboss-register.py writes, not all of them concurrently.
(7) If preflight is rejected anyway despite following the above, fix the specific prompt.txt line yourself (you have full shell access, same as was done manually ~15 times today) and restart -- do not give up or wait for external help; but if you exhaust ~5 self-fix attempts on the same phase without success, stop self-dispatching further phases and email the Owner via notify-owner.py describing the stuck state plainly, rather than looping forever.
(8) `systemctl --user reset-failed veridian-worker@<task-id>.service` is needed if a task restarts more than ~3 times in 30 minutes (StartLimitBurst) -- "Start request repeated too quickly" is a systemd-level issue, not a real rejection.
Full real precedent for a batch fully closed correctly this way: task-20260723-045924 (notify-owner build), task-20260723-052857/060600/064712 (the 3-phase execution-rules chain).

## SUCCESS_CRITERIA
Each phase leaves the audit document more accurate (real state improved AND documented, not just documented). Nothing marked DONE without real evidence (a citation the Owner or a future auditor could independently check). Zero fabricated progress. The chain either keeps running (self-dispatching) or stops with a clear, honest reason -- it should never appear to just silently vanish.

## EXPECTED_OUTPUT
Every phase: commit + push real changes, update ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml, checkpoint status=pending_review with real evidence in the note, self-dispatch the next phase (unless genuinely done or genuinely stuck). Send a notify-owner.py email roughly every 3-5 phases with real cumulative progress (not every single phase -- avoid spamming), and immediately (not batched) if something needs an Owner decision or if the chain is stopping.

## CONSTRAINTS
Every completion claim must cite a real file, command output, or commit hash. Sequential database writes preferred over concurrent ones for anything touching superboss-register.sqlite. The 4-repo merge and the veda-advisors data-coupling severance go into OWNER_DECISIONS_NEEDED_2026-07-23.yaml, not into automatic execution.

## COMPLEXITY_TIER
judgment