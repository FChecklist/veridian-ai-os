## OBJECTIVE
Implement phase_5_browser_execution_tiers from the real, already-detailed, already-Owner-amended plan at ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml (repo claude-control) -- the single largest not-yet-started phase in the VERIDIAN Architecture v2.0 implementation (phases 1-3 already merged, phase 4 is separately being conflict-resolved, phase 5 is next per ai-os/OWNER_ENGINE_TASK2_PHASE_PLAN_2026-07-27.yaml's real status refresh). Read that plan file's phase_5_browser_execution_tiers entry in full first -- objective, scope, and integration_point are already written there in real detail (10 named browser engines, a required WebLLM-vs-LiteRT technology decision, two Owner-directed end-user UI surfaces per /opt/veridian/ai-os/OWNER_DIRECTIVES/BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt, and the real reason a prior amendment in PR #72 was rejected) -- this task's own SCOPE below is a pointer into that existing detail, not a substitute for reading it.

## KNOWN_CONTEXT
Target repo is compliance-tracker (per phase_5's own repo field and the real prior art at repos/compliance-tracker/src/app/lit... + litert-spike-embeddings/). Read PR #72's real diff and rejection reason (`gh pr view 72 --repo FChecklist/compliance-tracker`) before writing any code -- the plan file states this phase was already amended once specifically to correct whatever PR #72 got wrong; do not repeat that mistake. This phase's own FIRST required step per ai-os/OWNER_ENGINE_TASK2_PHASE_PLAN_2026-07-27.yaml is registering litert-spike/litert-spike-embeddings in MASTER_INDEX.yaml (closes gap analysis item litert-spike-unregistered) -- do this before any new engine code.

## SCOPE
Follow ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's phase_5_browser_execution_tiers `scope` list exactly, in the order given there. Given the real size of this phase (10 engines), if it cannot realistically finish as one task within a reasonable session, checkpoint real, working, tested increments (one engine or one tightly-related engine-group per increment) rather than attempting all 10 in one uninterrupted pass -- each increment should be independently mergeable real progress, not a partial/broken intermediate state.

## SUCCESS_CRITERIA
Register litert-spike in MASTER_INDEX.yaml first: `grep -q 'litert-spike' ai-os/MASTER_INDEX.yaml` (repo claude-control) must pass before any engine code is written. For whatever real engine work is completed in this pass, its own real test suite must pass: report the exact pytest/vitest/jest command used and its real exit code in the final checkpoint.

## EXPECTED_OUTPUT
Real commits on a new branch against compliance-tracker (plus the one MASTER_INDEX.yaml registration commit against claude-control, per the FIRST required step above), opened as a real PR once at least one full, working, tested engine increment exists -- do not open a PR for a partial/broken state.

## CONSTRAINTS
Every currently-disabled cron entry and systemd `.timer` unit on the live server must stay in its present disabled state throughout this task. This is real product-shaping engineering work per an explicit Owner directive already on file -- do not silently narrow scope below what ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own phase_5 entry specifies without recording why in the checkpoint.

## COMPLEXITY_TIER
judgment
