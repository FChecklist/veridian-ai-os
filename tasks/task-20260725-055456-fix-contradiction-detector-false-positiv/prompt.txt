## OBJECTIVE
Fix a real, confirmed, recurring false-positive in scripts/tight_task_validation.py's contradiction detector. It has rejected 4 real, non-contradictory task prompts this session alone (2 manually written, 2 auto-generated by scripts/auto_phase_continuation.py), each time for the same root cause: it flags a keyword cluster as a "Constraints say not to do X, but that same thing is required elsewhere" contradiction whenever the same words appear in both a Constraints-section prohibition and anywhere else in the document -- even when the other mention is not actually a requirement (e.g. explaining WHY something is forbidden, or describing what NOT to do in matching language). Because scripts/auto_phase_continuation.py generates prompts from a fixed template that legitimately restates constraint-adjacent language (e.g. crontab-citation requirements, "do not duplicate an existing X" phrasing) across multiple sections, this same false-positive will keep silently blocking the unattended 30-minute cron for any future phase whose auto-generated prompt happens to reuse Constraints-like wording elsewhere -- not just today's one-off manual prompts.

## KNOWN_CONTEXT
- Real confirmed instances this session (read the git history / task logs for exact text if useful, but the pattern is what matters, not the specific past instances):
  1. A prompt whose Constraints said "do not attempt X (Temporal/OPA/Qdrant/local-model infra)" was rejected because KNOWN_CONTEXT elsewhere described what a spec named X as NOT implemented -- both mentions were negative/prohibitive, not a real conflict, but the detector flagged them as contradictory anyway.
  2. A prompt whose Constraints said "never write to path P" was rejected because other sections referenced path P in an unrelated, permitted context (a different file's content mentioning P for identification purposes, not proposing to write to it).
  3. An auto-generated Auditor Engine prompt whose Constraints said "do not add cron entries without an approved citation" was rejected because SCOPE elsewhere described building "a cron-driven entrypoint" -- a real requirement -- and the detector conflated the unconditional-sounding keyword overlap ("cron", "entries"/"entrypoint") with a genuine conflict, ignoring the conditional "without citation" qualifier.
  4. An auto-generated Wiring Engine prompt whose Constraints said "do not re-build or re-litigate anything already done" was rejected because OBJECTIVE elsewhere legitimately said "build the minimum real wiring where missing" -- again a real requirement, not a conflict with the constraint (which was about NOT re-building already-done work, not about prohibiting all building).
- Every one of these 4 was a real, correct prompt that had to be manually reworded to avoid restating the same keyword cluster in two places, purely to get past this check -- the underlying task content never needed to change. This is real wasted effort and a real risk: the unattended cron will hit this same failure mode on some future auto-generated phase and nobody will notice for up to 30+ minutes (or longer, since a rejected dispatch is not retried automatically -- confirm this by reading auto_phase_continuation.py's handling of a rejected dispatch_result).
- Read scripts/tight_task_validation.py's actual contradiction-detection function in full before changing anything -- understand its real current algorithm (this task's job is to describe the failure mode from the outside; the actual implementation approach, e.g. bag-of-words overlap vs a smarter negation-aware check, is for you to determine after reading the real code).

## SCOPE
1. Read scripts/tight_task_validation.py's contradiction-detection logic in full and identify precisely why it treats two mentions of overlapping keywords as a contradiction regardless of polarity (negative-vs-negative, or a genuine conditional like "without X" being stripped before comparison).
2. Fix the detector so it does NOT flag two mentions as contradictory when both are negative/prohibitive (e.g. "do not do X" appearing twice is agreement, not conflict), and correctly parses qualifiers like "without an approved citation" / "already done" / "where missing" as narrowing the scope of a requirement rather than making it unconditional.
3. Write a real regression test file (or extend an existing test file if scripts/tight_task_validation.py already has one -- check first) covering the 4 real failure patterns above as literal before/after test cases: each must have failed under the old logic and must pass under the fixed logic. Run the real test suite and paste real pass/fail output.
4. Re-validate the two auto-generated prompts still cached at /tmp/auto_phase_continuation_phase8-master-orchestration-cron-wiring-report-software.txt and /tmp/auto_phase_continuation_phase1-verify-complete-real-end-to-end.txt (if still present -- they may have been cleaned up already, in which case skip this step and note that in your checkpoint) against the fixed validator and confirm they now pass without needing manual rewording, as independent proof the fix generalizes beyond the literal 4 test cases.
5. Check what scripts/auto_phase_continuation.py does today when tight_task_validation.py rejects a dispatch (read its dispatch_result handling) -- if a rejected phase is silently left as "still pending" with no alert/log that would surface the problem to a human within a reasonable time, add a clear log line or a note field so a rejected auto-dispatch is at least visible in the next status check, rather than silently retried forever with no signal.

## SUCCESS_CRITERIA
- The 4 documented failure patterns each have a real test case that fails against a `git stash` of the pre-fix validator and passes against the fixed one -- show both runs' real output.
- `python3 -m pytest scripts/ -k tight_task_validation` (or whatever the real test invocation is once you've found/confirmed it) exits 0.
- A genuinely contradictory prompt (construct one real test case where Constraints truly conflicts with a real requirement elsewhere, e.g. "must add a cron entry" directly contradicted by "must never add any cron entry under any circumstance") is still correctly rejected -- prove the fix does not just disable the check entirely.

## EXPECTED_OUTPUT
Real committed and pushed code (PR on claude-control) with the fixed detector, the new/extended regression tests with real pass/fail output pasted verbatim, confirmation the two still-cached auto-generated prompts now validate cleanly (or an honest note if they'd already been cleaned up), and whatever auto_phase_continuation.py visibility improvement you made for future silent rejections.

## CONSTRAINTS
Keep the contradiction detector meaningfully strict -- the fix must not simply stop checking for contradictions altogether, since it does catch real conflicts (confirmed by the true-positive test case in SUCCESS_CRITERIA).

## COMPLEXITY_TIER
integrative
