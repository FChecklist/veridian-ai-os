Build/extend only workflow-track engines marked BUILD_NEW or EXTEND_EXISTING from PHASE-2-CROSSREF. Same wiring_registry rule as PHASE-3-BUILD-CALC. These are state machines, not calculation functions -- do not force the wrong shape.
