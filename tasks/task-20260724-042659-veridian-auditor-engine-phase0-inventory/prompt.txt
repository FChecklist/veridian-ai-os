## OBJECTIVE
Phase 0 of VERIDIAN AUDITOR ENGINE: produce a real, verified inventory + a concrete phased build plan covering PART1-6 of the Owner's directive (instruction INS-20260724-042330-8e4b), then install and wire the lowest-risk standalone OSS tools within this same session's budget. Do not build the full engine in this task -- this task's own output is the phase breakdown later phases execute against, mirroring the proven ai-os/STRATEGIC_PLAN_2026-07-21_v2.yaml precedent (real investigation, not assumption).

## SCOPE
1. For each of the 15 audit domains (business-capability, DDD, enterprise-architecture, clean-architecture, product-quality, data-model, workflow, api-contract, metadata, security, ux, ai-governance, integration, test-coverage, documentation), record: the mandatory standard citation from the Owner's directive, which named OSS tool (if any) covers it, whether that tool is installable and compatible with VERIDIAN's real stack (Node/TypeScript + Python + Postgres/Supabase, confirmed by reading package.json/requirements.txt in compliance-tracker, projexa, veda-advisors), and what real custom glue code is needed regardless of the tool (the tool never covers 100% -- it needs VERIDIAN-specific config/rules/wiring).
2. Install and smoke-test (real invocation against one real repo, capture real output) whichever of these are standalone binaries with no VERIDIAN-specific config needed first: Gitleaks, Trivy, Checkov, Spectral, OWASP Dependency-Check. Report exact version installed and exact command used for each. If any fails to install or is incompatible, record why -- do not silently skip.
3. Write ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml: a real phase-by-phase build plan (Phase 1..N) covering every domain in PART1/PART4, the PART2/PART3 tool mapping, the PART6 observability/audit-trail layer (7 repositories, one event schema, OpenTelemetry wiring, pgAudit), the entity-relationship/knowledge_engine registration requirement, and a genuine dependency table between phases (concrete mechanism per edge, not just ordering). Register it in knowledge_engine and MASTER_INDEX.yaml (registries.auditor_engine, live file, matching how self_sustaining_system_engine was registered).
4. Design (write as YAML/JSON schema, do not implement the enforcement mechanism yet -- that is a later phase) the one standard event schema from PART6, and the machine-language finding-record schema every audit domain's output must conform to (fields for: domain, standard_cited, clause_cited, artifact, finding, severity, NOT_COVERED_BY_STANDARD flag, remediation_type[software_fixable|ai_escalation_required], status).

## KNOWN_CONTEXT
- No OSS audit tooling is installed yet -- confirmed this session via `which sonar-scanner trivy gitleaks checkov spectral schemathesis` returning nothing for all of them.
- ai-os/audit198/ (run-audit.mjs) is a DIFFERENT, already-existing, narrower AI-prompt-based 198-rule VERIDIAN-specific audit engine. It is not the standards-based engine this directive describes and is out of scope to modify -- reference it only if genuinely relevant to avoid overlap, do not merge or replace it without a separate explicit decision.
- The Owner has pre-approved: using up to 95% of server resources for this initiative, defaulting to the Recommended option at any decision point without stopping to ask, phased/multi-task delivery, and machine-language-only communication between machine/software/AI layers (see feedback_dual_channel_communication_protocol -- this applies to prompt.txt content and any inter-component config, not to files meant for a later human reader).
- Per the Owner's PART5 mandatory rule: audits themselves must run as software/cron with zero AI in the loop; AI only reads results afterward. Do not build any audit-execution path that requires an LLM call to produce a finding -- LLM calls belong only in a later analysis/escalation stage, never in the audit-run itself, for the domains where a deterministic OSS tool exists. For domains with no deterministic OSS tool (business-capability, DDD, ux content judgment, some clean-architecture judgment calls), record in the phase plan that these need a distinct, clearly-labeled "AI Review" pass (as PART2's own tree structure already shows for those specific leaves) -- this is intentional per the Owner's own PART2 diagram, not a violation of the software-only rule, since PART2 explicitly lists "AI Review" as one of several named children under multiple audit software branches.
- Standing rule already established this session: crontab changes always require a real Owner-approval citation in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml before the live crontab can differ from ai-os/CRONTAB_APPROVED_SNAPSHOT.txt, enforced by preflight-guard.py's check_crontab_unauthorized_change. The Owner's blanket 95%-resources/Recommended-default approval in this directive counts as real approval for this initiative's cron needs -- record each such crontab decision in that file with status approved and approved_via citing this directive, then update CRONTAB_APPROVED_SNAPSHOT.txt to match, rather than stopping to ask again.

## SUCCESS_CRITERIA
- ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml exists, is valid YAML, and has one entry per of the 15 domains plus the PART6 observability layer, each with standard_cited/tool_mapping/custom_work_required/target_phase fields filled with real values, not placeholders.
- At least Gitleaks, Trivy, and Spectral (or a documented, specific reason per tool if genuinely not installable on this Ubuntu 24.04 host) are installed with a real version string and one real smoke-test output captured as evidence.
- knowledge_engine has new rows for AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml and for each installed tool, with real entity_relationships back to the domains they cover.
- MASTER_INDEX.yaml's live copy has a registries.auditor_engine entry.
- The event schema and finding-record schema are written as real, valid JSON Schema or YAML files under ai-os/, not embedded only in prose.

## EXPECTED_OUTPUT
Real committed and pushed changes (a PR on claude-control), the phase-plan YAML, the 2 schema files, real installed-tool version evidence, real knowledge_engine rows, and a final checkpoint summary stating exactly which of the 4 SCOPE items are done vs partially done vs deferred to Phase 1, each with a one-line reason.

## CONSTRAINTS
Limit this task to inventory, planning, schema design, and installing/smoke-testing the lowest-risk standalone scanners listed in SCOPE item 2. Leave full integration of each tool into an orchestrated pipeline, the entity-relationship graph population, the 7-repository observability layer, and the master report software for the phases this task's own plan defines -- those are Phase 1+ work, dispatched separately after this plan exists. Any crontab change follows the existing Owner-approval-citation pattern described in KNOWN_CONTEXT rather than being skipped or blocked on a fresh chat confirmation.

## COMPLEXITY_TIER
judgment
