You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously.

CONTEXT: Owner directive (2026-07-19): full end-to-end testing of PROJEXA across every real module, starting with realistic seed data for a company with 10 users + 1 CEO. This is Phase 1 of a 5-phase program (Phase 1: architecture discovery + seed data; Phase 2: E2E test writing/execution across all real modules; Phase 3: chat/composer command testing; Phase 4: gap document; Phase 5: fix implementation -- you are doing ONLY Phase 1 in this task).

A real architectural finding already confirmed (via Supabase MCP by a DB-access-capable session, before this task was written -- do not re-derive, use it): PROJEXA is a genuinely cross-system product. PROJEXA's own Supabase project (evpckeuxgvahguwsaeul, "projexa") holds only auth/org-membership/marketing-site tables (organizations, memberships, veridian_credentials, profiles, assistant_queries, conversations, todos, contact_requests) -- NOT the real construction-business data. The real business data (projects, RFIs, submittals, schedule, invoices, HR, etc.) lives in FChecklist/compliance-tracker's own database, under a compliance.organisations row (product-branch architecture -- PROJEXA is registered as a product branch of the same underlying platform). Confirmed: `democeo@projexa-ai.com` does NOT exist as a row in compliance.users -- PROJEXA's own Supabase Auth handles the actual login identity, and something (likely `veridian_credentials`, an API-key-based linkage) maps that PROJEXA-authenticated session to the corresponding compliance-tracker org. You must READ THE REAL CODE to find and confirm exactly how this handshake works -- do not guess further than the summary above.

YOUR TASK (Phase 1 only):

1. Read PROJEXA's own repo in full for the real auth/data-bridge mechanism: src/lib/db/schema.ts (its own tables), whatever service/route handles `veridian_credentials` (grep for it), and every `/api/v1/projexa/*`-calling client code in PROJEXA's own src/ (grep for "v1/projexa" or similar cross-system API calls). Document precisely: which PROJEXA pages/features read/write PROJEXA's own local DB (organizations/memberships/profiles/todos/conversations) vs. which call through to compliance-tracker's API for real business data.
2. Read compliance-tracker's real "product branch" architecture: src/lib/services/product-branch-service.ts, the product_branches/product_branch_modules tables (now in the `platform` Postgres schema, not `compliance` -- see drizzle/0245's own header for why), and the `/api/v1/projexa/*` route aliasing layer (find the real file, likely src/app/api/v1/projexa/ or a catch-all route) to confirm exactly which of PROJEXA's 40 real modules (accounting, budgets, change-orders, copilot, customers, dashboard, documents, employees, expenses, ffe, floor-plans, grc, hr, inventory, invoices, knowledge-base, kpis, labour, materials, meetings, mood-boards, payroll, permits, procurement, punch-list, purchase-orders, quotations, recruitment, reports, rfis, sales, sales-orders, schedule, scope, settings, site-diary, submittals, vendors, wiki, work-progress) have a REAL backing data model/API today vs. which render an empty/placeholder state because no real backing exists yet -- this is itself a genuine finding to report honestly, not something to paper over.
3. Design a NEW, clean test organisation in compliance-tracker's database (do NOT reuse `projexa_demo_org`/`demo_org`/the load-test orgs -- those are polluted with synthetic load-test data unrelated to this systematic test; create a fresh org, e.g. name it "Meridian Construction Group (E2E Test Org)" or similar, clearly labeled as this test program's own org so it's easy to identify/clean up later). Design a realistic org chart: 1 CEO + 10 employees across real, distinct construction-company roles that map to this codebase's real role/permission model (confirm the real role enum values by reading schema.ts's userRoleEnum -- do not invent role names that don't exist in the schema). Suggested realistic roles if they fit the real enum: CEO, Project Manager, Site Engineer, Site Supervisor, Quantity Surveyor, Procurement Manager, Safety Officer, HR Admin, Finance/Accounts, Design Lead, Document Controller -- adjust to whatever the real enum actually supports.
4. SEED REAL DATA for this org across every module confirmed real in step 2 -- not placeholder rows, believable construction-company data: at least 3-4 real projects with realistic names/budgets/timelines, RFIs, submittals, punch-list items, change orders, site-diary entries, schedule/Gantt tasks, BOQ line items, procurement/purchase-orders, quotations, sales-orders/invoices, HR records (employees, payroll, leave), documents, meetings, KPIs -- populate GENUINE volume (dozens of records per module, not 1-2 token rows) so Phase 2's E2E testing has real data to exercise, not empty states. Use compliance-tracker's own existing seed patterns/conventions (read src/db/seed.ts or equivalent for the established style) rather than inventing a new one.
5. Create the 11 real user accounts (CEO + 10) with real, working login credentials (through PROJEXA's own Supabase Auth, matching however the existing demo accounts were provisioned -- read whatever script/process created `democeo@projexa-ai.com` originally, likely referenced in PROGRESS.md or a memory/doc file, and replicate that process) so Phase 2/3 testing can actually log in as each of them.

DELIVERABLE: A written report (as this task's final output, and as a new file `PHASE1_SEED_REPORT.md` committed to a small PR against FChecklist/projexa, tier1 if it's docs-only or the seed script itself with no schema change, tier2 if it needed a real schema/migration change) covering: (a) the exact real auth/data-bridge architecture, with file:line citations; (b) the exact list of PROJEXA's 40 modules marked REAL-BACKING vs NO-REAL-BACKING-YET, with evidence for each; (c) the new test org's id/name and its 11 real login credentials; (d) exact counts of what was seeded per module (e.g. "4 projects, 23 RFIs, 15 submittals..."). This report is the direct input to Phase 2 -- it must be accurate and complete, not a rough guess.

Do NOT attempt Phase 2 (writing/running E2E tests) or Phase 3 (chat command testing) in this task -- those are separate, subsequent dispatches once this report exists. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first (check both PROJEXA's and compliance-tracker's claims files/conventions, whichever this cross-system work should register under -- use judgment, note your choice in the report).
