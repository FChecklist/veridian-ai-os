## OBJECTIVE
Close the same cross-repo review gap as before, this time for compliance-tracker PR #553 (Terminology Phase 4's real deliverable). Instruction INS-20260724-213642-6466.

## SCOPE
1. Read compliance-tracker PR #553's real diff in full ("Phase 4: migrate hardcoded terminology examples to registered..."). Confirm it genuinely matches Phase 4's spec (--suggest-fix mode matching findings against VARIABLE_DICTIONARY_2026-07-24.yaml, per-directory migration newest-first, real merged fixes or permanent exemption entries, migration_completion_pct as a real re-run metric).
2. If the content is real and correct, post a genuine structured `AUDIT: PASS` comment on PR #553 following compliance-tracker's own audit-protocol format (8 fields, matching the pattern already used for PR #552), then merge PR #553 once its required checks pass.
3. If real defects exist, fix them for real before posting AUDIT: PASS.
4. Update ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml (both live /opt/veridian/ai-os/ and repo-copy claude-control/ai-os/ paths) to record phase_4_existing_hardcoded_example_migration as done, citing compliance-tracker PR #553 as the real evidence.

## KNOWN_CONTEXT
- compliance-tracker PR #553 confirmed real (state=OPEN, checks include a FAILING `audit-check` because no audit comment was ever posted -- confirmed via `gh pr view 553 --repo FChecklist/compliance-tracker`).
- This is the identical gap pattern as Terminology Phase 3 / PR #552, already fixed once this session -- follow the same remediation.
- Owner directive: 100% real live implementation, keep driving every phase to completion.

## SUCCESS_CRITERIA
- `gh pr view 553 --repo FChecklist/compliance-tracker --json state,mergedAt` shows state MERGED.
- The Terminology phase plan (both copies) shows phase_4_existing_hardcoded_example_migration with a real done status citing PR #553.

## EXPECTED_OUTPUT
A real audit comment on PR #553, a real merge, the updated phase plan in both locations, and a final checkpoint confirming PR #553's real merged state.

## CONSTRAINTS
Limit corrections, if any are needed, to what's required to make PR #553's real content match Phase 4's spec.

## COMPLEXITY_TIER
integrative
