## OBJECTIVE
Integrate the 9 real, working Python files the Owner had Z.AI build (staged at /opt/veridian/workspace/zai-prompt-gateway-staging/ -- config.py, __init__.py, prompt-gateway-ENTRYPOINT.py, engine/{id_generator,classifier,prompt_engine,context_engine,snip_engine}.py, veridian-gateway.service.TEMPLATE) as a real, working pre-processing layer that compresses/classifies/prunes raw owner chat text BEFORE it becomes a --text argument to the REAL, existing, already-in-production python3 /opt/veridian/scripts/task-gateway.py (submit/start/log/close/register-automation/status). Do not recreate anything that already exists; do not duplicate.

## KNOWN_CONTEXT
- CRITICAL COLLISION, READ THIS FIRST -- IT PREVENTS A REAL DATA-LOSS INCIDENT: the staged file `prompt-gateway-ENTRYPOINT.py` is a Z.AI-authored script that is ALSO CALLED "task-gateway.py" in its own header comments and was originally meant to be deployed to the exact path `/opt/veridian/scripts/task-gateway.py`. That path is ALREADY the real, live, production Superboss dispatch entrypoint (submit/start/log/close/register-automation/status subcommands) that every single task this entire session (60+ tasks) has been dispatched through, including this one. It is a COMPLETELY DIFFERENT program (chat classification/compression, not task lifecycle management). If you `cp` or otherwise write it to `/opt/veridian/scripts/task-gateway.py`, you will destroy the real dispatch pipeline mid-operation. NEVER write any staged file to `/opt/veridian/scripts/task-gateway.py`. Deploy the new pipeline under its own namespace: `/opt/veridian/scripts/prompt_gateway/` (config.py, __init__.py, gateway.py [renamed from prompt-gateway-ENTRYPOINT.py], engine/*.py).
- The staged `veridian-gateway.service.TEMPLATE` references `User=veridian`/`Group=veridian` (do not exist on this server -- the real user is `rajat`) and points ExecStart at the colliding path. If you wire this into systemd at all, fix both: run as `rajat` (matching every other systemd --user unit already on this box), and point ExecStart at the new namespaced path. Do NOT create a new system user for this -- it doesn't need one, nothing else on this server has one.
- The 9 files are fully self-contained stdlib-only Python (confirmed: no network calls, no API keys, no external services) -- a deterministic ID generator (VD-YYYYMMDDHHMMSS-XXXX format), a keyword/regex chat classifier (6 categories), a noise-removal + English-to-machine-prompt template converter, a TF-IDF context pruner, and a snippet library. This VD- ID format is intentionally a 4th, separate ID namespace from this session's existing task-/KE-/INS- ID schemes (task IDs from veridian-task.py, Knowledge Engine IDs, Instruction IDs) -- keep it that way, scoped only to this new pipeline's own raw-chat preprocessing records, and say so explicitly in whatever you register in MASTER_INDEX.yaml so nobody later confuses a VD- id for a task id.
- Owner's own message also included a much larger declarative "OWNER_ENGINE" YAML spec naming several heavyweight orchestration/policy/vector-store/local-model-hosting products across 19 named "engines" (full list is in the Owner's original chat message). The 9 real Python files do NOT implement that whole spec -- they implement only the PROMPT_PROCESSING_PIPELINE + CONTEXT_MANAGEMENT_PIPELINE + a slice of the COMPRESSION_STACK boxes from it, using plain deterministic Python, none of the products the spec names (confirmed installed stack on this server today is limited to git/gh/Docker/Node/Python/uv/Claude Code CLI). Do not claim in any registry entry that the full OWNER_ENGINE spec is now implemented -- only claim what's real: a working chat-compression/classification pre-processing layer. Note everything else the spec names as an explicit, separate, NOT-done gap in your final checkpoint -- that remainder is genuinely much larger infra work, not something to silently fold into "done" here.

## SCOPE
1. Move the staged files into `/opt/veridian/scripts/prompt_gateway/` with the entrypoint renamed to `gateway.py` (so real usage is `python3 /opt/veridian/scripts/prompt_gateway/gateway.py --mode stdin`), fixing the internal `sys.path` insert in each engine/*.py file (they currently assume being one level under a `scripts/` dir with `config.py` as a sibling -- verify this still resolves correctly at the new path; fix if not) and updating VERIDIAN_BASE-derived paths in config.py to real writable locations under /opt/veridian/data (not colliding with existing ai-os/ or chatgpt-audit/ directories).
2. Run `python3 /opt/veridian/scripts/prompt_gateway/gateway.py --mode init` for real and confirm it actually initializes (creates dirs, seeds snippets, generates+parses a real test ID, classifies a real test string) -- paste the real output in your final checkpoint, not a paraphrase.
3. Prove the real, intended integration end-to-end: take a real, representative raw Owner-style chat instruction (make up something realistic, e.g. "please can you like fix the bug in the login endpoint, it's basically broken"), pipe it through `gateway.py --mode stdin --json-only` to get the compressed machine_prompt, and show the real noise/token reduction percentages it reports. Then show (does not have to actually dispatch a real task -- just prove the composition works) how that compressed output would be passed as the `--text` value to the REAL `task-gateway.py submit --text "<compressed output>" --source owner --session-id <id>` -- i.e. this new gateway is a pre-processing FILTER feeding the existing dispatcher, never a replacement for it.
4. If a systemd service is genuinely useful (e.g. a long-running named-pipe daemon per the .service template), rewrite `veridian-gateway.service.TEMPLATE` as a real, correct `systemd --user` unit (matching the existing veridian-worker@ pattern's conventions: runs as rajat, no fictional named pipes under /var/run unless you actually create and own that path) and install+enable+start it for real, OR if a persistent daemon isn't actually needed for this pipeline's real use case (CLI pre-processing before an owner-initiated dispatch, which is inherently on-demand, not streaming), explicitly justify NOT installing it rather than installing something unused -- document the real reasoning for whichever path you take.
5. Write ai-os/PROMPT_GATEWAY_2026-07-25.yaml documenting: real file layout, the VD- id namespace and how it's distinct from task-/KE-/INS- ids, the real init/E2E-test output, and the explicit list of what from the Owner's larger OWNER_ENGINE spec this does NOT implement (be specific: name the missing tools/engines).
6. Register in MASTER_INDEX.yaml (registries.prompt_gateway) and via superboss-register.py register-knowledge, citing this task and PR as evidence.

## SUCCESS_CRITERIA
- `test -f /opt/veridian/scripts/prompt_gateway/gateway.py` exits 0, and `test -f /opt/veridian/scripts/task-gateway.py` still has its ORIGINAL content (verify with `grep -c 'def submit_task\|argparse.*submit' /opt/veridian/scripts/task-gateway.py` returning the same non-zero count it had before this task started -- i.e. prove you did NOT overwrite it).
- `python3 /opt/veridian/scripts/prompt_gateway/gateway.py --mode init` exits 0.
- `echo "please can you like fix the bug in the login endpoint, it's basically broken" | python3 /opt/veridian/scripts/prompt_gateway/gateway.py --mode stdin --json-only` exits 0 and produces real, non-empty compressed output.
- `test -f ai-os/PROMPT_GATEWAY_2026-07-25.yaml` exits 0.

## EXPECTED_OUTPUT
Real committed and pushed code (PR on claude-control) with the namespaced prompt_gateway/ module, the real init + E2E-test command output pasted verbatim, the documentation file, the MASTER_INDEX + knowledge-engine registration, and a final checkpoint that HONESTLY separates what's real and done (the compression/classification pre-processing layer) from what the Owner's larger OWNER_ENGINE spec still needs (named explicitly) and was NOT attempted in this task.

## CONSTRAINTS
The pre-existing production dispatch script covered in KNOWN_CONTEXT above must remain byte-for-byte unchanged by this task -- confirm this via the SUCCESS_CRITERIA grep-count check, do not touch it at all. Do not attempt to build Temporal/OPA/Qdrant/local-model-inference infrastructure in this task -- that is real, separate, much larger follow-on work requiring its own scoping, not something to fold into this task's "done" claim.

## COMPLEXITY_TIER
integrative
