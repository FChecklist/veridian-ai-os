TASK: CRM schema + service-layer completion, Wave 1 of a larger CRM-completion effort. Additive-only, no breaking changes.

CONTEXT (read before writing any code): This repo's CRM (crm_leads, crm_opportunities, crm_accounts, crm_contacts, crm_stage_history in src/lib/db/schema.ts, plus src/lib/services/crm-service.ts and crm-accounts-service.ts) already has a working lead-to-client pipeline with AI scoring/explainability columns. The Owner has directed a full completion pass, studying Zoho/Odoo/Infisuite as reference (docs already exist in FChecklist/zoho-reverse-engineering, odoo-reverse-engineering, infisuite-reverse-engineering repos on this same server -- read the relevant docs/*.md files there before designing new tables, don't guess field shapes). Do NOT recreate the existing system -- extend it. This is Wave 1: schema + service layer ONLY. Do not touch API routes (src/app/api/crm/**) or pages (src/app/(app)/crm/**) -- those are separate, later waves that will build on what you ship here.

READ FIRST (in this order):
1. ai-os/boss/ACTIVE-CLAIMS.yaml -- confirm no other active claim conflicts with src/lib/db/schema.ts or src/lib/services/crm-service.ts / crm-accounts-service.ts. If one exists, stop and report instead of proceeding.
2. src/lib/db/schema.ts -- the full crm_leads / crm_opportunities / crm_accounts / crm_contacts / crm_stage_history table definitions (search for "crmLeads", "crmOpportunities", "crmAccounts", "crmContacts", "crmStageHistory"), and the surrounding comment style/conventions used throughout this file (bare-text FK-by-convention, no DB-level .references(), nullable companyId pattern, jsonb ai* columns, etc.) -- new tables/columns MUST follow the exact same conventions, not invent new ones.
3. src/lib/services/crm-service.ts and src/lib/services/crm-accounts-service.ts -- the full current function list and code style.
4. The Zoho/Odoo/Infisuite reverse-engineering docs on this server (paths: /opt/veridian/repos/zoho-reverse-engineering/docs/, /opt/veridian/repos/odoo-reverse-engineering/docs/, /opt/veridian/repos/infisuite-reverse-engineering/docs/) for the specific gaps below -- cite what you found from them in your PR description.

BUILD (all additive, new Drizzle migration via `bun run db:generate` after schema changes -- do not hand-write SQL):

A. Structured Lost Reason for crm_opportunities. Currently `stage` can be 'lost' but there's no reason captured (Odoo has a dedicated, configurable Lost Reasons taxonomy -- see odoo-reverse-engineering docs). Add a new table `crm_lost_reasons` (id, orgId, reasonText, isActive, createdAt -- org-level configurable list, not a hardcoded enum) and a nullable `lostReasonId` column on crm_opportunities (bare-text FK-by-convention, matching this schema's existing style). Add createLostReason/listLostReasons/deactivateLostReason functions to crm-service.ts, and update whatever function currently sets stage='lost' to accept an optional lostReasonId.

B. CRM Activities (Tasks/Meetings/Calls tied to a lead/opportunity/account/contact -- Zoho has Activities>Tasks with Subject/Due Date/Status/Priority; this repo's own crm_stage_history already establishes the polymorphic entityType+entityId pattern for CRM records, reuse that same pattern). New table `crm_activities`: id, orgId, entityType (text: 'lead'|'opportunity'|'account'|'contact'), entityId, activityType (text: 'task'|'meeting'|'call'), subject (text, required), dueDate (nullable date), status (text, default 'not_started': 'not_started'|'in_progress'|'completed'), priority (text, default 'normal': 'low'|'normal'|'high'), notes (nullable text), assignedToId (nullable text, bare FK-by-convention), createdById, createdAt, updatedAt, completedAt (nullable timestamp). Add full CRUD (createActivity, listActivitiesForEntity, updateActivity, completeActivity, deleteActivity) to a new src/lib/services/crm-activities-service.ts, following this codebase's existing service-file conventions exactly (look at how crm-accounts-service.ts is structured -- multi-tenant orgId scoping on every query, that pattern).

C. CRM Campaigns (Zoho has a Campaigns module -- Create/Import, name, status, dates; see zoho-reverse-engineering docs/crm/fields.md and its addendum for the exact real fields observed). New table `crm_campaigns`: id, orgId, name (required), campaignType (nullable text), status (text, default 'planning': 'planning'|'active'|'completed'|'cancelled'), startDate (nullable date), endDate (nullable date), budgetedCost (nullable numeric), actualCost (nullable numeric), expectedRevenue (nullable numeric), description (nullable text), ownerId (nullable text), createdById, createdAt, updatedAt. Add a nullable `campaignId` column to crm_leads (bare FK-by-convention) so leads can be attributed to a campaign (matches Odoo's Campaign/Medium/Source/Referred By pattern on opportunities -- see odoo-reverse-engineering docs -- but keep this addition minimal: just campaignId on crm_leads, don't add Medium/Referred By unless you find a real existing gap for them elsewhere in this schema). Add full CRUD to a new src/lib/services/crm-campaigns-service.ts, same conventions.

D. Audit crm-service.ts and crm-accounts-service.ts for missing basic CRUD. Specifically check: does every entity (lead, opportunity, account, contact) have a working get-by-id and a delete/archive function? This codebase generally prefers soft-delete/archive over hard delete for records with history (check how other entities in this schema handle deletion -- e.g. search for "isActive" or "archivedAt" patterns elsewhere) -- follow whatever this codebase's own established convention is, don't invent a new one. If a genuine gap exists (e.g. no way to delete/archive a lead via the service layer), close it. If you find these already exist, note that in your PR description as a checked-not-a-gap item -- do not skip the check.

CONSTRAINTS:
- Every new/modified DB query MUST be orgId-scoped (multi-tenant isolation) -- this is non-negotiable in this codebase, check RLS policy conventions used by existing crm_* tables and extend the same RLS policies to the 3 new tables (crm_lost_reasons, crm_activities, crm_campaigns) via the migration.
- Do not modify existing crm_leads/crm_opportunities/crm_accounts/crm_contacts columns -- only ADD new nullable columns (lostReasonId, campaignId) and NEW tables. Zero risk to existing data/rows.
- Run `bun run build` and existing tests (bun test if there's a relevant test command) before finishing -- must be green.
- Write or update `.test.ts` files for new service functions if this codebase has an existing test convention for services (check crm-accounts-service.test.ts if it exists as your template).

DELIVERABLE: a single PR against main, branch name starting `wave1-crm-schema-`, with a PR description that (a) lists every new table/column with a one-line rationale citing which reference doc informed it, (b) confirms RLS/orgId-scoping was extended, (c) confirms build+tests are green, (d) explicitly lists what you checked in item D and what if anything you found/fixed. Do not merge -- branch protection blocks direct-to-main anyway; open the PR and stop.

If you hit a genuine ambiguity you cannot resolve from reading the existing code/docs (not a preference question -- a real "I can't tell what's intended" gap), stop and document it clearly in the PR description rather than guessing silently.