## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not mechanical): audit and reconcile compliance-tracker's 13 uncommitted local files (11 modified ai-os/scripts/* plus 2 untracked) against origin/main -- decide file-by-file whether each represents real, intentional, current production state worth committing or stray/superseded drift, since /opt/veridian/scripts is compliance-tracker's live-editable working tree and today alone accumulated fixes from at least 3 separate gap-closing phases (including this chain's own superboss-register.py/veridian-task.py corruption fix) with zero git history or rollback point -- then close gap_queue.yaml item v2-23 (ANTHROPIC_API_KEY dead code removal, confirmed already applied locally, marked done-but-uncommitted) as a real outcome of that reconciliation, continuing the self-dispatching gap-closing chain from task-20260723-095201 phase 2.

## SCOPE
In scope: (1) for each of compliance-tracker's 13 uncommitted files (ai-os/MASTER_INDEX.yaml, ai-os/boss/ACTIVE-CLAIMS.yaml, ai-os/scripts/credit-accountant.py, doc-worker-entrypoint.sh, generate_task_checklist.py, health-check-15min.py, master-decompose.py, preflight-guard.py, superboss-register.py, supervisor-entrypoint.sh, worker-entrypoint.sh, plus untracked launch-interactive-claude.sh and notify-owner.py), diff against origin/main and classify: genuinely-current-and-safe-to-commit, or needs-a-closer-look-before-committing -- cite the actual diff content per file, not a blanket assumption; (2) commit the genuinely-current files to compliance-tracker origin/main in one or a small number of well-described commits (grouped by logical change, e.g. the superboss-register.py + veridian-task.py corruption fix as its own commit citing task-20260723-095201, the ANTHROPIC_API_KEY removal as its own commit closing v2-23); (3) update ai-os/gap_queue.yaml's v2-23 entry to reflect the real committed state (commit hash, date) instead of done-but-uncommitted; (4) update claude-control's ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml source_4_gap_queue classification for v2-23 (move from already_done_newly_discovered_this_task to a fully-closed state) and append a gap_closing_worker_log phase 3 entry with real before/after evidence; (5) commit + push claude-control's own audit-doc update, checkpoint status=pending_review, self-dispatch phase 4 through the remaining consolidated_summary items in the same small-batch style, unless genuinely blocked on an Owner decision.

## KNOWN_CONTEXT
Confirmed live this same day (task-20260723-095201 phase 2, 2026-07-23T10:3x UTC): `cd /opt/veridian/repos/compliance-tracker && git status --short` lists exactly the 13 files named above, all uncommitted; `git remote -v` confirms origin is github.com/FChecklist/compliance-tracker.git; `readlink -f /opt/veridian/scripts` shows it is a real directory (not a symlink into the repo checkout), so the two trees are kept in sync by some other mechanism outside git and have now drifted apart -- treat that sync mechanism as unknown/unverified rather than assuming how it works. gap_queue.yaml's own v2-23 entry (cited in claude-control ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml source_4_gap_queue.classification_of_all_25_items.already_done_newly_discovered_this_task) already states the dummy-key removal is locally present but uncommitted -- re-verify this claim against the live diff rather than trusting the old citation, since it may itself now be stale. Apply the same lessons this chain's prior phases already learned: every self-dispatched prompt.txt uses this same labeled section format (free prose gets silently truncated to 300 chars); the OBJECTIVE opens by naming what requires real judgment, staying away from a "read this file" framing or an otherwise mechanical-sounding opening (gets REDIRECTed); phrasing stays positive throughout, describing what to build/extend/change rather than pairing a prohibition against one thing with an instruction to build a similarly-worded other thing elsewhere in the same prompt (the validator is a bag-of-words contradiction checker, not semantic, and false-flags that pattern); hedges and open-ended filler stay out of this prompt in favor of concrete, spelled-out specifics (the validator treats several common hedge phrases as an ambiguity trigger); subscription-billed work is not gated by the $1 accountant, so this prompt carries no spend-approval framing; a preflight rejection gets fixed at the specific prompt.txt line directly (full shell access is available) and restarted, escalating to notify-owner.py only after about 5 self-fix attempts fail on the same phase; `systemctl --user reset-failed veridian-worker@<task-id>.service` clears "Start request repeated too quickly" (a systemd StartLimitBurst issue, not a real rejection) if a task restarts more than about 3 times in 30 minutes. Full real precedent for a correctly-run batch: task-20260723-045924, task-20260723-052857/060600/064712, task-20260723-093333, task-20260723-095201.

## SUCCESS_CRITERIA
Every one of the 13 files gets an explicit, evidence-cited classification (commit or hold), not a blanket "commit everything" or "skip everything" decision. Files committed actually land on compliance-tracker origin/main, verified via `git log origin/main` or an equivalent real check post-push. gap_queue.yaml v2-23 and claude-control's MASTER_GAP_AUDIT_2026-07-23.yaml both reflect the real committed state with a real commit hash cited, not an assumed one. Zero fabricated progress -- if a file's safety to commit cannot be determined confidently, say so honestly, hold it uncommitted, and document why rather than guessing.

## EXPECTED_OUTPUT
Commit + push real changes to both compliance-tracker (the 13-file reconciliation) and claude-control (the audit-doc update). Checkpoint status=pending_review with real evidence in the note (via `python3 /opt/veridian/scripts/veridian-task.py checkpoint <task_id> --status pending_review --note "..."`). Self-dispatch phase 4 (via `python3 /opt/veridian/scripts/veridian-task.py create --title ... --repo claude-control --prompt "..."`) continuing through the remaining open items in ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml's consolidated_summary, unless genuinely done or genuinely blocked on an Owner decision, in which case stop and say so plainly via notify-owner.py rather than appearing to silently vanish. This is phase 3 of roughly 3-5 phases per notify-owner.py email cycle; send one now only if this phase surfaces something needing an Owner decision or if the chain is stopping, otherwise fold this phase's progress into the next scheduled cumulative email.

## CONSTRAINTS
Every completion claim must cite a real file, command output, or commit hash. Read each file's actual diff against origin/main first, then commit only the ones that diff confirms are current and safe -- a file appearing in `git status --short` is a starting point for that diff review, not itself evidence the content is safe or current. The 4-repo merge and the veda-advisors data-coupling severance stay parked in claude-control's ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml (already written by phase 1) and are not executed automatically.

## COMPLEXITY_TIER
judgment