## OBJECTIVE
Master orchestration, cron wiring, report software

## SCOPE
- Orchestrate all domains' audit-run scripts under one cron-driven entrypoint (zero AI in the loop, per PART5)
- Build the master report software (aggregates finding-record rows across all domains + repos)
- File any new crontab entries through the existing Owner-approval-citation pattern (ai-os/OWNER_DECISIONS_NEEDED_*.yaml + CRONTAB_APPROVED_SNAPSHOT.txt) -- NONE filed this phase (Phase 0 adds no cron entries; deferred to whichever of Phase 1-7 first needs one)

## KNOWN_CONTEXT
This is phase-8 of ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml, part of the VERIDIAN AUDITOR ENGINE -- Phase 0 Inventory + Phased Build Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase-8, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260724-213832-phase7-part6-observability-audit-trail-l. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase-8" --tag domain:veridian-auditor-engine-0-inventory` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "phase: 8" ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml's own phases[] entry for phase-8 in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-implement anything from an earlier phase of this same plan file that already shipped and merged.
Search for an existing engine/mechanism before writing a new one, per this session's standing no-duplication
rule (see AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml's own inventory/precedent notes if present). If this
phase's real work ends up needing a scheduling mechanism beyond what SCOPE already covers, record it the
same way every earlier crontab addition this session was recorded (ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml
+ ai-os/CRONTAB_APPROVED_SNAPSHOT.txt) before activating it.

## COMPLEXITY_TIER
judgment
