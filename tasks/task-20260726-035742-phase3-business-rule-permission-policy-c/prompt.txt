## OBJECTIVE
Wire the approval-gate enforcement phase_1's bare lifecycle state machine deferred here (governance-lifecycle-state-machine), plus the Business/Policy engine group with no real prior art for THIS domain (Business Rule, Policy, Compliance, Governance, Audit, Cost Optimization -- gap analysis explicitly rejected the 20-Engines/compliance-tracker- product naming coincidences for these, see engine-business-rule/engine-policy/ engine-compliance/engine-governance/engine-audit evidence), reusing permission-service.ts (partially_implemented) and llm-client.ts's cost functions (partially_implemented) rather than rebuilding RBAC or cost estimation from scratch, and the GitOps prompt-lifecycle workflow.

## SCOPE
- Gap items closed: engine-business-rule, engine-dependency, engine-permission (wire permission-service.ts to prompt_registry rows), engine-policy, engine-compliance, engine-governance, engine-audit (wire to knowledge_engine's or 20-Engines' audit-log MECHANISM per gap analysis's explicit reuse note -- do not build a 3rd audit log), engine-cost-optimization (deepen llm-client.ts's estimateCostUsd/estimateCacheSavingsUsd into budget guardrails), governance-lifecycle-state-machine (approval gates + eval thresholds + canary duration), governance-gitops-workflow.
- Design (claude-control-side, config/schema only) the approval-gate + eval-threshold + canary-duration contract as a YAML schema, mirroring CAPABILITY_REGISTRY_SCHEMA_2026-07-24.yaml's own Phase-0-schema/Phase-1-live-wiring split -- defer live enforcement wiring (which needs phase_1's live table + phase_2's compiled-prompt objects) to this phase's own live-wiring sub-scope.

## KNOWN_CONTEXT
This is phase_3_governance_policy_cost_engines of ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml, part of the VERIDIAN_Architecture_v2.0.docx -- Phased Implementation Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_3_governance_policy_cost_engines, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260725-174858-phase1-prompt-registry-version-lifecycle. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_3_governance_policy_cost_engines" --tag domain:veridian-architecture-v2-0-docx` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_3_governance_policy_cost_engines" ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own phases[] entry for phase_3_governance_policy_cost_engines in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
