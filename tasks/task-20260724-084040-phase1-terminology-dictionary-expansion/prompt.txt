## OBJECTIVE
Execute Phase 1 of the real, already-designed VERIDIAN Terminology Standardization plan for real: expand Variable Dictionary coverage and start real guardrail rollout, not more design. Plan file: ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml (repo path claude-control/ai-os/), phase entry id=phase_1_dictionary_coverage_expansion.

## SCOPE
1. Read ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml's phase_1_dictionary_coverage_expansion entry in full, plus the existing ai-os-scripts/generate_variable_dictionary.py and its Phase-0 output (top-20-table coverage only).
2. Extend generate_variable_dictionary.py to cover real additional tables from ai-os/DATABASE_CATALOG.json beyond the top-20 (grow real coverage, do not just raise a limit number without verifying the new entries are real).
3. Take ai-os/TERMINOLOGY_GUARDRAIL_2026-07-24.py (already designed+smoke-tested on 3 files in Phase 0) and run it for real against a wider real set of files (at least 20 real prompt/template files in compliance-tracker), capture real findings.
4. Update the phase plan marking phase 1 status done with this real evidence (dictionary entry count before/after, guardrail findings count).

## KNOWN_CONTEXT
- Owner directive: 100% real live implementation, no more shallow planning-only work.
- Standing rules: crontab changes need an OWNER_DECISIONS_NEEDED_2026-07-23.yaml approval entry (Owner's blanket approval covers it); registry writes come from scripts not hand-authored prose; verify real PR/merge state via `gh pr view`, never trust task.yaml's self-report alone.

## SUCCESS_CRITERIA
- Variable Dictionary entry count is real and larger than the Phase 0 top-20-table baseline, each entry traceable to a real table.column.
- Guardrail script has real findings from a real run against at least 20 files, not just the original 3.
- Phase 1 shows completed status with this real evidence in the phase plan file.

## EXPECTED_OUTPUT
Real committed and pushed code (PR on claude-control), the real before/after dictionary counts and guardrail findings as evidence, and a final checkpoint stating what Phase 1 delivered vs what's deferred (full CI enforcement wiring).

## CONSTRAINTS
Limit this task to Phase 1 (dictionary expansion + wider guardrail run) only -- do not attempt full CI enforcement wiring in this same task. Any crontab need follows the existing Owner-approval-citation pattern rather than a fresh confirmation.

## COMPLEXITY_TIER
integrative
