You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously, including waiting for CI via `gh run watch <id> --exit-status`.

CONTEXT (already done live on the real Supabase database by a DB-access-capable session -- your job is ONLY to make src/lib/db/schema.ts match what already exists live, do NOT write any migration that creates/moves anything again):

A new Postgres schema `platform` now exists in the same database as `compliance` (Owner directive: Supabase free-tier limits us to 2 databases total, so this is a same-database compartment designed for an easy future split, not a second database). The following 22 tables and 3 enum types have ALREADY been moved live via `ALTER TABLE ... SET SCHEMA platform` / `ALTER TYPE ... SET SCHEMA platform`:

Tables: dynamic_chains, entity_relationships, worker_agents, worker_agent_domain_groups, worker_agent_domain_index, worker_agent_learnings, worker_agent_usage_log, worker_agent_versions, module_registry, product_branches, product_branch_modules, module_rule_configs, instruction_execution_cache, capability_improvement_proposals, task_capabilities, fde_requests, automation_rules, automation_rule_runs, ai_model_registry, ai_routing_policies, ai_routing_audit_log, ai_team_role_overrides.

Enum types: ai_router_scope, ai_model_status, ai_model_health.

Deliberately EXCLUDED (stay in `compliance`, do not touch): platform_assets, platform_applications -- compliance.auto_register_asset() (a trigger function) hardcodes writes to compliance.platform_assets; moving it would break every registration trigger on the tables above. This was verified via pg_get_functiondef before the live move, not guessed.

YOUR TASK -- make src/lib/db/schema.ts (and only that file plus anything it breaks) match this live reality:

1. Read src/lib/db/schema.ts in full first, understand its existing `export const complianceSchemaDB = pgSchema('compliance')` pattern (line 5) and how it's used throughout (`complianceSchemaDB.table(...)`, `complianceSchemaDB.enum(...)`).
2. Add a new export: `export const platformSchemaDB = pgSchema('platform')` near the existing complianceSchemaDB declaration.
3. For EACH of the 22 tables listed above: find its `complianceSchemaDB.table(...)` definition and change the schema object it's built from to `platformSchemaDB.table(...)` instead -- the table name string itself, all columns, indexes, and the exported TS identifier name stay EXACTLY the same. Do not touch any other table's definition.
4. For the 3 enum types (ai_router_scope, ai_model_status, ai_model_health): change their `complianceSchemaDB.enum(...)` definitions to `platformSchemaDB.enum(...)`.
5. Check every `relations(...)` block involving any of these 22 tables -- Drizzle relations work fine across schema objects as long as both sides are still exported from the same schema.ts file, but read each one to confirm nothing assumes same-schema-only behavior.
6. Run `bunx tsc --noEmit` -- fix every resulting type error. Expect none if this is done correctly (Drizzle's pgSchema doesn't change a table's TS type shape), but verify, don't assume.
7. Run `bun run lint` and `bun test` -- both must be clean.
8. Grep the entire src/ tree for any raw SQL string (not Drizzle query-builder calls) that hardcodes `compliance.dynamic_chains`, `compliance.worker_agents`, `compliance.module_registry`, `compliance.fde_requests`, `compliance.automation_rules`, `compliance.ai_model_registry`, `compliance.ai_routing_policies`, `compliance.ai_routing_audit_log`, `compliance.ai_team_role_overrides`, `compliance.instruction_execution_cache`, `compliance.capability_improvement_proposals`, `compliance.task_capabilities`, `compliance.entity_relationships`, `compliance.product_branches`, `compliance.product_branch_modules`, `compliance.module_rule_configs`, or any of the worker_agent_* satellite tables (e.g. inside a raw `sql\`...\`` template, a Supabase RPC call, or a comment referencing the old path) -- fix any hardcoded `compliance.<table>` reference among these 22 to `platform.<table>` instead. Do NOT touch references to platform_assets or platform_applications (those correctly stay compliance.*).
9. Do NOT create any new drizzle/*.sql migration file for the schema move itself -- that already happened live and will be committed separately by the Owner's own session as a hand-authored SQL file (matching this repo's own established convention for out-of-band-applied migrations, e.g. drizzle/0236's own header). If your diff includes a NEW migration file, delete it before committing -- this task is schema.ts (and any hardcoded-string fixes) ONLY.
10. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first; check for collisions (there is a concurrent, unrelated task migrating compliance-tracker to consume the veridian-ui-kit shared UI package -- that task does NOT touch schema.ts per its own instructions, so there should be no real collision, but check anyway).
11. Push, open a PR, post the standard 8-field `AUDIT: PASS` comment yourself, wait for CI.
12. This PR touches schema.ts but creates NO migration file and NO net schema change (the DB already reflects this) -- classify as TIER1 and self-merge once CI is fully green (Lint/Type Check/Build/Unit Tests/E2E/Asset Registry Coverage/Guardrail Presence/audit-check all pass; Vercel is known-rate-limited and non-required).
13. Report final status: PR number, CI result, merged status, and confirm explicitly that `bunx tsc --noEmit` passed with zero errors (not just "looks fine").
