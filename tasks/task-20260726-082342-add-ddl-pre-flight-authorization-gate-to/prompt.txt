## OBJECTIVE
Close a real governance gap found 2026-07-26: the standing rule "Supabase schema changes are tier2-by-definition, always held for human sign-off, never auto-merged" is only enforced today at the PR-merge step, not before a headless worker takes a live action. A worker task (task-20260726-071400-migration-drift-audit-and-reconciliation) executed live DROP TABLE/CREATE TABLE/ALTER TABLE/CREATE INDEX statements against the production Supabase project (pcrjmlpuqsbocqfwoxod) via the Supabase MCP apply_migration tool, entirely before any PR/CI/human review happened -- because its own dispatch prompt's SCOPE told it to, and nothing in the dispatch pipeline stops a prompt from authorizing that. Add a real, enforced pre-flight gate at dispatch time (not merge time) that blocks this from happening again without an explicit, citable Owner approval.

## KNOWN_CONTEXT
Read scripts/task-gateway.py's cmd_start() function in full first (around line 318-386). It already chains one pre-flight gate: tight_task_validation.py runs at line 337, right after the required-sections check and right before `veridian-task.py create` is called at line 351 -- if tight_task_validation.py returns valid:false, cmd_start() calls fail() and veridian-task.py create is never invoked, so no worker ever starts. This is the exact same insertion point and rejection pattern (a JSON error printed via fail(), non-zero exit, dispatch blocked) the new DDL-authorization gate should use -- read fail()'s definition and tight_task_validation.py's own CLI contract (it's invoked as `python3 tight_task_validation.py <prompt-file>` and returns JSON with a "valid" boolean) to match the established convention exactly rather than inventing a new one. The real incident this gate exists to prevent: a dispatch prompt's own ## SCOPE section told a worker to call Supabase MCP's apply_migration directly against production -- the worker did exactly what it was told, this is not a worker misbehaving, it's a missing check on what a dispatch prompt is allowed to authorize.

## SCOPE
1. Create a new script, scripts/ddl_authorization_check.py, invoked the same way as tight_task_validation.py (`python3 ddl_authorization_check.py <prompt-file>`, prints JSON with a "valid" boolean plus "reason"/"guidance" on rejection). It should scan the prompt-file's full text for DDL-executing language: references to Supabase MCP's apply_migration tool, or SQL DDL keywords (DROP TABLE, CREATE TABLE, ALTER TABLE, CREATE INDEX, TRUNCATE) appearing outside of a fenced code block that is clearly read-only (SELECT-only statements are fine and must NOT trigger this gate -- only DDL/write-DDL keywords should). If any such reference is found, the prompt is valid:false UNLESS the prompt-file's text also contains a line matching the literal pattern `PRE-APPROVED-LIVE-DDL:` followed by a non-empty, non-placeholder reference (e.g. an Owner approval note, a decision-log entry ID) -- in which case it passes.
2. Wire this new check into scripts/task-gateway.py's cmd_start(), immediately after the existing tight_task_validation.py check (after line ~349, before the `veridian-task.py create` call at line ~351), using the exact same run()/json.loads()/fail() pattern already used for tight_task_validation.py so the rejection JSON shape and dispatch-blocking behavior matches exactly.
3. Add a regression test (tests/ directory, matching the existing test-file naming convention already used for other pre-flight checks in this repo) covering: (a) a prompt with no DDL language passes, (b) a prompt referencing apply_migration and a DROP TABLE statement with no PRE-APPROVED-LIVE-DDL marker is rejected, (c) the same prompt WITH a valid PRE-APPROVED-LIVE-DDL marker passes, (d) a prompt containing only SELECT-style read-only SQL is not falsely rejected.
4. Update the one canonical protocol file (ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml in claude-control -- confirm this is still the single canonical file via scripts/check_single_protocol_file.py before editing, do not create a second one) with a short section documenting this new gate and the real 2026-07-26 incident that motivated it.

## SUCCESS_CRITERIA
- `python3 scripts/ddl_authorization_check.py /tmp/no-ddl-test-prompt.txt` (a prompt with no DDL language, create it as part of the test fixtures) exits 0 and prints valid:true.
- `python3 scripts/ddl_authorization_check.py /tmp/ddl-no-approval-test-prompt.txt` (a prompt referencing apply_migration with no PRE-APPROVED-LIVE-DDL marker) prints valid:false.
- `python3 -m pytest tests/ -k ddl_authorization -v` exits 0 with all 4 scenarios passing.

## EXPECTED_OUTPUT
A real PR against claude-control (task-gateway.py, veridian-task.py, and any other scripts referenced above all live in claude-control per this repo's existing layout -- confirm the real path via `find /opt/veridian/repos -name task-gateway.py` before assuming) with the new script, the wiring change, the regression tests, and the protocol-doc update.

## CONSTRAINTS
Do not touch compliance-tracker or any other repo in this task -- this gate belongs in the dispatch pipeline itself (claude-control). Do not weaken or bypass the existing prompt pre-flight check already wired into cmd_start() while adding this new one -- both must run. This is tier1 (scripts/docs only) but given it is a security/governance control, treat correctness as seriously as a higher tier -- a false negative (DDL language that slips through undetected) defeats the entire purpose. Do not merge yourself; this needs a fresh audit pass.

## COMPLEXITY_TIER
judgment
