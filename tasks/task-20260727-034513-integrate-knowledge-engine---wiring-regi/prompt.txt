## OBJECTIVE
Close the real gap found 2026-07-26/27 in ai-os/memory/superboss-register.sqlite: the `knowledge_engine` table (343 rows) has a real, 100%-populated `purpose` field and is genuinely useful memory, but the `wiring_registry` table (7,709 rows, kept current by scripts/generate_wiring_registry.py on a real 6-hourly cron) has NO purpose captured for 91% of its rows (5,019 `function` + 1,948 `file` entities) and only 27 of 1,948 files (1.4%) have any relationship linking them to something with a purpose -- meaning understanding "why does this file/function exist" currently requires opening and reading the code, exactly the manual-research problem this whole memory system was built to eliminate. Build ONE real, auto-updating, sub-second-searchable integration of knowledge_engine + wiring_registry + the architecture-layer engine inventory (ai-os/20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml's engine_inventory, 20 real entries) + the implementation layer (wiring_registry's function/file entities) -- extending existing software, not building a parallel system.

## KNOWN_CONTEXT
Read scripts/generate_wiring_registry.py in full first -- it is the real, only writer of wiring_registry, already cron-scheduled every 6 hours (confirmed live in rajat's crontab: "0 */6 * * * ... generate-wiring-registry ... generate_wiring_registry.py"). Read the real, current schemas directly from the live database before assuming anything: `wiring_registry` has columns entity_id/ts/entity_type/source_system/path/relationships/last_verified_ts/verification_status/source_ref/metadata_json; `knowledge_engine` has artifact_id/ts/artifact_path/content_hash/artifact_type/secondary_path/exists_on_disk/purpose/tags/entity_relationships/last_verified_ts/verification_status/metadata_json -- note knowledge_engine already has a first-class `purpose` column while wiring_registry only has purpose buried inside metadata_json for its 20 `engine`-type rows (confirmed live: engine-01 through engine-20's metadata_json contains a real "purpose" key sourced from the 20-engine inventory). Both tables already have real, working SQLite FTS5 virtual tables (wiring_registry_fts, knowledge_engine_fts, confirmed present in the schema) -- verify live whether these are actually populated and queryable (`SELECT * FROM wiring_registry_fts WHERE wiring_registry_fts MATCH '...'`) before assuming they need to be built from scratch; if they exist but are empty or stale, that is itself a real bug to fix, not a reason to build a new search mechanism. Real evidence sources already available to derive a genuine (not fabricated) purpose for each file: `git log --follow -- <path>` (real commit messages), each repo's own PROGRESS.md sections (which cite real files by path per task, per the convention already used throughout this session's dispatched tasks), each task's task.yaml prompt field (real dispatch prompts already describe what a task's files are for), and any existing doc-comment/README/module-level docstring already in the file itself.

## SCOPE
1. Extend generate_wiring_registry.py (do not create a parallel script) so that, on every run, it derives a real `purpose` string for every `file`-type entity it registers or re-verifies -- sourced from real evidence (the file's own top-of-file doc comment if one exists; otherwise the most recent real git log message touching that file; otherwise a real PROGRESS.md/task.yaml citation if one references that exact path) -- and writes it into wiring_registry's metadata_json under a real "purpose" key, consistent with how the 20 engine-type rows already store it. Do not fabricate a purpose when no real evidence exists for a file -- leave it explicitly null/empty and flagged, rather than inventing plausible-sounding text.
2. For `function`-type entities (5,019 rows, real per-function purpose annotation is not tractable at this scale and was explicitly excluded from this backfill's own scope by design): instead, ensure each function's real `relationships` field (already populated for all 5,019 via a "defined_in" link to its file) is genuinely resolvable to that file's now-real purpose via a join -- i.e. a function's effective purpose is "inherited" from its file at query time, not separately stored, avoiding 5,019 rows of duplicated/stale text. Build this as a real, tested SQL join or a thin Python helper function, not by writing purpose text onto each function row individually.
3. Build (or fix, if the existing wiring_registry_fts/knowledge_engine_fts tables turn out to be broken/stale/unpopulated) ONE unified, fast query interface -- a single real script, e.g. scripts/query-knowledge.py -- that takes a search term and returns real matches across knowledge_engine (343 governance/architecture entries), the 20-engine inventory, and wiring_registry (7,709 implementation entries, now file-purpose-enriched with function-purpose inherited via the join from item 2), using the existing SQLite FTS5 indexes for genuine sub-second search -- measure and report the real query latency, don't just assert it's fast.
4. Wire this script's cron/auto-update dependency correctly: it must always reflect the current database state with no separate manual regeneration step -- if generate_wiring_registry.py's existing 6-hourly cron is sufficient once item 1 is done, say so explicitly; only add a new cron entry if genuinely necessary, and if you do, follow the existing real approval convention (an entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a re-synced ai-os/CRONTAB_APPROVED_SNAPSHOT.txt copied to both /opt/veridian/ai-os/ and /opt/veridian/repos/claude-control/ai-os/, matching the pattern already used for every other cron addition this session) -- do not add a raw crontab entry without this, it will be rejected by the standing pre-flight check.
5. Add a real regression test proving: (a) a file with a real git-log/PROGRESS.md/docstring evidence trail gets a genuine, non-fabricated purpose written; (b) a file with no such evidence is left explicitly unpopulated, not fabricated; (c) querying scripts/query-knowledge.py for a real, known term returns the correct entities from all three layers (knowledge_engine, engine inventory, wiring_registry) with real, measured sub-second latency.

## SUCCESS_CRITERIA
- `python3 -c "import sqlite3; c=sqlite3.connect('ai-os/memory/superboss-register.sqlite'); cur=c.cursor(); cur.execute(\"SELECT COUNT(*) FROM wiring_registry WHERE entity_type='file' AND metadata_json LIKE '%purpose%'\"); print(cur.fetchone()[0])"` shows a real, substantially increased count of file entries with a genuine purpose captured (report the real before/after numbers -- do not claim 100% if real evidence didn't exist for every file).
- `time python3 scripts/query-knowledge.py "some real search term"` completes in real, measured sub-second wall-clock time and returns genuine matches spanning knowledge_engine, the engine inventory, and wiring_registry.
- `python3 -m pytest tests/ -k knowledge_integration -v` exits 0 with all three new regression scenarios passing.

## EXPECTED_OUTPUT
A real PR (compliance-tracker or claude-control, whichever repo generate_wiring_registry.py and the sqlite database actually live under per existing convention -- confirm the real path before assuming) with: the extended generate_wiring_registry.py, the new/fixed scripts/query-knowledge.py, the regression tests, and a short real report (in the PR body) of exactly how many file entities got a genuine purpose versus how many were left honestly unpopulated for lack of real evidence.

## CONSTRAINTS
Do not fabricate purpose text for any file lacking real evidence -- an honest gap is acceptable, a plausible-sounding invention is not. Do not create a parallel registry, a parallel search index, or a parallel cron job unless genuinely necessary and, if so, only through the existing crontab-approval convention. Do not merge yourself.

## COMPLEXITY_TIER
judgment
