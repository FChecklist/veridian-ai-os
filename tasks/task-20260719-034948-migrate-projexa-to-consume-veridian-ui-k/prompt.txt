You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously, including waiting for CI via `gh run watch <id> --exit-status`.

TASK: FChecklist/projexa must actually consume the shared FChecklist/veridian-ui-kit package's real shell/composer/panel/header components, not just its design tokens (colors/fonts) which it already consumes today. FChecklist/compliance-tracker just completed the equivalent migration (PR #471, merged) -- read that PR's diff first (`gh pr diff 471 --repo FChecklist/compliance-tracker`) as your primary reference for HOW to do this migration correctly and what shape the shared components expect, since it is the proven, working precedent.

Bump the dependency: `"@fchecklist/veridian-ui-kit": "github:FChecklist/veridian-ui-kit#v0.2.0"` in package.json (PROJEXA is currently pinned at v0.1.0; v0.2.0 adds the AppHeader component this task needs).

READ IN FULL BEFORE WRITING ANY CODE:
- veridian-ui-kit's README.md and every file under src/shell/, src/composer/, src/panel/, src/context/.
- compliance-tracker's now-merged PR #471 diff (the proven migration pattern) -- especially how it wrapped AppHeader inside its own AppTopbar.tsx (passing real search/notification/user-menu content as props) and AppSidebar inside its own AppSidebar.tsx (passing real nav data as props), while leaving AppShellFrame's actual usage gated behind an org feature flag for a safe incremental rollout. PROJEXA has no such existing feature-flag gate -- this migration can go unconditional (every PROJEXA org gets the new shell), since PROJEXA is a newer, smaller product with less blast radius than compliance-tracker's.
- This repo's own REAL, current implementation: src/app/(app)/layout.tsx (the shell), src/components/AppSidebar.tsx, src/components/AppTopbar.tsx (currently minimal -- only a sidebar-toggle, title, and theme toggle; missing search/notifications/user-menu entirely, confirmed by reading it), src/components/veri-chat/VeriComposer.tsx, VeriChatPanel.tsx, veri-chat-context.tsx -- these have PROJEXA's real, working data wiring (`/api/v1/projexa/capability-tree` or similar, `/api/assistant`, `/api/discuss`, `/api/todos`, `/api/conversations` -- confirm the exact real endpoint paths by reading these files, do not guess) that must NOT be lost.

WHAT TO DO:
1. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first; check for collisions.
2. Replace src/app/(app)/layout.tsx's hand-rolled flex/ResizablePanelGroup layout with the shared `AppShellFrame` (sidebar/header/composer/panel/homeThreadSlot/homeRoute="/dashboard" props), matching compliance-tracker's PR #471 pattern.
3. Replace AppSidebar.tsx's nav-rendering markup with the shared `AppSidebar` component, passing PROJEXA's real nav sections (Overview/Execution/Field/Design/Resources/Sales/GRC/Finance/HR/Intelligence -- read the current file for the exact real list) as data via props -- the shared component owns the generic shell/style only, PROJEXA's own real routes/icons/badges stay PROJEXA's own data.
4. Replace AppTopbar.tsx's markup with the shared `AppHeader` component. This is where the actual gap-closure happens: PROJEXA currently has NO search trigger, NO notification bell, NO user avatar/menu, NO right-panel-toggle button -- AppHeader's prop surface has slots for all of these (see its own source in veridian-ui-kit). Wire real behavior, not placeholders: if PROJEXA has an existing search mechanism (grep for one first -- there may not be one, in which case implement a minimal real one or omit that slot honestly, do NOT fake a non-functional search icon), a real notifications data source (check for a `/api/notifications` route or equivalent), and a real user-menu (profile/settings/logout, matching Supabase Auth sign-out the same way compliance-tracker's AppTopbar.tsx does it) and a right-panel-toggle wired to actually show/hide the panel. If any of these genuinely do not exist yet in PROJEXA (e.g. no notifications system at all), do NOT invent fake functionality -- omit that specific slot and clearly report it as a real, separate gap rather than shipping a decorative dead button.
5. Replace VeriComposer.tsx and VeriChatPanel.tsx usage with the shared package's `VeriComposer`/`PanelShell`/`ThreadView`, wiring PROJEXA's real capability-tree fetch and task/chat/todo/discuss dispatch endpoints through the shared components' callback props -- do not lose any real dispatch/fetch behavior in the swap.
6. Keep veri-chat-context.tsx as PROJEXA's own thin wrapper around the shared package's `createVeriChatContext()` factory, supplying PROJEXA's real data sources.
7. Run `bunx tsc --noEmit`, `bun run lint`, `bun test` -- all must be clean.
8. This should not need schema/migration changes -- if you find yourself needing one, stop and reconsider scope.
9. Push, open a PR, post the standard 8-field `AUDIT: PASS` comment yourself, wait for CI -- Lint/Type Check/Build/Unit Tests/E2E Tests must all be green (Vercel may be rate-limited and non-required, same as compliance-tracker's repo).
10. If TIER1 (no schema touch) and CI is fully green, self-merge (squash, delete branch).
11. Report final status: PR number, CI result, merged status, and an honest plain-English list of exactly what real functionality each AppHeader slot now has (search/notifications/user-menu/panel-toggle) vs. what was genuinely not available to wire and was honestly omitted instead of faked.

Use the existing demo accounts (democeo@projexa-ai.com etc., see this repo's own seed data / prior session notes if referenced in PROGRESS.md) as context for what real data these components should render against, if you need to reason about behavior -- but do not attempt live browser verification yourself, that will be done separately after merge.
