## OBJECTIVE
Extend the already-existing, already-built VERIDIAN Wiring Engine (ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml + ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json + scripts/generate_wiring_registry.py) so it becomes the single, unified entity-relationship graph for the WHOLE system -- Server + Vercel + Supabase + GitHub + Web Browser -- instead of Server-engines-only, and execute its own already-written Phase 3 (make it a live, auto-updating table, not a static snapshot). Do not build a new schema, new graph engine, or new registry -- this is a scope extension and live-wiring of what already exists, per the Owner's explicit "don't duplicate" instruction.

## KNOWN_CONTEXT
Read ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml, ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml, and scripts/generate_wiring_registry.py in full first -- all real, built 2026-07-25. The phase plan's own real, measured finding: of 19 real hops_through relationships in the current registry, zero target a gateway entity -- the graph today mostly captures "shares_implementation_with" (same-file) facts, not proven live call chains. The phase plan already defines phase_3_wiring_registry_live_wiring (add a 9th table `wiring_registry` to scripts/superboss-register.py, same FTS5+write-lock convention as its existing `capability_registry`/`knowledge_engine` tables, with register-entity/lookup-entity/list-entities CLI subcommands, then re-point generate_wiring_registry.py to upsert into it instead of only rewriting the JSON snapshot) -- this task executes that phase for real, not a rewrite of its design. Also read ai-os/DATABASE_CATALOG.json (449 tables/enums, compliance-tracker's live Supabase schema, generated 2026-07-26), the real Vercel+GitHub census merged this session (claude-control PR #85/#90, already in ai-os/MASTER_INDEX.yaml), and ai-os/MASTER_ARCHITECTURE_2026-07-22.yaml (the existing 5-analysis architecture consolidation) -- confirm none of these already model Supabase tables, Vercel deployments, GitHub repos, or browser-side components as first-class nodes in the SAME entity-relationship graph as the 20 engines/10 gateways before adding them (a real check, not an assumption).

## SCOPE
1. Execute phase_3_wiring_registry_live_wiring exactly as already specified in ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml: add the wiring_registry table + CLI subcommands to scripts/superboss-register.py, update scripts/generate_wiring_registry.py to upsert (ON CONFLICT(entity_id)) into it while still writing the human-readable JSON snapshot, and confirm idempotency (two consecutive runs produce 0 net new rows the second time).
2. Extend WIRING_ENGINE_SCHEMA_2026-07-25.yaml's entity_type enum to add 4 new real entity types: supabase_table (sourced from ai-os/DATABASE_CATALOG.json's real table list), vercel_project (sourced from the real 3 linked projects per the Vercel+GitHub census), github_repo (sourced from the real 7 repos in the same census), and browser_component (sourced from the real client-side files already cited in engine_inventory row 1/2/17 of ai-os/20_ENGINES_10_GATEWAYS_PHASE_PLAN_2026-07-24.yaml -- VeriComposer.tsx, ChainSelector.tsx, IntentCommandPalette.tsx, browser-intent-cache.ts -- do not invent new browser-side entities beyond what that real inventory already found).
3. Re-run scripts/generate_wiring_registry.py to produce a real, extended registry that includes these 4 new entity types as actual rows, with real shares_implementation_with or hops_through relationships to the existing 20 engines/10 gateways wherever a genuine, citable code relationship exists (e.g. a Supabase table a specific engine's service file actually queries) -- do not fabricate relationships that aren't real.
4. Set up (or confirm already-covered by) the existing generate_wiring_registry.py cron entry so the extended registry regenerates automatically -- per the phase plan's own crontab_approval_note, the Owner's standing 95pct-resources/recommended-default approval already covers this if the cron entry is consistent with the existing convention; do not add a NEW cron job if the existing one already covers regeneration, just confirm its schedule and that it now covers the extended scope.
5. Update ai-os/MASTER_INDEX.yaml's wiring_engine registry entry to reflect the real, extended scope and Phase 3 completion.
6. Do not attempt phase_1_end_to_end_gateway_verification (proving one full real request flow) in this same task -- that is a separate, larger phase already defined with its own real acceptance bar; this task's job is the schema extension + live-wiring only. Note phase_1's real current status (not_started) honestly in the output rather than claiming it's done.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py list-entities` (or equivalent new CLI command) returns real rows including at least one supabase_table, vercel_project, github_repo, and browser_component entity.
- Running `python3 scripts/generate_wiring_registry.py` twice in a row produces 0 new rows on the second run (real idempotency check).
- `python3 -c "import yaml; d=yaml.safe_load(open('ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml')); print(d)"` parses cleanly and shows the 4 new entity types in the schema's own enum.

## EXPECTED_OUTPUT
A real PR against claude-control implementing the schema extension, the live wiring_registry table + CLI, the extended registry generation, and the MASTER_INDEX.yaml update.

## CONSTRAINTS
Do not build a new engine, schema, or registry system -- this is an extension of ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml and scripts/generate_wiring_registry.py only. Do not attempt phase_1 (end-to-end proof) in this task. Do not fabricate entity relationships that aren't real and citable. Do not merge yourself.

## COMPLEXITY_TIER
judgment
