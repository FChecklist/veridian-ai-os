TASK ID: BACKEND-001
MODULE: backend

OBJECTIVE: Correct the openai/gpt-oss-120b (Groq) entry in llm-client.ts's MODEL_PRICING constant from promptPer1k 0.000036/completionPer1k 0.00018 to the verified Groq published rate of $0.15/$0.60 per 1M tokens (promptPer1k 0.00015/completionPer1k 0.0006).

FILES ALLOWED (you may only modify files matching these patterns):
  - src/lib/llm-client.ts

FILES FORBIDDEN (never touch, even if it seems related):
  - src/lib/llm-client.test.ts
  - src/lib/cost-guard.ts
  - src/lib/orchestra-model-resolver.ts
  - src/lib/ai-team/roster.ts
  - src/lib/db/schema.ts
  - drizzle/**
  - ai-os/**

INPUT: Current src/lib/llm-client.ts with the "openai/gpt-oss-120b" row in MODEL_PRICING (around line 150) set to { promptPer1k: 0.000036, completionPer1k: 0.00018 }, sourced by an inline comment (lines 146-149) citing an openrouter.ai/api/v1/models cross-check from 2026-07-10.

OUTPUT: src/lib/llm-client.ts with the same row updated to { promptPer1k: 0.00015, completionPer1k: 0.0006 }, and its preceding comment block updated to state the corrected source (groq.com/pricing, $0.15/$0.60 per 1M prompt/completion tokens, verified 2026-07-18) and note that the prior 2026-07-10 openrouter.ai cross-check figure was understated ~3.3-4x versus Groq's own published rate.

STEPS:
  1. Open src/lib/llm-client.ts and locate the MODEL_PRICING constant (starts ~line 143) and the "openai/gpt-oss-120b" entry (~line 150) along with its preceding comment block (~lines 146-149).
  2. Change the entry's values from { promptPer1k: 0.000036, completionPer1k: 0.00018 } to { promptPer1k: 0.00015, completionPer1k: 0.0006 }.
  3. Rewrite the preceding comment to cite the corrected source and date (groq.com/pricing, verified 2026-07-18, $0.15/$0.60 per 1M prompt/completion tokens) and briefly note this replaces the prior 2026-07-10 openrouter.ai-derived figure, which undercounted real Groq spend by roughly 3.3-4x.
  4. Do not change the sibling "gpt-oss-120b" (Cerebras, no "openai/" prefix) row on the following line -- it is a distinct provider entry for the same underlying model and is out of scope for this fix.
  5. Do not change any other row in MODEL_PRICING.
  6. Run `bun run build` (or the repo's type-check script) to confirm no type errors were introduced.

CONSTRAINTS:
  - Touch only the "openai/gpt-oss-120b" row and its directly preceding comment block -- no other MODEL_PRICING row, no other function in this file.
  - Do not rename the model key, change the shape of the pricing record, or alter estimateCostUsd()'s logic.
  - Do not touch the sibling Cerebras "gpt-oss-120b" row (no "openai/" prefix) -- different provider, different verified price, not part of this bug.
  - All API routes / Drizzle / auth rules in CLAUDE.md are not implicated by this change, but the general repo rule against touching files outside files_allowed still applies.
  - Do not edit src/lib/llm-client.test.ts in this task -- that is BACKEND-002's job, which depends on this task merging first so it asserts against the corrected constant.
  - No live network calls to groq.com in code or tests -- the price is a hardcoded constant as today; this task only changes its documented source/value.

VALIDATION: `bun run build` (or project type-check) passes with no new errors; a manual read of the diff confirms only the openai/gpt-oss-120b row's two numeric fields and its comment changed, and the sibling Cerebras row is untouched.

DONE CRITERIA: The openai/gpt-oss-120b row in src/lib/llm-client.ts's MODEL_PRICING reads promptPer1k 0.00015 / completionPer1k 0.0006 with an updated, accurately-sourced comment, and nothing else in the file changed.

---
Before writing any code: read the actual current implementation of the relevant file(s) first -- do not assume this task's description is still accurate, the codebase may have moved since this task was written. If FILES ALLOWED is too narrow to actually complete the objective, stop and report this in PROGRESS.md as a scope-definition problem rather than silently touching forbidden files -- a supervisor will re-scope it, not you.
Maintain a PROGRESS.md file in the repository root with '## Completed' and '## Remaining' sections, each a markdown checklist. Update it as you complete each meaningful step.