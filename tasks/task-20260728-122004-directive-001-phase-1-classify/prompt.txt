Classify all 87 rows in sap_mapping.sqlite's sap_reports table into a new engine_track column (calculation | workflow | hybrid). Calculation = pure function, deterministic input->output, no state (e.g. warranty cost %, GST split, cost variance). Workflow = has state/transitions/SLAs (e.g. ticket status flow, warranty-expiry lifecycle, retention-release eligibility). Add the column if it does not exist, populate every row, do not guess -- read each report's calculation_logic field and classify on real content.