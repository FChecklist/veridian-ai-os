## OBJECTIVE
Phase 12 of the continuous gap-closing loop. Close item 50 from ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml (automated mechanical enforcement of the standing "genuinely irreversible actions require Owner confirmation" policy). Current PARTIAL evidence: AI_ENGINEERING_POLICY.yaml's automation_scope_boundary.standing_exceptions.genuinely_irreversible_actions (crontab modifications, production DNS changes, live customer data changes) is a real, written policy, but nothing on this host mechanically enforces it -- enforcement today depends entirely on assistant judgment at the moment of action, with no automated gate that would catch or block a task that silently makes one of these changes anyway. Build the missing mechanical gate for the crontab case (the only one of the three with live evidence of actually being touched by tasks on this host so far -- see KNOWN_CONTEXT on DNS/customer-data scope).

## SCOPE
FIRST: run `python3 /opt/veridian/scripts/task-gateway.py submit --text "close governance item 50: mechanical crontab-change enforcement gate in preflight-guard.py" --source ai_agent --session-id phase12` -- cite the actual JSON (duplicate_found/active_collision_task_ids) as your zero-duplication check, same discipline as phase 11.

Re-verify live before building anything, do not trust this prompt's framing without checking yourself:
1. `crontab -l` -- capture the current live crontab as your baseline-of-record for this phase.
2. `grep -n "def check_\|^if __name__" /opt/veridian/scripts/preflight-guard.py` -- confirm the check_*() + sequential-call-in-__main__ pattern this phase should extend (as of phase 11's own recon: check_circuit_breaker, check_tight_task_schema, check_disk, check_mem, check_worktree, check_credit_accountant_approval, check_proxy_health, check_openrouter_balance, called in that order at the bottom of the file). Confirm this is still accurate; the file may have changed since.
3. `grep -rln "subprocess.*crontab\|\"crontab\"\|'crontab'" /opt/veridian/scripts/*.py` -- as of phase 11's recon this returned NOTHING (no script programmatically calls the `crontab` binary; all crontab changes on this host happen via an agent running a raw shell `crontab` command during a task, which is exactly the gap -- nothing currently catches that). Re-confirm this is still true.

Build: add a new `check_crontab_unauthorized_change(task_dir)` function to `/opt/veridian/scripts/preflight-guard.py`, wired into the existing sequential call list in its `__main__` block (same pattern as the other check_* calls). Behavior: compare the live `crontab -l` output against a canonical last-known-approved snapshot file (introduce one if none exists, e.g. `/opt/veridian/ai-os/CRONTAB_APPROVED_SNAPSHOT.txt`, seeded from the CURRENT live crontab you captured in step 1 above -- today's live crontab is the approved baseline since no unauthorized change has been detected yet). If they differ, FAIL the gate (matching the fail()/ok() convention already used by every other check_* in this file) UNLESS the task's own prompt.txt KNOWN_CONTEXT section cites a specific `ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml` entry id with an explicit approved status for that exact change (mechanically check for that citation string in the task's prompt.txt, not just trust an unverified claim of approval). Write a real test: temporarily construct a task_dir with a prompt.txt lacking any such citation, feed it a crontab diff (mock the crontab -l call or use a temp snapshot file, do not touch the real live crontab to test this), and prove the gate actually fails closed. Then prove it still passes clean when crontab is unchanged from the snapshot. Do not touch the real live crontab at any point in this phase -- this item is about building the *detector*, not about making or approving any crontab change (that stays a standing Owner-confirmation item per the policy this gate itself enforces).

## KNOWN_CONTEXT
zero_duplication_check_performed: via task-gateway.py submit (see SCOPE step 1)
prior_finding: Phase 11 (2026-07-23) picked this item after re-checking `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` and confirming its own commit (closing item 29) was still the latest, then surveyed the remaining target list [1,3,4,6,15,25,36,45,50,51,54,60] for a well-scoped, non-Owner-blocked target: items 36/51/54 all turned out to have their AI-buildable half already done by prior phases, with their ONLY remaining gap being a crontab schedule addition that AI_ENGINEERING_POLICY.yaml explicitly reserves for Owner confirmation (already correctly routed to ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml by those phases, not re-litigated here) -- so none of those three had any real remaining AI-closable work. Item 50 is different in kind: it is not itself a request to MAKE a crontab/DNS change, it is a request to BUILD THE GATE that would mechanically enforce the Owner-confirmation requirement on any FUTURE task that tries to -- fully AI-buildable without needing Owner sign-off on this phase's own actions, since building a detector is not itself an irreversible action.
dns_scope_note: this host has no evidence anywhere in scripts/ or ai-os/ of a DNS-modifying code path (this is a single dev VM, not a DNS-authoritative production host) -- phase 11 could not find a live DNS analog to build a gate for. Scope this phase's build to crontab only. If you find live evidence a DNS change path actually exists somewhere in this codebase that phase 11 missed, extend scope to cover it too; otherwise explicitly note DNS as out-of-scope-by-non-applicability in your evidence, do not fabricate a DNS gate for a mechanism that isn't there.
items_21_24_still_blocked: unchanged since phase 10/11 -- Owner-side `adm` group grant still not done, stays PARTIAL, out of scope this phase too (see prior phases' PROGRESS.md history if needed, not restated in full here).
governance_file_provenance: this phase's own worker branch should fast-forward-merge or rebase onto worker/task-20260723-162833-gap-closing-phase11-item29-auth-verifica (commit 59091f9 and later) rather than branching from master or the phase10 branch, since phase11's item-29 closure is now the latest canonical version of ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml. Check `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` yourself first in case another phase landed after phase 11.
task_gateway_close_defect_history: task-gateway.py close --audit-cmd requires the string to be a VERBATIM SUBSTRING of this file's own ## SUCCESS_CRITERIA section AND a real, directly-executable bash -c command -- phase 10 got this wrong (prose, not a command) and its own close attempt failed as a result (logged ACT-20260723-162614-43d2). Phase 11 wrote its SUCCESS_CRITERIA as a literal command for exactly this reason and its own close succeeded (audit_verdict: DONE, checkpoint_status: completed). This phase's SUCCESS_CRITERIA below is written the same way -- copy it verbatim at close time.

## SUCCESS_CRITERIA
grep -q "def check_crontab_unauthorized_change" /opt/veridian/scripts/preflight-guard.py && grep -q "check_crontab_unauthorized_change(" /opt/veridian/scripts/preflight-guard.py

## EXPECTED_OUTPUT
UPDATE_FILE: ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml item 50 -> DONE (or leave PARTIAL with fresh honest evidence if the build turns out incomplete), with the exact commands you ran, the real test proving the gate fails closed on an unauthorized diff and passes clean on none, and the real file diff for preflight-guard.py.
COMMIT+PUSH: to this phase's own worker branch (claude-control repo).
CHECKPOINT: `python3 /opt/veridian/scripts/task-gateway.py close --task-id <own-id> --audit-cmd "<copy the SUCCESS_CRITERIA line above VERBATIM>" --evidence "<your real grep/test output, or the audit_id of a failed attempt>"` -- cite the actual result either way.
NEXT_PHASE: create AND start Phase 13 with a real named target from the remaining list [1,3,4,6,15,25,36*,45,45,51*,54*,60] (* = already fully AI-buildable-portion-done, Owner-blocked on the cron half -- do not re-pick these unless you find NEW evidence the Owner already approved the specific cron change; 21/24 stay excluded, Owner-blocked on a different unrelated permission). Re-verify whichever target you pick against `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` first.

## CONSTRAINTS
Scope stays limited to building the crontab-change detection gate and its real test, since the underlying policy already exists in writing (AI_ENGINEERING_POLICY.yaml) -- this phase does not re-litigate or change that policy's substance. Do not make, approve, or simulate approval of any real crontab change as part of this phase; the only crontab-adjacent artifact this phase produces is the read-only approved-snapshot baseline file and the detector code, both of which must be provably inert against the current live crontab.

## COMPLEXITY_TIER
judgment