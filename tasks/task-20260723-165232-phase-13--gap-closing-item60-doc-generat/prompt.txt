## OBJECTIVE
Phase 13 of the continuous gap-closing loop. Close item 60 from ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml (automatic documentation generation for every script/service/log). Current PARTIAL evidence: generate_task_checklist.py, generate-system-diagram.py, and generate_quick_reference.py are real, working generator scripts (re-verify this claim yourself, do not trust it blindly), but nothing invokes them automatically -- a human/AI has to remember to run them by hand, so MASTER_INDEX.yaml and related generated docs can silently go stale between runs. Wire automatic invocation into system-sync.py, which already executes periodically -- see CONSTRAINTS for the one hard boundary on how this must be built.

## SCOPE
FIRST: run `python3 /opt/veridian/scripts/task-gateway.py submit --text "close governance item 60: automatic documentation generation wired into existing scheduled entry point" --source ai_agent --session-id phase13` -- cite the actual JSON (duplicate_found/active_collision_task_ids) as your zero-duplication check, same discipline as phases 11/12.

Re-verify live before building anything, do not trust this prompt's framing without checking yourself:
1. `grep -n "def main\|argparse\|open(.*['\"]w" /opt/veridian/scripts/generate_task_checklist.py /opt/veridian/scripts/generate-system-diagram.py /opt/veridian/scripts/generate_quick_reference.py` -- confirm each already writes its output to a real file (not stdout-only -- Phase 12's own prep recon on this same target already found this true as of 2026-07-23, but re-confirm, the files may have changed).
2. `grep -n "def main\|def mirror_drift_check\|def constitution_staleness_check\|def unindexed_files_check\|def resume_balance_blocked_check" /opt/veridian/scripts/system-sync.py` -- confirm the existing `--check {mirror,constitution,unindexed,resume-balance,all}` structure this phase should extend is still accurate, and confirm what periodic mechanism actually invokes this file today (Phase 12's recon found a 6-hourly invocation; re-derive this yourself rather than trusting that number).

Build: add a new check function to `/opt/veridian/scripts/system-sync.py` (e.g. `documentation_generation_check()`), following the same pattern as its existing checks (independent, individually skippable via a new `--check` choice, also included in `--check all`). It should invoke the three generator scripts named above as real subprocess calls (or direct function imports if cleaner) and report success/failure of each via the existing `findings`/`append_attention` convention already used by the other checks in this file. Confirm item 50's Phase-12 gate stays green throughout by diffing live state against its snapshot before and after your change (see CONSTRAINTS -- this phase's work should produce no delta there at all). Write a real test: run the new check both via `--dry-run` (matching the file's existing convention) and for real, and cite actual stdout proving the three generators ran and their output files' mtimes updated.

## KNOWN_CONTEXT
zero_duplication_check_performed: via task-gateway.py submit (see SCOPE step 1)
prior_finding: Phase 12 (2026-07-23) closed item 50 (mechanical enforcement gate for the standing policy referenced in CONSTRAINTS) after re-checking `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` and confirming its own commit (8b0877f) was the latest, then surveyed the remaining target list [1,3,4,6,15,25,36,45,60] (36/51/54 excluded, blocked on the same standing exception referenced in CONSTRAINTS; 21/24 excluded, blocked on the unrelated `adm` group permission) -- picked item 60 for Phase 13 specifically because its remaining gap (generators exist but nothing calls them periodically) can be closed by adding code inside a script that already runs periodically, fully AI-closable within the CONSTRAINTS boundary below.
item_60_current_partial_evidence: "RULES_ARTICLES_198.json mandates complete documentation; ai-os/MASTER_INDEX.yaml registry is real (86 entries as of 2026-07-22). But generate_quick_reference.py, generate-system-diagram.py, generate_task_checklist.py are manual-trigger only (print to stdout for hand-paste) and are not wired into any periodic mechanism -- documentation requires a human/AI to remember to run and splice these in, not an automatic process." -- Phase 12's prep recon found the "print to stdout for hand-paste" framing is now STALE (all three scripts already write real output files directly, confirmed by grep) -- re-verify this yourself as SCOPE step 1; if still true, note the correction in your evidence rather than restating the stale claim.
governance_file_provenance: rebase/fast-forward onto worker/task-20260723-164109-gap-closing-phase12-item50-crontab-enfor (commit 8b0877f and later), the branch holding Phase 12's item-50 closure, which is now the latest canonical version of ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml. Check `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` yourself first in case another phase landed after Phase 12.
task_gateway_close_defect_history: task-gateway.py close --audit-cmd requires the string to be a VERBATIM SUBSTRING of this file's own ## SUCCESS_CRITERIA section AND a real, directly-executable bash -c command -- phase 10 got this wrong (prose, not a command) and its own close attempt failed as a result (logged ACT-20260723-162614-43d2). Phases 11 and 12 wrote SUCCESS_CRITERIA as a literal command for exactly this reason and both closes succeeded. This phase's SUCCESS_CRITERIA below is written the same way -- copy it verbatim at close time.

## SUCCESS_CRITERIA
grep -q "def documentation_generation_check" /opt/veridian/scripts/system-sync.py

## EXPECTED_OUTPUT
UPDATE_FILE: ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml item 60 -> DONE (or leave PARTIAL with fresh honest evidence if the build turns out incomplete), with the exact commands you ran and the real stdout proving the three generators ran through system-sync.py.
COMMIT+PUSH: to this phase's own worker branch (claude-control repo).
CHECKPOINT: `python3 /opt/veridian/scripts/task-gateway.py close --task-id <own-id> --audit-cmd "<copy the SUCCESS_CRITERIA line above VERBATIM>" --evidence "<your real grep/test output, or the audit_id of a failed attempt>"` -- cite the actual result either way.
NEXT_PHASE: create AND start Phase 14 with a real named target from the remaining list [1,3,4,6,15,25,36*,45,51*,54*] (* = already fully AI-buildable-portion-done, blocked on the same standing exception -- do not re-pick these unless you find NEW evidence the Owner already approved that specific change; item 6 needs a faster check interval to fully close -- consider whether a mechanism other than the one named in CONSTRAINTS could deliver that before assuming it is blocked like 36/51/54; 21/24 stay excluded, blocked on a different unrelated permission). Re-verify whichever target you pick against `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` first.

## CONSTRAINTS
AI_ENGINEERING_POLICY.yaml's genuinely_irreversible_actions exception reserves persistent/standing server-config changes for explicit Owner confirmation, and item 50 (Phase 12) now mechanically enforces that at the preflight layer. Stay inside code changes to already-scheduled scripts for this build; leave timing/schedule configuration itself untouched, and if true closure needs that piece, defer it to ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml the way items 36/51/54 already do.

## COMPLEXITY_TIER
judgment