## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not a fixed-pattern operation): the schema change itself (one ALTER TABLE) is trivial, but correctly tagging the 18 untagged ops scripts is not -- each script's real module/status/priority must be inferred by reading its own free-text header docstring and understanding what it actually does (the 12 already-tagged scripts were not tagged by a fixed naming convention a regex could replicate; each was a real semantic read of what that specific script does). A wrong inferred tag is worse than no tag, since ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml's evidence trail depends on it being accurate. Extend the existing tagging capability (no new tagging system) so Parts 14-22 of ai-os/VERIDIAN_EXECUTION_RULES_2026-07-23.md move from PARTIAL/MISSING toward DONE for two concrete gaps in ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml: (1) 18 of 28 ops scripts in MASTER_INDEX.yaml's file_inventory have no module/status/priority tag; (2) superboss-register.py's system_index table has no tag column at all.

## SCOPE
In scope: (1) add a nullable tags column (TEXT, JSON-encoded list) to superboss-register.py's existing system_index table via ALTER TABLE -- the correct zero-duplication approach per ai-os/STANDING_DIRECTIVE.yaml precheck, plus a --tags argument to the existing index-add subcommand and a --tag filter on the existing search subcommand. (2) tag the 18 currently-untagged ops scripts named in MASTER_INDEX.yaml's file_inventory with a module tag (e.g. monitoring, logging, dispatch, audit, sync) inferred from each script's own header docstring, not invented. (3) update ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml's Part 14/15/20 entries with the new real evidence. This phase touches only claude-control's superboss-register.py and MASTER_INDEX.yaml, not the compliance-tracker repo's platform_assets Universal Metadata Registry. For Parts 16-19/21/22: if the system_index tags column change above moves their status, update the evidence; otherwise leave their status as-is with a one-line note explaining why, since building further mechanisms for them is a separate future phase.

## KNOWN_CONTEXT
This is phase 1 of a roadmap set out in ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml's roadmap_next_phases (produced by task-20260723-052857-execution-rules-phase0-analysis-and-buil, commit f71aaa9 in the claude-control repo). That same phase-0 task found and fixed a real ai-os/memory/superboss-register.sqlite corruption incident (now resolved, verified via postflight_audit_gate.py AUD-20260723-055909-480161) -- read that audit file's critical_finding_this_phase field first so this is not re-discovered. Check ai-os/STANDING_DIRECTIVE.yaml precheck (zero-duplication mandatory) and run superboss-register.py check-duplicate before writing any new script; this phase should not need any new script, only an ALTER TABLE and CLI-argument additions to the existing superboss-register.py.

## SUCCESS_CRITERIA
system_index table has a real tags column, populated for at least the 18 previously-untagged scripts, queryable via a working --tag filter on the search subcommand (demonstrate with one real query and its real output). ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml Part 14/15/20 entries updated with real post-change evidence (before/after tagged-script counts). Verified via postflight_audit_gate.py, not self-certified.

## EXPECTED_OUTPUT
Commit and push: the superboss-register.py diff, the updated ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml. Checkpoint status=pending_review citing the real audit_id/work_item_id from postflight_audit_gate.py. Send exactly one notify-owner.py email at the end of this phase with real evidence (commit hash, before/after tag counts, audit id). If this phase reaches a clean stopping point, self-dispatch phase 2 (pre_execution_checklist_automation, parts 38b/39/40, per the same roadmap file) via python3 scripts/veridian-task.py create -- this is the 2nd of a hard cap of 3 self-dispatched phases total set by the original phase-0 task; if nothing further is soundly scopeable, stop and say so instead of forcing a phase 3.

## CONSTRAINTS
Every completion claim in the checkpoint note must cite specific file paths, commit hashes, or command output as evidence.

## COMPLEXITY_TIER
judgment