## OBJECTIVE
Implement the Owner's 19-point mandatory-gate protocol (full text registered in knowledge_engine as KE-20260725-061008-8423, saved at ai-os/OWNER_DIRECTIVES/OWNER_ENGINE_MANDATORY_GATE_2026-07-25.txt -- read that file in full first, it is the authoritative spec, this OBJECTIVE only summarizes it): every raw Owner chat must be processed by the OWNER_ENGINE software (scripts/prompt_gateway/gateway.py, already built and merged in PR #60 -- do not rebuild it) BEFORE any AI reads or analyzes it, producing a machine-language/.yaml output prompt that AI works from instead of the raw text; all AI<->software and AI<->AI communication becomes strictly close-ended machine language; the Owner-facing channel becomes strictly simple 1-line English with a default-recommended option.

## KNOWN_CONTEXT
- scripts/prompt_gateway/gateway.py already exists, works, and is proven (real init, real stdin processing, real noise/token-reduction output) -- this task wires it into the mandatory path, it does not rebuild it.
- This directive tightens two pre-existing standing rules already in effect this session: machine-language-only written artifacts on this server, and a two-channel split (machine language to the server CLI, simple English to the Owner-facing laptop chat). This task's job is to make the OWNER_ENGINE gate itself real and enforced, not just a convention someone might forget -- the mechanism must make it structurally hard to skip, not just documented as a rule.
- The real place this needs to bite: any raw Owner instruction text (submitted via python3 scripts/task-gateway.py submit --text "...") should be run through gateway.py first, and the resulting machine-language output -- not the raw --text value -- should be what instruction validation/classification/dispatch actually operates on downstream.
- "AI can analyze the output prompt, ask this software query in machine language, get response, decide. Only if Software is not able to tell, than AI will ask the OWNER" -- this requires a real query mechanism: a way for an AI session (interactive or headless worker) to ask scripts/prompt_gateway/gateway.py (or its constituent engine/*.py modules) a follow-up machine-language question about an already-processed chat and get a structured machine-language answer back, without re-processing the whole raw text from scratch.
- The Owner's own scope note (do not miss this): "AFTER ITS IMPLEMENTED give a 1 line prompt that I will paste with the task and it understands that the chat not to be read, the chat has to be pasted in OWNER_ENGINE software in SUPERBOSS IN SERVER, run the OWNER_ENGINE software and work on its output prompt only." -- this task must produce a real, tested draft of that one-line trigger phrase as part of its deliverable, but the phrase is only handed to the Owner after independent verification outside this task (do not report this as delivered to the Owner yourself -- that final handoff step happens separately).

## SCOPE
1. Add a real `--gate` mode (or equivalent) to python3 scripts/task-gateway.py submit that, when the raw --text is Owner-sourced, pipes it through scripts/prompt_gateway/gateway.py first and uses gateway.py's real machine-language output (its final_output / machine_prompt field) as the instruction text that downstream validation/classification actually processes -- not the raw text. Make this the real default behavior for --source owner, not an opt-in flag nobody remembers to pass.
2. Build a real, minimal machine-language query interface: a CLI entrypoint (e.g. scripts/prompt_gateway/query.py, or a `--query` mode on gateway.py) that takes a chat_id (from a prior gateway.py run) plus a structured machine-language question and returns a structured machine-language answer using that chat's saved classification/context/entities -- no new raw-text reprocessing, no free-prose response.
3. Write a real, concrete decision procedure (as executable logic, not just prose) for "software could not tell -> ask Owner": define the specific real conditions under which the query interface in step 2 returns a genuine "insufficient information" signal (a structured field, not an English explanation), and confirm that signal is what should trigger an Owner-facing question -- wire this as a real, testable code path, not a documentation-only convention.
4. Establish the close-ended machine-language convention for AI<->AI and AI<->any-software calls as a real, reusable schema (e.g. a small JSON Schema or YAML contract under ai-os/) rather than an unenforced guideline -- and retrofit at least one real existing AI<->software call site on this server (pick one genuinely representative example, e.g. how a worker or the supervisor invokes another script/engine) to actually use it, as proof the convention is usable in practice, not just specified on paper.
5. Draft the real one-line Owner-facing trigger phrase per KNOWN_CONTEXT's note above, and real-world test it: run it once through the actual new --gate path with a realistic sample raw chat, and paste the real before/after (raw chat in, machine-language output prompt out) in your final checkpoint as proof the phrase's implied behavior actually happens end to end.
6. Write ai-os/OWNER_ENGINE_MANDATORY_GATE_IMPLEMENTATION_2026-07-25.yaml documenting what shipped, the real test evidence from step 5, and an honest list of any of the 19 points that remain aspirational/not mechanically enforced yet (e.g. if "AI will not waste tokens" or similar points can only be a practiced discipline rather than something code can enforce, say so plainly rather than claiming a code mechanism for something that is really a behavioral guideline).
7. Register in MASTER_INDEX.yaml (registries.owner_engine_mandatory_gate) and via superboss-register.py register-knowledge, linking back to KE-20260725-061008-8423.

## SUCCESS_CRITERIA
- `python3 scripts/task-gateway.py submit --text "<a realistic raw multi-sentence Owner-style instruction>" --source owner --session-id <id>` real-runs and its real output/logs show the text was routed through scripts/prompt_gateway/gateway.py before instruction creation (cite the real evidence -- a log line, a saved chat_id, or equivalent -- not a claim).
- A real query against the interface from SCOPE item 2 (using a chat_id from the test in the line above) returns real structured machine-language output, not prose.
- `test -f ai-os/OWNER_ENGINE_MANDATORY_GATE_IMPLEMENTATION_2026-07-25.yaml` exits 0.
- The retrofitted call site from SCOPE item 4 still passes its own existing tests/real invocation after the change (show the real before/after invocation output).

## EXPECTED_OUTPUT
Real committed and pushed code (PR on claude-control) for the gate wiring in task-gateway.py, the query interface, the machine-language contract schema, the retrofitted call site, the real end-to-end test evidence, the documentation file, the MASTER_INDEX + knowledge-engine registration, and a final checkpoint containing the drafted one-line Owner trigger phrase plus an honest breakdown of which of the 19 points are now mechanically real versus which remain a practiced-discipline-only guideline.

## CONSTRAINTS
For each of the 19 numbered points in the source directive, the checkpoint's honesty breakdown (EXPECTED_OUTPUT above) must classify it as either code-enforced or discipline-only -- a point with no code path behind it goes in the discipline-only bucket, full stop, regardless of how central it feels to the overall goal.

## COMPLEXITY_TIER
judgment
