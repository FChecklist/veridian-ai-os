Classify all 87 rows in sap_mapping.sqlite's sap_reports table into a new engine_track column (calculation | workflow | hybrid). Calculation = pure function, deterministic input->output, no state. Workflow = has state/transitions/SLAs. Add the column if missing, populate every row from real calculation_logic content, do not guess.
