## OBJECTIVE
Phase 11 of the continuous gap-closing loop. Formally close item 29 from ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml (real Claude Max auth vs GLM/OpenRouter proxy routing) -- Phase 10's re-verification (per v2.3 single_source_of_truth_rule, "re-verify before creating the next phase") found the live host state has ALREADY moved past the file's recorded PARTIAL status: supervisor-entrypoint.sh was switched to real Claude Max auth on 2026-07-23 (comment + code both confirm, all 3 of CLAUDE_CODE_OAUTH_TOKEN/ANTHROPIC_API_KEY/ANTHROPIC_BASE_URL are now `unset`, no proxy export line remains), --effort high is now set on every claude -p invocation (worker-entrypoint.sh, supervisor-entrypoint.sh, doc-worker-entrypoint.sh, credit-accountant.py, master-decompose.py), and veridian-glm-proxy.service is now disabled+inactive. None of this has been committed to the governance file yet -- that paperwork gap is this phase's job, plus a real re-verification (do not just trust Phase 10's re-check, confirm it yourself with fresh commands).

## SCOPE
FIRST: run `python3 /opt/veridian/scripts/task-gateway.py submit --text "close governance item 29: real Claude Max auth verification, supervisor-entrypoint.sh + effort flag + glm-proxy service" --source ai_agent --session-id phase11` -- cite the actual JSON (duplicate_found/prior_search_results) as your zero-duplication check.

Re-verify live, do not trust any prior phase's claim without checking yourself:
1. `grep -n "CLAUDE_CODE_OAUTH_TOKEN\|ANTHROPIC_API_KEY\|ANTHROPIC_BASE_URL" /opt/veridian/scripts/supervisor-entrypoint.sh /opt/veridian/scripts/worker-entrypoint.sh /opt/veridian/scripts/launch-interactive-claude.sh` -- confirm all three are `unset` (no `export ANTHROPIC_BASE_URL=http://127.0.0.1:8787` / proxy-routed key line) in every file that launches a real `claude -p`/`claude` session.
2. `grep -rn -- "--effort" /opt/veridian/scripts/*.sh /opt/veridian/scripts/*.py` -- confirm every real `claude -p` dispatch line (worker-entrypoint.sh, supervisor-entrypoint.sh, doc-worker-entrypoint.sh, credit-accountant.py, master-decompose.py) actually has `--effort high` set (this was the specific sub-gap the prior PARTIAL evidence flagged as missing).
3. `systemctl --user status veridian-glm-proxy.service` -- confirm `disabled`/`inactive`, and separately settle the still-open question the prior evidence left dangling: does anything in this codebase still have a legitimate reason to keep this unit installed (even disabled), or should it be fully removed? State your judgment call explicitly either way -- this is the judgment-call part of this item, the mechanical grep checks above are not.
4. Check `~/.claude/settings.json` for `model=sonnet` (already confirmed DONE previously, do not re-derive, just confirm it's still true).

If any of 1-3 comes back showing the gap is NOT actually closed (i.e. Phase 10's re-check or this phase's own re-check was wrong), report that honestly as still-PARTIAL with the specific real evidence -- do not force a DONE status to match this prompt's expectation.

## KNOWN_CONTEXT
zero_duplication_check_performed: via task-gateway.py submit (see SCOPE step 1)
prior_finding: Phase 10 (2026-07-23) re-verified this live (not from stale file evidence) while doing its own required re-verification of items 21/24/29 before dispatching this phase -- see this session's own action log (superboss-register.sqlite, source=ai_agent, around ts 16:24-16:26 on 2026-07-23) for the exact grep/systemctl output it found. That was a spot-check, not a full audit -- re-run it yourself per SCOPE above rather than citing it as your own evidence.
items_21_24_still_blocked: re-verified same session -- current user (rajat, groups rajat/sudo/docker) still has no `adm` group membership, so /var/log/auth.log stays unreadable and scripts/security-check.py's SSH-half still returns a real permission-denied error with empty findings. This is a genuine Owner-side system permission grant, not something this phase (or any AI agent) can close via software -- do NOT attempt to re-open or re-scope 21/24 this phase; they stay PARTIAL, blocked on ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml, unless you find NEW evidence the Owner already granted it.
governance_file_provenance: ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml's canonical version (per STANDING_DIRECTIVE.yaml's single_source_of_truth_rule) is now on worker/task-20260723-161158-gap-closing-phase10-ai-supervision-loggi (commit 052d1ec and later) -- branch this phase's own workspace from there (or merge it in) rather than from master, which is still missing phases 5-10's accumulated governance edits. Check `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` yourself before assuming any specific branch/commit is still the latest -- another phase may have landed in between.
task_gateway_close_defect_found_phase10: `task-gateway.py close --audit-cmd` requires the string to be a VERBATIM SUBSTRING of this file's own `## SUCCESS_CRITERIA` section AND a real, directly-executable `bash -c` command (postflight_audit_gate.py actually runs it). Phase 10's own SUCCESS_CRITERIA was authored as prose describing what to prove, not as a literal command, so its own close attempt could not succeed (verified, logged as ACT-20260723-162614-43d2) -- this is a real, disclosed, not-yet-fixed gap in how PHASE prompt.txt files are authored upstream of this task (out of this phase's scope to fix the generator, but IN scope to not repeat the same mistake). This phase's own SUCCESS_CRITERIA below is deliberately written AS a literal shell command for exactly this reason -- copy it verbatim at close time, do not paraphrase it.

## SUCCESS_CRITERIA
! grep -q 'ANTHROPIC_BASE_URL=http://127.0.0.1:8787' /opt/veridian/scripts/supervisor-entrypoint.sh && grep -q -- '--effort high' /opt/veridian/scripts/worker-entrypoint.sh && grep -q -- '--effort high' /opt/veridian/scripts/supervisor-entrypoint.sh && ! systemctl --user is-active --quiet veridian-glm-proxy.service

## EXPECTED_OUTPUT
UPDATE_FILE: ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml item 29 -> DONE (or leave PARTIAL with fresh honest evidence if your own re-verification contradicts Phase 10's spot-check), with the exact commands you ran and their real output as evidence, plus your explicit judgment call on the veridian-glm-proxy.service disposition question (SCOPE step 3).
COMMIT+PUSH: to this phase's own worker branch (claude-control repo).
CHECKPOINT: `python3 /opt/veridian/scripts/task-gateway.py close --task-id <own-id> --audit-cmd "<copy the SUCCESS_CRITERIA line above VERBATIM>" --evidence "<your real grep/systemctl output, or the audit_id of a failed attempt>"` -- cite the actual result either way, same discipline as Phase 10.
NEXT_PHASE: create AND start Phase 12 with a real named target from the remaining list [1,3,4,6,15,25,36,45,50,51,54,60] (21/24 excluded -- Owner-blocked, see KNOWN_CONTEXT). Re-verify whichever target you pick against `git log --all --oneline -- '*GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml'` first in case another phase already closed it on a branch this phase didn't start from.

## CONSTRAINTS
Scope stays limited to item 29's re-verification and governance-file closure, since it is an already-live-fixed item, not new construction. Items 21 and 24 stay out of scope this phase, tracked separately (see KNOWN_CONTEXT).

## COMPLEXITY_TIER
judgment