## OBJECTIVE
Fix the real root cause of a confirmed mass-mislabeling incident in the backlog-dedup tooling: scripts/plan_backlog_completion.py (currently only on unmerged branch chore/backlog-completion-plan-script, open PR #102, claude-control repo -- it has never itself been merged or deployed to /opt/veridian/scripts/) is a READ-ONLY planner: it groups repeat task attempts by naive slug/rca-prefix regex matching (build_groups()) and computes a per-group classification (classify()), then writes ONLY a JSON+Markdown plan (ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.{json,md}). It contains NO code path that ever writes status/duplicate_of/superseded_reason into any real ai-os/tasks/*/task.yaml. Despite that, on 2026-07-27, 32 real task.yaml files were hand-edited to set status=superseded with superseded_reason text citing "Collapsed by plan_backlog_completion.py dedup 2026-07-27", falsely attributing an automated collapse decision to a tool that never made one and never performed a write. Independently cross-checking all 32 against that same plan run's own JSON classification field shows only 2 of 32 have a classification category that even arguably means "already done" (ALREADY_DONE_STALE_STATUS); the other 30 have categories the plan itself labeled PR_PENDING_MERGE (real open PRs awaiting merge -- explicitly "do not redispatch" but also never meant "safe to silently mark duplicate"), PR_CLOSED_NEEDS_REDISPATCH, SUBSTANTIAL_WORK_NO_PR_NEEDS_REVIEW, or OWNER_OR_REVIEWER_DECISION_NEEDED -- none of which mean "collapse, work is done." This task closes both the missing-real-verification gap in the tool's own classify()/build_groups() logic AND the missing-real-apply-mechanism gap (there is currently no auditable code path that performs a task.yaml collapse at all -- today it can only happen by an unaudited hand-edit, which is exactly what caused this incident).

## KNOWN_CONTEXT
Read scripts/plan_backlog_completion.py in full first (git show chore/backlog-completion-plan-script:scripts/plan_backlog_completion.py -- 405 lines, single commit b8c65ed, PR #102 still OPEN/unmerged). Key functions: build_groups() (~line 97) groups tasks purely via RCA_TARGET_RE = r'^rca-task-(\d{8})-(\d{6})-' matching an "rca-task-<date>-<time>-" title prefix back to the original task created at that exact timestamp -- pure lineage/naming-convention matching, zero content verification. classify() (~line 195) then, for the resolved group, regex-scans steps_text() (completed_steps/remaining_steps fields) across ALL tasks in the group for "PR #(\d+)" mentions, takes the MAX PR number found anywhere in that combined text, checks its live `gh pr view` state, and if ANY such referenced PR shows mergedAt set, labels the whole group ALREADY_DONE_STALE_STATUS with action "patch task.yaml status to completed" -- with no check that the merged PR's actual diff has anything to do with the original task's own SCOPE/SUCCESS_CRITERIA. Neither function ever writes to task.yaml; the script's only file output is via argparse --json-out/--md-out (default ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.{json,md}); grep confirms zero yaml.dump/open(...,'w') calls targeting task.yaml anywhere in the file.

Concrete historical fixture case (use exactly this data for the regression test -- do not invent synthetic substitutes for it, this is the real incident): task-20260726-083946-fix-task-lifecycle--real-branch-resoluti (claude-control repo) was dispatched with SCOPE/SUCCESS_CRITERIA to fix two real gaps in scripts/supervisor-entrypoint.sh + scripts/veridian-task.py + scripts/tight_task_validation.py: (1) task.yaml's 'branch' field going stale after a worker pushes to a different branch than its own, and (2) a HOLD_FOR_OWNER_SIGNOFF prompt marker having zero real enforcement. Its own SUCCESS_CRITERIA (read ai-os/tasks/task-20260726-083946-fix-task-lifecycle--real-branch-resoluti/prompt.txt for the exact, verbatim, currently-real text -- do not paraphrase it) is mechanically runnable: `python3 -m pytest tests/ -k "branch_resolution or hold_for_signoff" -v` plus two grep checks. The task's own worker DID produce a real, complete implementation (checkpoint commit 6efac81 on branch worker/task-20260726-083946-fix-task-lifecycle--real-branch-resoluti at 2026-07-26T08:52:33Z, "invocation failed, exit 1" -- checkpointed anyway per the auto-checkpoint design), but that specific tracked branch/task was NEVER merged; instead, a content-identical fix landed on master completely independently 41 minutes later as commit e6c7049 (2026-07-26T09:33:13Z, "Fix stale PR branch + prose-only hold-for-signoff in task lifecycle"), via a totally different, untracked path: PR #81 (same content, opened from the tracked lineage) was CLOSED without merging (a separate incident, already logged in this assistant's own memory, where its branch was mistakenly deleted after a misread "already merged" audit comment), then recovered by force-pushing the still-reachable commit object to a new branch and reopening as PR #84 ("Recover lifecycle fix e6c7049..."), which WAS merged (2026-07-26T10:19:37Z) -- and PR #84 is not referenced anywhere in task-20260726-083946's or its RCA chain's own checkpoint text, so no lineage-based or "diff the successor's commits" check would ever find it. Two follow-up refinements (cdbf6d7, 0bd63d1) landed on master afterward. Confirmed as of this investigation: `python3 -m pytest tests/ -k "branch_resolution or hold_for_signoff" -v` run against current master passes 8/8; both regression-test files the original SUCCESS_CRITERIA required (tests/veridian_task_branch_resolution_test.py, tests/hold_for_signoff_test.py) exist on master. So the original task's real objective genuinely IS satisfied on master today -- just not via any task/PR any dedup routine currently tracks.

Separately, and unrelated to any of the above: an RCA chain was later dispatched against task-20260726-083946 because ITS OWN task.yaml kept looping with 9 identical "PRE-FLIGHT REJECTED (crontab_unauthorized_change, transient)" checkpoints for ~11 hours (unrelated cause: this same day's OOM/crontab-stop incident, see this assistant's own memory on the Hetzner server for full detail) -- task-20260726-195236-rca-task-20260726-083946-fix-task-lifecy, then task-20260727-034446-rca-task-20260726-083946-fix-task-lifecy (merged as claude-control PR #104, `gh pr view 104 --json body` confirms its actual diff only adds `crontab_unauthorized_change` to worker-entrypoint.sh's/doc-worker-entrypoint.sh's hard-stop reason lists plus one new regression test -- nothing about branch resolution or hold-for-signoff). plan_backlog_completion.py's own build_groups() aliased task-20260726-083946 into the SAME group as this RCA chain purely because of the "rca-task-20260726-083946-..." title-prefix match, and its own classify() output for that group (verify via `python3 -c "import json; d=json.load(open('/opt/veridian/ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.json')); [print(i) for i in d['issues'] if '083946' in i['representative_task_id']]"`) is actually category="PR_CLOSED_NEEDS_REDISPATCH", action="PR #81 was closed without merging -- needs fresh redispatch with root-cause fix." -- i.e. the tool's own real output for this exact case said "redispatch," not "collapse." Whatever hand-edited task-20260726-083946/task.yaml to status=superseded / duplicate_of=task-20260726-195236-... / superseded_reason="Collapsed by plan_backlog_completion.py dedup 2026-07-27 -- same underlying issue as task-20260726-195236... (3 total attempts in chain)" directly contradicted the cited tool's own real output while falsely attributing the decision to it.

A saved audit script already exists at (ephemeral, /tmp on the server, may not survive a reboot -- re-derive if gone) /tmp/audit_dedup_mismatches.py: it loads ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.json, builds a task_id -> classification.category map from all_attempt_ids across all 47 issues, then for every ai-os/tasks/*/task.yaml with status=superseded and "plan_backlog_completion" in its superseded_reason, checks whether that task's own classification category was ALREADY_DONE_STALE_STATUS. Result: 32 total such task.yaml files; only 2 have category ALREADY_DONE_STALE_STATUS; the other 30 have categories PR_PENDING_MERGE, PR_CLOSED_NEEDS_REDISPATCH, SUBSTANTIAL_WORK_NO_PR_NEEDS_REVIEW, or OWNER_OR_REVIEWER_DECISION_NEEDED. Re-run and re-verify this count yourself against the live filesystem rather than trusting this number blindly (per this project's own standing rule against repeating unverified claims) -- treat the script as a verified starting point to extend, not a black box to trust.

ROUND 2 (this task was already dispatched once; a prior invocation reached pending_review, PR #102 got a real, structured code-review pass, and it correctly came back Verdict: fail -- read that review's own real comment fresh via `gh pr view 102 --json comments -q '.comments[0].body'` rather than trusting this paraphrase, since it is the authoritative, current version). Summary of what it found, confirmed independently real: (a) the actual live executable that performed this morning's incident is NOT the git-tracked scripts/plan_backlog_completion.py at all -- it is /opt/veridian/ai-os/execute_backlog_plan.py (confirmed real, present, executable, dated 2026-07-27 03:37, containing phase1_mark_duplicates()/phase2_mark_already_done()/etc. that call save_task_yaml() -> open(path,"w") directly), driven by a second untracked file /opt/veridian/ai-os/plan_backlog_completion.py (confirmed real, present, dated 2026-07-27 03:09) -- both live entirely outside any git repo and are untouched by the round-1 diff, meaning the live pipeline that caused the incident is still fully capable of reproducing it verbatim; (b) the new --apply path's main() only writes status=superseded to non-representative duplicates, never patches the representative/original task's own status to 'completed' despite classify()'s own ALREADY_DONE_STALE_STATUS action text promising exactly that; (c) verify_success_criteria()'s command execution uses subprocess.run(cmd, shell=True, ...) gated only by a weak cmd.startswith(('python3 -m pytest','pytest ','grep ')) check in extract_verifiable_commands() -- not a real allowlist, and a real shell-injection risk on text sourced from prompt.txt.

## SCOPE
1. Fix the root cause in plan_backlog_completion.py (continue on its existing branch chore/backlog-completion-plan-script / PR #102 -- push additional commits there rather than opening a rival PR for the same file, since it has never been merged): before classify() concludes ALREADY_DONE_STALE_STATUS (or any category that a future apply step might treat as collapse-eligible) for an issue, it must parse the ORIGINAL task's own SUCCESS_CRITERIA field from its prompt.txt (reuse scripts/tight_task_validation.py's existing parse_labeled_fields() -- do not write a second parser), extract whichever SUCCESS_CRITERIA lines are mechanically runnable shell commands (pytest invocations, grep patterns against named files -- this project's existing prompts already write SUCCESS_CRITERIA in exactly this runnable style, see the fixture case above), and actually run them against the real current state of the default branch (not just scan the group's own checkpoint prose for a PR-number mention) before ever concluding the objective is satisfied. If SUCCESS_CRITERIA cannot be mechanically parsed/run (prose-only), the result must be a distinct, clearly-labeled "cannot auto-verify, needs human read" category -- never silently treated as equivalent to a verified pass.
2. Build the real apply mechanism that does not currently exist anywhere in code: a new, explicit, separately-invoked function/CLI mode (e.g. a `--apply` flag, clearly gated, requiring the same run to have also passed the SUCCESS_CRITERIA verification in (1)) that is the ONLY code path allowed to write status=superseded/duplicate_of/superseded_reason to a real task.yaml. It must refuse to write a collapse for any issue whose classification category is not the (1)-verified category, and the superseded_reason text it writes must cite the actual verified evidence (real commit SHA(s)/PR number that was checked to satisfy SUCCESS_CRITERIA), never a generic "same underlying issue as X" that was not itself independently checked.
3. Add regression tests reproducing the exact historical fixture from KNOWN_CONTEXT above (task-20260726-083946 and its full real attempt/RCA lineage, PR #81/closed, PR #104/unrelated-crontab-fix, PR #84/e6c7049-the-real-untracked-fix) as test fixture data (synthetic copies under a tests/fixtures/ path or equivalent -- do not run against or mutate any real production ai-os/tasks/ data from within the test suite). The test(s) must assert: the fixed classify()/verification logic does NOT credit the RCA chain (task-20260726-195236/034446, PR #104) with satisfying the original SUCCESS_CRITERIA (since PR #104's real diff doesn't touch branch resolution or hold-for-signoff), and does not silently produce a collapse-eligible category for this case on the basis of lineage/lineage-chain-length alone.
4. Using the existing /tmp/audit_dedup_mismatches.py script in KNOWN_CONTEXT as a verified starting point (re-derive if missing), build this into a real, reusable, non-mutating audit/report mode of the same tool (or a small adjacent script) that scans all real ai-os/tasks/*/task.yaml for status=superseded entries whose superseded_reason cites this dedup routine, cross-checks each against what the FIXED verification logic from (1) would actually conclude today, and writes a real report artifact (e.g. ai-os/DEDUP_MISMATCH_MANUAL_REVIEW_2026-07-27.yaml) listing every mismatch found (expect approximately 30, re-verify the real current count yourself) with each entry's task_id, its recorded duplicate_of, its real classification category, and a one-line reason it doesn't qualify. This mode must be read-only with respect to ai-os/tasks/*/task.yaml -- prove this in the PR description with a before/after diff of ai-os/tasks/ showing zero changes from running it.
5. For the ~30 flagged real records (including task-20260726-083946's own), this task's own deliverable stops at listing them in the report artifact from (4) for manual Owner review -- do not invoke the new (2) apply mode against any of them during this task's own execution; exercise/prove the apply mode's write behavior only against the isolated test fixtures from (3).
6. Do not touch, read into scope, or modify anything under ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br/ (a separate, already-dispatched, unrelated real bug fix in flight) or its git branch/workspace.
7. Close the round-1 review's finding (a): neutralize /opt/veridian/ai-os/execute_backlog_plan.py and the untracked /opt/veridian/ai-os/plan_backlog_completion.py copy so neither can independently reproduce the incident again -- bring the real logic under version control as part of this same PR (move/rewrite them to call the fixed, git-tracked scripts/plan_backlog_completion.py's classify()/verify_success_criteria()/apply path rather than their own untracked copies), and either delete the stale ai-os/BACKLOG_COMPLETION_PLAN_2026-07-27.{json,md} artifacts or clearly mark them superseded-by-the-fix so they cannot be mistaken for current. A future re-run of "the real backlog dedup pipeline" must go through the fixed, tested code path -- not the untracked originals.
8. Close finding (b): main()'s --apply loop must patch the representative/original task's own status to 'completed' when a group is verified ALREADY_DONE_STALE_STATUS (matching classify()'s own promised action text), not just mark non-representative duplicates superseded. Add a test exercising this against the representative task specifically.
9. Close finding (c): replace verify_success_criteria()'s shell=True execution with a real, safe path -- tokenize each extracted SUCCESS_CRITERIA command (e.g. shlex.split) and execute via subprocess.run(argv_list, shell=False) against a real allowlist of exact permitted program names (python3 with -m pytest, pytest, grep -- reject anything else outright, including any command containing shell metacharacters/pipes/redirects after tokenization). Add a test proving a SUCCESS_CRITERIA line containing shell metacharacters (e.g. a `; rm` or piped command) is rejected rather than executed.

## SUCCESS_CRITERIA
- `python3 -m pytest tests/ -k "dedup" -v` (or whatever real test-file name is added for this fix, matching this repo's existing naming convention) exits 0, including at least one test that reproduces the task-20260726-083946/PR-104/PR-84 fixture and asserts the fixed logic does not credit PR #104 with satisfying the original SUCCESS_CRITERIA.
- `grep -n "parse_labeled_fields\|SUCCESS_CRITERIA" scripts/plan_backlog_completion.py` shows the fix actually reuses tight_task_validation.py's existing parser and actually inspects SUCCESS_CRITERIA text, not just PR-mention regex scanning.
- Running the new audit/report mode against the real, current ai-os/tasks/ directory produces a report artifact whose flagged-mismatch count and task-id list can be independently spot-checked (by re-running the equivalent of /tmp/audit_dedup_mismatches.py's comparison logic) to match, and a `git status`/diff of ai-os/tasks/ taken immediately before and after running it shows zero files changed.
- `git log` on ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br shows no new commits/modifications attributable to this task.
- `ls /opt/veridian/ai-os/execute_backlog_plan.py /opt/veridian/ai-os/plan_backlog_completion.py` (the untracked, live-culprit copies) either no longer exist, or `diff`/inspection shows they now delegate to the fixed, git-tracked scripts/plan_backlog_completion.py rather than containing their own independent write logic -- re-running the real backlog-dedup pipeline today must go through the fixed code path.
- A test asserting --apply patches the representative/original task's own status to 'completed' for a verified ALREADY_DONE_STALE_STATUS fixture (not only non-representative duplicates) passes.
- `grep -n "shell=True" scripts/plan_backlog_completion.py` returns nothing for the SUCCESS_CRITERIA execution path, and a test proving a shell-metacharacter-bearing fake SUCCESS_CRITERIA line is rejected (not executed) passes.

## EXPECTED_OUTPUT
Additional commits pushed to the existing branch chore/backlog-completion-plan-script (updating open PR #102 in place -- do not open a second competing PR for the same file) containing: the classify()/build_groups() SUCCESS_CRITERIA-verification fix, the new real apply-mechanism (gated, auditable, cited-evidence-only), the regression test(s) using the real historical fixture, and the new non-mutating audit/report mode plus one real run of it committed as ai-os/DEDUP_MISMATCH_MANUAL_REVIEW_2026-07-27.yaml (or equivalent) for Owner review. Given this changes a routine with direct, demonstrated potential to mislabel real task-tracking state at scale, this PR must include, in its EXPECTED_OUTPUT/CONSTRAINTS section, the marker `HOLD_FOR_OWNER_SIGNOFF: true` so it is not auto-merged without an explicit review of what it found and fixed.

## CONSTRAINTS
HOLD_FOR_OWNER_SIGNOFF: true
The marker above means: do not auto-merge regardless of tier/audit verdict; hold for explicit Owner review given this task touches the integrity of real task-tracking records at scale (30+ existing records potentially affected).
Do not modify, redispatch, or otherwise touch anything in or referencing ai-os/tasks/task-20260727-041903-fix-escalate-hardcoded-repo-and-stale-br -- that is a separate, already in-flight fix and is explicitly out of scope here.
This task's own execution must not invoke the new apply mode against any currently-existing task record, including task-20260726-083946's own -- produce the manual-review report artifact only; save any real collapse-writing for a future, separately-dispatched task after Owner review of that report.
This is a fix to an existing, not-yet-merged tool (scripts/plan_backlog_completion.py on PR #102) -- extend it in place; do not build a parallel second dedup script.
This is a corrective round after a real structured review already returned Verdict: fail on this same PR -- read that review comment fresh from PR #102 first, address all three findings it raised (SCOPE items 7/8/9 above), and expect a fresh re-review after you push.

## COMPLEXITY_TIER
judgment
