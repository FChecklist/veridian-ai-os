## OBJECTIVE
Consolidate the 6 real task-dispatching/status scripts on VERIDIAN-DEV (supervisor-sweep.sh, queue-dispatcher.py, module-queue-dispatcher.py, auto_phase_continuation.py, veridian_status_monitor.py, veridian_remediation_dispatcher.py) into 3 scripts sharing one concurrency-gating library, closing the exact root cause of today's real OOM-kill incident: three independent worker-spawn code paths (systemctl start veridian-supervisor@, veridian-task.py create, task-gateway.py submit/start) with no shared concurrency cap between them. This is a real production fix for infrastructure that already caused one real multi-hour outage today (2026-07-26) -- correctness matters more than speed here.

## KNOWN_CONTEXT
A prior research pass read all 6 scripts in full and confirmed the following real findings (verify them yourself by reading the real current files, do not simply trust this summary -- the files may have changed since):
1. `supervisor-sweep.sh` unconditionally runs `systemctl --user start "veridian-supervisor@${task_id}.service"` for every matching stuck task_dir in one loop, no cap, no lock. Its own log `supervisor-sweep-20260726-190039.log` shows it started 3 separate units in a single tick at 19:00:39 UTC -- 1 second after the real OOM-kill logged at 19:00:38 UTC.
2. `queue-dispatcher.py` and `module-queue-dispatcher.py` have a near-byte-identical dispatch core (same queue_lock() fcntl pattern, same running_worker_count() reading `systemctl --user list-units "veridian-worker@*" --state=running`, same task_status() reading task.yaml, same dispatch call to veridian-task.py create) but enforce SEPARATE concurrency caps (5 vs 3) with no shared lock -- a real TOCTOU race if both ever ran. module-queue-dispatcher.py is not currently in the crontab (confirmed via `grep -i module-queue`) but is not dead code -- it has one real live queue item at ai-os/queues/backend.yaml (BACKEND-001) and its own docstring says its cap is meant to be shared with queue-dispatcher.py's pool, not additive.
3. `auto_phase_continuation.py` dispatches via a THIRD, separate entrypoint (task-gateway.py submit then start) with NO concurrency cap of its own at all -- it dispatches every ready phase found, entirely unaware of the other two paths' current load. This is the real, confirmed root cause the crontab's own incident-note already names: "too many concurrent task-dispatching cron jobs, no shared concurrency cap."
4. `veridian_status_monitor.py`'s scan_phases_ready() literally re-executes `auto_phase_continuation.py --dry-run` every 10-minute tick purely to populate a read-only status field -- duplicating that script's full plan-scan + per-phase `gh pr list --head worker/<id> --state merged` + `git show master:...` cross-reference work redundantly.
5. `veridian_remediation_dispatcher.py --apply` deliberately does NOT spawn a full worker for its "mechanical" fix class (ci_timing_race, transient_merge_retry) -- it runs 2 narrow direct `gh` CLI calls instead, per the Owner's existing "audit run by software, not AI" principle. Preserve this distinction exactly -- do not change the mechanical path to spawn a worker.
6. As of this task's dispatch, EVERY cron job on the box is disabled (crontab -l shows a blanket #STOPPED-ALL-CRON-2026-07-26# prefix on everything, plus a more specific #PAUSED-OOM-INCIDENT-2026-07-26 marker on the 3 dispatch-heavy jobs). This gives a safe, non-live window to build and test in. Do NOT add anything back to the live crontab as part of this task -- that requires a separate, explicit Owner decision after this work is reviewed.
7. `ai-os/gap_queue.yaml` has had `dispatch_paused: true` and 21 `held_task_ids` since an explicit Owner directive on 2026-07-20. The consolidated dispatcher must continue to honor this exactly -- consolidation must not silently resume paused dispatch.
8. Real generate_wiring_registry.py (confirmed live, not a backup file) already imports superboss-register.py in-process via `importlib.spec_from_file_location` (around its own lines 111-115) and calls real functions directly rather than shelling out -- use this exact same pattern for any new wiring_registry/knowledge_engine writes this task adds, not a subprocess call per event.

## SCOPE
1. Read all 6 real current scripts (/opt/veridian/scripts/{supervisor-sweep.sh,queue-dispatcher.py,module-queue-dispatcher.py,auto_phase_continuation.py,veridian_status_monitor.py,veridian_remediation_dispatcher.py}) in full before writing any new code -- confirm the KNOWN_CONTEXT findings against the real current files first.
2. Create /opt/veridian/scripts/dispatch_core.py (new shared library) with: a single task_status_sync() that walks ai-os/tasks/*/task.yaml once and is reusable by all consolidated scripts; a single running_worker_count() that counts BOTH veridian-worker@* AND veridian-supervisor@* currently-running units together; a single acquire_dispatch_lock()/release using one real flock file at /opt/veridian/ai-os/locks/worker-spawn.lock; one shared CONCURRENCY_CAP value (a single config, not three separate hardcoded numbers).
3. Create /opt/veridian/scripts/dispatch-tick.py merging supervisor-sweep.sh + queue-dispatcher.py + module-queue-dispatcher.py: same real sub-behaviors as today (pending_review-no-review detection, adopted-task pickup, gap_queue.yaml dispatch honoring dispatch_paused/held_task_ids/existing_scope_conflict(), module-queue dispatch against the same shared pool as gap_queue dispatch, not a separate one) -- but every spawn call site (systemctl start, veridian-task.py create) must acquire dispatch_core.py's lock and check the shared cap first.
4. Create /opt/veridian/scripts/phase-continuation-tick.py: refactor of auto_phase_continuation.py's real logic (unchanged plan-scan/cross-check/tier2-backfill behavior) except its task-gateway.py submit/start calls must also acquire the shared lock/cap, and it must write its ready-phase result to ai-os/PHASE_READY_CACHE.json (a real JSON file, timestamped) at the end of every run.
5. Create /opt/veridian/scripts/status-remediation-tick.py merging veridian_status_monitor.py + veridian_remediation_dispatcher.py --apply (same chained relationship as today): status_monitor's scan_phases_ready() must read ai-os/PHASE_READY_CACHE.json instead of re-invoking phase-continuation-tick.py's discovery logic; remediation classification and the 2-narrow-gh-call mechanical path must be preserved exactly unchanged.
6. Wire in the wiring_registry/knowledge_engine integration: each of the 3 new scripts should, at the end of every tick, call superboss-register.py's real functions in-process (same importlib pattern as generate_wiring_registry.py) to upsert one wiring_registry row per script per tick (entity_type='cron_job', relationships containing last_run_ts/last_run_status/dispatched_this_tick), plus one additional wiring_registry row per actually-dispatched task (entity_type='dispatch_event', relationships containing dispatched_by/source_queue_or_plan/worker_unit/ts).
7. Do not add the new scripts to the live crontab -- leave that for a separate Owner-approved step. Do not delete the 6 old scripts -- rename them with a `.superseded-by-consolidation-2026-07-27` suffix so they're clearly retired but recoverable, and leave a short README note in the same directory explaining the mapping from old to new.
8. Add real regression/integration tests (tests/ directory or wherever this repo's existing test convention lives) covering: the shared lock genuinely prevents two simultaneous spawn attempts from both succeeding (a real concurrency test, not just a unit test of the lock function in isolation), the gap_queue dispatch_paused/held_task_ids behavior is preserved, and the phase-continuation-tick -> status-remediation-tick cache handoff works end to end.

## SUCCESS_CRITERIA
- `python3 -m pytest tests/ -k "dispatch_core or dispatch_tick or phase_continuation or status_remediation" -v` exits 0.
- `grep -c "acquire_dispatch_lock" scripts/dispatch-tick.py scripts/phase-continuation-tick.py` returns a nonzero count for both files -- confirming the shared lock is actually used at every real spawn call site, not just imported.
- A real concurrency test (e.g. two processes/threads attempting to acquire the lock and spawn simultaneously) demonstrates only one succeeds within the cap at a time.
- `ls /opt/veridian/scripts/*.superseded-by-consolidation-2026-07-27` shows the 6 original scripts renamed, not deleted.

## EXPECTED_OUTPUT
A real PR (or a small, clearly-sequenced set of PRs if the diff is large) against the repo where these scripts live, implementing dispatch_core.py, the 3 consolidated scripts, the wiring_registry/knowledge_engine integration, the renamed-not-deleted old scripts, and the regression tests. Include in the PR body: the real cron schedule these 3 scripts would use once approved (do not add it to the live crontab yourself), and an explicit note that dispatch_paused/held_task_ids in gap_queue.yaml were preserved unchanged.

## CONSTRAINTS
Do not add anything to the live crontab as part of this task -- re-enabling scheduled dispatch requires a separate, explicit Owner decision after this consolidation is reviewed and merged. Do not change veridian_remediation_dispatcher.py's mechanical-fix behavior (the 2 narrow direct gh calls, no worker spawn) -- preserve that distinction exactly. The gap-queue's existing pause flag and held-task exclusion list are Owner-set state from a prior directive -- carry them forward exactly as-is, with no change to their current values. Do not merge yourself; this touches the exact infrastructure that caused today's real outage and needs a fresh, thorough audit pass.

## COMPLEXITY_TIER
judgment
