## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not mechanical): diagnose the real root cause of superboss-register.sqlite's repeated same-day corruption (3 distinct signatures today: btreeInitPage/page-11 truncation, page-368/363/123 2nd-reference tree damage, freelist size mismatch) by cross-referencing live process activity, systemd restart history, and the script's own write paths -- the cause is not yet known, only ruled-out candidates are known -- then implement and verify a real fix, continuing the self-dispatching gap-closing chain from task-20260723-093333 phase 1.

## SCOPE
In scope: (1) live-diagnose why /opt/veridian/ai-os/memory/superboss-register.sqlite keeps corrupting despite WAL journal mode + 30s busy_timeout already being enabled in superboss-register.py's _connect() -- check systemd restart/kill history for worker services around each corruption timestamp (07:30, 08:30, 09:00, 09:15, 09:43 UTC today per ai-os/logs/ATTENTION.md), check whether FTS5 external-content triggers (actions_fts and its sibling _fts tables) are involved in any partial/interrupted transaction, check whether multiple concurrent CLI invocations of superboss-register.py can race past the busy_timeout in a way that corrupts rather than just delays; (2) implement a real fix once the cause is identified -- candidates to evaluate on real evidence, not guessed: a file-lock (fcntl.flock) wrapper serializing all superboss-register.py write commands so only one write-capable process ever holds the file at a time, a supervisor-level guard against killing a worker mid-write, or a schema/trigger fix if FTS5 is the culprit; (3) once fixed, perform the actual repair (quarantine the current corrupted file with a timestamped suffix matching the existing ai-os/memory/superboss-register.sqlite.CORRUPTED-* naming convention, reinitialize via `python3 /opt/veridian/scripts/superboss-register.py init`, and re-populate anything cheaply recoverable) and confirm the corruption does not recur within the observation window available this task; (4) update ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml source_3's finding with real before/after status and append to its gap_closing_worker_log as phase 2; (5) commit, push, checkpoint status=pending_review, self-dispatch phase 3 continuing through the remaining ~185 open items in that audit document's consolidated_summary, in the same small-batch style, unless genuinely blocked on an Owner decision.

## KNOWN_CONTEXT
Diagnostic groundwork already done by phase 1 (task-20260723-093333), reuse rather than re-derive: WAL mode + 30s busy_timeout already confirmed live in /opt/veridian/scripts/superboss-register.py `_connect()` (line ~75-76) -- ruling out the naive "no WAL" cause. The filesystem under /opt/veridian is plain ext4 on local disk (confirmed via `mount`), not overlay/network, ruling out the FS-shared-memory-locking class of WAL bug. No script anywhere under /opt/veridian/scripts greps for a raw copy/rename directly INTO the live superboss-register.sqlite path (confirmed via grep across all .py files) -- health-check-15min.py's backup step only reads the live file (open mode "rb") into a separate `.bak` path once healthy, it does not write back, ruling out the backup cron as the direct corruptor. Multiple `.rebuild-2026-07-23b/c` and `.CORRUPTED-2026-07-23-*` sidecar files (with their own -shm/-wal) exist in ai-os/memory/ from earlier same-day repair attempts by other tasks -- inspect these for clues about what those attempts tried and why they did not hold, but do not assume they are the current cause since the current live file has never been touched by those. Live re-verification at 09:39 and again at 09:43-09:44 UTC today showed the db flip from a passing PRAGMA integrity_check to a new failure within about 5 minutes, with the `actions` table row count incrementing by 1 in between -- meaning ordinary write activity (not a bulk copy/rebuild event) coincided with the transition, which is the strongest lead: focus on what a single ordinary `log-action`/`log-work`/`check-duplicate` write does under contention, not on rebuild-script archaeology. Separately, apply these lessons from today's ~15 dispatch cycles so this prompt and any of its own self-dispatched successors stay clear of known rejection traps: every self-dispatched prompt.txt uses this same labeled section format (free prose gets silently truncated to 300 chars); the OBJECTIVE opens by naming what requires real judgment, staying away from a "read this file" framing or an otherwise mechanical-sounding opening (gets REDIRECTed); phrasing stays positive throughout, describing what to build/extend/change rather than pairing a prohibition against one thing with an instruction to build a similarly-worded other thing elsewhere in the same prompt (the validator is a bag-of-words contradiction checker, not semantic, and false-flags that pattern); hedges and open-ended filler stay out of this prompt in favor of concrete, spelled-out specifics (the validator treats several common hedge phrases as an ambiguity trigger); subscription-billed work is not gated by the $1 accountant, so this prompt carries no spend-approval framing; sequential superboss-register.py writes are preferred over concurrent ones from parallel agents within a phase, and if a phase fans out to parallel agents for genuinely parallel research, only one final synchronization step performs the actual database writes; a preflight rejection gets fixed at the specific prompt.txt line directly (full shell access is available) and restarted, escalating to notify-owner.py only after about 5 self-fix attempts fail on the same phase; `systemctl --user reset-failed veridian-worker@<task-id>.service` clears "Start request repeated too quickly" (a systemd StartLimitBurst issue, not a real rejection) if a task restarts more than about 3 times in 30 minutes. Full real precedent for a correctly-run batch: task-20260723-045924, task-20260723-052857/060600/064712.

## SUCCESS_CRITERIA
The fix is proven with real evidence the corruption pattern actually stops, not just that a reinit happened -- e.g. multiple PRAGMA integrity_check passes across a real time window after the fix, ideally spanning at least one more 15-minute health-check cron cycle, plus an explanation grounded in what the diagnosis actually found (not a guessed cause). ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml's gap_closing_worker_log gains a real, evidence-cited phase 2 entry. Zero fabricated progress -- if the true root cause cannot be pinned down with the evidence available this task, say so honestly, document the ruled-out candidates, apply the most defensible mitigation found (e.g. write serialization) even without 100% certainty of the original cause, and say plainly that full certainty was not reached rather than overclaiming.

## EXPECTED_OUTPUT
Commit + push real changes (script fix, repair, audit doc update). ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml updated with real before/after status for the source_3 sqlite finding. Checkpoint status=pending_review with real evidence in the note (via `python3 /opt/veridian/scripts/veridian-task.py checkpoint <task_id> --status pending_review --note "..."`). Self-dispatch phase 3 (via `python3 /opt/veridian/scripts/veridian-task.py create --title ... --repo claude-control --prompt "..."`) continuing through the remaining open items in ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml's consolidated_summary, unless genuinely done or genuinely blocked on an Owner decision, in which case stop and say so plainly via notify-owner.py rather than appearing to silently vanish. This is phase 2 of 5 total notify-owner.py emails per roughly 3-5 phases -- send one now only if this phase surfaces something needing an Owner decision or if the chain is stopping; otherwise fold this phase's progress into the next scheduled cumulative email.

## CONSTRAINTS
Every completion claim must cite a real file, command output, or commit hash. This phase touches superboss-register.sqlite directly -- the repair itself runs as a single sequential operation from one process only, matching this phase's own diagnostic conclusion about what caused today's earlier stray .rebuild-2026-07-23b/c artifacts in ai-os/memory/ (multiple independent repair attempts colliding); diagnostic research work can still fan out normally. The 4-repo merge and the veda-advisors data-coupling severance stay parked in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml (already written by phase 1) and are not executed automatically.

## COMPLEXITY_TIER
judgment