VERIDIAN Review Framework gap-closure: Calculation Engine / Formula Library & Customization.
This covers 3 related finding(s) from the framework evaluation. Close all of them in one coherent PR if they share the same module/area -- do not create a separate PR per finding if they're naturally one piece of work.

Findings to address:
- [Medium] Reusable Formula Library
  Gap: The library is a developer-facing TypeScript module set, not a browsable/reusable end-user 'formula library' UI -- only 26 of 211 implemented engines (12.3%) are wired into the Chain Selector for actual dispatch
  Recommended approach: Continue CAPABILITY_COVERAGE.md's own roadmap: wire Accounting (20) and Payroll (18) engines into Chain Selector next, the two highest-count unwired categories.
- [Medium] Industry Specific Calculation Library
  Gap: Most industry-vertical categories (Accounting, Payroll, Inventory, Manufacturing) remain 0% wired despite being implemented; Manufacturing (11 engines) is explicitly out of scope
  Recommended approach: Follow the documented roadmap order (Accounting -> Payroll -> Inventory -> Income Tax).
- [Low] Custom Formula Builder
  Gap: Custom Formula Builder (org-defined formula authored via UI, not code) does not exist
  Recommended approach: Lower priority than closing the Chain Selector wiring gap (row 14); revisit once a concrete customer need surfaces via FDE requests.

Before writing any code: read the actual current implementation of the relevant module(s) first -- do not assume the gap description is still accurate, the codebase has moved since this evaluation was written. If a finding turns out to already be resolved, or the described gap doesn't match what you find in the code, say so in PROGRESS.md rather than making an unnecessary change.
Do not touch src/lib/services/permission-service.ts's shared ERP_ACTION_ROLES table structure or any other in-flight worker's declared scope -- if your area genuinely needs a new permission-service entry, add it additively (new keys only).
Maintain PROGRESS.md with '## Completed' / '## Remaining' checklists as usual.