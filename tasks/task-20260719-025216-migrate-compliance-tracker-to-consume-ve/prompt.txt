You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously, including waiting for CI via `gh run watch <id> --exit-status`.

TASK: FChecklist/compliance-tracker (VERIDIAN AI OS) must actually consume the shared FChecklist/veridian-ui-kit package's real shell/composer/panel/header components, not just its design tokens. This repo is the SOURCE those components were extracted from, so this migration is the regression gate before PROJEXA does the same -- if this repo's real UI looks and behaves identically afterward, the package is proven.

Bump the dependency first: `"@fchecklist/veridian-ui-kit": "github:FChecklist/veridian-ui-kit#v0.2.0"` in package.json (it is currently pinned at v0.1.0, and v0.2.0 adds AppHeader which this task needs).

READ IN FULL BEFORE WRITING ANY CODE:
- veridian-ui-kit's README.md and every file under src/shell/, src/composer/, src/panel/, src/context/ (its real, generic, callback-prop-based API).
- This repo's own src/components/AppShell.tsx, AppSidebar.tsx, AppTopbar.tsx, src/components/veri-chat/VeriComposer.tsx, VeriChatPanel.tsx, veri-chat-context.tsx -- these are the REAL, currently-live implementations the shared package's components were ported from. AppShell.tsx in particular has a lot of real, product-specific logic that must NOT be lost: HealthRibbon, TrialBanner, OnboardingChecklist, GlobalChatDock (+ isDockHiddenForPath), HelpWidget, TaskVisibilityPanel, the stage_0-user redirect to /stage0-chat, BYOB white-label branding CSS custom properties, the resizable-panel persisted layout (useDefaultLayout/ssrSafeLocalStorage), and the unread chat/AI/connector count polling.
- ai-os/CONSTITUTION.yaml's navigation_and_intent section and compliance-tracker/veridian-scope-selector-in-home.html (the reference mockup) -- confirm what this repo's real "home route" is (grep AppShell.tsx and this repo's routing for the actual home page, likely /home) and note the file's own comment that AppShell currently does NOT hide the right panel on that route despite the mockup showing it should -- this migration is expected to CORRECT that drift by using AppShellFrame's real homeRoute-aware merge behavior, not just preserve today's non-merging behavior.

WHAT TO DO:
1. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first; check for collisions with any other active work.
2. Replace AppShell.tsx's own inline layout markup with the shared `AppShellFrame` (sidebar/composer/panel/homeThreadSlot/homeRoute props) -- keep every one of AppShell's existing widgets (HealthRibbon, TrialBanner, OnboardingChecklist, GlobalChatDock, HelpWidget, TaskVisibilityPanel, stage_0 redirect, BYOB branding vars, resizable-layout persistence, unread-count polling) rendered exactly as they are today, just inside/around the new shared frame instead of the old hand-rolled flex layout.
3. Replace AppSidebar.tsx's nav-rendering markup with the shared `AppSidebar` component, passing this repo's real nav sections/items as data (props), keeping this repo's own real route list, icons, and badges (unread counts etc.) as its own data -- the shared component only owns the generic shell/style, not this repo's actual navigation content.
4. Replace AppTopbar.tsx's markup with the shared `AppHeader` component, passing compliance-tracker's real search trigger, notification dropdown (with its real API-backed unread count/list), invite-a-team-member button, and user avatar/logout menu as slot props -- keep every one of those behaviors working exactly as today, wired through AppHeader's props instead of hardcoded in a local header component.
5. Replace veri-chat/VeriComposer.tsx and VeriChatPanel.tsx with the shared package's `VeriComposer`/`PanelShell`/`ThreadView`, wiring this repo's real capability-tree fetch, task/chat/todo CRUD, and dispatch endpoints through the shared components' callback props -- do not lose any real dispatch/fetch behavior in the swap.
6. Keep veri-chat-context.tsx as this repo's own thin wrapper around the shared package's `createVeriChatContext()` factory (per the original design), supplying this repo's real data sources.
7. Run `bunx tsc --noEmit`, `bun run lint`, `bun test` -- all must be clean, not just "mostly clean."
8. If this diff happens to touch drizzle/*.sql or schema.ts, treat it as TIER2 and stop short of merging (report for Owner sign-off instead). This task should NOT need schema changes -- if you find yourself adding one, stop and reconsider whether it's actually necessary for a UI-shell migration.
9. Push, open a PR, post the standard 8-field `AUDIT: PASS` comment yourself, wait for CI (`gh run watch --exit-status`) -- Lint/Type Check/Build/Unit Tests/E2E Tests/Asset Registry Coverage/Guardrail Presence/audit-check must all be green (Vercel is known-rate-limited and non-required, ignore it).
10. If TIER1 and CI is fully green, self-merge (squash, delete branch).
11. Report final status: PR number, tier, CI result, merged status, and a plain-English list of exactly which widgets/behaviors you verified are preserved (not just "looks fine").

This is compliance-tracker's own live, actively-used production app -- do not skip any of its existing real functionality to make the migration simpler. If any single piece (e.g. GlobalChatDock) genuinely cannot be cleanly composed with the shared AppShellFrame without a deeper change than this task's scope, stop, do NOT paper over it, and report that specific blocker honestly instead of silently dropping the feature.
