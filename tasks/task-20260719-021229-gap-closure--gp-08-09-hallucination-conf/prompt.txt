You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" or "check back later" -- complete every step synchronously in this single invocation, including waiting for CI via `gh run watch <id> --exit-status` (which blocks until done).

TASK: Close this real, honestly-tagged gap in ai-os/CONSTITUTION.yaml on FChecklist/compliance-tracker:

GP-08/GP-09 (currently PARTIALLY_ENFORCED / NOT_YET_BUILT in ai-os/CONSTITUTION.yaml guardrail_protocols section 9): src/lib/policy-enforcement-engine.ts (read in full first) is a real, working policy/hallucination-prevention gate today, but GP-09 specifically (a numeric confidence score attached to AI-generated claims/proposals) does not exist anywhere in this codebase yet -- confirm this yourself via a fresh grep for "confidence" across src/lib before writing anything, do not assume.
Scope a NARROW, real addition matching the constitution's own framing ("hallucination prevention is done by lower-tier AI, strict/narrow/tightened instructions"): add a confidenceScore field (0-1 float) to the existing AI-team dispatch response shape (find the real shape returned by roster.ts/ai-team dispatch -- read it first) that a Tier-1 (narrow, cheap) verification pass computes AFTER a higher-tier AI produces a claim/proposal, by checking the claim's specific factual assertions (e.g. "this function exists", "this table has this column") against this codebase's actual current state (grep-verifiable claims only -- do not attempt semantic/subjective confidence scoring, that is out of scope and would not be a real, testable mechanism).
Wire this ONE real call site: wherever an AI-team dispatch response is currently persisted/logged (orchestra-execution-logger.ts -- read it first), attach the computed confidenceScore before logging. If confidence is below a reasonable threshold (e.g. 0.5), flag the row (a boolean or status field, follow this codebase's existing enum conventions) rather than silently passing it through -- do not auto-block, only flag for review, since auto-blocking would be a much larger, riskier change out of this task's scope.
Add real tests covering: a claim citing a function/file that genuinely exists (high confidence), and a claim citing one that does not exist in this repo (low confidence, correctly flagged).

STEPS (do all of them, in order, no shortcuts):
1. Read the cited CONSTITUTION.yaml section IN FULL before writing any code. Read the actual current implementation of any file named below -- do not assume from the gap description alone.
2. Check ai-os/boss/ACTIVE-CLAIMS.yaml and open PRs (`gh pr list`) first to make sure nobody else is already working this exact gap. If a genuine duplicate claim exists, stop and report that instead of proceeding.
3. Register a new claim entry in ai-os/boss/ACTIVE-CLAIMS.yaml for this work (append, don't rewrite existing entries), commit it as its own small first commit.
4. Implement the real, narrowly-scoped fix. No MVP shortcuts, no placeholder/TODO code, no invented APIs -- extend the existing real mechanism named in the gap description. Follow this codebase's existing conventions (RLS posture, tenant-scoping via withTenantContext, existing naming/structure) by reading 2-3 similar existing files first.
5. Add real tests (unit and/or the existing test patterns in this repo) covering the new behavior, not just happy-path.
6. Run `bunx tsc --noEmit`, `bun run lint`, `bun test` locally and fix everything before pushing.
7. If this diff touches drizzle/*.sql or src/lib/db/schema.ts: before creating your migration file, run `git ls-tree -r origin/main --name-only -- drizzle/ | grep '\.sql$' | xargs -n1 basename | sed -E 's/^([0-9]+)_.*/\1/' | sort -n | tail -3` to find the REAL current highest migration number (main moves fast, other tasks may have landed since you started) and number your migration above that -- do not trust any number from an earlier read.
8. Push, open a PR against main.
9. Post a structured `AUDIT: PASS` PR comment yourself with exactly these 8 fields: Objective Understood, Standards Reviewed, Scope Confirmed, Evidence Recorded, Severity Classified (bare word: critical/high/medium/low/none), Verdict (pass/fail), Corrective Action Owner, Re-Audit Scheduled.
10. Wait for CI via `gh run watch <run-id> --repo FChecklist/compliance-tracker --exit-status`. KNOWN ISSUE this session: if `gh pr view <num> --json mergeStateStatus` shows DIRTY/CONFLICTING, GitHub Actions will create ZERO check-suites for any push to that branch, even empty-commit retriggers or close/reopen. If you hit this, the fix is: merge origin/main into your branch until mergeable=MERGEABLE, then push to a brand-new branch name and open a FRESH PR from that branch against main (referencing and closing the original PR as superseded) -- this reliably makes CI resume. Do this yourself rather than reporting it as blocked.
11. Classify tier: if the diff touches drizzle/*.sql or src/lib/db/schema.ts, it is TIER2 -- do NOT merge, report as ready for Owner sign-off instead. Otherwise it is TIER1 -- once all required checks are green (Lint, Type Check, Build, Unit Tests, E2E Tests, Asset Registry Coverage Check, Guardrail Presence Check, audit-check all pass; the Vercel check is known-rate-limited and non-required, ignore it), self-merge via `gh pr merge <num> --squash --delete-branch`.
12. Report final status: PR number, tier, CI result, merged or awaiting-sign-off.
