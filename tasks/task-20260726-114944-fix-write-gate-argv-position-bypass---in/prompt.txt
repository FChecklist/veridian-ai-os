## OBJECTIVE
Fix real, live-reproduced AUDIT: REJECT findings on the current claude-control PR #80 branch (interactive-session write-gate, round 4). Round 3 correctly fixed the spoofable-cgroup-file and refspec-parsing bugs (both verified holding up under live testing), but the reviewer found two further real gaps: a trivial argv-position bypass, and a structural concern that the cgroup-verification premise itself may be forgeable via ordinary systemd-run usage.

## KNOWN_CONTEXT
Read ai-os/OWNER_DIRECTIVES/interactive-session-guard.bashrc-snippet in full first. Real findings, live-reproduced by the reviewer in a sandboxed harness matching this task's own test methodology: (1) both the git and gh wrapper scripts detect the guarded subcommand by checking only argv[1] literally (`[ "$1" = "push" ]` for git; `$1=pr $2=merge` or `$1=api` for gh) -- any invocation with a global flag before the subcommand walks straight past the gate undetected. Reproduced live: `git -C /tmp push origin master`, `git --git-dir=/tmp/.git push origin master`, and `gh --repo some/repo pr merge 1 --merge` all reached the real binary with zero detection -- these are ordinary, everyday command forms, not adversarial tricks, meaning the gate can be bypassed by accident. (2) Lower-confidence but structurally serious: an ordinary interactive process can run `systemd-run --user --unit=veridian-worker@fake.service <command>`, which creates a genuinely kernel-tracked cgroup matching the guard's own worker-unit regex AND a genuine INVOCATION_ID -- meaning the cgroup-verification approach may not actually distinguish "a real dispatched worker" from "an interactive session that ran systemd-run to impersonate one," since both produce authentic systemd tracking data. The guard's current claim that cgroup membership "cannot" be forged to impersonate a real worker unit needs to be either proven false and fixed, or proven true with a real explanation of what specifically prevents `systemd-run --user` impersonation.

## SCOPE
1. Fix the argv-position bypass: rewrite both wrapper scripts' subcommand detection to scan the FULL argument list for the real subcommand token, correctly skipping over any preceding global flags (both git and gh have well-defined, real, finite sets of global flags that can precede a subcommand -- read `git --help`/`gh --help` and their real global-option lists rather than guessing, and handle both `--flag value` and `--flag=value` forms). The detection must correctly identify `push`/`pr merge`/`api` as the real subcommand regardless of how many recognized global flags precede it.
2. Investigate the systemd-run impersonation concern directly and empirically: in a real sandboxed test, run `systemd-run --user --unit=veridian-worker@fake-test.service --scope <command>` (or the real equivalent) and check whether the resulting cgroup path and INVOCATION_ID actually satisfy the guard's current checks. If they do, this is a real, confirmed structural gap -- close it by cross-referencing the cgroup's derived task_id against a REAL, currently-registered task: the guard should verify that /opt/veridian/ai-os/tasks/<task_id>/task.yaml genuinely exists and its status is genuinely in_progress (or equivalent live-tracked state), not just that the cgroup path string matches a worker-shaped regex pattern. A `systemd-run`-forged fake unit would have a task_id with no real, corresponding, in_progress task_dir, and should be rejected. If your empirical test shows the impersonation does NOT actually work (e.g. some other real systemd/cgroup property already prevents it), document precisely why with real evidence, and do not claim it's fixed if you have not proven it.
3. Update the KNOWN LIMITATIONS section honestly with whatever residual gaps remain after this round's real, empirical testing -- do not claim more coverage than what was actually verified live.
4. Extend the regression tests to cover: the argv-position bypass scenarios reproduced above (all must now be blocked), and a real or realistically-simulated systemd-run impersonation attempt (must now be blocked if SCOPE item 2's cross-reference check is implemented).

## SUCCESS_CRITERIA
- `git -C /tmp push origin master`, `git --git-dir=/tmp/.git push origin master`, and `gh --repo some/repo pr merge 1 --merge` (run in a non-systemd interactive shell) are all now blocked.
- A real or realistic simulation of `systemd-run --user --unit=veridian-worker@fake-test.service` followed by a guarded command is blocked, because fake-test.service has no real corresponding entry in /opt/veridian/ai-os/tasks/.
- `python3 -m pytest tests/ -k write_gate -v` exits 0 with all new scenarios passing.

## EXPECTED_OUTPUT
Push directly to the existing PR #80 branch in FChecklist/claude-control (find it via `gh pr view 80 --repo FChecklist/claude-control --json headRefName`). Do not open a new PR.

## CONSTRAINTS
Do not claim the systemd-run impersonation gap is closed unless you have empirically verified it in a real test -- if genuinely uncertain after real testing, disclose the uncertainty honestly rather than asserting either way. Do not merge yourself; this needs a fresh audit pass, and given three prior rounds on this exact file, the reviewer should be explicitly asked to attempt live reproduction of the argv-bypass and systemd-run scenarios, not review the diff alone.

## COMPLEXITY_TIER
judgment
