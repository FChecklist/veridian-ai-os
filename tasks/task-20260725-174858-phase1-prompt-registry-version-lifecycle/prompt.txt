## OBJECTIVE
CORRECTED 2026-07-25 by task-20260725-160908's re-verification: a real prompt registry/version store already exists and is LIVE in production (compliance.prompt_templates/prompt_versions, resolvePromptTemplate() called from ~20 real call sites in repos/compliance-tracker -- see ai-os/VERIDIAN_ARCHITECTURE_V2_GAP_ANALYSIS_2026-07-25.yaml known_partial_prior_art.prompt_os). The original plan below to add a NEW prompt_registry table to scripts/superboss-register.py's SQLite DB would have built exactly the disconnected parallel system the Owner's zero-duplication directive forbids. This phase is corrected to EXTEND the real, live Postgres tables instead: add semantic (MAJOR.MINOR.PATCH) versioning, a real Draft/Review/Staging/Production/Deprecated lifecycle_state column (replacing the current free-text label), diff/rollback support, and expand the metadata schema (2.6) on top of prompt_templates/prompt_versions' existing ~50 real columns -- not a fresh datastore.

## SCOPE
- Gap items closed: engine-prompt-registry, engine-prompt-version, engine-prompt-lifecycle, engine-prompt-validation, metadata-schema-200-fields, governance-lifecycle-state-machine (bare state machine only -- the approval-gate ENFORCEMENT itself is phase_3 scope, not this phase's, per that item's own note distinguishing the state machine from its governance gates).
- CORRECTED: do NOT add a prompt_registry table to scripts/superboss-register.py's SQLite DB (the original plan here). Instead, extend repos/compliance-tracker's real Drizzle schema: add a lifecycle_state enum column (Draft/Review/Staging/Production/Deprecated) to compliance.prompt_versions alongside (not replacing, to avoid breaking the ~20 live resolvePromptTemplate() call sites) its existing label column, add MAJOR/MINOR/PATCH integer columns, and a diff/rollback API in prompt-os-service.ts next to the existing createPromptVersion/listPromptVersions.
- Expand the metadata schema on compliance.prompt_templates/prompt_versions (2.6's 18 categories) as a real Drizzle migration + JSON Schema doc (ai-os/PROMPT_METADATA_SCHEMA_2026-07-25.schema.json) at minimum-viable field depth per category, not literally all 200+ fields verbatim -- match this session's own precedent (AUDITOR_ENGINE's metadata domain used the same minimum-real-schema approach).
- Register this extension (not a new table) in MASTER_INDEX.yaml and via scripts/superboss-register.py register-knowledge, citing this gap analysis + phase plan as evidence, and add a corresponding entity in ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json (already tracks compliance.prompt_templates/prompt_versions as generic `table` entities via DATABASE_CATALOG.json -- add the new lifecycle_state/version columns' relationship evidence on the next scripts/generate_wiring_registry.py run, do not create a second, parallel entity for the same table).

## KNOWN_CONTEXT
This is phase_1_prompt_registry_lifecycle_foundation of ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml, part of the VERIDIAN_Architecture_v2.0.docx -- Phased Implementation Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_1_prompt_registry_lifecycle_foundation, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
none (entry phase). This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_1_prompt_registry_lifecycle_foundation" --tag domain:veridian-architecture-v2-0-docx` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_1_prompt_registry_lifecycle_foundation" ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own phases[] entry for phase_1_prompt_registry_lifecycle_foundation in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
