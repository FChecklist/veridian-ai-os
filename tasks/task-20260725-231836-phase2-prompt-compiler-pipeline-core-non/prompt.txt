## OBJECTIVE
Extend scripts/prompt_gateway/'s real but narrow prompt_engine.py/classifier.py/ context_engine.py (gap analysis: engine-prompt-compiler, engine-intent, engine-context all partially_implemented) into the document's own Layer 2-5 pipeline (Lightweight Analysis through Verification, pipeline-lightweight-analysis through pipeline-verification) and the remaining Intelligence & Matching engines with no prior art (Entity, Variable, Ranking, Recommendation, Similarity, Workflow, Capability, Confidence -- multi-signal). Depends on phase_1 because compiled/versioned output needs somewhere real to be stored and versioned. AMENDED 2026-07-25 per the Owner's real verbatim UX directive (/opt/veridian/ai-os/OWNER_DIRECTIVES/BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt, registered in knowledge_engine as KE-20260725-184749-e0e0; this re-derives and replaces the amendment rejected in PR #72 -- see scope note below for why): this phase's Stage 4 Machine Prompt Builder / Prompt Compiler Engine deepening work is also the machine-language-output contract that both of phase_5's end-user input surfaces (structured mode-pill/option-chain, free-text chat) compile into before handoff to server-side SOFTWARE execution -- a deterministic second pass, not a second mandatory AI call (see phase_5's corrected scope for the full credit-governance reconciliation).

## SCOPE
- Gap items closed: engine-prompt-compiler (deepen), engine-prompt-optimizer, engine-prompt-expansion, engine-prompt-testing, engine-prompt-benchmark, engine-prompt-portability, engine-prompt-ab, engine-prompt-ranking, engine-prompt-recommendation, engine-prompt-similarity, engine-intent (deepen to multi-level), engine-entity, engine-variable, engine-context (deepen), engine-confidence (deepen to multi-signal), pipeline-lightweight-analysis (deepen), pipeline-context-assembly (deepen), pipeline-prompt-construction (deepen), pipeline-verification.
- OWNER DECISION CHECK: confirm ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml id veridian-architecture-v2-target-repo-anchor is resolved before writing any application code -- this is the first phase in this plan that does. If unresolved, this phase's own task must re-raise it rather than default silently.
- OWNER-VERIFIED UX REQUIREMENT (2026-07-25, corrects and replaces the amendment rejected in PR #72, which (a) cited OWNER_DECISIONS_NEEDED_2026-07-23.yaml id veridian-architecture-v2-target-repo-anchor as containing the Owner's quotes "in full" when that entry only ever held a paraphrase, and (b) upgraded Gateway G05 to a mandatory second AI pass without reconciling registries.credit_spend_governance -- both defects are corrected below, not just reworded): the Owner's real verbatim words, saved at /opt/veridian/ai-os/OWNER_DIRECTIVES/BROWSER_NATIVE_END_USER_ARCHITECTURE_2026-07-25.txt (knowledge_engine KE-20260725-184749-e0e0), describe two input options -- Option 1 "{dynamic mode pills + dynamic option chain} + chat input in the web browser", Option 2 "END USER direct writing the input in the chat via web browser" -- both routing through a two-stage pipeline: "The browser-native AI platform makes the complete machine language output of the end user prompt and gives to the SOFTWARE to execute. FIRST execution in the browser-native AI platform , SECOND execution in the SERVER by the SOFTWARE. SOFTWARE FIRST APPROACH." This phase's own Stage 4 Machine Prompt Builder (2.2 Stage 13-18, pipeline-prompt-construction) and Prompt Compiler Engine (2.1 Prompt Core Engines, "Natural language intent to optimized machine prompt via DSPy-style declarative compilation pipeline", line 79) ARE the machine-language-output contract the Owner's FIRST pass produces -- the document's own vocabulary already covers this; no new engine is required. CREDIT-GOVERNANCE RECONCILIATION: the same source file also carries an interpretive note, clearly separated from and not attributed to the Owner's verbatim words above, resolving the literal-vs-intent reading of "SECOND execution in the SERVER by the SOFTWARE": Stage 2 is deterministic server-side SOFTWARE execution of this phase's compiled machine-language output (the same execution model OWNER_ENGINE/task-gateway.py already uses for the Owner's own requests), with Gateway G05 invoked only as-needed/escalation WITHIN that software execution -- per registries.credit_spend_governance's standing "software first, AI second, minimal credits only when needed" propose/report two-gate rule (ai-os/MASTER_INDEX.yaml), i.e. AI credits spent only when a specific request genuinely requires them. This is NOT a second mandatory AI pass on every completed end-user request, and this phase's pipeline-prompt-construction/pipeline-verification deepening work must not implement it as one; the compiled contract must be written to phase_1's compliance.prompt_versions row via this phase's own compiled/versioned storage integration_point so both of phase_5's input surfaces compile to the identical output shape.
- Dispatch as a compliance-tracker-scoped task (per meta.cross_repo_dispatch_note) extending prompt_engine.py/classifier.py/context_engine.py's real algorithms into the target repo's own application code, backed by phase_1's prompt_registry table for versioned storage.

## KNOWN_CONTEXT
This is phase_2_compiler_pipeline_intelligence_engines of ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml, part of the VERIDIAN_Architecture_v2.0.docx -- Phased Implementation Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_2_compiler_pipeline_intelligence_engines, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260725-174858-phase1-prompt-registry-version-lifecycle. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_2_compiler_pipeline_intelligence_engines" --tag domain:veridian-architecture-v2-0-docx` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_2_compiler_pipeline_intelligence_engines" ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own phases[] entry for phase_2_compiler_pipeline_intelligence_engines in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
