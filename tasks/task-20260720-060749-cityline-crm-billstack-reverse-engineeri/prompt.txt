You are reverse-engineering live web applications from the front end only (no source code access — black-box exploration via Playwright MCP browser tools). Authorized exercise, credentials provided directly by the system's owner.

## Targets (same base URL, two separate logins/modules)
- URL: http://citylinenetworks.infisuite.in/
- CRM login: crm_admin@citylinenetworks.infisuite.in / 1234 (try infi123 if 1234 fails — two password conventions exist across this Owner's infisuite tenants, confirm which one and note it clearly in SUMMARY.md)
- Billstack login: bill_admin@citylinenetworks.infisuite.in / 1234 (same password fallback note applies)

This is a DIFFERENT tenant from the already-documented bharatnet CRM/Billstack — same platform, likely similar structure but a real second data point. See `docs/crm-bharatnet/` and `docs/billstack-bharatnet/` (if present) in this repo for reference depth/format.

## Job
For EACH login separately: full page-by-page, function-by-function documentation: 1. Purpose. 2. Navigation path. 3. UI/page design. 4. Data fields (name/type/required/validation). 5. Underlying data structure (inferred entities + relationships). 6. Functionality/logic (actions, business rules, conditions). 7. Inputs and outputs. 8. Data conditions.

Also explicitly compare against the bharatnet tenant already documented: what's identical (platform-level), what's tenant-specific (data/config), any structural differences.

## Methodology
1. Log in to CRM first, screenshot dashboard, map nav to `docs/cityline-crm/00-navigation-map.md`, commit+push.
2. Document CRM modules under `docs/cityline-crm/<module-slug>.md`, screenshots under `screenshots/cityline-crm/`.
3. Repeat for Billstack: `docs/cityline-billstack/`, `screenshots/cityline-billstack/`.
4. Final `docs/cityline-crm/SUMMARY.md` and `docs/cityline-billstack/SUMMARY.md`, each with data model, function list, platform-vs-tenant comparison notes, Known Gaps.

## Hard rules
- No password in committed files. No fabricating unobserved functionality — mark inferences as inferred. Document errors/bugs as-is.
- Commit+push after each module.
- Read-only: no creating/editing/deleting real records, no submitting forms with data, no triggering payments/emails/SMS.
- Maintain PROGRESS.md, update after each step, commit+push incrementally — this task may be interrupted and resumed.