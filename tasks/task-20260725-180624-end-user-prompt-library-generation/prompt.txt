## OBJECTIVE
Stand up the real "END USER PROMPT LIBRARY" the Owner requested: 10,000 pre-loaded real prompts modeling what actual VERIDIAN AI OS end users (owners, HODs, employees, vendors, customers, finance managers, across many industries) type as natural-language ERP requests. Reuse the existing ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml + /opt/veridian/chatgpt-prompt-library sandbox + scripts/chatgpt_promptlib_guard.py + the Z.AI manual-bridge generate/ingest pair (scripts/generate_chatgpt_promptbatch_request.py, scripts/ingest_chatgpt_promptbatch_response.py, built and merged PR #54) -- do not build a second, parallel prompt-library system. Produce the real, complete, descriptive first request the Owner will hand to Z.AI, and a real, honest total-batch-count for reaching 10,000.

## KNOWN_CONTEXT
- Read ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml in full first -- 14 real columns already designed (Prompt ID, Prompt, Intent, Business Domain, Module, Capability, Business Process, Entity, Action, Variables, Keywords, Expected Software Capability, Expected Output, AI Required Yes/No, Confidence), a real `<Entity.Attribute>` placeholder convention reusing ai-os/VARIABLE_DICTIONARY_2026-07-24.yaml (60 tables / 894 placeholders), and a real guard/coverage/duplicate-detection toolchain. This schema was designed but never exercised (zero real CSV rows exist) -- your job is to finally exercise it at real scale, extended for the Owner's specific new framing, not replace it.
- Real, known gap you must resolve before generation can be meaningful: the schema's `Capability` column requires an exact match against the live capability_registry table, which as of the schema's own authoring had only 10 real registered rows (`python3 scripts/superboss-register.py list-capabilities`) -- nowhere near enough to anchor 10,000 prompts across "thousands of various functionalities" as the Owner describes. Check the real current row count first (it may have grown since 20-Engines/Wiring-Engine work this session); if still far too small for real per-prompt anchoring, use the real, much larger VCEL engine registry (src/lib/engines/, 247 real engines per this session's own prior VCEL work, ~41 implemented) and/or ai-os/MASTER_INDEX.yaml's real module list as the join key instead, and say explicitly which real source you anchored to and why.
- The Owner's own new requirement, verbatim, that the existing schema does not yet capture: prompts must cover "various end users... owners, head of departments, with thousands of various functionalities... employees, vendors, customers" -- add two real new columns to the schema (do not invent a third parallel schema): `End User Role` (real enum: Owner, HOD, Employee, Vendor, Customer, Finance Manager, or others you find real evidence for in existing role/persona data -- check ai-os/AI_ROSTER_CATALOG.json and any existing RBAC/role tables in compliance-tracker for real role names rather than inventing generic ones) and `Industry` (real value, or an explicit `Generic / Cross-Industry` value where a prompt genuinely isn't industry-specific -- most ERP prompts will legitimately be Generic; do not force industry-specificity that isn't real).
- The Owner's own worked examples (verbatim): "complete all invoices for march", "give latest dso update {DSO = DAILY SALES OUTSTANDING}", "give status of material", "give vendor payment", "employee on leave" -- these are real, representative of the "noise + keywords + variable" phrasing style Z.AI must generate at scale (natural, slightly noisy human phrasing, not formal API-call syntax).
- 10,000 real prompts cannot be one Z.AI paste -- batch realistically (target the same order of magnitude as the working ChatGPT-audit Security request, ~20KB/~400 lines per request is a reasonable single-paste ceiling) and report the real total batch count honestly.

## SCOPE
1. Check the real current capability_registry row count and the real VCEL engine registry state; decide and document which real source anchors the (extended) Capability/Module columns for this library, per KNOWN_CONTEXT.
2. Extend ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml in place with the two new real columns (End User Role, Industry) -- update its csv_schema.columns list and example_row, do not create a second schema file.
3. Extend scripts/generate_chatgpt_promptbatch_request.py (or add a real, clearly-related sibling mode if a clean extension isn't possible -- justify which) so it can generate a real, complete, ready-to-paste Z.AI request implementing the extended schema, targeting real uncovered (End User Role x Business Domain) combinations rather than the old capability-gap-only targeting, batched to the real size ceiling in KNOWN_CONTEXT.
4. Run it for real to produce the first real batch (target ~200-300 real prompts in this first batch, covering a real, deliberately diverse spread of roles/domains/industries -- not all one role or one domain) at /opt/veridian/chatgpt-prompt-library/_pending_requests/.
5. Compute and report the real total number of batches needed to reach 10,000 prompts at this batch's real size.
6. Update ai-os/CHATGPT_PROMPTLIB_BLOCKER_2026-07-24.yaml: the real blocker (missing OPENAI_API_KEY) is resolved via the manual Z.AI bridge, same as the ChatGPT-audit workspace's own blocker resolution earlier this session -- record this real status change.
7. Register the schema extension and the new generation mode in MASTER_INDEX.yaml and via superboss-register.py register-knowledge.

## SUCCESS_CRITERIA
- `python3 -c "import yaml; d=yaml.safe_load(open('ai-os/CHATGPT_PROMPT_SCHEMA_2026-07-24.yaml')); cols=[c['name'] for c in d['csv_schema']['columns']]; assert 'End User Role' in cols and 'Industry' in cols; print('OK')"` exits 0.
- A real batch request file exists under /opt/veridian/chatgpt-prompt-library/_pending_requests/ with a real, non-empty, complete, Z.AI-ready prompt (full schema instructions + real placeholder-convention rules + real, diverse targeting -- paste its real content, or at minimum its real byte size and first/last 30 lines, into your final checkpoint).
- The final checkpoint states a real, computed total-batches-needed number for reaching 10,000 prompts.

## EXPECTED_OUTPUT
Real committed and pushed code (PR on claude-control) for the extended schema and generation script, the real first batch request file, the updated blocker-resolution record, the MASTER_INDEX + knowledge-engine registration, and a final checkpoint with: the real capability/module anchoring decision and why, the real total-batch-count, and the real first-batch file's exact path so the Owner can be handed its content directly.

## CONSTRAINTS
Do not force an `Industry` value onto a prompt that is genuinely cross-industry -- `Generic / Cross-Industry` is a real, valid, expected-to-be-common value, not a fallback to avoid.

## COMPLEXITY_TIER
integrative
