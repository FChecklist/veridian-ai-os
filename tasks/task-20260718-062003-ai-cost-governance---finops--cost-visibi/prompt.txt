VERIDIAN Review Framework gap-closure: AI Cost Governance & FinOps / Cost Visibility & Attribution.
This covers 4 related finding(s) from the framework evaluation. Close all of them in one coherent PR if they share the same module/area -- do not create a separate PR per finding if they're naturally one piece of work.

Findings to address:
- [Low] AI Cost Governance & FinOps: Per-tenant AI cost visibility available to Finance
  Gap: Per-tenant visibility exists at the API/service level; UI-surfaced Finance dashboard usage not independently verified this session
  Recommended approach: Check for and confirm/build the consuming UI page
- [Medium] AI Cost Governance & FinOps: Cost attribution reconciles against actual OpenRouter/provider invoices
  Gap: Cost figures are estimated from token counts, never reconciled against real provider invoices
  Recommended approach: Manual monthly reconciliation first (low effort), automate later if drift is found to be significant/recurring
- [Medium] AI Cost Governance & FinOps: Cost-per-report and cost-per-AI-action are measurable, not estimated
  Gap: Cost-per-action figures are estimated, not measured against real billing
  Recommended approach: See row 79
- [Low] AI Cost Governance & FinOps: Cross-repo (compliance-tracker + projexa) AI spend is visible as one combined figure
  Gap: Unified visibility is an architectural byproduct, not a deliberately engineered cross-repo FinOps feature
  Recommended approach: Documentation + a guardrail-presence-style CI check flagging any new provider-API-key usage added to projexa

Before writing any code: read the actual current implementation of the relevant module(s) first -- do not assume the gap description is still accurate, the codebase has moved since this evaluation was written. If a finding turns out to already be resolved, or the described gap doesn't match what you find in the code, say so in PROGRESS.md rather than making an unnecessary change.
Do not touch src/lib/services/permission-service.ts's shared ERP_ACTION_ROLES table structure or any other in-flight worker's declared scope -- if your area genuinely needs a new permission-service entry, add it additively (new keys only).
Maintain PROGRESS.md with '## Completed' / '## Remaining' checklists as usual.