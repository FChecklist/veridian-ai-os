## OBJECTIVE
Merge 8 new GitHub repos (from instruction INS-20260724-095812-0523's list) into the existing xpost GitHub catalog from earlier today (2 prior batches already merged). Reference-only, no build.

## SCOPE
1. Find the existing xpost catalog artifact (search knowledge_engine / superboss-register for "xpost" or "github catalog", check both live and repo-copy paths for ai-os/XPOST_GITHUB_CATALOG_2026-07-24.yaml given this session's known live-vs-repo drift).
2. Add one row per new repo from the instruction's list: repo_path, one_line_purpose, source_tweet_url, high_relevance flag (true for DietrichGebert/ponytail -- explicitly flagged relevant to VERIDIAN's own token-cost concerns). Record the 5 no-repo-found/flagged items too (same pattern as prior batches), explicitly noting the "free-forever Claude Code router" claim as unverified/not-recommended rather than silently omitting it.
3. Register the updated catalog in knowledge_engine with entity_relationships for DietrichGebert/ponytail to this session's known token-cost-control work.

## KNOWN_CONTEXT
- This is explicitly archival. Do not evaluate, install, or integrate any of these repos now.
- Registry writes must come from a script, not hand-authored prose, per this session's standing rule.
- Zipstack/unstract is a duplicate already catalogued in batch 1 -- do not add it again.

## SUCCESS_CRITERIA
- All 8 new repos appear as rows in the same catalog structure as the prior 2 batches, merged not duplicated.
- The catalog is queryable via the existing query-knowledge CLI (a query for "ponytail" or "openworker" returns a hit).

## EXPECTED_OUTPUT
Real committed and pushed change (PR on claude-control) if the catalog lives in a repo-tracked file, or a direct live-file write matching this session's pattern otherwise. Final checkpoint states total row count now vs before this task.

## CONSTRAINTS
No cloning, no dependency installs, no code changes to any of the 8 repos or to VERIDIAN itself beyond the catalog file and its knowledge_engine registration.

## COMPLEXITY_TIER
mechanical
