## OBJECTIVE
Close 3 real, reference-doc-confirmed CRM schema/service gaps in compliance-tracker, additive-only: (1) a structured Lost Reason for crm_opportunities (Odoo has this, this repo only has free-text stage='lost'), (2) a crm_activities table for Tasks/Meetings/Calls tied to any CRM record (Zoho has this, this repo has crm_stage_history's polymorphic pattern to reuse but no activity-tracking table at all), (3) a crm_campaigns table (Zoho has this, this repo has zero campaign concept). Also audit crm-service.ts and crm-accounts-service.ts for missing get-by-id/delete-or-archive functions on lead/opportunity/account/contact and close any real gap found. This is Wave 1 of a larger CRM-completion effort -- schema + service layer ONLY, no API routes, no pages.

## SCOPE
Files to create: src/lib/services/crm-activities-service.ts, src/lib/services/crm-campaigns-service.ts. Files to modify additively only: src/lib/db/schema.ts (add 3 new tables: crm_lost_reasons, crm_activities, crm_campaigns; add 2 new nullable columns: crm_opportunities.lostReasonId, crm_leads.campaignId; add corresponding Drizzle migration via `bun run db:generate`), src/lib/services/crm-service.ts (add createLostReason/listLostReasons/deactivateLostReason; add get-by-id/delete-or-archive for lead+opportunity if missing), src/lib/services/crm-accounts-service.ts (add get-by-id/delete-or-archive for account+contact if missing). This is a schema-and-service-layer-only slice of a larger multi-wave CRM completion effort; later, separate waves cover the HTTP route layer, the page/UI layer, and chat wiring.

## SUCCESS_CRITERIA
A single PR opened against main (branch name starting wave1-crm-schema-) containing: the 3 new tables + 2 new nullable columns + RLS policies extended to the new tables, following this schema.ts file's own existing conventions exactly (bare-text FK-by-convention columns, no DB-level .references(), same comment style) -- verified by reading the existing crmStageHistory/crmAccounts table definitions first, not guessed. The new crm-activities-service.ts and crm-campaigns-service.ts each have full CRUD (create/list/update/delete-or-archive) with every query orgId-scoped, matching crm-accounts-service.ts's existing structure. crm-service.ts's lost-reason functions exist and whatever function sets an opportunity to stage='lost' accepts an optional lostReasonId. The CRUD audit of crm-service.ts/crm-accounts-service.ts is documented in the PR description either as "gap found and closed" or "checked, already exists" for each of lead/opportunity/account/contact -- not skipped. `bun run build` passes. Zero changes to any existing crm_leads/crm_opportunities/crm_accounts/crm_contacts row-affecting logic.

## EXPECTED_OUTPUT
One GitHub PR against FChecklist/compliance-tracker main, not merged (branch protection blocks direct merge anyway -- open it and stop). PR description must: (a) list every new table/column with a one-line rationale citing which of the 3 reference repos (zoho-reverse-engineering, odoo-reverse-engineering, infisuite-reverse-engineering, all cloned at /opt/veridian/repos/ on this server) and which specific doc file informed it -- read those docs/*.md files before designing anything, do not guess field shapes, (b) confirm RLS/orgId-scoping was extended to all 3 new tables, (c) confirm `bun run build` is green, (d) explicitly state the CRUD-audit finding for each of lead/opportunity/account/contact.

## CONSTRAINTS
File surface for this task is exactly what SCOPE names above (schema.ts, the two new service files, crm-service.ts, crm-accounts-service.ts) -- the HTTP route layer and the page/UI layer belong to separate, later waves and stay as they are today. All 4 existing tables (crm_leads, crm_opportunities, crm_accounts, crm_contacts) keep every current column exactly as-is; this task is purely additive (new tables, 2 new nullable columns). Migrations come from running `bun run db:generate` against the updated schema.ts, never hand-written SQL. New FK-style columns follow the bare-text, app-level-validation-only convention already used by every existing companyId/leadId/accountId-style column in this file -- no DB-level .references() anywhere in this schema, so none should be introduced here either. Read ai-os/boss/ACTIVE-CLAIMS.yaml first; if another active claim's file scope genuinely overlaps this task's file surface, stop and report that instead of proceeding. Where a genuine ambiguity comes up that the existing code/docs can't resolve (a real "which way was this meant to go" gap, not a style preference), document it plainly in the PR description rather than guessing silently.

## COMPLEXITY_TIER
integrative

## KNOWN_CONTEXT
This repo's CRM (crm_leads, crm_opportunities, crm_accounts, crm_contacts, crm_stage_history, all in src/lib/db/schema.ts, plus src/lib/services/crm-service.ts and crm-accounts-service.ts) already has a working lead-to-client pipeline with AI scoring/explainability jsonb columns (aiScore, aiRejectedAlternatives, aiAssumptions, aiConfidence pattern -- do not touch these, just match the style if relevant). crm_stage_history already establishes a polymorphic entityType ('lead'|'opportunity') + entityId pattern for cross-entity history -- crm_activities should reuse that exact pattern, extended to 'account'|'contact' too. Zoho/Odoo/Infisuite reverse-engineering docs already exist on this server specifically to ground this kind of gap-closure work in real reference behavior rather than invented fields -- use them. The Owner's own directive was "study Zoho/Odoo/Infisuite as reference... don't recreate the existing system, complete it... 100%, zero gaps, no assumptions."