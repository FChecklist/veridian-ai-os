## OBJECTIVE
Wire OWNER_ENGINE (the mandatory gate that processes every raw Owner chat message into machine-language output before any AI reads it) and the TASK/STATUS/COMPLETE-IT lifecycle software (task-gateway.py's submit/start/status commands, backed by veridian-task.py, supervisor-entrypoint.sh, risk-tier.py, tight_task_validation.py, credit-accountant.py, backfill_phase_self_report.py, ddl_authorization_check.py) into ONE integrated, end-to-end software pipeline with a single trigger point, zero duplicated logic, and no manual hand-off step between the two systems. Today they are two separate systems an AI session must manually bridge by hand (read OWNER_ENGINE's classified output, then separately construct and run task-gateway.py commands) -- that manual bridging step is exactly the kind of AI-does-it-manually gap the Owner has repeatedly flagged this session and wants closed with real software, not a session's own discipline.

## KNOWN_CONTEXT
Read all of the following real, existing files in full before writing any code -- this task is explicitly a REUSE-AND-REWIRE task, not a new-system task, per the Owner's own standing instruction ("ALREADY MOST IS BUILD, REUSE IT, REWIRE IT... NO NEW ENGINE TO BE BUILD UNTILL NECESSARY"):
- scripts/prompt_gateway/gateway.py and scripts/prompt_gateway/engine/{id_generator,classifier,prompt_engine,context_engine,snip_engine,document_engine}.py and scripts/prompt_gateway/query.py -- OWNER_ENGINE's real current classification/gating pipeline.
- scripts/task-gateway.py (submit/start/log/close/status/register-automation commands), scripts/veridian-task.py (create/checkpoint/resume-context/record-usage/status), scripts/supervisor-entrypoint.sh, scripts/risk-tier.py, scripts/tight_task_validation.py, scripts/credit-accountant.py (propose/report/status/prune), scripts/backfill_phase_self_report.py, scripts/ddl_authorization_check.py -- the real TASK/STATUS/COMPLETE-IT lifecycle software, including three fixes dispatched in parallel this same session against overlapping files (task-20260726-091933, a real branch-resolution + HOLD_FOR_OWNER_SIGNOFF fix touching supervisor-entrypoint.sh and veridian-task.py; task-20260726-091956, a DDL pre-flight gate hardening pass touching ddl_authorization_check.py and task-gateway.py; task-20260726-092047, an interactive-session write-gate touching a shell-profile snippet) -- read the CURRENT state of these files fresh (do not assume any specific prior version), and if any of those three tasks' PRs are still open/unmerged when this task runs, coordinate rather than conflict: prefer basing this work on top of whichever of those three have already merged, and note in the PR body which are still pending.
- ai-os/WIRING_ENGINE_PHASE_PLAN_2026-07-25.yaml, ai-os/WIRING_ENGINE_SCHEMA_2026-07-25.yaml, ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json -- a real, existing, cron-driven (every 6 hours, scripts/generate-wiring-registry, confirmed in rajat's live crontab) initiative already scoped as "real end-to-end gateway verification + registry hardening" for OWNER_ENGINE specifically. This task's objective (OWNER_ENGINE <-> TASK-lifecycle integration) is related but distinct in scope from WIRING_ENGINE's own objective (gateway verification/registry hardening) -- read WIRING_ENGINE's phase plan fully to confirm this distinction is real and not already covered, and if any part of this task's objective turns out to already be in WIRING_ENGINE's scope, extend that existing plan/registry rather than creating a competing one.
- The real credit-accountant.py gap discovered live during this session (2026-07-26): task-gateway.py's dispatch flow (cmd_start()) never calls credit-accountant.py's `propose` command, yet worker-entrypoint.sh's checkpoint loop calls `credit-accountant.py report` unconditionally, which fails with "no matching approved plan for this task_id/increment" whenever a plan was never proposed -- confirmed live for at least 2 real dispatched tasks this session, contributing to real worker failures (compounded by, but distinct from, a separate real Anthropic API 429 session-limit error that was the actual root cause of those 2 specific failures -- both are real, separate findings, do not conflate them).

## SCOPE
1. Design and implement a single, real entrypoint function/script (extend scripts/prompt_gateway/gateway.py itself, do not create a new parallel gateway) that: receives a raw Owner message, runs it through OWNER_ENGINE's existing classification (category/intent), and when the classified intent is a task-dispatch, status-query, or completion-query request, programmatically calls the real task-gateway.py submit/start/status functions directly (as an in-process function call or a controlled subprocess invocation with real argument construction from the classified fields) -- rather than requiring a human or a separate AI turn to manually read OWNER_ENGINE's output and hand-construct task-gateway.py CLI invocations.
2. Fix the real credit-accountant.py gap found above: task-gateway.py's cmd_start() should call `credit-accountant.py propose --task-id <id> --plan <real-plan-description>` as part of dispatch, before or immediately after `veridian-task.py create`, so worker-entrypoint.sh's later `report` calls have a real, matching approved plan to validate against instead of failing.
3. Ensure zero duplicated logic: if OWNER_ENGINE's classifier and task-gateway.py's tight_task_validation.py both independently do any overlapping validation (e.g. both checking for placeholder/vague language, both extracting keywords), consolidate into one shared real implementation rather than two parallel ones -- audit for this specifically and report what, if anything, was found duplicated and how it was consolidated.
4. Add a real, end-to-end integration test: a raw Owner-style chat message requesting a real task gets classified by OWNER_ENGINE, dispatched through the new single entrypoint, and a real (test-mode/dry-run, not a live production dispatch) task-gateway.py invocation is confirmed to have been constructed correctly from the classification output.
5. Document the resulting single, integrated flow in the one canonical protocol file (ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml -- confirm via scripts/check_single_protocol_file.py first) as the authoritative description of how a TASK/STATUS/COMPLETE-IT request now flows end to end through one software system, superseding any prior description of these as two separately-operated systems.

## SUCCESS_CRITERIA
- `grep -n "task-gateway\|task_gateway" scripts/prompt_gateway/gateway.py` shows a real, direct call from OWNER_ENGINE's gateway into the task-lifecycle pipeline, not just a documentation reference.
- `grep -n "credit-accountant.py propose\|credit_accountant.*propose" scripts/task-gateway.py` shows the real propose-call fix is present.
- `python3 -m pytest tests/ -k "gateway_task_integration or owner_engine_task" -v` exits 0 with the new end-to-end integration test passing.

## EXPECTED_OUTPUT
A real PR (or a small, clearly-sequenced set of PRs if the diff is large) against claude-control implementing the integration, the credit-accountant.py fix, the deduplication findings/fixes, the integration test, and the protocol-doc update. Given the scope and prior-art density here, this is explicitly a judgment-tier task expected to take real, substantial reading and design time -- do not rush to a superficial "call task-gateway.py from a new function" without first confirming the deeper duplication/gap questions in KNOWN_CONTEXT and SCOPE items 2-3.

## CONSTRAINTS
This is a reuse-and-extend task -- do not create a new parallel gateway, a new parallel task-dispatch system, or a new parallel registry alongside WIRING_ENGINE's existing one. If, after reading WIRING_ENGINE's real current phase plan, any part of this objective is already scheduled there, extend that plan instead of duplicating it and say so explicitly in the PR body. Do not merge yourself; this needs a fresh, thorough audit pass given its central role in the overall system.

## COMPLEXITY_TIER
judgment
