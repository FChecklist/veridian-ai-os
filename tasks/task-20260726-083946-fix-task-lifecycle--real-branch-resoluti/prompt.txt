## OBJECTIVE
Close two real, recurring gaps in the EXISTING TASK/STATUS/COMPLETE-IT lifecycle software (scripts/supervisor-entrypoint.sh + scripts/veridian-task.py + scripts/task-gateway.py, already built and in daily use) by extending them, not by building parallel new infrastructure. Real incidents this session (2026-07-26), all from the SAME two root causes: (1) PR561's corrective fix, PR562's corrective fix, and PR78's corrective fix all had their real review/audit silently fail to post to GitHub because supervisor-entrypoint.sh's existing `gh pr create --head "$BRANCH"` call always uses the WORKER'S OWN task-derived branch name, even when a dispatch prompt explicitly instructs the worker to push its real commits to a different, pre-existing branch -- the script then can't find a PR to comment on or merge, and a human has to manually intervene every single time. (2) PR563's corrective task was dispatched with an explicit prompt-level instruction "must be held for Owner sign-off, do not merge under any circumstance" -- but this natural-language constraint has zero effect on the ACTUAL merge decision, which is made purely by the existing deterministic tier classifier (scripts/risk-tier.py) plus the audit verdict; the task auto-merged anyway (as a separate, near-empty PR) because nothing in the existing pipeline reads or enforces prompt-level hold instructions.

## KNOWN_CONTEXT
Read scripts/supervisor-entrypoint.sh in full first, specifically: the BRANCH variable (line ~45, read from task.yaml's own 'branch' field), the PR-creation block (`gh pr create --head "$BRANCH"` and its `gh pr list --head "$BRANCH"` fallback), and the tier1-auto-merge block that runs after a real AUDIT: PASS. Also read scripts/veridian-task.py's `create` command to see exactly where task.yaml's 'branch' field is set (this is almost certainly always the worker's own freshly-created branch, set at task-creation time, before the worker does any real work) -- this is the actual root cause of gap (1): the script has no way to know a worker later pushed its real commits somewhere else, because task.yaml's 'branch' field is fixed at creation time and never updated. Also read scripts/risk-tier.py's classify function and scripts/task-gateway.py's cmd_start() (already extended once this session with a DDL pre-flight check, and again with a prompt-format check -- read the current file fresh, do not assume its shape from memory) to see exactly how tier/hold decisions currently flow into supervisor-entrypoint.sh's merge step, so gap (2)'s fix hooks into that same existing decision point rather than adding a new one.

## SCOPE
1. Fix gap (1) at the root: before supervisor-entrypoint.sh's PR-creation/lookup block runs, determine the REAL branch the worker actually pushed real commits to -- not assume it always equals task.yaml's original 'branch' field. The most direct fix: have veridian-task.py's worker-completion path (wherever it currently reads task.yaml's 'branch' to hand off to supervisor-entrypoint.sh) instead read the real current branch from the worker's own git workspace at completion time (`git -C $WORKSPACE rev-parse --abbrev-ref HEAD`, or equivalent), so if a corrective task's own prompt instructed it to check out and push to a different existing branch, that's the branch task.yaml/supervisor-entrypoint.sh actually uses for `gh pr create --head`/`gh pr list --head`/the eventual merge -- closing the gap without adding a new field or a new script, just making the existing 'branch' value reflect real state instead of a stale creation-time assumption.
2. Fix gap (2) at the root: add ONE new, real, machine-readable field to the EXISTING task dispatch prompt template (the same `## CONSTRAINTS`/`## EXPECTED_OUTPUT` structured-header system tight_task_validation.py already parses -- read scripts/tight_task_validation.py's parse_labeled_fields() first) -- a literal marker line, e.g. `HOLD_FOR_OWNER_SIGNOFF: true`, that a dispatch prompt can include in its EXPECTED_OUTPUT or CONSTRAINTS section. Extend tight_task_validation.py's existing field parser to also extract this marker into task.yaml at creation time (a real boolean field, not prose), and extend supervisor-entrypoint.sh's existing tier1-auto-merge block to check this real field FIRST -- if true, skip the auto-merge step entirely regardless of tier/verdict, and post a comment stating it is held for Owner sign-off, same as this session already does by hand for PR563. This makes "hold for sign-off" a real, enforced software behavior instead of inert prose.
3. Do not create a new script, a new task type, or a new parallel pipeline for either fix -- both are edits to the three existing files named above, following their own existing code conventions and patterns exactly (read the surrounding code style before editing).
4. Add regression tests for both fixes (tests/ directory, matching this repo's existing test-file conventions) reproducing: a corrective task whose real commits land on a different pre-existing branch than its own task-derived one (must find/comment/merge-eligible-check the REAL branch), and a task whose prompt includes HOLD_FOR_OWNER_SIGNOFF: true with a tier1-eligible approve verdict (must NOT auto-merge).

## SUCCESS_CRITERIA
- `python3 -m pytest tests/ -k "branch_resolution or hold_for_signoff" -v` exits 0 with both new regression tests passing.
- `grep -n "HOLD_FOR_OWNER_SIGNOFF" scripts/tight_task_validation.py scripts/supervisor-entrypoint.sh` shows the new field is read in both the parser and the merge-decision point.
- `grep -n "rev-parse --abbrev-ref HEAD\|task.yaml.*branch" scripts/veridian-task.py` shows the branch field now reflects real completion-time state rather than only creation-time state.

## EXPECTED_OUTPUT
A real PR against claude-control with both fixes (can be one PR or two -- whichever keeps the diff reviewable; if two, note the dependency clearly in each PR body) and the regression tests. Update the one canonical protocol file (ai-os/OWNER_DIRECTIVES/PROTOCOL_OWNER_AI.yaml -- confirm via scripts/check_single_protocol_file.py first) with a short section documenting HOLD_FOR_OWNER_SIGNOFF as the real, now-enforced way to require sign-off on a dispatch, referencing the 2026-07-26 PR563 incident where the prose-only version was silently ignored.

## CONSTRAINTS
This task builds on and must remain compatible with two other tasks dispatched in parallel this same session against the same repo (claude-control): task-20260726-082342 (DDL pre-flight authorization gate, adds a new pre-flight check function to task-gateway.py's cmd_start()) and task-20260726-083833 (interactive-session write-gate, a shell-profile change plus a committed reference copy). Read the current state of task-gateway.py and the repo fresh before editing -- do not assume it matches any version described here from memory, since parallel work may have already changed it. If a merge conflict with either of those two tasks' branches is likely, note it clearly in the PR body rather than silently resolving it in a way that could drop the other task's real work. Do not merge yourself.

## COMPLEXITY_TIER
judgment
