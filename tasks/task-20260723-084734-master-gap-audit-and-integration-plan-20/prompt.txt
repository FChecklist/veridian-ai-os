## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not a status lookup): produce one honest, evidence-based, consolidated gap list and a logical stepwise integration plan across everything the Owner has asked about today -- the 41-part execution rules, the 198-rule/article checklist, the paused gap_queue.yaml backlog, and 7 GitHub repos' integration readiness. Each of these requires reading real files and making a judgment call about current state versus claimed state -- not something a single script can answer, since the honest answer requires cross-referencing several independent, partially-stale audit trails against each other and against live repo/file state.

## SCOPE
In scope: (1) Run whatever real software-driven audit mechanism exists for ai-os/RULES_ARTICLES_198.json (per its own meta.purpose field: "evidence-backed gap-analysis verdict, produced by software scripts, not free-text AI judgment") -- find it, run it, report real pass/fail counts. If no such mechanism actually exists despite the file's own claim, report that as a finding, do not fabricate results. (2) Evaluate gap_queue.yaml's current state: dispatch_paused=true, 25 queued items, 21 held_task_ids -- for each, determine if it is still relevant, already done elsewhere, or genuinely open; do not just count them, read what each one actually is. (3) Consolidate today's own two audits (ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml: 26 DONE/21 PARTIAL/13 MISSING before item-29 fix; ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml: 41-part results) with the 198-rule audit and gap_queue findings into ONE real consolidated gap list -- overlaps merged, not double-counted. (4) For the 7 repos (compliance-tracker, claude-control, projexa, veridian-ui-kit -- merge target; veda-advisors -- keep isolated/offline; infisuite-reverse-engineering, odoo-reverse-engineering -- keep isolated, no frontend), assess actual current integration readiness: real dependency conflicts, real schema overlaps, real blockers -- not a guess. (5) Produce a real, logical, stepwise plan for the merge + full gap closure, sized honestly (this is genuinely large work, size it as such, do not compress it to sound smaller than it is).

## KNOWN_CONTEXT
Zero customers currently -- this is pre-launch system-building, not live-system maintenance, so breaking changes during integration carry lower immediate risk than they would with real users, but should still be done carefully. Today's already-verified real work (do not re-derive, cite as evidence): GLM proxy decommissioned and all real claude -p call sites now use Sonnet 5 High subscription auth (5 scripts fixed); credit-accountant.py's $1-increment gate now correctly applies only to metered-API work, never subscription work (Owner-clarified rule, implemented in preflight-guard.py, verified working); a real superboss-register.sqlite corruption was found and fully repaired today with zero data loss (all 9 tables verified row-for-row); automatic word-for-word laptop-chat transcript logging plus automatic FTS5-searchable indexing are both live and verified (reusing the existing instructions table, not a new one). The "2056 gaps" figure the Owner referenced is historical (2026-07-19 audit trail, gap_queue.yaml provenance), not a current live count -- do not treat it as today's baseline without checking what of it is still actually open.

## SUCCESS_CRITERIA
A single real, evidence-backed document exists (ai-os/MASTER_GAP_AUDIT_2026-07-23.yaml or similar, following the same DONE/PARTIAL/MISSING + evidence format already proven twice today) covering all four input sources above, deduplicated, with a real total gap count the Owner can trust because every entry cites a file/command/commit, not a claim. A separate real stepwise plan document exists for the repo consolidation, with concrete phases, real blockers named, no invented timeline pressure. gap_queue.yaml's 25 queued items are each individually classified (still-open / already-done / superseded), not left as an unexamined pile.

## EXPECTED_OUTPUT
Commit and push the consolidated audit and the stepwise plan documents. Checkpoint status=pending_review with real summary numbers (total real gaps found across all sources, how many of gap_queue's 25 are genuinely still open). Send one notify-owner.py email with the honest headline numbers and a link/path to the full documents -- simple English, per Owner's own communication rule for that channel.

## CONSTRAINTS
Every DONE verdict in the consolidated audit must cite a real file, command output, or commit hash. A rule/gap already covered by today's two existing audits should be referenced from there, not re-investigated from scratch. Report the honest scale of remaining work plainly rather than minimizing it.

## COMPLEXITY_TIER
judgment