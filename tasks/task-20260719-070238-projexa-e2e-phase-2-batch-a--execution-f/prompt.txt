You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously.

CONTEXT: Owner directive: full end-to-end testing of PROJEXA, zero gaps, no assumptions. This is Phase 2 (of 5) of that program -- Phase 1 (already merged, FChecklist/projexa PR #45) documented the real architecture and seeded a real test org "Meridian Construction Group (E2E Test Org)" in compliance-tracker's live database with 1,007 rows of realistic data (4 projects, 32 RFIs, 24 submittals, 40 punch-list items, 12 change orders, 60 site-diary entries, 60 schedule tasks, etc.). 11 real PROJEXA login accounts now exist and are verified working (email format: firstname.lastname@meridian-construction.e2e-test.projexa-ai.com, password MeridianE2E2026! for all, CEO is arjun.mehta@..., a "member"-role account for anyone else e.g. kavita.iyer@...). READ `PHASE1_SEED_REPORT.md` (in FChecklist/projexa's repo root, already merged to main) IN FULL FIRST -- it has exact file:line citations for every module's real backing service/table, the exact org id, and important nuances (some modules are thin re-export aliases, `documents`/`labour` use a `root:true` routing bypass, `permits` has no dedicated table, etc.) that you must account for, not rediscover from scratch.

YOUR SCOPE (Batch A -- Core Execution & Field modules only): dashboard, schedule, scope, rfis, submittals, punch-list, change-orders, site-diary, work-progress, meetings.

TASK: Write and RUN real, working Playwright E2E tests (this repo already has `bunx playwright test` wired in CI, even though it currently runs `--pass-with-no-tests` since no real test files exist yet -- add real ones under a `tests/` or `e2e/` directory, whichever this repo's playwright.config.ts already expects, read that file first) that:
1. Log in as a real seeded user (use `arjun.mehta@meridian-construction.e2e-test.projexa-ai.com` / `MeridianE2E2026!` for anything needing owner/admin-level access, a member account otherwise) via the real login flow.
2. For EACH of the 10 modules in your scope: navigate to the real page, verify the seeded data actually renders (e.g. schedule shows the real 60 seeded tasks across 4 projects, RFIs page shows the real 32 seeded RFIs with correct status distribution, etc. -- assert on REAL seeded values from PHASE1_SEED_REPORT.md, not just "page loads"), and exercise at least one real write operation per module where the module supports one (e.g. create a new RFI, update a punch-list item's status, add a site-diary entry) -- verify the write actually persists (reload and confirm, or check via a follow-up API read).
3. Test every real filter/sort/search control each module page actually has (read each page's real component code first to know what controls genuinely exist -- do not invent controls that aren't there).
4. Record EVERY test failure with full detail: what was expected, what actually happened, the real error/stack trace, a screenshot if the test framework captures one automatically. Do not soften or summarize away a real failure.
5. If a module's page 500s, shows a wrong/stale value, a broken control, a console error, or diverges from PHASE1_SEED_REPORT.md's documented real backing in any way -- that is a real, reportable gap for Phase 4's gap document. Note it precisely (file, route, expected vs actual) in your final report, don't just mark the test "failed" with no detail.

STEPS:
1. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first (compliance-tracker's, per Phase 1's own precedent for cross-repo claims -- read that file's header for the protocol). Check for collisions with 2 sibling Batch B/C testing tasks likely running concurrently on the SAME 10-module scope boundary you have -- if you see a claim for "PROJEXA E2E Phase 2 Batch B" or "Batch C" already registered, that confirms no overlap (different module lists), proceed.
2. Write the real Playwright tests as described.
3. Run `bunx playwright test` for real (not --pass-with-no-tests) against the actual live projexa-ai.com site (this is testing a live, real deployed environment, not a local dev server -- confirm playwright.config.ts's baseURL, adjust if you need to point it at the live site for this run specifically, using an env var override rather than permanently changing the committed config's default if that default is meant for local dev).
4. Run `bunx tsc --noEmit`, `bun run lint` on any new test files -- must be clean.
5. Push your findings + the new test files to a new branch, open a PR, post the standard 8-field `AUDIT: PASS` (or `AUDIT: FAIL` if you found genuine test-infrastructure problems, separate from real product gaps you're reporting) comment, wait for CI.
6. Classify tier: test-files-only with no schema/migration change = TIER1, self-merge once CI is green. If your write-operation tests need new compliance-tracker data (they shouldn't -- Phase 1 already seeded plenty), do NOT create new schema, just use the existing seeded records.
7. Your MOST IMPORTANT deliverable: a written `PHASE2_BATCH_A_FINDINGS.md` (committed in this PR) listing, per module, what was tested, what passed, and every real gap found with enough detail that Phase 4 (gap document) and Phase 5 (fix implementation) can act on it directly without re-investigating. Be honest and specific -- a vague "some issues found" is not acceptable, cite exact routes/fields/expected-vs-actual values.

Report final status: PR number, CI result, exact pass/fail counts, and the top 3 most significant real gaps found (if any) in your response.
