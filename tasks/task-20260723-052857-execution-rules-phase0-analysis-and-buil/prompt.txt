## OBJECTIVE
TASK REQUIRING AI JUDGMENT (not template-doable writing): classify roughly 200 individual rules across 41 parts of the Owner 2026-07-23 VERIDIAN AI Execution Rules (ai-os/VERIDIAN_EXECUTION_RULES_2026-07-23.md) against real, current system state on this server -- crontab, systemd units, script contents, log contents. Each classification requires interpreting whether an existing mechanism genuinely, partially, or does not satisfy a stated rule -- a semantic judgment call across many heterogeneous files, not a pattern match. Then build the smallest set of genuinely new, low-risk items the audit surfaces, self-dispatching up to 3 further bounded phases for anything larger, then stop and report -- never continuing unboundedly.

## SCOPE
In scope: (1) cross-reference every one of the ~41 parts against everything already built on this server (crontab, systemd --user units, scripts/*, ai-os/scripts/*, ai-os/*.yaml governance docs, superboss-register.sqlite, notify-owner.py, file_inventory.py, credit-accountant.py, run-logged.sh, and every other file under scripts/ and ai-os/scripts/) -- most of Parts 1-13, 32 (Software-first, search-first, server-only, logging, history, monitoring) substantially overlap what already exists; verify precisely, do not assume. (2) Produce ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml: per-part status (DONE/PARTIAL/MISSING) with concrete evidence, same format as the proven GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml from earlier today. (3) Identify and build the smallest set of genuinely new, high-value, low-risk items first (e.g. wiring existing capability into a formal tagging convention on top of what superboss-register.sqlite already stores, rather than building a new tagging system from scratch; extending existing logging to cover any literally-missing categories). (4) Substantial new subsystems (Part 32 SUPERBOSS-IN-SERVER execution core; Parts 33-35 conversation-memory stores) belong in the roadmap as their own future task entries, scoped for later phases. Once this phase reaches a clean stopping point, self-dispatch the next roadmap phase via `python3 scripts/veridian-task.py create`, up to a hard cap of 3 self-dispatched phases total. After the 3rd phase completes (or sooner if nothing further is soundly scopeable), stop self-dispatching, send a final notify-owner.py email with the full roadmap and real progress, and leave any remaining phases as documented, ready-to-dispatch entries for the Owner or a future session to approve individually.
This phase reports real, per-part, evidence-based status only (per Owner Part 37 Truthfulness Rules, binding on this task's own reporting) -- the roadmap itself is the honest measure of progress, not a completion percentage.

## KNOWN_CONTEXT
This is the same server/task-worker system used for the earlier 2026-07-23 governance-audit-and-build work today (see ai-os/GOVERNANCE_AUDIT_RESULT_2026-07-23.yaml, real precedent for the audit format and the labeled tight-task-schema format this prompt itself uses). scripts/notify-owner.py exists, works, sends real email via Resend to raajat.agarwal@gmail.com (verified message id d08a0b6e-35a4-4c70-a0fa-bef7e26be6a9 same day) -- this is the ONLY channel to reach the Owner going forward; the Owner has closed the laptop and will not see chat output. Owner explicitly said: no continuous feedback needed, only notify if input is genuinely required (a real blocking decision, not routine progress), or once there is real completed/verified work to report. Batch findings into meaningful checkpoints rather than one email per minor step. Server resource ceiling already applied: systemd slice app-veridian-worker.slice capped at CPUQuota=640% (80% of 8 vCPU) and MemoryMax=12800M (80% of 16GB) -- this already covers "use up to 80% of server resources", no further action needed on that specific point.

## SUCCESS_CRITERIA
ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml exists with real per-part evidence, not assumptions. At least one genuinely new, verified, committed-and-pushed capability exists beyond what was true before this task started. The self-dispatch chain either reaches a natural stopping point (everything soundly scopeable has been attempted) or is still running further phases autonomously. One notify-owner.py email has been sent summarizing real, verified progress -- with concrete evidence (commit hashes, file paths, message ids), not a status claim without proof.

## EXPECTED_OUTPUT
Commit and push: ai-os/EXECUTION_RULES_AUDIT_2026-07-23.yaml, plus whatever real code/config changes were verified in this phase. Checkpoint status=pending_review with a summary of what was verified done vs what was deferred to a self-dispatched follow-up task (include that task's id if one was created). Send exactly one notify-owner.py email at a natural checkpoint (end of this phase, or sooner only if a genuine blocking decision is hit) with real evidence, worded in simple English per Owner's own communication rule.

## CONSTRAINTS
Every completion claim in the checkpoint note must cite specific file paths, commit hashes, or command output as evidence. This phase delivers the roadmap plus the small verified wins in Expected Output; larger subsystems stay as roadmap entries for later phases. Notifications stay limited to the single end-of-phase email described in Expected Output, plus one additional email only for a genuine blocking decision. Check ai-os/STANDING_DIRECTIVE.yaml precheck for the zero-duplication mandate first.

## COMPLEXITY_TIER
judgment