## OBJECTIVE
Remaining AI Review lane (read-only against Phase 1-4 findings)

## SCOPE
- ddd domain -- aggregate/entity/value-object semantic correctness (dependency-cruiser cannot check this; business-capability's map itself is co-scheduled into Phase 4, see above)
- ux domain -- Nielsen-heuristic and content-quality judgment (axe-core covers only the WCAG technical slice, done in Phase 1)
- workflow domain -- business-logic-correctness judgment (the transition-legality slice is software, see Phase 6)
- ai-governance domain -- bias/transparency/appropriate-use judgment (promptfoo covers only the deterministic slice, see Phase 6)

## KNOWN_CONTEXT
This is phase-5 of ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml, part of the VERIDIAN AUDITOR ENGINE -- Phase 0 Inventory + Phased Build Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase-5, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260724-083724-phase1-auditor-engine-security-scanners, task-20260724-115044-phase2-api-contract-authoring-linting-co, task-20260724-122143-phase3-data-model-static-quality-linting, task-20260724-135839-phase4-architecture-judgment-domains-dep. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase-5" --tag domain:veridian-auditor-engine-0-inventory` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "phase: 5" ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml's own phases[] entry for phase-5 in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
