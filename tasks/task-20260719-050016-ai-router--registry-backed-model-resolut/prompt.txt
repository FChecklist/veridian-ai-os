You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously, including waiting for CI via `gh run watch <id> --exit-status`.

CONTEXT: An evaluation (by a DB-access-capable session, already done, not by you) of FChecklist/compliance-tracker's AI routing found the platform's core principle -- "every AI router is model-agnostic, a model can be swapped without a code deploy" -- is genuinely true for role-level model ASSIGNMENT (src/lib/ai-team/roster-overrides.ts's resolveEffectiveModel(), backed by the ai_team_role_overrides table, already lets an admin swap which model a role uses live) but has two real, closable gaps, plus one new real model to register, plus one new AI Router scope to add. Read every file named below IN FULL before writing any code -- do not assume from this summary alone.

Gap 1 -- src/lib/ai-team/roster-overrides.ts's isKnownModel()/knownModels() currently source their "is this a real, callable model" check from `AI_TEAM_ROSTER.map(r => r.model)` (roster.ts's own static array) -- meaning an override can only REASSIGN a role to a model that's ALREADY hardcoded somewhere in roster.ts; introducing a genuinely new model still requires a code change. Fix: source isKnownModel()/knownModels() from the `platform.ai_model_registry` table instead (already exists live -- read src/lib/ai-router/mother-router.ts's header and the ai_model_registry Drizzle definition in schema.ts first to understand its real shape: provider/model/tier/status/costPer1kInput/costPer1kOutput/healthStatus). A model is "known" if it has an `status = 'active'` row in ai_model_registry. This makes onboarding a genuinely new model a DB insert, not a deploy -- while model-tier-eligibility.ts's JUDGMENT_ELIGIBLE/INTEGRATIVE_ELIGIBLE hardcoded Sets stay EXACTLY as they are (do NOT touch that file -- that hardcoding is a deliberate safety guardrail per AGENTS.md Operating Rule 9: which models are TRUSTED for judgment-critical work must require a real code review, not a live flip. Only the "is this a real/known model at all" check moves to the registry, not "is it trusted for tier X").

Gap 2 -- src/lib/orchestra-model-resolver.ts hardcodes PLATFORM_DEFAULT_PROVIDER/PLATFORM_DEFAULT_MODEL ("groq"/"openai/gpt-oss-120b"), PLATFORM_FALLBACK_MODEL ("meta-llama/llama-3.3-70b-instruct:free"), CEREBRAS_GPT_OSS_MODEL ("gpt-oss-120b"), ESCALATED_PROVIDER/ESCALATED_MODEL ("openrouter"/"z-ai/glm-5.2") as TS constants -- read the whole file first to understand the REAL failover chain logic these feed (Groq primary -> Cerebras paid failover on the same model -> escalation on floor-tier-escalation.ts's signals). Fix: add a `role` text column to platform.ai_model_registry (e.g. values like 'platform_default', 'platform_fallback', 'cerebras_failover', 'escalated_default' -- at most one active row per role at a time, same partial-unique-index pattern ai_routing_policies already uses for its one-active-per-scope constraint) and have orchestra-model-resolver.ts look up each of these 4 by role from the registry instead of a hardcoded constant, keeping the FAILOVER SEQUENCE/DECISION LOGIC itself in code (do not try to move "when to fail over" into data, only "which model/provider is assigned to which named role in the chain"). Fail safe if a registry lookup errors or finds no row: fall back to today's hardcoded literal values as a last resort (never let a DB hiccup break the platform-default AI path entirely) -- log a warning when this fallback path is hit.

New model to register -- Owner confirmed (2026-07-19) `openai/gpt-oss-20b` (via Groq, same family as the existing gpt-oss-120b) is a real model that should exist in the registry as a genuinely new, cheaper floor-tier option alongside 120B. It is NOT currently referenced anywhere in this codebase (confirmed via grep before this task was written) -- add a real ai_model_registry row for it (tier='mechanical', provider='groq', reasonable cost_per_1k_input/output figures if you can find Groq's published pricing for it, else leave those columns null rather than guessing) but do NOT wire it in as the new platform default -- that's a separate decision, not this task's scope. Just make it a real, known, registry-listed model.

New scope -- add `'customer_success'` as a 4th value to the `platform.ai_router_scope` Postgres enum and to mother-router.ts's `AiRouterScope` TypeScript union (`"software_team" | "end_user_org" | "sales_marketing" | "customer_success"`), matching the pattern `sales_marketing` already established (roster.ts-baseline + policy-override, see computeSalesMarketingResolution() as your direct template -- write a computeCustomerSuccessResolution() the same way). This scope has no real dispatch call site wired to it yet in this codebase -- that is fine and expected for this task (same "registry exists, not every call site adopted yet" posture Mother Router's own header already documents for its other 3 scopes) -- do not invent a fake call site, just add the scope/resolution-function plumbing so it is ready for a future real integration.

STEPS:
1. Read in full: src/lib/ai-team/roster-overrides.ts, src/lib/ai-team/roster.ts, src/lib/orchestra-model-resolver.ts (entire file, it is long), src/lib/ai-router/mother-router.ts, src/lib/model-tier-eligibility.ts (read-only, do not touch), and schema.ts's ai_model_registry/ai_routing_policies definitions (note: these tables live in the `platform` Postgres schema now, not `compliance` -- a same-database schema compartment created 2026-07-19, see drizzle/0245's own header for why -- import them from wherever schema.ts now exports them, the TS import path/identifier names are unchanged even though the underlying Postgres schema moved).
2. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first; check for collisions.
3. Implement Gap 1, Gap 2, the new model row, and the new scope, in that order, as 4 logically separate commits on one branch/PR (easier to review, still one PR).
4. Add real tests: isKnownModel() against a registry-backed model vs an unregistered one; orchestra-model-resolver's registry-lookup-with-fallback-to-hardcoded-literal path (mock/force a lookup failure and confirm the safe fallback fires); computeCustomerSuccessResolution() mirroring roster-overrides.test.ts/mother-router.test.ts's existing test shapes for the sibling scopes.
5. Run `bunx tsc --noEmit`, `bun run lint`, `bun test` -- all clean.
6. Before creating any new migration file, find the REAL current highest migration number: `git ls-tree -r origin/main --name-only -- drizzle/ | grep '\.sql$' | xargs -n1 basename | sed -E 's/^([0-9]+)_.*/\1/' | sort -n | tail -3` (main moves fast).
7. Push, open a PR, post the standard 8-field `AUDIT: PASS` comment yourself, wait for CI.
8. This PR touches drizzle/*.sql and schema.ts (new column, new enum value, new seed row) -- classify as TIER2, do NOT self-merge. Report as ready for Owner sign-off instead.
9. Report final status: PR number, CI result, and a plain-English confirmation of exactly what is now registry-backed vs. what remains intentionally hardcoded (model-tier-eligibility.ts) and why.
