You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously.

CONTEXT: Owner directive: full end-to-end testing of PROJEXA, zero gaps, no assumptions, INCLUDING testing via real natural-language chat commands through the AI copilot, not just clicking UI. This is Phase 2 (of 5) -- Phase 1 (merged, FChecklist/projexa PR #45) documented the real architecture and seeded a real test org "Meridian Construction Group (E2E Test Org)" in compliance-tracker's live database (1,007 rows across finance/sales/HR: 12 purchase orders, 8 quotations, 6 sales orders, 12 sales invoices, 33 payslips, 15 leave requests, 25 documents, 12 meetings, etc.). 11 real PROJEXA login accounts exist and are verified working (firstname.lastname@meridian-construction.e2e-test.projexa-ai.com, password MeridianE2E2026! for all; CEO is arjun.mehta@..., a Finance manager is deepak.joshi@..., HR admin is sneha.reddy@...). READ PHASE1_SEED_REPORT.md (FChecklist/projexa repo root, already merged) IN FULL FIRST for exact file:line citations and real nuances -- especially the `copilot` module's own real description: "a fixed 7-tool dispatcher, not an open-ended chat surface" -- read `api/v1/projexa/assistant/route.ts` and `dispatchTool()` (task-execution-engine.ts) to know the REAL 7 tools before writing any chat-command test, do not invent tool capabilities that don't exist.

YOUR SCOPE (Batch C -- Finance, Sales, HR modules PLUS chat/copilot command testing): accounting, budgets, invoices, quotations, sales-orders, sales, customers, employees, payroll, recruitment, hr, kpis, reports, wiki, knowledge-base, grc, copilot.

TASK PART 1 (16 modules, same rigor as every other batch): Write and RUN real Playwright E2E tests that log in as an appropriate real seeded user (deepak.joshi@... for Finance modules, sneha.reddy@... for HR/recruitment, arjun.mehta@... for anything cross-cutting), verify seeded data renders with the REAL documented counts, exercise at least one real write per module where supported (verify it persists), test real filter/sort/search controls (read the real component code first), and record every failure/gap in full detail (expected vs actual, real error).

TASK PART 2 (chat commands -- the Owner's explicit ask, do not skip): Using the real Copilot interface (the composer at the bottom of every PROJEXA page, or the dedicated /copilot page), issue REAL natural-language chat commands as a logged-in seeded user and verify the response is correct against the REAL seeded data. Write at least 10 distinct real chat-command test cases covering the actual 7-tool dispatch surface you confirmed from reading the code -- e.g. (adapt to what the 7 tools actually are, do not assume): "what's the status of RFI #X", "show me overdue invoices", "how many active projects do we have", "what's John's leave balance" (using a real seeded employee name), etc. For each: record the exact command typed, the exact response received, whether it was factually correct against the seeded data, and whether it stayed within the tool's real scope (the report already confirms this is NOT a general chat -- verify it correctly refuses/redirects a request outside its 7-tool scope, e.g. asking it to "delete project X" or "give me a joke", rather than hallucinating an answer).

STEPS:
1. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml (compliance-tracker's, per Phase 1's precedent). Sibling tasks Batch A (execution/field) and Batch B (resources/design) run concurrently on disjoint scopes -- no overlap, proceed independently.
2. Write the real Playwright tests for Part 1, plus a documented, reproducible test log for Part 2 (chat commands can be scripted via Playwright driving the real composer UI, or documented as a manual-but-real transcript if the composer's real-time streaming response is hard to assert on programmatically -- your choice, but it must be a REAL interaction against the REAL live app, not a hypothetical).
3. Run `bunx playwright test` for real against the live projexa-ai.com site.
4. Run `bunx tsc --noEmit`, `bun run lint` on new files -- must be clean.
5. Push, open a PR, post the standard 8-field AUDIT comment, wait for CI.
6. TIER1 if test-files-only with no schema/migration change -- self-merge once CI green.
7. MOST IMPORTANT deliverable: `PHASE2_BATCH_C_FINDINGS.md` (committed in this PR) covering both parts -- per-module test results + every real gap (Part 1) AND the full chat-command transcript with correctness verdicts (Part 2), specific enough for Phase 4/5 to act on directly.

Report final status: PR number, CI result, exact pass/fail counts for Part 1, all 10+ chat command results for Part 2, and the top 3 most significant real gaps found across both parts.
