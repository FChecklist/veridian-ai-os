## OBJECTIVE
Fix and redispatch the real subset of tasks from ai-os/TASK_COMPLETION_AUDIT_2026-07-26.json whose real, verified reason for being incomplete was a genuine AUDIT: FAIL verdict from the real review pipeline (the superboss_rejected_audit_fail bucket, 14 real tasks found in this session own live bucketing pass). Unlike Tier 1 (a fixed pipeline bug), these tasks had real, substantive content problems -- each needs its actual rejection reason read and fixed, not a blind re-run of the same prompt.

## KNOWN_CONTEXT
Read ai-os/TASK_COMPLETION_AUDIT_2026-07-26.json and re-derive the real task list whose last checkpoint note contains the phrase Superboss rejected (this is the real, live-verified bucket from this session own bucketing pass -- do not hardcode a list, re-derive it live). For each one, its real ai-os/tasks/ID/review.json (if present) contains the actual, specific reason the review rejected it -- read that in full for every task before deciding anything.

## SCOPE
1. For each real task in this bucket, read its review.json real summary and issues fields in full.
2. Do a real relevance check: is the underlying objective still real and unmet on the current default branch of its target repo (some may have been superseded by later, unrelated work in the 6+ days since) -- skip and record why for any that are moot.
3. For every task whose real rejection reason is still a genuine, unresolved gap, write a real corrective follow-up prompt (same structured format this session has used all day: OBJECTIVE naming the real prior rejection, KNOWN_CONTEXT citing the real review findings verbatim, SCOPE addressing exactly those findings, SUCCESS_CRITERIA with a real runnable command, EXPECTED_OUTPUT pushing to the real existing PR branch if one exists or a fresh dispatch if not, CONSTRAINTS, COMPLEXITY_TIER) and dispatch it via scripts/task-gateway.py submit and start.
4. Track real outcomes per task (skipped-moot, corrective-fix-dispatched-with-new-task-id, could-not-process) in a real, dated report at ai-os/TIER2_AUDIT_FIX_REPORT_2026-07-26.md.

## SUCCESS_CRITERIA
- python3 -c "print(open('ai-os/TIER2_AUDIT_FIX_REPORT_2026-07-26.md').read().count('|'))" prints a real row count consistent with all approximately 14 real tasks in this bucket having an explicit outcome, no silent omissions.
- python3 scripts/task-gateway.py status --task-id NEWID (for each real dispatched corrective task_id recorded in the report) returns a real, verifiable status.

## EXPECTED_OUTPUT
A real PR against claude-control with the report, plus the real corrective tasks dispatched as separate, independent worker tasks.

## CONSTRAINTS
Do not blindly re-run any original prompt unchanged -- every redispatch in this tier must address the real, specific rejection reason found in that task own review.json. Skip anything whose objective is already confirmed moot. Do not merge yourself.

## COMPLEXITY_TIER
judgment
