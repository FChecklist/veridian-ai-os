TASK: Rescue and merge PR #418 on FChecklist/compliance-tracker (branch worker/task-20260718-053006-ai-architecture--multi-modal---multi-lan).

CONTEXT: This PR was opened by a now-stopped autonomous worker dispatcher. Its CI is currently failing (audit-check and/or Unit Tests) and it may be stale against main. Your job: get it genuinely green and merge it, OR report exactly why it cannot be merged. Do not fake success.

STEPS (in order):
1. `gh pr checkout 418` (this resolves the REAL PR head branch correctly -- do NOT assume it matches this task's own worktree branch name, they are different things). Then `git fetch origin main && git merge origin/main --no-edit` (merge, not rebase -- simpler conflict resolution given multiple PRs merge to main concurrently). If PROGRESS.md conflicts, `git checkout --ours PROGRESS.md && git add PROGRESS.md` (it's a per-task scratch file, always keep your own). Resolve any OTHER conflict by reading both sides for real.
2. If this PR adds any drizzle/*.sql migration file(s): check `git ls-tree origin/main -- drizzle/` for the real highest existing migration number, renumber this PR's migration file(s) to the next sequential number(s) after that (update the filename and any internal references), and re-verify `bun run db:generate` / the migration applies cleanly against the schema.
3. Run `bun install --frozen-lockfile && bunx tsc --noEmit && bun run lint && bun test` locally. Fix any REAL failures (read the actual error, fix the actual bug -- do not skip/silence a test, do not weaken a guardrail per AGENTS.md Rule 9). If a failure is a pre-existing, confirmed-unrelated flake (verify by checking if it ALSO fails on a fresh checkout of origin/main with none of this PR's changes), document that finding plainly rather than guessing.
4. Push the rebased/fixed branch.
5. Read the PR's own diff in full yourself. Post a structured `AUDIT: PASS` or `AUDIT: FAIL` PR comment with all 8 required fields (Objective Understood, Standards Reviewed, Scope Confirmed, Evidence Recorded, Severity Classified [one of: critical/high/medium/low/none, bare word only], Verdict [pass/fail], Corrective Action Owner, Re-Audit Scheduled) -- see .github/workflows/mandatory-audit-check.yml and src/lib/audit-protocol.ts for the exact required shape. Keep every field concise -- no "n/a", no long sentences in Severity Classified.
6. Wait for CI to go green on the rebased commit (`gh run watch <id> --exit-status`).
7. Classify tier: if this PR's final diff touches ANY drizzle/*.sql or src/lib/db/schema.ts, it is TIER2 -- do NOT merge, report back "TIER2, CI green, audited, ready for Owner sign-off" and STOP. Otherwise it is TIER1 -- if CI is fully green and your own audit verdict is PASS, merge it now (`gh pr merge 418 --squash --delete-branch`).
8. Report final status via task checkpoint: PR number, tier, final CI state, merged or awaiting-signoff, and a one-line summary of what real work you did (rebase conflicts resolved, migration renumbered from X to Y, real bugs fixed, etc).

CONSTRAINTS: Never weaken/remove a guardrail. Never force-push over someone else's unreviewed work without understanding it first. Never merge a TIER2 (schema-touching) PR yourself under any circumstance. Use ONLY the Claude Code CLI already configured on this server -- do not invoke any other AI model or external API.
