You are running headlessly, one-shot, with no ongoing session. Never defer any step to "wait for a notification" -- complete every step synchronously, including waiting for CI via `gh run watch <id> --exit-status`.

CONTEXT: FChecklist/projexa's shared-package migration (merged, PR #42+#43) honestly disclosed that AppHeader's `searchSlot`/`notificationSlot` were omitted rather than faked, because neither search nor notifications existed anywhere in PROJEXA before that migration. The Owner has now directed closing this as a REAL gap, not a permanent disclosure -- this task builds genuine, working search and notifications for PROJEXA, matching the reference mockup (compliance-tracker/veridian-scope-selector-in-home.html) which shows both in its header, and matching FChecklist/compliance-tracker's own real, working implementations as the pattern to follow (not copy verbatim -- PROJEXA's entities are different).

READ IN FULL BEFORE WRITING ANY CODE:
- FChecklist/compliance-tracker's src/components/search-command.tsx (the real search UI: a command-palette dialog, keyboard-shortcut triggered, showing grouped standard + semantic results) and its backing API route (search for `/api/search` or similar in that repo -- find the real route file and read it in full).
- FChecklist/compliance-tracker's src/lib/db/schema.ts's `notifications` table definition and src/app/api/notifications/route.ts (the real notification GET/read-marking API).
- FChecklist/veridian-ui-kit's AppHeader component (src/shell/AppHeader.tsx) -- confirm its exact `searchSlot`/`notificationSlot` prop contract so what you build plugs in correctly.
- FChecklist/projexa's own real schema (src/lib/db/schema.ts or wherever its Drizzle tables live) -- find its REAL searchable entities (likely: projects, RFIs, submittals, punch list items, change orders, site diary entries, documents, tasks -- confirm the actual table names and columns by reading the schema, do not guess) and its REAL notification-worthy events (task assigned, RFI raised/answered, submittal status change, punch list item created, deadline approaching -- confirm which of these already have a real trigger point in existing service code before claiming an event as wireable).

BUILD, FOR REAL:
1. **Search**: A command-palette search (same UX pattern as compliance-tracker's `search-command.tsx` -- keyboard shortcut, dialog, grouped results) querying PROJEXA's real entities. Backing API route does real DB queries (ILIKE/full-text against title/name columns is fine -- semantic/embedding search is NOT required for this task, that would be a separate, larger effort; a real working keyword search across PROJEXA's real tables is the actual scope). Wire it into AppTopbar.tsx's `searchSlot` prop (currently `false`).
2. **Notifications**: A new `notifications` table (mirror compliance-tracker's real column shape: id/userId/orgId/title/message/type/isRead/metadata/createdAt -- add orgId since PROJEXA is multi-tenant, confirm the real tenant-scoping pattern this repo uses by reading an existing org-scoped table's RLS policy first) + a real API route (GET list + unread count, PATCH mark-as-read) + a bell dropdown UI (same pattern as compliance-tracker's AppTopbar.tsx notification section) wired into AppTopbar.tsx's `notificationSlot` prop (currently `false`). Wire REAL trigger events -- pick 2-3 genuinely real, already-existing service-layer call sites in PROJEXA (e.g. "RFI created," "submittal status changed," "task assigned") and add a real notification insert at each, rather than building an unused, never-fired table. Do not invent fake/placeholder notification content.
3. Do NOT build semantic/AI-powered search, do NOT build push notifications/email digests, do NOT touch compliance-tracker itself -- this task is PROJEXA-only, and scoped to real keyword search + real in-app notifications, matching what the mockup's header actually shows (a search icon and a bell icon), not a larger notification platform.

STEPS:
1. Register a claim in ai-os/boss/ACTIVE-CLAIMS.yaml first (this repo's own claims file, or check if PROJEXA has adopted the same ACTIVE-CLAIMS.yaml convention -- read compliance-tracker's version of that file's own header for the protocol if PROJEXA doesn't have one yet, and add one following the same pattern if it's missing).
2. Design and add the `notifications` table (real RLS: org-scoped read/write for app_runtime, service_role bypass -- follow this repo's own existing RLS convention exactly, confirmed by reading an existing table's policy first, not guessed).
3. Build the search API route + notifications API routes (GET/PATCH).
4. Build the search command-palette component and the notification bell dropdown component, following veridian-ui-kit's AppHeader slot contract.
5. Wire both into AppTopbar.tsx, replacing the `false` props with the real components.
6. Add real tests for both new API routes.
7. Before creating any migration file, find PROJEXA's real current highest migration number the same way every rescue this session did: `git ls-tree -r origin/main --name-only -- drizzle/ | grep '\.sql$' | xargs -n1 basename | sed -E 's/^([0-9]+)_.*/\1/' | sort -n | tail -3` (if PROJEXA uses a different migrations directory/convention than compliance-tracker, confirm the real path first).
8. Run `bunx tsc --noEmit`, `bun run lint`, `bun test`, `bun run build` -- all clean.
9. Push, open a PR, post the standard 8-field `AUDIT: PASS` comment, wait for CI.
10. This touches a new migration/schema -- classify as TIER2, do NOT self-merge. Report as ready for Owner sign-off.
11. Report final status: PR number, CI result, exactly which real events fire real notifications, and confirm search genuinely queries PROJEXA's real data (not a stub/placeholder).
