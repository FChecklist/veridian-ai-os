## OBJECTIVE
Fix two real, confirmed gaps found live 2026-07-26: (1) a recurring bug where a tier1-approved PR's automated merge step does not actually execute -- confirmed for claude-control PR #78 (had to be merged by hand, later corrected as a real mistake per standing rule) and PR #79 (currently sitting approved, mergeStateStatus CLEAN, mergeable MERGEABLE, but never actually merged) -- and (2) a real, existing but untracked "branch recovery" PR (claude-control PR #84, recovering commit e6c7049 after a branch was mistakenly deleted following a misread review verdict) that has zero task_dir/task.yaml entry, making it permanently invisible to scripts/supervisor-sweep.sh's discovery loop (which only scans /opt/veridian/ai-os/tasks/*/task.yaml) -- meaning PR #84 will never receive a real audit unless this is fixed.

## KNOWN_CONTEXT
Read scripts/supervisor-entrypoint.sh's merge-execution block in full first (search for "MERGE-DETECTION-BLOCK-START"). It already fixed one known false-failure mode (2026-07-24 incidents on PR #10/13/14: a successful merge looked like a failure because branch-deletion's own exit code was combined with the merge's) by judging success solely via a fresh `gh pr view --json state,mergedAt` call. Despite that fix, PR #78 and PR #79 both show a genuinely different symptom: `gh pr view` confirms `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN` (i.e. GitHub itself says it's ready to merge), Superboss's own review approved it with tier1, yet the PR remained OPEN and unmerged with no error surfaced anywhere in supervisor.log for either PR -- meaning either the merge block's own entry condition (`$VERDICT = "approve" && $TIER = "tier1" && $SCOPE_OK = "1"`) was somehow not satisfied despite the checkpoint note claiming approval, or the merge attempt ran but its own outcome-check silently swallowed a real failure. Diagnose this precisely by reading the real, current SCOPE_OK/TIER/VERDICT variable derivation earlier in the script, not by guessing.

## SCOPE
1. For gap (1): trace supervisor-entrypoint.sh's full merge-decision path from the review verdict through the entry condition to the actual `gh pr merge` call. Reproduce the real failure if possible (e.g. by examining PR #79's actual supervisor.log line-by-line for what happened after "Superboss rejected/approved" was logged) and fix the real root cause -- whether that is a race in the CI-polling loop (lines checking mergeStateStatus BLOCKED/BEHIND), a variable-derivation bug, or something else found by reading the real code. Add explicit, real logging at each decision point in this block if the current logging is insufficient to diagnose a repeat of this exact symptom in the future.
2. For gap (2): add a real mechanism so that any PR representing real, reviewable work -- whether created by the normal task-gateway.py dispatch flow or by a recovery/safety-net action outside it -- gets a real task_dir/task.yaml entry so supervisor-sweep.sh's existing discovery loop finds it. The most direct fix: extend veridian-task.py with a new `adopt` (or similarly named) command that creates a real task_dir/task.yaml for an EXISTING branch/PR (rather than `create`'s current behavior of always spawning a fresh worker/branch), then use it to register the real, existing claude-control PR #84 into the tracking system as part of this task's own deliverable -- so PR #84 gets picked up by the next supervisor-sweep cycle and receives its overdue real audit.
3. Add regression tests for both fixes (tests/ directory, matching existing conventions): one reproducing the merge-approved-but-not-executed symptom and asserting the fixed decision path executes the merge; one confirming `veridian-task.py adopt` produces a task_dir/task.yaml that supervisor-sweep.sh's own discovery loop (read its real glob/status-check logic, don't guess) correctly picks up.

## SUCCESS_CRITERIA
- `gh pr view 79 --repo FChecklist/claude-control --json state,mergedAt` reflects a real, genuine merge (via the fixed automated path, not a manual gh pr merge run by this task) once this fix's own PR is itself merged and the next supervisor cycle runs against PR #79.
- `ls /opt/veridian/ai-os/tasks/ | grep -i recovered` (or whatever real task_id the adopt command assigns to PR #84) shows a real task_dir now exists for it.
- `python3 -m pytest tests/ -k "merge_execution or task_adopt" -v` exits 0 with both new regression tests passing.

## EXPECTED_OUTPUT
A real PR against claude-control with both fixes, the regression tests, and the real registration of PR #84 (via the new adopt command, run as part of this task, not by hand). Note in the PR body the real diagnosis found for gap (1) -- do not speculate in the PR body if the diagnosis is inconclusive; say so honestly and describe what further evidence would be needed.

## CONSTRAINTS
Do not manually run `gh pr merge` on PR #78, PR #79, PR #84, or this task's own PR at any point -- the entire point of this task is to make the automated path work, not to patch around it by hand again. Do not merge yourself.

## COMPLEXITY_TIER
judgment
