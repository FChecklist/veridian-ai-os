VERIDIAN Review Framework gap-closure: AI Maintainability / AI Safe-Change Capability.
This covers 5 related finding(s) from the framework evaluation. Close all of them in one coherent PR if they share the same module/area -- do not create a separate PR per finding if they're naturally one piece of work.

Findings to address:
- [Low] AI Can Safely Understand Module
  Gap: Understanding quality varies with file size
  Recommended approach: Prioritize splitting the largest files (see row 33/40 recommendation) to make understanding uniformly easy.
- [Medium] AI Can Safely Modify Module
  Gap: CI gate does not include comprehensive behavioral test coverage
  Recommended approach: Require a minimum test-coverage delta (or at least one new test) on PRs touching previously-untested files.
- [Medium] AI Can Generate Tests for Module
  Gap: No systematic test-generation tooling
  Recommended approach: Add a coverage-gap report (e.g. via vitest coverage) to prioritize which untested files to target next.
- [Medium] AI Can Refactor Module
  Gap: Refactor safety net is incomplete
  Recommended approach: Same recommendation as row 69: raise test coverage before large refactors, starting with the highest-traffic untested files.
- [Low] AI Can Explain Module Accurately
  Gap: No gap of note
  Recommended approach: Maintain the existing header-comment discipline for new files.

Before writing any code: read the actual current implementation of the relevant module(s) first -- do not assume the gap description is still accurate, the codebase has moved since this evaluation was written. If a finding turns out to already be resolved, or the described gap doesn't match what you find in the code, say so in PROGRESS.md rather than making an unnecessary change.
Do not touch src/lib/services/permission-service.ts's shared ERP_ACTION_ROLES table structure or any other in-flight worker's declared scope -- if your area genuinely needs a new permission-service entry, add it additively (new keys only).
Maintain PROGRESS.md with '## Completed' / '## Remaining' checklists as usual.