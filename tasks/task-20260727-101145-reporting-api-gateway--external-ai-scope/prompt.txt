## OBJECTIVE
Build the external-AI-facing reporting API gateway for compliance-tracker's existing Reports & Analysis Engine, per Owner directive CRITICAL_ERP_REPORTING_MODULE_WITH_AI_API_INTEGRATION. This is NOT a new reporting engine -- a real, live 200-row `report_definitions` catalog with a generic dispatcher (`executeReportDefinition()`) already exists (built 2026-07-13, Wave 175). The genuine gap, confirmed by a Phase 0 investigation on 2026-07-27, is narrow: expose the existing engine through the repo's existing external-API pattern with a scoped, tenant-bound token, and wire existing export functions to its output. STRICT_TENANT_ISOLATION is the Owner's explicit top security requirement for this task.

## KNOWN_CONTEXT
Verified real state as of 2026-07-27 via direct code read + live Supabase query, do not re-derive:
- `compliance.report_definitions`: 200 rows (109 built, 85 data_gap, 6 planned), 100% org_id IS NULL (platform-wide catalog). `executeReportDefinition()` in `src/lib/services/report-engine-service.ts:1432`, `TABLE_REGISTRY` whitelist at line 206.
- `src/app/api/reports/*` (definitions, definitions/[id]/run, catalog, etc.) exist today but use ONLY `requireAuth()` (browser session cookie) -- zero `requireAuthOrApiKey()` usage anywhere under that path. This is the exposure gap.
- A real, live, already-versioned external contract exists for OTHER domains: `src/app/api/v1/{compliance,erp,construction,...}/` (51 route dirs, `src/app/api/v1/openapi.json` present). `src/app/api/v1/compliance/route.ts:1-6` documents the intent explicitly: "a customer's own AI, a mobile app, or a reseller's white-labeled app gets a versioned path." There is currently NO `/api/v1/reports/...` directory -- this is the concrete thing to build, following this repo's own existing convention, not inventing a new one.
- **REUSE, do not build new auth**: `requireAuthOrApiKey()` (`src/lib/supabase/auth-guard.ts:392-426`) + `validateApiKey()` (`src/lib/supabase/api-key-auth.ts`) is a complete, live, tenant-bound API-key system already used by other `/api/v1/*` routes -- `Authorization: Bearer vk_...`, SHA-256 hashed, looked up in `compliance.api_keys`, carries `orgId`/`scopes`/`rateLimitPerMinute`, rate-limited via `api_key_request_log`. `requireRoleOrScope()` gates writes uniformly for both session and API-key callers.
  - KNOWN PAST INCIDENT to be aware of (already fixed, do not reintroduce): a hand-minted demo key (`projexa_demo_key`) with unrestricted scopes and no rate limit was found live in production traffic through 2026-07-15, fixed via an env-gated allowlist (`DEMO_API_KEY_IDS` in `api-key-auth.ts:24-53`). Any new report-scoped key issuance must go through the real, audited key-creation path, never a hardcoded/demo key.
- **REUSE, do not build new tenant isolation**: `withTenantContext({ orgId, ... }, fn)` (`src/lib/db/tenant-scoped.ts`) is already wired into `executeReportDefinition()` at ~30 call sites. This means the engine itself is ALREADY RLS-safe for a non-browser caller -- a new endpoint just needs to resolve `orgId` from the scoped API key (via `requireAuthOrApiKey()`) and pass it straight into the existing `executeReportDefinition(ctx, id, params)` call. Do not write new tenant-isolation logic inside the report engine.
- Export functions already exist and are reusable: `src/lib/report-export.ts`'s `exportPPTX`/`exportDocx`/`exportHTML`, plus direct `xlsx`/`XLSX` usage in `src/app/(app)/reports/page.tsx:19,22` via `buildExportRows()` (line 284). These are generic `ExportRow[]`-in/file-out functions currently wired ONLY to the compliance-items DataTable on that page, NOT to report_definitions execution results (the `ReportCatalogList` at line 629 is a pure browser with zero export). Point these existing functions at report-engine output; do not write new export logic.

## SCOPE
1. Build `src/app/api/v1/reports/` following the existing `/api/v1/*` convention (check `src/app/api/v1/compliance/route.ts` and at least one other for the real, current pattern -- auth, error shape, response envelope). Real endpoints needed: list available report_definitions for the caller's org (respecting platform-wide org_id-null visibility), and execute a given report_definitions row by id/slug with params, using `requireAuthOrApiKey()` for auth and the EXISTING `executeReportDefinition()` for execution. Output format: raw JSON (for AI analysis) as the default; support a `?format=csv` or similar using the EXISTING export functions for human/CSV/Excel/PDF download, not new export code.
2. Wire the existing `report-export.ts` functions (and the `xlsx` usage pattern from `reports/page.tsx`) to accept report_definitions execution results as an additional `ExportRow[]` source, not just the compliance-items DataTable.
3. Scoped-token issuance/management for report access: check whether `compliance.api_keys`' existing `scopes` field can express "read:reports" cleanly, or needs a real, minimal extension. Do not build a parallel key-management system -- extend the existing one if genuinely needed.
4. Document the real, intended external-AI usage flow (a customer creates a scoped `read:reports` key in their own account settings -- check if a real UI/API for self-service key creation already exists; if not, note that as a separate, smaller follow-up, not blocking this task) and provide a real `curl` example against a real report in the PR description.

## SUCCESS_CRITERIA
- **Real tenant-isolation penetration test, exactly as the Owner specified**: attempt to access Org A's report data using an API key scoped to Org B. MUST fail securely (401/403, zero data leakage) -- write this as a real, automated test, not a manual check, and it must be part of the PR's test suite.
- A real `curl` command in the PR description hitting a live report via a real scoped token, returning correct JSON, runnable by a human reviewer.
- `npx tsc --noEmit` clean, `bun test` passing including the new tenant-isolation test.
- `get_advisors(security)` via Supabase MCP shows zero new findings.
- Export wiring: a real test proving report_definitions execution results correctly flow through the existing export functions to produce a valid CSV/Excel output.

## EXPECTED_OUTPUT
A real PR against compliance-tracker through the normal task-gateway dispatch pipeline (AGENTS.md Rule 6/7(c) -- structured audit-verdict comment required before merge, no direct push to main). Given STRICT_TENANT_ISOLATION is explicitly the Owner's top concern, expect this to be classified tier2 (HOLD_FOR_OWNER_SIGNOFF) -- that is correct and expected, not a problem to work around.

## CONSTRAINTS
Every currently-disabled cron entry and systemd `.timer` unit on the live server must stay in its present disabled state throughout this task. Do NOT build a new auth/token mechanism -- extend `requireAuthOrApiKey()`/`compliance.api_keys` if genuinely needed, reuse as-is otherwise. Do NOT write new tenant-isolation/RLS logic inside the report engine -- `withTenantContext()` already covers it; your job is getting the right `orgId` into the existing call, not re-deriving isolation. External AI callers (ChatGPT/z.ai) get read access to structured JSON report data ONLY through this proxy API -- they must never receive direct database credentials or a raw SQL surface.

## COMPLEXITY_TIER
judgment
