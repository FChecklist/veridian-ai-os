## OBJECTIVE
Migrate existing hardcoded examples to registered placeholders

## SCOPE
- Add a --suggest-fix mode to the guardrail (or its ported .mjs form) that, for each finding, searches ai-os/VARIABLE_DICTIONARY_2026-07-24.yaml for a placeholder whose entity/attribute name matches the finding's surrounding code context (e.g. a company-name literal inside a Clients-related function maps to <Clients.Name>), and emits a suggested diff -- never auto-applies without human review.
- Work through Phase 2's per-directory findings baselines newest/highest-signal-first (src/lib/services before src/db/seed.ts), converting each into either (a) a merged PR replacing the literal with its suggested placeholder, or (b) a permanent exemption-file entry with a stated reason (e.g. seed.ts local-dev fixtures are explicitly out of scope, per Phase 2's own classification).
- Track migration_completion_pct = (findings_resolved / findings_total_at_phase_2_baseline) per directory as the real metric, re-run via the same guardrail script -- not a manually-updated number.

## KNOWN_CONTEXT
This is phase_4_existing_hardcoded_example_migration of ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml, part of the VERIDIAN Terminology Standardization -- Phased Rollout Plan initiative. Read that file in
full before starting -- this OBJECTIVE/SCOPE text is copied verbatim from its own phases[] entry for
phase_4_existing_hardcoded_example_migration, not invented here. Its depends_on phase(s) are confirmed done via real merged PR(s) for task(s):
task-20260724-140446-fix-terminology-phase3-pr552-review-gap. This task was auto-dispatched by scripts/auto_phase_continuation.py per instruction
INS-20260724-113032-8032, which exists specifically because this same initiative's phased dispatch had
previously stalled after an earlier phase with nothing to continue it automatically -- do not stop after
this phase either without updating the plan file per EXPECTED_OUTPUT below, so the next cron tick can find
real next-phase work if one exists.

## SUCCESS_CRITERIA
- `python3 scripts/superboss-register.py query-knowledge "phase_4_existing_hardcoded_example_migration" --tag domain:veridian-terminology-standardization-phased-rollout` returns at least one registered row for this phase's real deliverable once you register it (same convention this plan file's own `registration:` block or prior phases already use).
- `grep -A3 "id: phase_4_existing_hardcoded_example_migration" ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml` shows this phase's own status updated away from its current not-done value (e.g. to `status: done`), committed as part of this task's real diff -- not hand-edited prose, produced by whatever script/process you use to close this phase out.

## EXPECTED_OUTPUT
Real, committed and pushed code (PR on claude-control) implementing this phase's scope above. Update
ai-os/TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml's own phases[] entry for phase_4_existing_hardcoded_example_migration in place (same status/status_detail or
evidence convention the plan file's already-done phases use) to record what shipped and real evidence, so
a future run of auto_phase_continuation.py can correctly detect this phase as done and move on to whatever
depends on it.

## CONSTRAINTS
Do not re-build or re-litigate anything already marked done in an earlier phase of this same plan file.
Do not duplicate an existing engine/mechanism -- search first, per this session's standing no-duplication
rule (see TERMINOLOGY_STANDARDIZATION_PHASE_PLAN_2026-07-24.yaml's own inventory/precedent notes if present). Any crontab change needs an approved
entry in ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml plus a matching ai-os/CRONTAB_APPROVED_SNAPSHOT.txt
update, per the Owner's existing blanket approval -- do not add cron entries without that citation.

## COMPLEXITY_TIER
judgment
