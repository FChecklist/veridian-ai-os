# Generated by ai-os-scripts/generate_engines_gateways_inventory.py -- DO NOT HAND-EDIT.
# Re-run the script to regenerate after any of the cited paths change.
gateway_inventory:
  - gateway_no: 1
    gateway_id: "G01"
    gateway_name: "Owner"
    purpose: "Boundary through which the human Owner's own instructions/directives enter the system and get logged/tracked."
    exists_as:
      - scripts/superboss-register.py
      - scripts/notify-owner.py
      - ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml
      - ai-os/OWNER_DIRECTIVES/CHATGPT_AUDIT_AND_PROMPT_LIBRARY_WORKSPACE_2026-07-24.txt
    verified_on_disk: true
    coverage: partial
    gap_description: "Real, live pieces exist for both directions -- Owner-in (superboss-register.py's log-instruction subcommand and instructions table, the exact mechanism that registered INS-20260724-053123-0e5a itself) and Owner-out (notify-owner.py, rate-limited, plain-English by design). But they are two separate scripts sharing no Authenticate/Authorize pipeline -- log-instruction does not authenticate its caller (any process on the box can insert an instruction row claiming Owner origin), so this is a structured ledger, not an access boundary in the pipeline sense the Owner's own instruction describes."
  - gateway_no: 2
    gateway_id: "G02"
    gateway_name: "Engineering"
    purpose: "Boundary for engineering/dev-tooling requests -- CI, code-quality gates, and AI-dev-team dispatch."
    exists_as:
      - repos/compliance-tracker/.github/workflows/ci.yml
      - repos/compliance-tracker/.github/workflows/ai-dispatch.yml
      - repos/compliance-tracker/.github/workflows/ai-team-workforce.yml
      - scripts/quality-gate.sh
      - scripts/scope-check.py
    verified_on_disk: true
    coverage: partial
    gap_description: "Real, enforced dev-tooling gates exist (CI lint/typecheck/build, a pre-merge quality gate, file-ownership/scope-collision checks), but they are independent scripts/GitHub-Actions workflows triggered at different points, not one named Engineering gateway that authenticates/authorizes a caller before executing -- checks embedded in a pipeline, not a routing boundary a caller goes through."
  - gateway_no: 3
    gateway_id: "G03"
    gateway_name: "Organization"
    purpose: "Boundary that resolves and enforces tenant/organization context for every request."
    exists_as:
      - repos/compliance-tracker/src/lib/services/context.ts
      - repos/compliance-tracker/src/lib/supabase/auth-guard.ts
      - repos/compliance-tracker/src/lib/db/tenant-scoped.ts
      - repos/compliance-tracker/src/lib/services/org-provisioning-service.ts
    verified_on_disk: true
    coverage: partial
    gap_description: "ServiceContext/ReadContext's mandatory orgId (Engine 2's own Context Engine finding) plus withTenantContext's Postgres RLS session vars are real, enforced org-scoping used consistently across service calls -- genuine LoadOrgContext. But it is woven into auth-guard.ts's requireAuth() per-route, not a standalone named Organization gateway other subsystems route through as one addressable Authenticate->Authorize->LoadOrgContext boundary."
  - gateway_no: 4
    gateway_id: "G04"
    gateway_name: "UserChannel"
    purpose: "Boundary for end-user-facing product channels (chat, portal, guest access) into the platform."
    exists_as:
      - repos/compliance-tracker/src/lib/supabase/auth-guard.ts
      - repos/compliance-tracker/src/app/api/veri-chat
      - repos/compliance-tracker/src/app/api/guest-chat
      - repos/compliance-tracker/src/app/api/me/route.ts
    verified_on_disk: true
    coverage: partial
    gap_description: "requireAuth()/requireRole() in auth-guard.ts is a real, universally-used per-route Authenticate+Authorize gate, and veri-chat/guest-chat are real end-user channel surfaces built on it. But it is invoked ad hoc at the top of each individual route.ts file, not centralized behind one UserChannel gateway component -- no middleware.ts file exists anywhere in the repo."
  - gateway_no: 5
    gateway_id: "G05"
    gateway_name: "AI"
    purpose: "Boundary all AI/model calls route through for provider/model resolution, policy, and audit."
    exists_as:
      - repos/compliance-tracker/src/lib/ai-router/mother-router.ts
      - repos/compliance-tracker/src/lib/model-tier-eligibility.ts
    verified_on_disk: true
    coverage: partial
    gap_description: "Mother Router (resolveModel(), versioned ai_routing_policies, ai_routing_audit_log) is real, live, audit-logged AI routing -- the strongest single candidate of all 10 gateways for matching the Owner's fixed-pipeline description. But its own header comment documents a mechanical grep finding 35 files still bypassing it by calling resolveModelConfig()/checkTierEligibility() directly -- a documented decision not (yet) to migrate them, so roughly a third of this one repo's AI dispatch call sites don't route through it at all."
  - gateway_no: 6
    gateway_id: "G06"
    gateway_name: "BusinessServices"
    purpose: "Boundary wrapping internal business-domain service calls (accounting, PMS, HR, etc.)."
    exists_as:
      - repos/compliance-tracker/src/lib/services
    verified_on_disk: true
    coverage: partial
    gap_description: "A large, real services/ directory (263 files -- erp-accounting-service.ts, pms-meeting-service.ts, org-branding-service.ts, etc.) exists and every function in it does take a ServiceContext/ReadContext (G03's own finding), so some uniform contract is enforced per-call. But there is no single BusinessServices gateway component -- each service module is called directly from its own route handler(s); this is a naming/directory convention, not a gateway other components route calls through."
  - gateway_no: 7
    gateway_id: "G07"
    gateway_name: "DataKnowledge"
    purpose: "Boundary for accessing/tracking data and knowledge-store artifacts (metadata, relationships, provenance)."
    exists_as:
      - scripts/superboss-register.py
      - scripts/knowledge_registry_multisource.py
      - ai-os/MASTER_INDEX.yaml
    verified_on_disk: true
    coverage: partial
    gap_description: "Same real infrastructure engine_inventory already rated coverage=full for Engine 15 (Knowledge Engine) -- a live SQLite+FTS5 store with a register-knowledge/query-knowledge CLI and a recurring multi-source cron refresh. As a gateway concept, though, it is a data store with a CLI, not a request boundary other components authenticate/authorize through first -- no Authenticate/Authorize step exists in front of register-knowledge/query-knowledge themselves."
  - gateway_no: 8
    gateway_id: "G08"
    gateway_name: "Integration"
    purpose: "Boundary for exchanging data with external systems/APIs (inbound and outbound)."
    exists_as:
      - repos/compliance-tracker/src/lib/webhook-deliver.ts
      - repos/compliance-tracker/src/lib/webhooks/vercel-signature.ts
      - repos/compliance-tracker/src/app/api/webhooks/vercel-deployment/route.ts
      - repos/compliance-tracker/src/app/api/connectors/route.ts
      - repos/compliance-tracker/src/lib/supabase/platform-application-auth.ts
    verified_on_disk: true
    coverage: partial
    gap_description: "Real outbound webhook delivery (Engine 10's own finding), real inbound webhook receipt with signature verification (Vercel deployment events), a real OAuth-connector framework (Composio, Drive/Gmail), and a real cross-product platform-application-key Authenticate step all exist and are live. None are unified under one Integration gateway -- each is its own auth/validation path with its own credential class (webhook signature vs. platform key vs. Composio OAuth)."
  - gateway_no: 9
    gateway_id: "G09"
    gateway_name: "ObservabilityAudit"
    purpose: "Boundary/loop surfacing system health and writing the audit trail for actions taken."
    exists_as:
      - scripts/health-check-15min.py
      - scripts/veridian-task-watchdog.py
      - repos/compliance-tracker/src/lib/audit.ts
      - ai-os/AUDITOR_ENGINE_PHASE_PLAN_2026-07-24.yaml
    verified_on_disk: true
    coverage: partial
    gap_description: "Real, live 15-minute health monitoring (systemd/DB/disk/mem) and a real per-write logActivity() in-app audit function both exist and are used (same finding as engine_inventory's Engine 19/20 rows). But these are two unrelated systems -- server-ops health cron vs. in-app DB audit log -- with no shared schema, and neither is a gateway boundary other components route calls through; logActivity() is a side effect at the end of a service function, not an enforced WriteAuditLog step of a shared pipeline."
  - gateway_no: 10
    gateway_id: "G10"
    gateway_name: "Infrastructure"
    purpose: "Boundary/deploy layer for provisioning and running the server-side process fleet."
    exists_as:
      - reconciliation/claude-control/systemd/veridian-task-watchdog.service
      - scripts/supervisor-entrypoint.sh
      - scripts/worker-entrypoint.sh
      - scripts/sync-repos.sh
    verified_on_disk: true
    coverage: partial
    gap_description: "Real systemd-managed worker/supervisor process lifecycle (veridian-worker@/veridian-supervisor@ units referenced throughout health-check-15min.py and supervisor-entrypoint.sh) and a real deployed watchdog unit exist. But no single Infrastructure gateway component exists, just a set of shell entrypoints and systemd units -- and the live server has no top-level /opt/veridian/systemd directory (units live staged under reconciliation/claude-control/systemd/ instead), honestly reported rather than assumed."
