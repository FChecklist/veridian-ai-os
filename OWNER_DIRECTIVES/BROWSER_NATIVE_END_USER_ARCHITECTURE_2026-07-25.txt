SUPERSEDED BY PROTOCOL_OWNER_AI.yaml (2026-07-26 consolidation) for its generic
Owner<->AI protocol phrasing only (recommended-option default, 10-min comms,
zero-gaps/zero-dup/100%, auto-phase-continue -- see PROTOCOL_OWNER_AI.yaml articles
A5, A6, A9, A10). This file is RETAINED at this exact path/name, VERBATIM, because it is
cited by exact path 4 times by ai-os/VERIDIAN_ARCHITECTURE_V2_PHASE_PLAN_2026-07-25.yaml
(lines 142,169,387,421, as knowledge_engine artifact KE-20260725-184749-e0e0) and by
ai-os/WIRING_ENGINE_REGISTRY_2026-07-25.json -- its product-architecture content (below,
including the INTERPRETIVE NOTE) is the real citation target and is NOT duplicated into
PROTOCOL_OWNER_AI.yaml (out of scope -- product/architecture directive, not a generic
Owner<->AI protocol rule). Do not add new generic protocol content here; add it to
PROTOCOL_OWNER_AI.yaml.

OWNER DIRECTIVE 2026-07-25 -- BROWSER-NATIVE END-USER ARCHITECTURE (VERBATIM)
Given directly in chat, mid-way through Task 2 (VERIDIAN_Architecture_v2.0.docx) phase dispatch.

Work only through direct session between "VERIDIAN-DEV (167.233.220.35) direct session with Claude Code CLI SONNET 5.0 HIGH subscription"

let existing work continue without onwners intervention, all phases to be completed automatic , one after another.

a browser-native AI platform should be merged with the existing system, so that the END USER, who will use the system (not the OWNER) , can work through the browser-native AI platform which is merged with the existing system
for the end user
option 1 - [for the  the {dynamic mode pills + dynamic option chain} + chat input in the web browser  at the end user side] and than the browser-native AI platform merged with the existing system takes over. The browser-native AI platform makes the complete machine language output of the end user prompt and gives to the SOFTWARE to execute. FIRST execution in the browser-native AI platform , SECOND execution in the SERVER by the SOFTWARE. SOFTWARE FIRST APPROACH.
option 2 - [END USER direct writing the input in the chat via web browser at the end user side ]  and than the browser-native AI platform merged with the existing system takes over.  The browser-native AI platform makes the complete machine language output of the end user prompt and gives to the SOFTWARE to execute. FIRST execution in the browser-native AI platform , SECOND execution in the SERVER by the SOFTWARE. SOFTWARE FIRST APPROACH.

IMPORTANT - FOR END USER THE BROWSER IS THE ONLY WAY TO INTERACT WITH THE SYSTEM.

1. after you analyze the gaps
2. check the "LOGIC" for option 1 and option 2
3. understand the gaps
4. in the existing system - merge the new requirements
5. dont create new engines until required

USE SOFTWARE AND SCRIPTS for implementation.
DO PROPER AUDIT AND GAP ANALYSIS
NO WORK TO BE DONE BY AI AS A MANUAL WORKER
ALL WORK TO BE DONE USING SOFTWARE, SOFTWARE PROGRAMS, SOFTWARE SYSTEMS, SCRIPTS
HAVE PROPER METADATA, TAGGING, INDEXING, WIRING
ALL EXISTING ENGINES TO BE USED AND UPDATED
NO NEW ENGINE TO BE BUILD UNTILL NECESSARY
USE EXISTING SYSTEM TO FILL THE GAPS
USE EXISTING SYSTEM TO INTEGRATE

COMPLETE ALL PHASES ONE AFTER ANOTHER
AFTER ONE PHASE IS OVER AUDIT IT, ANALYZE IT THAN IMPLEMENT IT
THAN MOVE TO NEXT PHASE AUTOMATICALLY
YOU ARE THE OWNER, YOU TAKE DECISIONS, THE RECOMMENDED OPTION IS DEFAULT AND ACCEPTED

COMMUNICATE WITH THE OWNER EVERY 10 MINUTES IN 1 LINE ONLY.
YOU HAVE OWNER PERMISSIONS.
THE DEFAULT ACCEPTANCE IS THE RECOMMENDED OPTION
COMPLETE ALL THE TASKS END TO END.
ALL PHASES TO BE COMPLETED
ENSURE ZERO GAPS
ENSURE ZERO DUPLICATION
ENSURE 100% IMPLEMENTATION

complete this 100% . no stopping.

INTERPRETIVE NOTE (added by this assistant, NOT part of the Owner's verbatim words above,
recorded separately so paraphrase and verbatim text are never conflated again):
"SECOND execution in the SERVER by the SOFTWARE" literally names SOFTWARE, not AI --
this must be read consistently with the system's own pre-existing, real credit-spend
governance principle (MASTER_INDEX.yaml registries.credit_spend_governance: software
first, AI second, minimal credits only when genuinely needed). The correct reading is:
Stage 2 is deterministic SERVER-SIDE SOFTWARE execution of the machine-language output
Stage 1 produced (the same execution model OWNER_ENGINE/task-gateway.py already uses for
the Owner's own requests) -- Gateway G05 (the real AI-call boundary) is invoked WITHIN
that software execution only as-needed/escalation, exactly as it already works for every
other real request path in this system today, NOT as a second mandatory AI pass on every
single end-user interaction. Any phase-plan amendment must reflect this reading explicitly
and reconcile with the existing credit-governance gate, not silently upgrade G05 to
mandatory.
